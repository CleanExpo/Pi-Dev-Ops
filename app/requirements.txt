fastapi>=0.111
uvicorn[standard]>=0.30
websockets>=12
python-multipart>=0.0.9
httpx>=0.27
pyyaml>=6
python-dotenv>=1.0
bcrypt>=4.0
claude-agent-sdk>=0.1
tenacity>=8.2
PyGithub>=2.0
anthropic>=0.40
fastmcp==3.2.4
google-genai==1.73.1
# Security floor-pins for transitive deps (RA-3042/3043/3044). These are pulled
# in transitively (anthropic/PyGithub/fastmcp → cryptography+PyJWT; pip build →
# setuptools) and were never pinned, so a resolver could regress to a vulnerable
# version and the security scanner keeps re-filing. Floors guarantee the deployed
# image can only resolve patched versions. Current resolves already satisfy these.
cryptography>=46.0.6   # RA-3042: CVE-2026-34073 DNS name-constraint bypass + 6 more
PyJWT>=2.12.0          # RA-3044: CVE-2026-32597 `crit` header not validated (auth bypass)
setuptools>=78.1.1     # RA-3043: CVE-2024-6345 RCE via malicious package index + path traversal
# Test dependencies
anyio[trio]>=4.0
pytest-asyncio>=0.23
