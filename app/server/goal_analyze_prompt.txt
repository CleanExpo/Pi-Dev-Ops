Create the smallest set of Linear tickets a junior can implement. Human reviews first. JSON only.

Rules:
- Goal = what must exist. Acceptance = how success is proven. Brief = context only.
- Do not invent repos, files, APIs, tables, routes, or existing features. Mark unknowns.
- Reuse what the brief already has. No extra features.
- Maximum 6 parent tickets. Max 6 sub-tasks each. Prefer fewer.
- Split by outcome, not by sentence. Every acceptance line maps to a ticket.
- Acceptance is Given/When/Then and observable. Include only relevant states.
- Omit empty keys. One short sentence per field. Do not repeat text.
- Do not claim work was done. Do not mention Ready for Pi-Dev or pi-dev:autonomous.

Keys (fill only what is needed):
goal_analysis: summary, problem, users, desired_outcome, acceptance_summary, existing_functionality, new_work, scope.included, scope.excluded, unknowns, implementation_strategy, risk
user_flow: summary, happy_path, failure_paths, edge_cases
tickets[]: id, priority, title, summary, hierarchy, user_story, scope, affected_surfaces, implementation, sub_tasks[{title,description,scenario,details}], scenarios, acceptance, verification, edge_cases, dependencies, junior_notes, rationale
implementation_order[{ticket,reason}]
final_review: acceptance_coverage, missing_requirements, unknowns, unnecessary_scope, main_risks, smallest_viable_path

Goal:
{{GOAL}}

Acceptance:
{{ACCEPTANCE}}

Project Brief:
{{PROJECT}}
