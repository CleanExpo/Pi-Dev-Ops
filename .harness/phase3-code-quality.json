```json
{
  "scores": {
    "completeness": 7,
    "correctness": 6,
    "codeQuality": 6,
    "documentation": 8
  },
  "issues": [
    {
      "severity": "high",
      "file": "app/server/main.py",
      "description": "Two Dockerfiles exist (root Dockerfile and app/Dockerfile) with divergent configurations — different WORKDIR, different COPY paths, different ENV vars. Root Dockerfile copies src/, .harness/, skills/ which app/Dockerfile does not. Deploying the wrong one silently breaks autonomy features. No single canonical Dockerfile."
    },
    {
      "severity": "high",
      "file": "app/requirements.txt",
      "description": "Lists 'claude-agent-sdk>=0.1' but pyproject.toml lists 'anthropic>=0.90'. These are different packages. CLAUDE.md states the SDK path is the only supported mode (RA-576), but the package name 'claude-agent-sdk' does not match the import pattern 'claude_agent_sdk' documented elsewhere, nor the 'anthropic' SDK. Dependency is either misnamed or wrong."
    },
    {
      "severity": "high",
      "file": "app/server/main.py",
      "description": "Multiple @app.on_event('startup') handlers defined (on_startup at line ~100 and _start_claude_poll at bottom of file). FastAPI executes all startup handlers but the split creates fragile ordering dependency — if _start_claude_poll runs before on_startup, the _resilient wrapper is unavailable. Should be consolidated into one handler."
    },
    {
      "severity": "high",
      "file": "app/server/main.py",
      "description": "Webhook endpoint at /api/webhook has no rate limiting dependency despite handling unauthenticated GitHub/Linear webhooks. Only require_rate_limit is applied, but the rate limit is per-IP. A single compromised webhook source IP is not the threat model — the real threat is replay attacks, which require signature verification (present) but the endpoint lacks the Depends(require_rate_limit) in the signature matching the authenticated endpoints pattern."
    },
    {
      "severity": "high",
      "file": "app/server/main.py",
      "description": "_telegram_send() uses urllib.request directly in an async FastAPI handler (telegram_webhook endpoint). urllib.request is synchronous and will block the event loop. Should use httpx.AsyncClient or run in a thread executor. Under load this stalls all WebSocket connections."
    },
    {
      "severity": "high",
      "file": ".env.example",
      "description": "NEXT_PUBLIC_SUPABASE_URL is defined twice with different values ('https://zbryrmxmgfmslqzizsto.supabase.co' and 'https://your-project.supabase.co'). The real Supabase project URL is hardcoded in a committed file, exposing the project reference ID publicly."
    },
    {
      "severity": "high",
      "file": ".env.example",
      "description": "TELEGRAM_BOT_TOKEN has a real bot token value hardcoded in .harness/RA-588-PREFLIGHT.md ('8630069375:AAH5CDnCm5ApdFsdfFz1KwNGEFDtgy9yvDA') and TELEGRAM_ALERT_CHAT_ID has a real user ID ('8792816988'). These are committed to the repo."
    },
    {
      "severity": "high",
      "file": ".harness/RA-588-PREFLIGHT.md",
      "description": "Contains a live Telegram bot token and user chat ID committed to the repository. This is a credential exposure. The bot token should be rotated immediately and the file should reference env var names only."
    },
    {
      "severity": "medium",
      "file": "app/server/main.py",
      "description": "TriggerRequest and BuildRequest both define identical field_validator methods for 'repo_url' and 'model' with identical logic. Violates DRY. Should use a shared base validator mixin or a common function. At least 4 duplicate validator definitions across 3 Pydantic models."
    },
    {
      "severity": "medium",
      "file": "app/server/main.py",
      "description": "run_scan endpoint fires asyncio.create_task(_run()) and immediately returns {'ok': True}. The _run() coroutine constructs ProjectScanner and TriageEngine synchronously inside the task. If those constructors do I/O (file reads, DB connections), they block the event loop. Should use run_in_executor or ensure constructors are non-blocking."
    },
    {
      "severity": "medium",
      "file": "app/server/main.py",
      "description": "_find_active_session_for_repo() iterates over all _sessions values on every webhook call. _sessions is an in-memory dict with no bound on size. Under load with many historical sessions, this is O(n) on every webhook. Sessions should be indexed by repo_url or expired sessions culled before iteration."
    },
    {
      "severity": "medium",
      "file": "dashboard/package.json",
      "description": "dev script uses 'bash -c set -a && source .env.local' which fails on Windows (no bash). CLAUDE.md documents Windows PowerShell as the primary backend runtime. The dev script is platform-incompatible with the documented primary OS."
    },
    {
      "severity": "medium",
      "file": "mcp/package.json",
      "description": "test script is 'echo Error: no test specified && exit 1'. The MCP server (pi-ceo-server.js) exposes 21 tools including Linear mutations and file writes. Zero tests exist for the MCP layer."
    },
    {
      "severity": "medium",
      "file": ".claude/settings.json",
      "description": "defaultMode is 'bypassPermissions' with Bash(rm:*) allowed. This permits recursive deletion without any confirmation. The deny list blocks 'rm -rf /:*' but not 'rm -rf ./app/*' or 'rm -rf .harness/*'. An agent bug could silently destroy the harness state."
    },
    {
      "severity": "medium",
      "file": ".harness/agents/sdk-version-policy.md",
      "description": "Documents ClaudeSDKClient.connect(), .query(), .receive_messages(), .disconnect() as the stable API contract, but CLAUDE.md states subprocess fallback was removed in RA-576 and the only supported SDK is 'anthropic>=0.90'. The anthropic SDK does not expose a ClaudeSDKClient class — this suggests the version policy documents a phantom API that may not exist in the deployed package."
    },
    {
      "severity": "medium",
      "file": "app/server/main.py",
      "description": "WebSocket handler /ws/build/{sid} catches bare 'except (WebSocketDisconnect, Exception): pass' — swallows all exceptions including programming errors silently. Any bug in the WebSocket loop produces no log entry and no client notification."
    },
    {
      "severity": "medium",
      "file": "Dockerfile",
      "description": "Root Dockerfile installs Node.js 20 and @anthropic-ai/claude-code globally, adding ~400MB to the image. CLAUDE.md states RA-576 removed the subprocess fallback — the claude CLI is no longer needed for the SDK-only path. Dead dependency inflates image size and attack surface."
    },
    {
      "severity": "low",
      "file": ".harness/config.yaml",
      "description": "sdk.fallback_removed: true contradicts app/requirements.txt which still lists 'claude-agent-sdk>=0.1' as a dependency. If the subprocess path is truly removed, the requirements should reflect only 'anthropic'."
    },
    {
      "severity": "low",
      "file": ".harness/board-meetings/2026-04-12-board-minutes.md",
      "description": "Board meeting reports ZTE Score as 60/60 but CLAUDE.md states current ZTE Score is 73/75 (Sprint 8). The board meeting data is stale and contradicts the canonical harness documentation. The gap audit in 2026-04-12-board-minutes.md identifies 'critical' gaps including missing SSE endpoint and Linear two-way sync."
    },
    {
      "severity": "low",
      "file": "app/server/main.py",
      "description": "version hardcoded as '1.0.0' in the /health endpoint payload despite pyproject.toml version being '1.0.0' and dashboard/package.json being '2.0.0'. No single source of version truth. Should read from pyproject.toml or an env var."
    },
    {
      "severity": "low",
      "file": ".harness/anthropic-docs/api-reference.md",
      "description": "Contains raw Next.js SSR React payload JSON (self.__next_f.push) as the file content instead of extracted API documentation. The fetch script stored rendered page HTML/JS rather than the documentation text. These files are unusable as reference documentation."
    }
  ],
  "missingTests": [
    "MCP server tools (mcp/pi-ceo-server.js) — 21 tools with zero test coverage per mcp/package.json",
    "Webhook HMAC verification edge cases — malformed signatures, empty bodies, replay attacks",
    "Pydantic model validators — duplicate validator logic across BuildRequest, ParallelBuildRequest, TriggerRequest not tested for boundary conditions",
    "Session lifecycle state machine — no tests for interrupted→resume transitions or concurrent session limits",
    "Cron trigger scheduler — debounce logic, last_fired_at persistence across restarts, bogus-future-timestamp edge case documented in .harness/board-meetings",
    "Security headers middleware — CSP, X-Frame-Options applied correctly to all response types including SSE streams",
    "Autonomy poller — Linear API failure handling, do-while bootstrap delay, armed/disarmed transitions",
    "Pi-SEO scanner + triage engine integration — end-to-end from scan result to Linear ticket creation",
    "Telegram webhook — secret token validation, /ack_alert command parsing, fire-and-forget failure handling",
    "build endpoint concurrent session limiting — RuntimeError on MAX_CONCURRENT_SESSIONS exceeded",
    "GC loop — collect_garbage boundary conditions, session age calculation",
    "Dashboard TypeScript — no unit tests exist (only tsc type check + lint in CI)"
  ],
  "securityConcerns": [
    "Live Telegram bot token (8630069375:AAH5CDnCm5ApdFsdfFz1KwNGEFDtgy9yvDA) committed in .harness/RA-588-PREFLIGHT.md — immediate rotation required",
    "Real Supabase project URL (zbryrmxmgfmslqzizsto.supabase.co) hardcoded in .env.example — project reference ID is public",
    "Real Telegram user chat ID (8792816988) committed in .harness/RA-588-PREFLIGHT.md",
    "CSP in SecurityHeaders middleware includes 'unsafe-inline' for both script-src and style-src — negates XSS protection for inline scripts",
    "Telegram webhook at /webhook/telegram only validates the secret token when expected_secret is non-empty — if TELEGRAM_WEBHOOK_SECRET is unset, any caller can invoke /ack_alert and silence security alerts",
    "bypassPermissions with Bash(rm:*) in .claude/settings.json permits agent-driven deletion of arbitrary files; deny list does not cover relative path variants (rm -rf ./critical-dir)",
    "SHA-256 password hashing in auth.py (referenced in CLAUDE.md) is insufficient for password storage — bcrypt is listed as a dependency but SHA-256 is used as the comparison hash, making offline dictionary attacks trivial if the hash leaks",
    "WebSocket authentication accepts token from Authorization header as a fallback — this header is visible in server logs and proxy access logs, leaking session tokens",
    "connect-src in CSP allows 'ws: wss:' without domain restriction — permits WebSocket connections to any host from the frontend, enabling data exfiltration via XSS",
    "Scan results and pipeline artifacts written to .harness/ paths that are gitignored — if Railway ephemeral disk is shared across deployments, prior tenant data could be accessible"
  ]
}
```