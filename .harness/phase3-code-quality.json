```json
{
  "scores": {
    "completeness": 7,
    "correctness": 6,
    "codeQuality": 6,
    "documentation": 8
  },
  "issues": [
    {
      "severity": "high",
      "file": "app/server/main.py",
      "description": "Two Dockerfiles exist (root Dockerfile and app/Dockerfile) with divergent configurations. Root Dockerfile copies src/ and .harness/ into /pi-ceo; app/Dockerfile does not. Both expose port 8080, but CLAUDE.md and README describe local port 7777. No single authoritative Dockerfile — deployment surface is ambiguous."
    },
    {
      "severity": "high",
      "file": "app/server/main.py",
      "description": "Undefined variable `_log` used in the `/webhook/telegram` handler (lines referencing `_log.info`, `_log.error`, `_log.warning` inside `_telegram_send` and `telegram_webhook`). The module-level logger is named `log`, not `_log`. This causes NameError at runtime when any Telegram webhook fires."
    },
    {
      "severity": "high",
      "file": "app/server/main.py",
      "description": "SSE endpoint `GET /api/sessions/{sid}/logs` streams `session.output_lines` but the WebSocket endpoint `/ws/build/{sid}` does the same. The gap audit (`board-meetings/2026-04-12-gap-audit.json`) flags the SSE endpoint as non-existent; it now exists in main.py but was filed as a critical discrepancy indicating the spec and implementation were misaligned and likely written concurrently rather than one being derived from the other."
    },
    {
      "severity": "high",
      "file": "app/server/main.py",
      "description": "`on_startup` is decorated twice — once at line ~100 (restore_sessions + background tasks) and again later (`_start_claude_poll`). FastAPI supports multiple `on_event('startup')` handlers, but having them split across the file without a single startup function creates ordering ambiguity and makes the startup sequence hard to audit. The `linear_todo_poller` task runs regardless of `config.AUTONOMY_ENABLED`, contradicting the log message that says the poller is 'DISABLED'."
    },
    {
      "severity": "high",
      "file": "app/requirements.txt",
      "description": "`claude-agent-sdk>=0.1` is listed as a dependency but this package does not exist on PyPI under that exact name. CLAUDE.md references `claude_agent_sdk` (underscore) and the SDK migration plan references importing `ClaudeSDKClient` from it. This will cause a `pip install` failure in production Docker builds. The package is also absent from `pyproject.toml`, creating a split between the two dependency manifests."
    },
    {
      "severity": "high",
      "file": ".harness/board-meetings/2026-04-12-gap-audit.json",
      "description": "The gap audit records 3 critical issues: no Linear two-way sync (issueUpdate mutation never called from webhook.py or sessions.py), SSE streaming endpoint missing, and Linear webhook route not visible. These are confirmed unimplemented features claimed complete in spec — not documentation gaps."
    },
    {
      "severity": "high",
      "file": ".harness/agent-sdk-metrics/2026-04-11.jsonl",
      "description": "All SDK metrics show `latency_s: 0.0` or `0.001` which is physically impossible for real Anthropic API calls (minimum ~300ms). These metrics are clearly from mock/stub runs, meaning the `.harness/agent-sdk-metrics/` corpus does not represent real production traffic. CLAUDE.md states 'Every SDK invocation emits a row' as a verified fact — that claim is false for the data visible here."
    },
    {
      "severity": "medium",
      "file": "app/server/main.py",
      "description": "The `BuildRequest.valid_model` validator restricts models to `opus | sonnet | haiku` but CLAUDE.md and config reference full model IDs like `claude-sonnet-4-6-20250514`. Actual `claude -p` subprocess calls and SDK calls will use the config-resolved full model string. The validator creates false validation that will reject valid model aliases if the client passes anything other than the three short strings."
    },
    {
      "severity": "medium",
      "file": "app/server/main.py",
      "description": "Webhook endpoint at `POST /api/webhook` handles both GitHub and Linear in a single route with an if/elif/else. When neither `x-github-event`+`x-hub-signature-256` nor `linear-signature` is present, it raises HTTP 400. However, the Telegram webhook lives at a separate `POST /webhook/telegram` route — inconsistent URL scheme (`/api/webhook` vs `/webhook/telegram`). The Telegram route also lacks the `require_rate_limit` dependency that every other POST endpoint has."
    },
    {
      "severity": "medium",
      "file": "app/server/main.py",
      "description": "`POST /api/scan` creates a background task via `asyncio.create_task(_run())` but `_run()` is a plain `async def` that uses `await` internally. The function returns immediately with `{'ok': True, 'message': '...'}` but there is no way for the client to poll scan status — no session ID returned, no status endpoint linked. Scan errors are silently swallowed (`return {'error': ...}` inside the background task goes nowhere)."
    },
    {
      "severity": "medium",
      "file": "dashboard/package.json",
      "description": "`lucide-react` is pinned at `^1.8.0` — the actual lucide-react package is at v0.x (e.g. 0.441.0) on npm. Version 1.x does not exist on npm at time of writing. This will cause `npm install` to fail. The package name may be an older fork or typo."
    },
    {
      "severity": "medium",
      "file": "mcp/package.json",
      "description": "`zod` is declared as `^4.3.6` but zod v4 is not yet released (current stable is 3.x). `@modelcontextprotocol/sdk ^1.29.0` may also not exist at that version. These version numbers suggest either future-dated package.json from AI generation or miscopied semver — running `npm install` against this file will likely fail."
    },
    {
      "severity": "medium",
      "file": "app/server/main.py",
      "description": "DRY violation: `valid_url` and `valid_model` validators are duplicated verbatim across `BuildRequest`, `ParallelBuildRequest`, and `TriggerRequest` Pydantic models. These should be extracted to a shared base class or standalone validators. Current state means any change to URL or model validation must be made in three places."
    },
    {
      "severity": "medium",
      "file": ".claude/settings.json",
      "description": "`permissions.defaultMode` is set to `bypassPermissions` with hardcoded absolute paths (`/Users/phill-mac/Pi-CEO/Pi-Dev-Ops`) in the WorktreeCreate/WorktreeRemove hooks. This makes the settings non-portable and will silently fail on any machine other than Phill's Mac. The path is also committed to a potentially-public repo."
    },
    {
      "severity": "medium",
      "file": "dashboard/tsconfig.json",
      "description": "`scripts` directory is excluded from TypeScript compilation but `dashboard/scripts/create-sandbox-snapshot.ts` is referenced in `package.json` as `\"create-snapshot\": \"tsx scripts/create-sandbox-snapshot.ts\"`. Excluding the directory means `tsc --noEmit` will not type-check that script, creating a silent type-safety gap."
    },
    {
      "severity": "medium",
      "file": ".env.example",
      "description": "`NEXT_PUBLIC_SUPABASE_URL` is declared twice in the same file — once at the top with a real-looking value (`https://zbryrmxmgfmslqzizsto.supabase.co`) and once at the bottom under the Frontend section with `https://your-project.supabase.co`. The real project URL appearing in the committed example file is a data exposure concern."
    },
    {
      "severity": "medium",
      "file": "Dockerfile",
      "description": "The root Dockerfile installs the `claude` CLI via `npm install -g @anthropic-ai/claude-code` but CLAUDE.md states `TAO_USE_AGENT_SDK=1` is the only supported mode as of Sprint 8 (subprocess fallback removed — RA-576). The CLI is installed for zero-benefit in SDK-only mode, adding ~200MB to the image and creating a false impression that the CLI path is still active."
    },
    {
      "severity": "low",
      "file": "pyproject.toml",
      "description": "`pyproject.toml` lists `anthropic>=0.90` as a dependency but `app/requirements.txt` does not include `anthropic` at all — it lists `claude-agent-sdk>=0.1` instead. The incident post-mortem (`.harness/INCIDENT-2026-04-12.md`) documents that the missing `anthropic` package in the watchdog sandbox caused a CRITICAL false-positive. The dependency split between pyproject.toml and requirements.txt is the root of that class of failures."
    },
    {
      "severity": "low",
      "file": "app/server/main.py",
      "description": "The `health` endpoint hardcodes `\"version\": \"1.0.0\"` but the `pyproject.toml` declares `version = \"1.0.0\"` and `dashboard/package.json` declares `version: \"2.0.0\"`. No single source of truth for version strings. The health endpoint version is stale by at least one major version."
    },
    {
      "severity": "low",
      "file": ".harness/anthropic-docs/api-reference.md",
      "description": "All five Anthropic docs files (api-reference.md, claude-code-overview.md, mcp-overview.md, models-overview.md, tool-use.md) contain raw Next.js RSC/SSR serialized JSON payloads and inline CSS from the docs site rather than clean markdown content. The fetch script scraped the rendered HTML but did not strip boilerplate. These files are ~90% noise, making them useless as RAG context and consuming significant storage."
    },
    {
      "severity": "low",
      "file": ".harness/BOARD-DELIBERATION-ENHANCED-2026-04-12.md",
      "description": "The board deliberation document (6000+ words) and ARCHITECTURE-V2.md (~3000 words) are committed to the harness directory. While useful as decision records, they are extremely large for a `.harness/` directory that is intended as an operational state store. No pruning or archival strategy exists. Combined with `triage-cache.json` at 353KB and multiple build logs, the harness directory will grow without bound."
    }
  ],
  "missingTests": [
    "Integration tests for the full 5-phase build pipeline (plan→generate→evaluate→test→ship) — only unit tests exist per CI config",
    "Tests for auth.py password verification and session token HMAC signing/verification",
    "Tests for webhook.py GitHub and Linear HMAC signature verification with invalid/missing signatures",
    "Tests for sessions.py restore_sessions() persistence round-trip (write then reload)",
    "Tests for the SSE streaming endpoint /api/sessions/{sid}/logs cursor-based delivery",
    "Tests for the Telegram webhook /webhook/telegram including /ack_alert parsing and the secret-token check",
    "Tests for cron.py trigger scheduling, firing, and state persistence",
    "Tests for autonomy.py linear_todo_poller under conditions where LINEAR_API_KEY is absent",
    "Tests for pipeline.py PipelineState serialization/deserialization",
    "Frontend type tests for dashboard/lib/phases.ts SSE event parsing",
    "Tests for the rate limiting middleware (require_rate_limit) — verifying 30 req/min enforcement",
    "Tests for config.py env var parsing edge cases (empty strings, missing vars, invalid values)",
    "End-to-end smoke test coverage for the /api/build/parallel fan-out route",
    "Tests for gc.py garbage collection logic (session age thresholds, status filtering)"
  ],
  "securityConcerns": [
    "TELEGRAM_BOT_TOKEN value `8630069375:AAH5CDnCm5ApdFsdfFz1KwNGEFDtgy9yvDA` is committed in plaintext in .harness/RA-588-PREFLIGHT.md — this is a live bot token that should be rotated immediately",
    "TELEGRAM_ALERT_CHAT_ID value `8792816988` (Phill's personal Telegram user ID) committed in .harness/RA-588-PREFLIGHT.md",
    "Real Supabase project URL `https://zbryrmxmgfmslqzizsto.supabase.co` committed in .env.example — project IDs should not appear in committed example files",
    "`.claude/settings.json` sets `defaultMode: bypassPermissions` which grants the agent unrestricted file system and command access. This is committed to the repo and applies to any Claude session run in this directory, not just sanctioned automation sessions",
    "The Telegram webhook at /webhook/telegram only validates the secret token if `expected_secret` is non-empty. If TELEGRAM_WEBHOOK_SECRET is not set (common in development), all Telegram webhook requests are accepted without authentication — identical vulnerability to the one flagged in dashboard/app/api/telegram/route.ts in the gap audit",
    "WebSocket authentication at /ws/build/{sid} accepts tokens from the Authorization header as a fallback. The token is extracted with `.replace('Bearer ', '')` which strips exactly one occurrence — a `Bearer Bearer token` value would bypass the strip. Minor but indicates the auth header parsing is not using a proper parser",
    "Content-Security-Policy in SecurityHeaders middleware allows `script-src 'self' 'unsafe-inline'` — inline scripts are permitted, weakening XSS protection significantly. Should use nonces or hashes instead",
    "The `bypassPermissions` mode is active in .claude/settings.json and passed to every ClaudeAgentOptions instance. This means any prompt injection attack on a build session could cause arbitrary file writes, git operations, or command execution within the Railway container filesystem",
    "Password is hashed with SHA-256 (not bcrypt) according to auth.py reference in README ('bcrypt with transparent SHA-256 migration'). SHA-256 password hashes are fast to brute-force. The migration path should ensure all passwords are eventually stored as bcrypt, not SHA-256",
    "app/requirements.txt includes `python-dotenv>=1.0` but pyproject.toml does not — if the Railway deployment uses pyproject.toml for dependency resolution, .env file loading support may be absent in production, causing silent failures for any code calling `load_dotenv()`"
  ]
}
```