```json
{
  "scores": {
    "completeness": 7,
    "correctness": 6,
    "codeQuality": 5,
    "documentation": 7
  },
  "issues": [
    {
      "severity": "high",
      "file": "app/server/sessions.py",
      "description": "Bug at line ~220: `if 'af' in dir()` uses dir() instead of locals(). 'af' is a local variable and dir() returns module/class/instance attributes, not local scope. Should be `if 'af' in locals()`. This means the file count in the session summary will always show '?' even when af is populated."
    },
    {
      "severity": "high",
      "file": "app/server/auth.py",
      "description": "_req_log dict grows unbounded. IPs are never evicted — only per-IP windows are trimmed. A burst of unique IPs will leak memory indefinitely. No background cleanup task exists (acknowledged in spec as P1-C but not implemented)."
    },
    {
      "severity": "high",
      "file": "app/server/sessions.py",
      "description": "_sessions dict is in-memory only with no persistence. Server restart loses all session state. No GC for old workspace directories — disk accumulates on every build. Both known (spec P1-A, P1-B) but unimplemented."
    },
    {
      "severity": "high",
      "file": "dashboard/app/api/analyze/route.ts",
      "description": "GitHub token passed as a query parameter (?token=ghp_...). Tokens in URLs appear in server logs, proxy logs, browser history, and Referer headers. Should be passed as Authorization header or POST body."
    },
    {
      "severity": "high",
      "file": "dashboard/app/api/telegram/route.ts",
      "description": "Telegram webhook authentication uses token comparison via string equality in URL query param (searchParams.get('token') !== token). Bot token leaks in server access logs. No HMAC signature verification of the Telegram payload (X-Telegram-Bot-Api-Secret-Token header)."
    },
    {
      "severity": "high",
      "file": "app/server/config.py",
      "description": "SESSION_SECRET regenerates on every restart when env var is not set (secrets.token_hex(32) called at module load). All existing sessions are invalidated on restart — documented in spec but not mitigated. The env var is named TAO_SESSION_SECRET in config.py but SESSION_SECRET in README/docs — naming inconsistency."
    },
    {
      "severity": "medium",
      "file": "app/server/auth.py",
      "description": "create_session_token() calls hmac.new() which is not a standard Python hmac API — should be hmac.new(key, msg, digestmod) but the stdlib function is hmac.new(), which is correct. However the token format is raw JSON payload + '.' + hex sig, not URL-safe. The payload contains unescaped characters that could cause issues if the token is passed in headers or URLs without encoding."
    },
    {
      "severity": "medium",
      "file": "app/server/sessions.py",
      "description": "run_build() mixes concerns: cloning, workspace management, Claude invocation, git push, and summary generation all in one 130-line async function. No sub-function decomposition. Violates SRP and makes testing impossible."
    },
    {
      "severity": "medium",
      "file": "app/server/sessions.py",
      "description": "em() (emit message) is a cryptic function name. parse_event() uses bare `except:` on line ~55 which swallows all exceptions silently. Multiple bare `except` blocks throughout sessions.py suppress errors without logging."
    },
    {
      "severity": "medium",
      "file": "dashboard/lib/claude.ts",
      "description": "runPhaseCLI() spawns child_process.spawn() in a Next.js API route running on Vercel. Vercel serverless functions cannot spawn persistent subprocesses — claude CLI will not be available. The CLI mode is non-functional on Vercel. The fallback in actions/route.ts also uses spawn(). This is a deployment correctness bug."
    },
    {
      "severity": "medium",
      "file": "dashboard/app/api/analyze/route.ts",
      "description": "Phase 8 (COMMIT & PREVIEW) is hardcoded outside the PHASES loop (only slices 0-7 of PHASES array). The phase_update event for phaseId 8 is sent manually. This breaks the DRY loop pattern and means PHASES[7] (id:8) is never processed through applyPhaseResult()."
    },
    {
      "severity": "medium",
      "file": "dashboard/lib/phases.ts",
      "description": "applyPhaseResult() for phase 7 maps nextActions as (parsed.nextActions as Array<{action:string}>).map(a => a.action). If Claude returns nextActions as string[] (which the prompt allows), this produces undefined for each element. Type mismatch between prompt schema and parser."
    },
    {
      "severity": "medium",
      "file": "dashboard/hooks/useSSE.ts",
      "description": "EventSource error handler parses (e as MessageEvent).data — SSE onerror events do not carry a data payload. JSON.parse of undefined/null will throw. The catch is absent, meaning a network drop could throw an unhandled exception in state update."
    },
    {
      "severity": "medium",
      "file": "mcp/pi-ceo-server.js",
      "description": "MCP server reads .harness/ from local filesystem only (HARNESS_DIR path). When analysing external repos via the Vercel dashboard, harness files are written to GitHub branches — not to the local .harness/ dir. The MCP tools will return stale or missing data for any dashboard-run analysis. The analyze_repo tool listed in the file header comment does not appear in TOOLS array — dead documentation."
    },
    {
      "severity": "medium",
      "file": "_deploy.py",
      "description": "Bootstrap script uses open(f, 'w').write(c) without context manager — file handles not explicitly closed. ROOT path is hardcoded to Windows path r'C:\\Pi Dev Ops', making this non-portable. Script overwrites pyproject.toml with a version missing FastAPI and other runtime deps (only httpx and pyyaml listed), contradicting the actual pyproject.toml in the repo."
    },
    {
      "severity": "medium",
      "file": "dashboard/app/(main)/dashboard/page.tsx",
      "description": "sanitize() function strips <, >, \", & from repo URL but does not validate URL structure. A malformed URL passes sanitization and is sent to the API. URL validation belongs in the API layer (analyze/route.ts already does this), but the client-side sanitize() gives false confidence."
    },
    {
      "severity": "low",
      "file": "app/server/sessions.py",
      "description": "cleanup_session() function is defined but never called anywhere. Old workspace directories accumulate on disk. The function exists as dead code."
    },
    {
      "severity": "low",
      "file": "dashboard/app/api/actions/route.ts",
      "description": "github_issues action slices gaps to 5 (gaps.slice(0, 5)) with no user feedback that truncation occurred. Silent data loss. Individual issue creation failures are silently skipped (catch with no logging)."
    },
    {
      "severity": "low",
      "file": "dashboard/components/ResultCards.tsx",
      "description": "Bar component hardcodes 10 segments. For ZTE score (max 60) this component is not used — but the pattern is inconsistent. Values > max would produce negative repeat counts for the empty segments (Math.round(61/10) = 7 → negative for 10-7 segments only if value exceeds max)."
    },
    {
      "severity": "low",
      "file": "dashboard/app/(main)/layout.tsx",
      "description": "NAV active detection uses exact path match (path === href). Navigation to /dashboard sub-routes or /chat/something would not highlight the correct nav item. Should use path.startsWith(href) for robustness."
    },
    {
      "severity": "low",
      "file": "app/server/main.py",
      "description": "WebSocket handler (ws_build) has a race condition: session.output_lines may be appended to between the 'current = session.output_lines[last:]' read and the 'last = len(session.output_lines)' update. Lines could be missed or double-sent in high-throughput scenarios. Use a copy or index-based approach."
    },
    {
      "severity": "low",
      "file": "app/static/index.html",
      "description": "All JavaScript is inline in index.html with no CSP nonce. The SecurityHeaders middleware sets script-src 'self' 'unsafe-inline' which allows XSS via injected scripts. The static frontend bypasses the stricter CSP applied to the Next.js dashboard."
    }
  ],
  "missingTests": [
    "app/server/auth.py — verify_password, create_session_token, verify_session_token, check_rate_limit (zero test coverage on security-critical code)",
    "app/server/sessions.py — run_build() lifecycle, parse_event() JSON parsing, kill_session() subprocess termination",
    "dashboard/lib/phases.ts — applyPhaseResult() for all 8 phase IDs, tryParseJson() with malformed/markdown-wrapped JSON",
    "dashboard/lib/github.ts — parseRepoUrl() edge cases (SSH URLs, trailing slash, .git suffix), fetchRepoContext() file prioritization and cap",
    "dashboard/hooks/useSSE.ts — SSE event dispatch, state transitions (idle→running→done, idle→running→error)",
    "dashboard/lib/claude.ts — runPhaseCLI() error handling (non-zero exit code, spawn failure), CLI vs SDK mode selection",
    "mcp/pi-ceo-server.js — dispatch() routing, callTool() for all 6 tools, readHarness() missing file fallback",
    "Integration: full analyze flow (GitHub fetch → phase loop → harness push → PR creation)",
    "Security: rate limiting enforcement, HMAC token tamper detection, session expiry"
  ],
  "securityConcerns": [
    "GitHub token (ghp_*) transmitted as URL query parameter in SSE request (/api/analyze?token=...) — visible in server logs, proxy logs, browser history, Referer headers. Move to Authorization header.",
    "Telegram bot token used as URL-based auth (?token=BOT_TOKEN) — same log exposure risk. Should use X-Telegram-Bot-Api-Secret-Token header with HMAC validation per Telegram docs.",
    "SESSION_SECRET auto-generation at module import (config.py) means it changes on every cold start when env var is not set. In cloud deployments with ephemeral containers this invalidates all sessions on scale events.",
    "CSP in next.config.ts includes 'unsafe-eval' in script-src — required by Next.js dev mode but should be removed in production builds. No environment distinction.",
    "app/static/index.html served with 'unsafe-inline' script-src — the entire frontend JS is inline, negating XSS protection. No nonce or hash-based CSP.",
    "Rate limit state (_req_log in auth.py) is per-process in-memory. With multiple uvicorn workers (--workers N) or multiple Docker replicas, each worker has independent state — effective rate limit multiplies by worker count.",
    "src/tao/agents/__init__.py is empty stub — if the agents module is ever imported and called, it will fail silently or with AttributeError. No guard or NotImplementedError.",
    "CORS allows all Vercel subdomain previews via specific hardcoded domains but TAO_ALLOWED_ORIGINS accepts arbitrary origins from env var with no validation — could be misconfigured to allow unwanted origins.",
    "Workspace directories (app/workspaces/) are created for every build session and never automatically deleted — potential disk exhaustion on long-running deployments.",
    "claude CLI subprocess in sessions.py runs with full process permissions and network access. No sandbox, no resource limits (CPU/memory/disk), no timeout on the Claude execution itself (only git operations have timeouts)."
  ]
}
```