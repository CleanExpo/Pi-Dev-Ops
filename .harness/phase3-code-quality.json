```json
{
  "scores": {
    "completeness": 7,
    "correctness": 6,
    "codeQuality": 5,
    "documentation": 7
  },
  "issues": [
    {
      "severity": "high",
      "file": "READY_TO_DEPLOY.md",
      "description": "Live GitHub token (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), Linear API key (lin_api_DUkmVuRx1ZdjKKfyjVlGKqLAleHmaEcpKTq7En4i), and production TAO_PASSWORD ('PiDevOpsTestPassword2024') committed in plaintext to repository. These must be rotated immediately."
    },
    {
      "severity": "high",
      "file": "app/server/sessions.py",
      "description": "GitHub push URL is hardcoded: 'https://github.com/CleanExpo/Pi-Dev-Ops' (line ~450 in push phase). Any repo cloned will attempt to push to this fixed URL regardless of the actual repo_url parameter, causing pushes to the wrong repository or silent failures."
    },
    {
      "severity": "high",
      "file": "app/server/sessions.py",
      "description": "_read_stdout and _read_stderr are defined as inner async functions inside the generator block but referenced again in the retry evaluator block (retry_proc usage at ~line 490). The inner functions close over 'proc' not 'retry_proc', meaning retry streaming is broken — stderr/stdout from retry Claude run will not be correctly associated."
    },
    {
      "severity": "high",
      "file": "app/server/auth.py",
      "description": "hmac.new() is called but the correct function is hmac.new() — actually this is 'hmac.new' which does not exist in Python's hmac module. The correct call is 'hmac.new(key, msg, digestmod)'. This will throw AttributeError at runtime on every login and session token creation/verification, breaking all authentication."
    },
    {
      "severity": "high",
      "file": "app/server/sessions.py",
      "description": "run_build() is launched as asyncio.create_task() but _read_stdout and _read_stderr inner functions are defined inside the generator loop body, making them unavailable in the evaluator retry block where they are referenced. This is a NameError at runtime during evaluator retry paths."
    },
    {
      "severity": "high",
      "file": "app/server/tiers/config.py (via _deploy.py)",
      "description": "load_config() in src/tao/tiers/config.py iterates raw.get('tiers', []) but .harness/config.yaml uses an 'agents' key (dict), not a 'tiers' list. _load_harness_config() in sessions.py then checks for 'agents' key directly on the returned dict — but load_config() only returns {'total_token_budget', 'tiers'}, so 'agents' key is always missing. _select_model() will always fall back to 'sonnet'."
    },
    {
      "severity": "medium",
      "file": "app/server/main.py",
      "description": "@app.on_event('startup') is deprecated in FastAPI >= 0.93. Should use lifespan context manager pattern. Will generate DeprecationWarning in current FastAPI versions and will be removed in future versions."
    },
    {
      "severity": "medium",
      "file": "app/server/sessions.py",
      "description": "run_build() accepts 'model' parameter but _select_model() is also called inside create_session() before run_build() is invoked. The resolved model from create_session() is passed to run_build() as 'model', but run_build() also passes it directly to Claude without going through _select_model() for the evaluator phase — the evaluator model is read from config.EVALUATOR_MODEL, not from the session model. This is correct by design but undocumented and confusing."
    },
    {
      "severity": "medium",
      "file": "app/server/sessions.py",
      "description": "list_sessions() at line ~60 references 'last_phase' key but BuildSession dataclass field is 'last_completed_phase'. The dict key emitted is 'last_phase' while the spec.md and dashboard types expect 'last_completed_phase'. Inconsistent field naming between API response and internal model."
    },
    {
      "severity": "medium",
      "file": "app/server/orchestrator.py",
      "description": "_decompose_brief() imports run_cmd and _select_model from sessions.py, creating a tight circular dependency risk. orchestrator.py imports from sessions.py (create_session, em, run_cmd, BuildSession, _sessions, run_build), while sessions.py imports from brief.py. Any import ordering issue will cause ImportError at startup."
    },
    {
      "severity": "medium",
      "file": "app/server/sessions.py",
      "description": "The 'af' variable in the push phase is defined inside a try block but referenced in the final summary em() call outside the try block: 'len(af) if af in locals()'. locals() check is unreliable in async coroutines. If push phase raises an exception before 'af' is assigned, the fallback '?' is used — but this pattern is fragile and should use a pre-initialized variable."
    },
    {
      "severity": "medium",
      "file": "app/server/cron.py",
      "description": "cron_loop() imports create_session inside the loop body on every iteration ('from .sessions import create_session'). This deferred import is stated as avoiding circular dependency but actually re-executes the import machinery every 60 seconds. Should be imported once at module level with careful ordering."
    },
    {
      "severity": "medium",
      "file": "dashboard/.env.example",
      "description": "PI_CEO_PASSWORD is documented in .env.example but the actual production password 'PiDevOpsTestPassword2024' is committed in READY_TO_DEPLOY.md. The .env.example provides false security assurance."
    },
    {
      "severity": "medium",
      "file": "app/server/brief.py",
      "description": "_get_skill_context() silently swallows all exceptions with bare 'except Exception: pass'. If skills loading fails for any reason (ImportError, YAML parse error, file not found), the brief is built without skill context and no diagnostic is emitted to the session output or logs."
    },
    {
      "severity": "medium",
      "file": "_deploy.py",
      "description": "Bootstrap script uses hardcoded Windows absolute path 'C:\\Pi Dev Ops' and open() without context manager ('open(f, \"w\", ...).write(c)'). File handles are never explicitly closed. On Windows this can cause file locking issues. Also overwrites critical files like pyproject.toml with simplified versions that drop required dependencies (fastapi, uvicorn, websockets, etc. reduced to just httpx, pyyaml)."
    },
    {
      "severity": "low",
      "file": "app/server/main.py",
      "description": "Multiple route handlers use inline 'raise HTTPException' on single lines with semicolons (e.g., 'if not repo_url: raise HTTPException(400, ...)') making them hard to read and violating PEP 8. Consistent throughout main.py."
    },
    {
      "severity": "low",
      "file": "app/server/sessions.py",
      "description": "parse_event() has a bare 'except:' clause (line ~100) catching all exceptions including KeyboardInterrupt and SystemExit. Should be 'except Exception' at minimum, or more specifically 'except (json.JSONDecodeError, KeyError, TypeError)'."
    },
    {
      "severity": "low",
      "file": "CLAUDE.md",
      "description": "CLAUDE.md is entirely stub content ('Add brief project context here', 'Add details about how to set up'). Despite .harness/lessons.jsonl noting that 'CLAUDE.md is the highest single-leverage documentation fix', this file remains empty. Contradicts the system's own institutional memory."
    },
    {
      "severity": "low",
      "file": "app/server/gc.py",
      "description": "collect_garbage() pops sessions from the dict while iterating: 'for sid, session in list(sessions.items())' then 'sessions.pop(sid, None)'. This is safe due to list() copy but the function also calls persistence.delete_session_file() — if that raises OSError, the session is already removed from _sessions dict but the file remains. Error count increments but no recovery path."
    },
    {
      "severity": "low",
      "file": ".harness/anthropic-docs/api-reference.md",
      "description": "Anthropic docs files contain raw Next.js RSC serialization payloads (self.__next_f.push, $RC, etc.) rather than actual documentation content. The fetch script is scraping rendered HTML and storing JavaScript bundle chunks, not the actual markdown documentation. These files are effectively useless as reference material."
    },
    {
      "severity": "low",
      "file": "Dockerfile",
      "description": "Two Dockerfiles exist: root Dockerfile and app/Dockerfile with different WORKDIR, COPY paths, and ENV configurations. Root Dockerfile copies src/ and .harness/ and skills/ but app/Dockerfile does not. These will produce different runtime environments. No indication which is canonical for production Railway deployment."
    }
  ],
  "missingTests": [
    "Unit tests for classify_intent() — keyword priority ordering (hotfix > bug > chore > spike > feature) has no automated coverage",
    "Unit tests for verify_session_token() — expiry, tampered signature, malformed token",
    "Unit tests for _parse_evaluator_dimensions() — malformed output, missing OVERALL line, unicode em-dash variants",
    "Unit tests for _should_skip() phase resume logic",
    "Unit tests for collect_garbage() — terminal vs active session discrimination, orphan detection",
    "Integration tests for WebSocket /ws/build/{sid} — auth rejection (code 4001), session not found, streaming completion",
    "Integration tests for webhook HMAC verification — replay, wrong secret, missing header",
    "Tests for fan_out() orchestrator — decomposition failure fallback, tier escalation on worker launch failure",
    "Tests for cron trigger matching — minute/hour matching, debounce (last_fired_at within 90s), disabled trigger skip",
    "Tests for persistence.py path traversal — session ID with '../', non-alphanumeric characters",
    "Tests for brief.py build_structured_brief() — skill context injection, lesson context fallback to general lessons",
    "Tests for load_config() / _select_model() — config.yaml agent block parsing, fallback to 'sonnet'",
    "E2E test for evaluator retry loop — below-threshold first attempt triggers retry with critique injection",
    "E2E test for push retry with auth failure hard-stop vs transient network error retry"
  ],
  "securityConcerns": [
    "GitHub PAT (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) and Linear API key (lin_api_DUkmVuRx1ZdjKKfyjVlGKqLAleHmaEcpKTq7En4i) committed in READY_TO_DEPLOY.md — must be revoked immediately",
    "Production password 'PiDevOpsTestPassword2024' committed in READY_TO_DEPLOY.md and DEPLOYMENT_STATUS.md — weak, guessable, and now public",
    "CSP header includes 'unsafe-inline' for both script-src and style-src (app/server/main.py SecurityHeaders middleware) — weakens XSS protection significantly for any future inline script surface",
    "hmac.new() bug in auth.py means session token verification is broken — if this resolves to a different code path rather than AttributeError, tokens could be accepted without signature verification",
    "Webhook endpoint (POST /api/webhook) is not behind require_auth — only HMAC verification. If WEBHOOK_SECRET is empty string, the verify_github_signature check uses empty key HMAC which is cryptographically weak but non-zero",
    "claude -p subprocess receives the full brief including injected lessons and skill context as a command-line argument — visible in process listings (ps aux) on the server host",
    "Session workspace paths use sanitized IDs (_safe_sid) but workspace root is user-configurable via TAO_WORKSPACE env var — no validation that WORKSPACE_ROOT is outside web-accessible directories",
    "rate limiting keyed on request.client.host — in Railway deployment behind a proxy, all requests may share the same client.host (the proxy IP), making rate limiting ineffective. X-Forwarded-For explicitly excluded by design but this creates a DoS vector",
    "SameSite=None cookie set for cloud deployments allows cross-site request inclusion — combined with the broad CORS allowlist (*.vercel.app wildcard via TAO_ALLOWED_ORIGINS), this increases CSRF surface"
  ]
}
```