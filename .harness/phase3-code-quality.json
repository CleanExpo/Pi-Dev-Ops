```json
{
  "scores": {
    "completeness": 7,
    "correctness": 6,
    "codeQuality": 7,
    "documentation": 8
  },
  "issues": [
    {
      "severity": "critical",
      "file": "app/server/main.py",
      "line": null,
      "description": "No SSE endpoint for build log streaming exists in visible code. main.py has StreamingResponse imported and a `/api/sessions/{sid}/logs` endpoint defined, but the gap-audit JSON (.harness/board-meetings/2026-04-12-gap-audit.json) separately flags this as a critical discrepancy — the spec claims SSE streaming distinct from WebSocket, but only WebSocket /ws/build/{sid} is confirmed present. The SSE endpoint at line ~220 of main.py (the generate() function) does exist in the provided code, but the gap audit classifies it as unverified. Needs integration test to confirm."
    },
    {
      "severity": "critical",
      "file": "app/server/main.py",
      "line": null,
      "description": "Linear two-way sync is absent. webhook.py parses Linear events and generates briefs, but no code path visible in main.py or sessions.py posts build outcomes (state changes, comments) back to Linear via GraphQL issueUpdate mutation. The gap audit confirms this: 'zero outbound Linear API calls' in webhook.py. Sessions complete without closing or commenting on the originating ticket."
    },
    {
      "severity": "critical",
      "file": "app/requirements.txt",
      "line": 7,
      "description": "`claude-agent-sdk>=0.1` is listed in requirements.txt but this package name does not match the actual SDK. CLAUDE.md and config.yaml reference `claude_agent_sdk` and `ClaudeSDKClient`, and sdk-migration-plan.md references `telegram-bot/src/claude/sdk_integration.py`. The package name `claude-agent-sdk` on PyPI is likely wrong or a private package — this will cause Railway deploys to fail with a pip install error unless the correct package name is used. pyproject.toml omits this dependency entirely."
    },
    {
      "severity": "high",
      "file": "app/server/main.py",
      "line": null,
      "description": "The Telegram webhook endpoint at `/webhook/telegram` (line ~300) only handles `/ack_alert` commands. The marathon system relies on bidirectional Telegram for `fix <check>`, `brief me`, `idea:`, and `note:` routing — none of these are handled in the FastAPI webhook. They exist only in the Cowork-side `marathon_telegram_inbox.py` / `marathon_watchdog.py` scripts, which die when the Mac sleeps. The Railway-hosted server has no inbox processor."
    },
    {
      "severity": "high",
      "file": "app/server/main.py",
      "line": 468,
      "description": "The `/api/sessions/{sid}/logs` SSE endpoint uses `asyncio.sleep(0.3)` polling inside the generator. Under concurrent sessions this creates 0.3s latency per update and wastes event-loop cycles. No backpressure mechanism exists — if the client disconnects, the generator continues running until the session reaches terminal state, leaking coroutines."
    },
    {
      "severity": "high",
      "file": ".harness/agents/sdk-version-policy.md",
      "line": null,
      "description": "The SDK version policy references `ClaudeSDKClient.connect()`, `.query()`, `.receive_messages()`, `.disconnect()` as the stable API surface — but sdk-migration-plan.md warns that `_query.receive_messages()` is a private method (prefixed `_`) that should not be used. If sessions.py uses the private API, any SDK patch release can silently break all builds."
    },
    {
      "severity": "high",
      "file": "app/server/main.py",
      "line": 385,
      "description": "`_telegram_send()` uses `urllib.request.urlopen` synchronously inside an async FastAPI handler at the `/webhook/telegram` endpoint. This blocks the event loop for up to 8 seconds (the timeout) on every Telegram reply. Should be replaced with `httpx.AsyncClient` or run in a thread executor."
    },
    {
      "severity": "high",
      "file": ".harness/board-meetings/2026-04-12-gap-audit.json",
      "line": null,
      "description": "Gap audit identifies that `verify_github_signature` uses `hmac.new(...)` — the audit incorrectly self-corrects and says this is valid, but `hmac.new` is indeed the correct Python API. However the audit also flags that `restore_sessions()` is truncated mid-function in the provided source. If restore_sessions silently fails on startup, all persisted sessions are lost across Railway redeploys, contradicting the 'phase checkpoints survive restart' claim."
    },
    {
      "severity": "high",
      "file": "dashboard/app/api/telegram/route.ts",
      "line": null,
      "description": "Telegram webhook secret validation in the Next.js route passes when `incomingSecret` is null/missing even if `TELEGRAM_WEBHOOK_SECRET` is set. Per gap audit: 'the check passes regardless — this is a security gap.' Any unauthenticated POST can trigger Telegram bot commands including /build."
    },
    {
      "severity": "medium",
      "file": "app/server/main.py",
      "line": 157,
      "description": "The `on_startup` event handler is registered twice — once for `restore_sessions` + background tasks, and again for `_start_claude_poll`. FastAPI supports multiple `on_event('startup')` handlers but the ordering is implicit. If `_start_claude_poll` runs before the server socket is ready, the Claude CLI version check may race with the health endpoint."
    },
    {
      "severity": "medium",
      "file": ".harness/cron-triggers.json",
      "line": null,
      "description": "`last_fired_at` values for active scan triggers contain Unix timestamps from year ~2026 (e.g. 1775952090) but the incident report states cron timestamp bugs cause debounce failures on redeploy. The `fallback-dryrun-quarterly`, `analyse-lessons-weekly`, `intel-refresh-monday`, `board-meeting-weekly`, and `scout-weekly-monday` all show `last_fired_at: null` — meaning these have never fired in production. Board meetings reference these as active infrastructure."
    },
    {
      "severity": "medium",
      "file": "app/server/main.py",
      "line": 255,
      "description": "The `/api/webhook` endpoint returns `{skipped: True}` with HTTP 200 when a GitHub/Linear webhook has no matching event. Webhook providers (GitHub, Linear) may retry on non-2xx responses, but returning 200 for skipped events means there is no way to distinguish 'received and processed' from 'received and ignored' in webhook delivery logs."
    },
    {
      "severity": "medium",
      "file": "Dockerfile",
      "line": 22,
      "description": "The root Dockerfile copies `src/` into the image (`COPY src/ ./src/`) but pyproject.toml defines the package as `tao` sourced from that directory. app/Dockerfile does NOT copy src/. If the wrong Dockerfile is used (app/Dockerfile vs root Dockerfile), the TAO engine is silently absent and `import tao` fails at runtime with no clear error."
    },
    {
      "severity": "medium",
      "file": ".env.example",
      "line": 74,
      "description": "`NEXT_PUBLIC_SUPABASE_URL` is defined twice in .env.example — once with the actual project URL (`zbryrmxmgfmslqzizsto.supabase.co`) and once as a placeholder (`https://your-project.supabase.co`). The real URL is hardcoded in a file committed to the repo, which leaks the Supabase project identifier publicly."
    },
    {
      "severity": "medium",
      "file": "mcp/package.json",
      "line": 8,
      "description": "MCP server test script is `echo 'Error: no test specified' && exit 1`. The MCP server exposes 21 tools including Linear operations (`linear_create_issue`, etc.) with zero test coverage. A malformed Linear API response or schema change will silently return errors to Claude Desktop with no CI gate catching it."
    },
    {
      "severity": "low",
      "file": "dashboard/package.json",
      "line": 12,
      "description": "`lucide-react@^1.8.0` is an unusually high major version for this library — the stable lucide-react is currently at ~0.x in most codebases. Version `^1.8.0` may pull an incompatible breaking release or resolve to a non-existent version, causing `npm ci` to fail in CI."
    },
    {
      "severity": "low",
      "file": "app/server/main.py",
      "line": 201,
      "description": "The `_IS_CLOUD` detection checks `RAILWAY_ENVIRONMENT`, `RENDER`, and `FLY_APP_NAME` env vars but not `PORT` (Railway's primary signal). If Railway changes its env var naming, cookie `SameSite=None` + `Secure` will not be set and cross-origin dashboard authentication breaks silently."
    },
    {
      "severity": "low",
      "file": ".harness/anthropic-docs/api-reference.md",
      "line": null,
      "description": "Anthropic docs files contain raw Next.js RSC serialization payloads (self.__next_f.push JSON blobs) rather than actual documentation content. The fetch script captured the JavaScript bundle, not the rendered HTML. These files are noise and will mislead any agent reading them for API reference."
    }
  ],
  "missingTests": [
    "MCP server tools (mcp/pi-ceo-server.js) — zero tests for any of the 21 tools including Linear operations",
    "Telegram webhook handler — no test for /ack_alert flow or the missing-secret bypass vulnerability",
    "WebSocket /ws/build/{sid} endpoint — no integration test for auth rejection (code 4001) or stream termination",
    "Session resume endpoint — no test that an 'interrupted' session correctly resumes from last_completed_phase",
    "Parallel build fan-out (POST /api/build/parallel) — no test for worker coordination or partial failure",
    "Cron trigger scheduling — no test that cron_loop correctly debounces and handles last_fired_at=null on first run",
    "Linear two-way sync — no test exists because the feature doesn't exist; missing feature masquerading as missing test",
    "Auth token HMAC signing/verification roundtrip in auth.py",
    "Rate limiting behavior under concurrent requests (require_rate_limit dependency)",
    "GC loop (gc_loop) — no test that expired sessions are correctly collected and workspace directories removed",
    "SDK routing logic — no test that TAO_USE_AGENT_SDK=0 raises ImportError as documented in CLAUDE.md",
    "Vercel drift check endpoint (/api/health/vercel) — no test for the vercel_monitor module",
    "Pipeline phase sequencing — no end-to-end test from /api/spec through /api/ship"
  ],
  "securityConcerns": [
    "dashboard/app/api/telegram/route.ts: Telegram webhook accepts unauthenticated POSTs when TELEGRAM_WEBHOOK_SECRET is set but X-Telegram-Bot-Api-Secret-Token header is absent — any attacker can trigger /build commands",
    ".env.example line 74: Real Supabase project URL (zbryrmxmgfmslqzizsto.supabase.co) hardcoded and committed — leaks project identifier to anyone with repo access",
    "app/server/main.py line ~385: _telegram_send() uses synchronous urllib blocking the async event loop — not a direct security issue but creates a DoS vector where a slow Telegram API response can stall the FastAPI worker",
    "app/server/main.py: /api/webhook endpoint returns HTTP 200 for unsigned webhooks when WEBHOOK_SECRET is not configured (line ~270: raises HTTPException 500 only if secret IS configured but signature fails) — if secret is unset the endpoint processes unsigned GitHub events with no authentication",
    ".claude/settings.json: defaultMode is bypassPermissions globally — any code path that invokes Claude inherits this, meaning a prompt injection in user-supplied repo_url or brief could execute arbitrary bash commands within the allowed list (git, curl, wget, rm are all permitted)",
    "app/requirements.txt: claude-agent-sdk>=0.1 with no upper bound pin — a malicious or breaking future release would be auto-installed on Railway redeploy",
    "app/server/main.py: SESSION_SECRET auto-generated and persisted to app/data/.session-secret — if the Railway ephemeral filesystem rotates this file, all existing session tokens are invalidated but no alert is raised; users get silent 401s"
  ],
  "positives": [
    "Security headers middleware (SecurityHeaders class in main.py) correctly sets CSP, X-Frame-Options DENY, X-Content-Type-Options, and X-XSS-Protection on every response",
    "Timing-safe password comparison via hmac.compare_digest in auth.py prevents timing attacks on the login endpoint",
    "Pydantic field validators on BuildRequest/TriggerRequest/LessonRequest provide strong input validation with clear error messages at the API boundary",
    "The _resilient() wrapper pattern for background tasks with crash-recovery and auto-restart is well-designed and prevents silent background task death",
    "Atomic file writes documented in CLAUDE.md (write-to-.tmp-then-os.replace()) prevent corrupt session state on crash",
    "Three-tier CI pipeline (python + frontend + smoke-prod jobs in .github/workflows/ci.yml) with correct job dependencies and branch targeting",
    "The escalation contract (.harness/ESCALATION.md) explicitly lists what the watchdog will NEVER do autonomously — a well-reasoned safety boundary for an agentic system",
    "The gap audit infrastructure (.harness/board-meetings/2026-04-12-gap-audit.json) demonstrates genuine self-auditing capability — the system identified its own Linear sync gap and created Linear tickets for it",
    "CLAUDE.md is genuinely useful and specific — cites exact file paths, line numbers, and patterns rather than generic advice",
    "The duplicate webhook detection (_find_active_session_for_repo) prevents multiple concurrent sessions from being spawned for the same repo on rapid webhook delivery"
  ]
}
```