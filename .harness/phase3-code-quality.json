```json
{
  "scores": {
    "completeness": 7,
    "correctness": 6,
    "codeQuality": 6,
    "documentation": 8
  },
  "issues": [
    {
      "severity": "high",
      "file": "app/server/main.py",
      "description": "Two @app.on_event('startup') decorators registered (line ~120 and ~430 for _start_claude_poll). FastAPI silently runs both but this is deprecated and fragile — use lifespan context manager instead. The second startup handler is also defined after route handlers, scattered across the file."
    },
    {
      "severity": "high",
      "file": "app/server/main.py",
      "description": "_log referenced at line ~320 (telegram_webhook uses _log.warning, _log.info, _log.error) but only 'log' is defined at module level (line 21). _log is undefined — NameError at runtime on any Telegram webhook call that hits a log statement."
    },
    {
      "severity": "high",
      "file": "app/server/main.py",
      "description": "Telegram webhook endpoint POST /webhook/telegram has no rate limiting dependency — all other write endpoints use Depends(require_rate_limit) but this one is unprotected, allowing unlimited bot-token probing and replay attacks."
    },
    {
      "severity": "high",
      "file": "app/requirements.txt",
      "description": "claude-agent-sdk>=0.1 is a non-existent/unofficial package name. CLAUDE.md and sdk-version-policy.md reference 'claude_agent_sdk' as the import name, and the actual SDK is the Anthropic Python SDK. This will cause pip install failures in Docker builds unless the package exists under this name in a private registry — not documented anywhere."
    },
    {
      "severity": "high",
      "file": "app/server/main.py",
      "description": "scan endpoint POST /api/scan fires asyncio.create_task(_run()) and immediately returns {ok: True} without awaiting. The _run() coroutine catches no exceptions from run_autopr/run_autopr_all — errors are silently dropped. The caller receives 200 with no way to detect failure."
    },
    {
      "severity": "high",
      "file": ".harness/board-meetings/2026-04-12-gap-audit.json",
      "description": "Gap audit identifies that webhook.py has no outbound Linear API calls (no issueUpdate mutation, no state transitions back to Linear). This means the Linear two-way sync claimed in board minutes and CLAUDE.md ('Linear In Review transition at sessions.py:672-675') cannot be verified from visible code and the gap audit marks it critical."
    },
    {
      "severity": "high",
      "file": "app/server/main.py",
      "description": "login endpoint reads body.get('password', '') with no content-type check or Pydantic model — raw await request.json() will throw a 500 if Content-Type is not application/json. All other POST endpoints use Pydantic models; login is the outlier and the most sensitive endpoint."
    },
    {
      "severity": "medium",
      "file": "app/server/main.py",
      "description": "DRY violation: repo_url validation logic (strip + startswith check) is copy-pasted across BuildRequest, TriggerRequest Pydantic validators (lines ~45-50 and ~100-105). A shared validator or base class would eliminate this duplication."
    },
    {
      "severity": "medium",
      "file": "app/server/main.py",
      "description": "model validation ('opus|sonnet|haiku') duplicated verbatim in both BuildRequest.valid_model and TriggerRequest.valid_model validators. Should be a shared constant or mixin."
    },
    {
      "severity": "medium",
      "file": "app/server/main.py",
      "description": "_find_active_session_for_repo is a module-level function but uses the module-global _sessions dict directly without being a method or accepting it as a parameter — implicit global dependency makes it untestable in isolation."
    },
    {
      "severity": "medium",
      "file": "app/server/main.py",
      "description": "POST /api/webhook handler is 80+ lines with two completely separate code paths (GitHub vs Linear) differentiated by header presence. The function violates single responsibility and the 40-line function rule in CLAUDE.md. Should be split into handle_github_webhook() and handle_linear_webhook() helpers."
    },
    {
      "severity": "medium",
      "file": "app/server/main.py",
      "description": "ALLOWED_ORIGINS list is built at module load time from os.environ — if TAO_ALLOWED_ORIGINS changes at runtime (e.g. Railway env var update without redeploy), the CORS list is stale. This is a configuration ordering issue, not critical but inconsistent with the dynamic config pattern elsewhere."
    },
    {
      "severity": "medium",
      "file": "app/server/main.py",
      "description": "SecurityHeaders middleware uses 'unsafe-inline' in both script-src and style-src CSP directives. This negates most XSS protection the CSP provides. The README claims CSP headers as a security feature but the implementation is permissive."
    },
    {
      "severity": "medium",
      "file": "app/server/main.py",
      "description": "POST /api/scan fires a background task and returns immediately, but the _run() inner coroutine is defined as async def yet called via background_tasks.add_task(lambda: asyncio.create_task(_run())) — actually it uses asyncio.create_task(_run()) directly in the route handler, not BackgroundTasks. This creates a task attached to the event loop but with no error propagation or cancellation handle stored anywhere."
    },
    {
      "severity": "medium",
      "file": "Dockerfile",
      "description": "Two Dockerfiles exist (root Dockerfile and app/Dockerfile) with diverging content — root copies src/ and .harness/ and skills/ which app/Dockerfile does not. Neither is marked canonical. CLAUDE.md says 'Docker (backend)' without specifying which Dockerfile. The root docker build command in README.md ('cd app && docker build') would use app/Dockerfile, not the root one."
    },
    {
      "severity": "medium",
      "file": ".harness/agent-sdk-metrics/2026-04-11.jsonl",
      "description": "SDK metrics show persistent pattern of 3 entries per timestamp (1 success + 'RuntimeError: Connection failed' + timeout). The success rows all show output_len=14 (a constant stub value), session_id='', phase=''. These are not real production metrics — they appear to be synthetic test rows written during SDK development. The metrics infrastructure that CLAUDE.md claims is used for production monitoring decisions is populated with fabricated data."
    },
    {
      "severity": "medium",
      "file": "dashboard/package.json",
      "description": "lucide-react version ^1.8.0 — lucide-react's major version is currently 0.x (pre-1.0 as of knowledge cutoff). Version ^1.8.0 does not exist in the npm registry. This will cause npm install failures unless the package has released v1.x since knowledge cutoff."
    },
    {
      "severity": "medium",
      "file": "dashboard/package.json",
      "description": "next version ^16.2.3 — Next.js current major is 15.x. Version 16 does not exist. This is either a typo for 15.2.3 or a fictional version number. The build logs confirm 16.2.2/16.2.3 is actually being used (Vercel build logs show it), suggesting a private fork or internal versioning scheme not documented anywhere."
    },
    {
      "severity": "medium",
      "file": ".env.example",
      "description": "NEXT_PUBLIC_SUPABASE_URL is defined twice in the same file — once with the real Supabase project URL (zbryrmxmgfmslqzizsto.supabase.co) and once as a placeholder (https://your-project.supabase.co). The real Supabase project URL with the subdomain is committed to the repository in plain text, exposing the project identifier."
    },
    {
      "severity": "medium",
      "file": ".claude/settings.json",
      "description": "WorktreeCreate and WorktreeRemove hooks hardcode an absolute Mac path (/Users/phill-mac/Pi-CEO/Pi-Dev-Ops). These hooks will silently fail on any other machine or in Docker/Railway environments. Should use relative paths or environment variables."
    },
    {
      "severity": "low",
      "file": "app/server/main.py",
      "description": "get_monitor_digest uses glob.glob with reverse=True sort on filenames to get 'latest' — this only works correctly if filenames sort lexicographically in chronological order (YYYYMMDD-HHMM.json format does). However the import is done inside the function body (import glob as _glob) — this inline import pattern is used inconsistently throughout main.py and contradicts CLAUDE.md conventions."
    },
    {
      "severity": "low",
      "file": "app/server/main.py",
      "description": "Multiple imports done inside function bodies (from .pipeline import ..., from .scanner import ..., from .autopr import ...) scattered throughout route handlers. This delays ImportError detection to first request rather than server startup, and makes dependency graph unclear."
    },
    {
      "severity": "low",
      "file": "pyproject.toml",
      "description": "pyproject.toml lists anthropic>=0.90 but app/requirements.txt does not include anthropic — it includes claude-agent-sdk>=0.1 instead. The two dependency manifests are inconsistent. CLAUDE.md says sessions.py uses SDK-only mode, but the anthropic package version is only pinned in pyproject.toml."
    },
    {
      "severity": "low",
      "file": ".harness/anthropic-docs/api-reference.md",
      "description": "All Anthropic docs files contain raw Next.js RSC streaming payload (self.__next_f.push...) mixed with actual content — the fetch script scraped the page source including JavaScript bundles rather than extracting clean markdown. These files are essentially unreadable and uncacheable for RAG purposes, contradicting their stated purpose."
    },
    {
      "severity": "low",
      "file": "app/server/main.py",
      "description": "VERSION hardcoded as '1.0.0' in the /health response (line ~490) while package versions in pyproject.toml and mcp/package.json are 1.0.0 and 3.0.0 respectively, and dashboard/package.json is 2.0.0. No single source of truth for version number."
    }
  ],
  "missingTests": [
    "Authentication flow: login with correct/incorrect password, token expiry, cookie behavior in cloud vs local mode",
    "WebSocket /ws/build/{sid} handler: authentication rejection (code 4001), session-not-found path, streaming behavior",
    "Webhook signature verification: GitHub HMAC validation, Linear HMAC validation, missing-secret error paths",
    "Rate limiting: exceed 30 req/min per IP and verify 429 response",
    "Session lifecycle: create → build → kill → resume, interrupted status handling, phase checkpoints",
    "Parallel build fan-out: POST /api/build/parallel with n_workers validation (boundary: 0, 1, 8, 9)",
    "Cron trigger CRUD: create/list/delete triggers, minute/hour boundary validation",
    "Garbage collection: collect_garbage with active/terminal sessions, max_age enforcement",
    "Telegram webhook: /ack_alert with valid key, missing key, invalid secret header",
    "Security headers middleware: verify X-Frame-Options, CSP, X-Content-Type-Options present on all responses",
    "Pipeline endpoints: /api/spec, /api/plan, /api/test, /api/ship state machine transitions",
    "Health endpoint: degraded status when disk_free_gb is None, autonomy.armed field accuracy",
    "Autonomy poller: LINEAR_API_KEY missing → poll skip without error, first poll within 10s of startup",
    "MCP server tools: all 21 tools in pi-ceo-server.js with mock filesystem state",
    "Dashboard TypeScript: hooks/useSSE.ts exponential backoff reconnection logic",
    "SSE /api/sessions/{sid}/logs streaming: after= cursor behavior, terminal status close signal"
  ],
  "securityConcerns": [
    "Real Supabase project URL (zbryrmxmgfmslqzizsto.supabase.co) committed in .env.example — exposes project identifier publicly in the repository",
    "Telegram bot token (8630069375:AAH5CDnCm5ApdFsdfFz1KwNGEFDtgy9yvDA) and Telegram chat ID (8792816988) hardcoded in .harness/RA-588-PREFLIGHT.md — these are production credentials committed to the repository",
    "CSP allows 'unsafe-inline' for both script-src and style-src in SecurityHeaders middleware (main.py) — neutralizes XSS protection while being advertised as a security feature",
    "POST /webhook/telegram has no rate limiting (missing Depends(require_rate_limit)) unlike all other write endpoints — allows unrestricted probing",
    "login endpoint uses raw await request.json() without Pydantic validation — missing content-type enforcement on the most sensitive endpoint",
    "TELEGRAM_WEBHOOK_SECRET check in /webhook/telegram only rejects when both sides non-empty — if expected_secret is set but header is absent, the conditional short-circuits: 'if expected_secret and secret != expected_secret' passes when secret is empty string and expected_secret is truthy. This is a bypass when the client sends no secret header.",
    ".claude/settings.json defaultMode is bypassPermissions with Write(*) and Edit(*) allowing unrestricted filesystem writes — appropriate for autonomous operation but the deny list ('rm -rf /:*') uses pattern matching that may not catch all destructive variants",
    "SDK metrics in .harness/agent-sdk-metrics/ contain empty session_id fields — if these are real production metrics, it means production SDK calls are not being correlated to sessions, making security audit trails incomplete",
    "TAO_USE_FALLBACK=1 comment in .env.example describes a path that makes direct Anthropic API calls — if accidentally enabled in production, bypasses the Claude Max zero-cost model and incurs unexpected billing",
    "pyproject.toml and requirements.txt dependency split means Docker builds from app/ directory may not install the anthropic package (only claude-agent-sdk which may not exist), creating a silent fallback failure mode"
  ]
}
```