```json
{
  "scores": {
    "completeness": 7,
    "correctness": 6,
    "codeQuality": 6,
    "documentation": 8
  },
  "issues": [
    {
      "severity": "high",
      "file": "app/requirements.txt",
      "description": "Contains 'claude-agent-sdk>=0.1' which does not exist on PyPI. CLAUDE.md (line 55) confirms the package is 'claude_agent_sdk' but the actual pip package name is unverified — this will cause Railway deploys to fail silently or hard-crash at import time. Diverges from pyproject.toml which omits this dependency entirely."
    },
    {
      "severity": "high",
      "file": "app/server/main.py",
      "description": "Two @app.on_event('startup') handlers defined (lines ~75 and ~290 for _start_claude_poll). FastAPI processes both but the pattern is fragile and deprecated in Starlette 0.21+. Should be consolidated into a single lifespan context manager. The second handler also recreates a task already covered by _resilient(), risking duplicate polling loops."
    },
    {
      "severity": "high",
      "file": "app/server/main.py",
      "description": "Telegram /webhook/telegram endpoint (line ~230) conditionally rejects requests only when expected_secret is non-empty AND header doesn't match. If TELEGRAM_WEBHOOK_SECRET is unset, the endpoint accepts any caller unconditionally — unauthenticated /ack_alert calls can suppress real escalation alerts. Gap also noted in .harness/board-meetings/2026-04-12-gap-audit.json (low severity there, but the production impact is high)."
    },
    {
      "severity": "high",
      "file": "app/server/main.py",
      "description": "POST /api/scan fires asyncio.create_task(_run()) and immediately returns 200 with no session ID or handle. The caller has no way to poll for completion, retrieve errors, or determine whether the scan actually ran. The background task swallows all exceptions silently — if ProjectScanner.scan_all() raises, the error is lost."
    },
    {
      "severity": "high",
      "file": "Dockerfile",
      "description": "Root Dockerfile and app/Dockerfile are nearly identical (both install python:3.12-slim, Node 20, claude CLI, copy requirements) but diverge in COPY paths and working directories. Two maintained Dockerfiles for the same service creates drift risk. CLAUDE.md and README only document the root Dockerfile; the app/Dockerfile is an orphan that will produce a different image silently."
    },
    {
      "severity": "high",
      "file": ".harness/agents/sdk-version-policy.md",
      "description": "SDK version policy references ClaudeSDKClient.connect(), query(), receive_messages(), disconnect() as the stable API contract, but CLAUDE.md (SDK Architecture section) states the subprocess fallback was removed in RA-576 and TAO_USE_AGENT_SDK=0 raises ImportError at startup. The sdk-phase2-rollout.md still documents rollback via 'Set TAO_USE_AGENT_SDK=0' — this is a contradictory contract that will mislead operators during incidents."
    },
    {
      "severity": "medium",
      "file": "app/server/main.py",
      "description": "BuildRequest.valid_model validator accepts only 'opus', 'sonnet', 'haiku' (short aliases) but config.py and CLAUDE.md reference full model IDs like 'claude-sonnet-4-6'. The mapping between alias and full model ID is implicit — if the mapping table in config.py is wrong or missing an alias, builds silently use the wrong model with no error."
    },
    {
      "severity": "medium",
      "file": "app/server/main.py",
      "description": "POST /api/build parallel (fan_out) endpoint accepts n_workers 1–8 with no check against config.MAX_CONCURRENT_SESSIONS. Requesting 8 parallel workers when MAX_CONCURRENT_SESSIONS=3 will either fail mid-fan-out or exceed the concurrency limit depending on implementation order in orchestrator.py."
    },
    {
      "severity": "medium",
      "file": "app/server/main.py",
      "description": "POST /api/monitor background task calls run_monitor_cycle() via background_tasks.add_task(_run) where _run is an async def that calls a sync function. FastAPI's BackgroundTasks runs tasks in a thread pool for sync callables but wraps async callables differently — mixing async wrapper around a sync call in a BackgroundTask can cause thread exhaustion under load."
    },
    {
      "severity": "medium",
      "file": "dashboard/package.json",
      "description": "'lucide-react': '^1.8.0' — lucide-react is currently at 0.x versioning (latest stable ~0.468 at time of audit). Version 1.8.0 does not exist; this will fail npm ci in CI. The CI workflow (ci.yml) will silently succeed only if package-lock.json pins an older resolved version, masking the bad semver range."
    },
    {
      "severity": "medium",
      "file": ".env.example",
      "description": "NEXT_PUBLIC_SUPABASE_URL appears twice with different values ('https://zbryrmxmgfmslqzizsto.supabase.co' and 'https://your-project.supabase.co'). The real project URL is embedded in a committed example file. Combined with the Supabase project URL being a non-secret (it's the anon key that's sensitive), this is low-risk but indicates the file was edited inconsistently and will confuse new contributors."
    },
    {
      "severity": "medium",
      "file": ".harness/anthropic-docs/api-reference.md",
      "description": "The fetched Anthropic docs files (api-reference.md, claude-code-cli-reference.md, claude-code-overview.md, mcp-overview.md, models-overview.md, tool-use.md) contain raw HTML/JavaScript Next.js hydration payloads, not parsed documentation. The fetch captured the page source including self.__next_f.push() serialisation blobs. These files are committed to .harness/ and will be read by agents as context — feeding raw JS hydration data as 'documentation' will corrupt agent context windows."
    },
    {
      "severity": "medium",
      "file": "mcp/package.json",
      "description": "mcp/package.json test script is 'echo Error: no test specified && exit 1'. The CI workflow does not run MCP tests. The MCP server (pi-ceo-server.js) exposes 21 tools including Linear issue creation and harness file reads — zero test coverage on the tool that touches production Linear state."
    },
    {
      "severity": "medium",
      "file": ".harness/cron-triggers.json",
      "description": "fallback-dryrun-quarterly trigger uses 'day_of_month' and 'month' fields. The cron dispatch code in app/server/cron.py is not visible in the provided context, but cron.py must support these fields or the trigger silently never fires. The .harness/config.yaml references this trigger as a risk-register control (R-02) — a silently-skipped quarterly fallback test is a compliance gap."
    },
    {
      "severity": "low",
      "file": "dashboard/tsconfig.json",
      "description": "'scripts' directory is excluded from TypeScript compilation, but dashboard/package.json has a 'create-snapshot' script that runs 'tsx scripts/create-sandbox-snapshot.ts'. tsx bypasses tsconfig exclusions, so type errors in scripts/ won't surface in 'npx tsc --noEmit' CI check — type-unsafe scripts can be deployed without warning."
    },
    {
      "severity": "low",
      "file": "app/server/main.py",
      "description": "_telegram_send() uses urllib.request.urlopen synchronously inside an async FastAPI handler (/webhook/telegram). A slow or timing-out Telegram API call (timeout=8s) will block the event loop for up to 8 seconds. Should use httpx.AsyncClient or run in a thread executor."
    },
    {
      "severity": "low",
      "file": "app/server/main.py",
      "description": "GET /api/monitor/digest uses glob.glob() and open() synchronously in an async route handler. For large digest directories this blocks the event loop. Low impact now but will degrade under concurrent load."
    },
    {
      "severity": "low",
      "file": ".github/workflows/ci.yml",
      "description": "secrets-scan job runs 'python scripts/secrets_check.py --repo-root .' but does not install the script's dependencies first. If secrets_check.py imports any third-party package (e.g. regex, yaml) not in the stdlib, the job will fail with ImportError rather than actually scanning for secrets — a broken security gate that appears to pass."
    },
    {
      "severity": "low",
      "file": ".github/workflows/smoke_test.yml",
      "description": "Smoke test workflow and ci.yml both trigger on push to main and pull_request to main, meaning both run in parallel on every PR. The smoke_test.yml starts a local server and runs 28 checks; ci.yml python job also runs pytest. There is duplication and no deduplication — PRs run the server startup twice in parallel on the same runner, which can cause port 7777 conflicts if jobs overlap."
    },
    {
      "severity": "low",
      "file": ".harness/PERMISSIONS.md",
      "description": "PERMISSIONS.md grep-verify command (line ~130) checks for 'claude.*-p' patterns missing CLAUDE_EXTRA_FLAGS, but SDK-only mode (RA-576) removed all subprocess paths. The grep command will return zero results on a correct codebase AND on a broken codebase where subprocess calls were re-added without the flag — the verification step is now a false-negative detector."
    }
  ],
  "missingTests": [
    "app/server/webhook.py — no tests for GitHub HMAC verification, Linear HMAC verification, or the parse_linear_event/linear_issue_to_brief path",
    "app/server/autonomy.py — linear_todo_poller is a production-critical background loop with zero test coverage visible in the test suite",
    "app/server/cron.py — cron_loop dispatch logic including the fallback-dryrun-quarterly day_of_month/month field handling",
    "app/server/triage.py — TriageEngine.triage() and triage_all() which auto-create Linear tickets from scan findings",
    "app/server/scanner.py — ProjectScanner.scan_project() and scan_all() including health summary aggregation",
    "app/server/pipeline.py — all 6 Ship Chain phases (spec, plan, build, test, review, ship) including the hard gate in run_ship_phase()",
    "app/server/orchestrator.py — fan_out() parallel session creation and result aggregation",
    "mcp/pi-ceo-server.js — all 21 MCP tool handlers including Linear issue creation, harness reads, and ZTE score parsing",
    "app/server/vercel_monitor.py — check_deployment_drift() which is surfaced as a health endpoint",
    "app/server/auth.py — bcrypt/SHA-256 migration path and timing-safe comparison",
    "app/server/gc.py — garbage collection logic for session and workspace cleanup",
    "scripts/smoke_test.py — the smoke test script itself has no self-tests; a broken smoke test will silently pass the CI gate",
    "Integration test: full pipeline from POST /api/build through WebSocket /ws/build/{sid} to terminal status — no end-to-end test visible",
    "Security: Telegram webhook secret validation (both present and absent TELEGRAM_WEBHOOK_SECRET cases)"
  ],
  "securityConcerns": [
    "app/server/main.py /webhook/telegram: unauthenticated when TELEGRAM_WEBHOOK_SECRET is unset — any caller can invoke /ack_alert and silence production escalation alerts",
    ".env.example: real Supabase project URL ('zbryrmxmgfmslqzizsto') committed to repo; combined with SUPABASE_SERVICE_ROLE_KEY accidentally set, this would be a full database credential leak",
    ".claude/settings.json: 'defaultMode': 'bypassPermissions' with Write(*), Edit(*), Bash(rm:*), Bash(mv:*), Bash(curl:*) granted globally — any Claude session spawned in this working directory has unrestricted filesystem write and network access; a prompt injection in any analysed repo brief could execute arbitrary writes",
    "app/server/main.py ContentSecurityPolicy: 'script-src: self unsafe-inline' — inline script execution permitted, negating XSS protection for the dashboard served from the same origin as the authenticated API",
    "app/requirements.txt: 'claude-agent-sdk>=0.1' is an unpinned, potentially non-existent package; if a malicious package of this name is published to PyPI it would be installed automatically on next Railway deploy (dependency confusion attack surface)",
    ".harness/ESCALATION.md rule 5: 'Call any paid API (OpenAI, Anthropic) in a loop without a circuit breaker' is listed as a never-do item, but no circuit breaker implementation is visible in sessions.py or the SDK runner — the guardrail is documented but not enforced in code",
    "app/server/main.py POST /api/webhook: when WEBHOOK_SECRET or LINEAR_WEBHOOK_SECRET is unset, returns HTTP 500 with 'Webhook secret not configured' — this leaks configuration state to unauthenticated callers; should return 401 regardless",
    ".harness/anthropic-docs/*.md: raw Next.js hydration JavaScript stored in .harness/ and read by agents as documentation context — if an attacker can influence the fetched page content (MITM or Anthropic CDN compromise), malicious JavaScript payloads would be injected into agent context windows"
  ]
}
```