{"ts": "2026-04-07T00:00:00Z", "source": "architecture-review", "category": "persistence", "lesson": "_sessions is an in-memory dict — any server restart loses all running sessions. Always persist status to disk atomically after every state change.", "severity": "warn"}
{"ts": "2026-04-07T00:00:01Z", "source": "architecture-review", "category": "persistence", "lesson": "Use write-to-.tmp-then-os.replace() for JSON file writes. os.replace() is atomic on NTFS and POSIX — a crash mid-write leaves the old file intact, not a corrupt half-written one.", "severity": "info"}
{"ts": "2026-04-07T00:00:02Z", "source": "architecture-review", "category": "security", "lesson": "Always sanitise session IDs before using them in file paths. re.sub(r'[^a-zA-Z0-9]', '', sid) prevents path traversal attacks (e.g. sid='../../etc/passwd').", "severity": "warn"}
{"ts": "2026-04-07T00:00:03Z", "source": "architecture-review", "category": "security", "lesson": ".claude/settings.local.json should never be committed to git — it contains local tool allowlists specific to one machine and is a minor info-leak if shared.", "severity": "warn"}
{"ts": "2026-04-07T00:00:04Z", "source": "architecture-review", "category": "rate-limit", "lesson": "_req_log in auth.py accumulates IP keys forever if never cleaned up. Prune stale IPs (last request >120s ago) every 5 minutes inline inside check_rate_limit() — no background task needed in asyncio.", "severity": "warn"}
{"ts": "2026-04-07T00:00:05Z", "source": "architecture-review", "category": "gc", "lesson": "app/workspaces/ fills disk if completed sessions are never cleaned. Terminal sessions (complete/failed/killed/interrupted) should be GC'd after GC_MAX_AGE (default 4h). Also scan for orphan dirs not referenced by _sessions.", "severity": "warn"}
{"ts": "2026-04-07T00:00:06Z", "source": "architecture-review", "category": "windows", "lesson": "On Windows, asyncio.create_subprocess_exec requires the event loop to be ProactorEventLoop (default in Python 3.8+ on Windows). Never use SelectorEventLoop — it doesn't support subprocess pipes.", "severity": "info"}
{"ts": "2026-04-07T00:00:07Z", "source": "architecture-review", "category": "deployment", "lesson": "Railway terminates TLS and proxies requests. Do NOT add TrustedHostMiddleware restricting to 127.0.0.1 — it blocks all cloud traffic. Use ALLOWED_ORIGINS for CORS control instead.", "severity": "warn"}
{"ts": "2026-04-07T00:00:08Z", "source": "architecture-review", "category": "claude", "lesson": "The TAO engine in src/tao/ is stub scaffolding not used by the web server. All intelligence is delegated to 'claude -p' subprocess via stream-json output. Parse events: system, assistant (text+tool_use), tool_result, result.", "severity": "info"}
{"ts": "2026-04-07T00:00:09Z", "source": "architecture-review", "category": "claude", "lesson": "claude -p subprocess mode requires Claude Code CLI in PATH. In cloud (Railway), Claude Code is NOT available — cloud deployments must use ANTHROPIC_API_KEY + SDK mode. Check ANALYSIS_MODE env var.", "severity": "warn"}
{"ts": "2026-04-07T00:00:10Z", "source": "architecture-review", "category": "architecture", "lesson": "CLAUDE.md is loaded into every future Claude session. Keeping it accurate and detailed (file map, conventions, smoke tests) eliminates all ramp-up time for AI sessions and is the highest single-leverage documentation fix.", "severity": "info"}
{"ts": "2026-04-07T00:00:11Z", "source": "architecture-review", "category": "architecture", "lesson": "Pi Dev Ops is a split-screen companion: Left = Claude Desktop CLI writing code; Right = Pi Dev Ops orchestrating, tracking, and pushing to Linear. It is NOT a standalone IDE. Every architectural decision should serve this companion model.", "severity": "info"}
{"ts": "2026-04-08T00:00:00Z", "source": "cycle-3-audit", "category": "architecture", "lesson": "All Priority 1+2 items (13 issues) completed in a single overnight session. Pattern: full-clearance sprint with no authority gates produces 10x throughput vs issue-by-issue execution.", "severity": "info"}
{"ts": "2026-04-08T00:00:01Z", "source": "cycle-3-audit", "category": "architecture", "lesson": "ZTE score moved 35→50 in two sprints (P1+P2). The highest leverage actions were: CLAUDE.md population (+4 pts total across sessions), evaluator tier (+2), PITER+ADW (+4), webhook triggers (+2).", "severity": "info"}
{"ts": "2026-04-08T00:00:02Z", "source": "cycle-3-audit", "category": "claude", "lesson": "MCP SDK subpath imports are required: '@modelcontextprotocol/sdk/server/mcp.js' and '.../server/stdio.js'. The top-level '@modelcontextprotocol/sdk' import does not export McpServer directly.", "severity": "warn"}
{"ts": "2026-04-08T00:00:03Z", "source": "cycle-3-audit", "category": "security", "lesson": "Webhook HMAC verification must use hmac.compare_digest() (timing-safe). Both GitHub (x-hub-signature-256: sha256=<hex>) and Linear (Linear-Signature: <hex>) use HMAC-SHA256 but with different header formats.", "severity": "warn"}
{"ts": "2026-04-08T00:00:04Z", "source": "cycle-3-audit", "category": "architecture", "lesson": "The sandbox IS the workspace directory (app/workspaces/{session_id}/). Each session gets an isolated clone. Sandbox enforcement = verify workspace exists before Phase 4 and refuse to run claude -p outside it.", "severity": "info"}
{"ts": "2026-04-08T09:18:13Z", "source": "smoke-test", "category": "smoke-test", "lesson": "Smoke test lesson \u2014 safe to delete", "severity": "info"}
{"ts": "2026-04-08T09:19:19Z", "source": "smoke-test", "category": "smoke-test", "lesson": "Smoke test lesson \u2014 safe to delete", "severity": "info"}
{"ts": "2026-04-09T02:48:44Z", "source": "smoke-test", "category": "smoke-test", "lesson": "Smoke test lesson \u2014 safe to delete", "severity": "info"}
