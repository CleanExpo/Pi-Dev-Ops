# Improvement Proposal: Security

**Generated:** 2026-04-10  
**Source:** lessons.jsonl — 3 entries (3 warnings)  
**Proposed action:** Create a SKILL.md entry  
**Target:** new skill file: `skills/SECURITY.md`

## Recurring Lessons (3 occurrences)

- ⚠️ **[architecture-review]** Always sanitise session IDs before using them in file paths. re.sub(r'[^a-zA-Z0-9]', '', sid) prevents path traversal attacks (e.g. sid='../../etc/passwd').
- ⚠️ **[architecture-review]** .claude/settings.local.json should never be committed to git — it contains local tool allowlists specific to one machine and is a minor info-leak if shared.
- ⚠️ **[cycle-3-audit]** Webhook HMAC verification must use hmac.compare_digest() (timing-safe). Both GitHub (x-hub-signature-256: sha256=<hex>) and Linear (Linear-Signature: <hex>) use HMAC-SHA256 but with different header formats.

## Proposed Content

Add a new skill or expand an existing one covering these security patterns:

```markdown
# SKILL: Security Best Practices

## When to apply
Whenever code touches security-related logic.

## Rules
- Always sanitise session IDs before using them in file paths. re.sub(r'[^a-zA-Z0-9]', '', sid) prevents path traversal attacks (e.g. sid='../../etc/passwd').
- .claude/settings.local.json should never be committed to git — it contains local tool allowlists specific to one machine and is a minor info-leak if shared.
- Webhook HMAC verification must use hmac.compare_digest() (timing-safe). Both GitHub (x-hub-signature-256: sha256=<hex>) and Linear (Linear-Signature: <hex>) use HMAC-SHA256 but with different header formats.
```

## Review Required

This proposal was auto-generated. A human must review and apply it.
Close this Linear ticket when applied or explicitly rejected.