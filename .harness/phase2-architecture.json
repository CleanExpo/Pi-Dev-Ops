```json
{
  "techStack": [
    "Python 3.11+",
    "FastAPI",
    "Uvicorn",
    "WebSockets",
    "Next.js 16",
    "React 19",
    "TypeScript",
    "Tailwind CSS",
    "Node.js 20",
    "@anthropic-ai/sdk",
    "@modelcontextprotocol/sdk",
    "@octokit/rest",
    "Supabase (PostgreSQL + pgvector)",
    "claude_agent_sdk",
    "xterm.js",
    "Docker",
    "Railway",
    "Vercel",
    "GitHub Actions",
    "Zod",
    "bcrypt",
    "httpx",
    "pyyaml",
    "Telegram Bot API"
  ],
  "pattern": "other",
  "entryPoints": [
    "app/server/main.py",
    "dashboard/app/",
    "mcp/pi-ceo-server.js",
    "scripts/marathon_watchdog.py",
    "scripts/marathon_telegram_inbox.py"
  ],
  "keyDependencies": [
    "fastapi>=0.111",
    "uvicorn[standard]>=0.30",
    "anthropic>=0.90",
    "claude-agent-sdk>=0.1",
    "next@^16.2.3",
    "react@^19.0.0",
    "@anthropic-ai/sdk@^0.36.3",
    "@modelcontextprotocol/sdk@^1.29.0",
    "@supabase/supabase-js@^2.101.1",
    "@octokit/rest@^21.0.2",
    "websockets>=12",
    "bcrypt>=4.0",
    "httpx>=0.27",
    "zod@^4.3.6",
    "xterm@^5.3.0"
  ],
  "dataFlow": "User submits repo URL + brief via Next.js dashboard → POST /api/build on FastAPI backend → create_session() spawns async build via Claude Agent SDK (claude_agent_sdk, TAO_USE_AGENT_SDK=1 mandatory as of RA-576) → 5-phase pipeline (clone → plan → generate → evaluate → ship) executes in isolated workspace → real-time output streamed to browser via WebSocket /ws/build/{sid} and SSE /api/sessions/{sid}/logs → evaluator grades diff and gates merge → session state persisted to disk (write-to-.tmp-then-os.replace()) and Supabase → Linear ticket updated via webhook.py → git push via GitHub PAT. Parallel inbound paths: GitHub/Linear webhooks trigger builds autonomously; Telegram bot (@piceoagent_bot) provides mobile control; autonomy poller (autonomy.py, 5-min cron) picks up Linear Todo issues; Pi-SEO scanner writes findings to .harness/scan-results/ and triage engine creates Linear tickets; MCP server (mcp/pi-ceo-server.js, stdio JSON-RPC 2.0) exposes 21 tools to Claude Desktop.",
  "architectureNotes": "Pi-CEO is a Tiered Agent Orchestrator (TAO) with three distinct runtime environments: Railway (always-on FastAPI backend + autonomy poller + cron scheduler via cron-triggers.json), Vercel (Next.js dashboard), and local MCP server (stdio). The system is SDK-only as of Sprint 8/RA-576 — subprocess fallback removed, TAO_USE_AGENT_SDK=0 raises ImportError at startup by design. The .harness/ directory functions as the persistent state layer (config.yaml, lessons.jsonl, cron-triggers.json, scan-results/, board-meetings/) while Supabase (gate_checks, alert_escalations, triage_log tables) provides the append-only observability log — all Supabase writes are fire-and-forget to prevent blocking the build pipeline."
}
```