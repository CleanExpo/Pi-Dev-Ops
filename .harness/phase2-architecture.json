```json
{
  "techStack": [
    "Python 3.11+",
    "FastAPI",
    "Uvicorn",
    "WebSockets",
    "Next.js 16",
    "React 19",
    "TypeScript",
    "Tailwind CSS",
    "Node.js 20",
    "Claude Code CLI (@anthropic-ai/claude-code)",
    "@anthropic-ai/sdk",
    "@octokit/rest",
    "@modelcontextprotocol/sdk",
    "@supabase/supabase-js",
    "xterm.js",
    "Docker",
    "Hatchling (Python build)",
    "PyYAML",
    "HTTPX",
    "Zod"
  ],
  "pattern": "other",
  "entryPoints": [
    "app/server/main.py",
    "mcp/pi-ceo-server.js",
    "dashboard/app/(main)/dashboard/page.tsx",
    "app/run.ps1",
    "Dockerfile"
  ],
  "keyDependencies": [
    "fastapi>=0.111",
    "uvicorn[standard]>=0.30",
    "websockets>=12",
    "next@^16.2.2",
    "react@^19.0.0",
    "@anthropic-ai/sdk@^0.36.3",
    "@octokit/rest@^21.0.2",
    "@modelcontextprotocol/sdk@^1.29.0",
    "@supabase/supabase-js@^2.101.1",
    "xterm@^5.3.0",
    "pyyaml>=6",
    "httpx>=0.27",
    "zod@^4.3.6"
  ],
  "dataFlow": "User submits repo URL + brief via Next.js dashboard (Vercel) → POST /api/build to FastAPI backend (Railway/local) → run_build() spawns 'claude -p' subprocess in isolated workspace → stdout parsed as stream-json events → emitted via WebSocket /ws/build/{sid} to browser terminal → evaluator pass (second claude -p) grades diff → git push back to GitHub. Parallel path: GitHub/Linear webhooks or cron triggers fire create_session() without UI involvement. MCP server (stdio JSON-RPC) reads .harness/ files and exposes tools to Claude Desktop.",
  "architectureNotes": "Three-component system: (1) FastAPI backend handles session orchestration, auth, webhook ingestion, and subprocess management — all AI work is delegated to 'claude -p' CLI subprocesses, never direct SDK calls, to exploit zero-cost Claude Max subscription; (2) Next.js dashboard on Vercel is the human interface with a dual-mode execution path (CLI via backend or direct Anthropic SDK); (3) stdio MCP server bridges Claude Desktop to harness state files. The TAO engine in src/tao/ provides schemas, skill registry, and budget tracking but is thin scaffolding — actual intelligence lives in the subprocess calls. Session state is in-memory dict (_sessions) with atomic JSON persistence to disk, making multi-server deployment impossible by design."
}
```