```json
{
  "techStack": [
    "Python 3.11+",
    "FastAPI",
    "Uvicorn",
    "WebSockets",
    "Next.js 16",
    "React 19",
    "TypeScript",
    "Tailwind CSS",
    "Node.js 20",
    "@anthropic-ai/sdk",
    "@modelcontextprotocol/sdk",
    "@octokit/rest",
    "@supabase/supabase-js",
    "xterm.js",
    "Supabase (PostgreSQL + pgvector)",
    "Docker",
    "Claude Agent SDK (claude_agent_sdk)",
    "Ollama (Qwen 3 14B / Llama 3.3 8B)",
    "n8n",
    "Telegram Bot API",
    "Linear API",
    "GitHub Actions",
    "Railway",
    "Vercel",
    "zod",
    "bcrypt",
    "httpx",
    "pyyaml"
  ],
  "pattern": "other",
  "entryPoints": [
    "app/server/main.py",
    "dashboard/app/",
    "mcp/pi-ceo-server.js",
    "scripts/marathon_watchdog.py",
    "scripts/marathon_telegram_inbox.py"
  ],
  "keyDependencies": [
    "fastapi>=0.111",
    "uvicorn[standard]>=0.30",
    "anthropic>=0.90",
    "claude-agent-sdk",
    "next@^16.2.3",
    "react@^19.0.0",
    "@anthropic-ai/sdk@^0.36.3",
    "@modelcontextprotocol/sdk@^1.29.0",
    "@octokit/rest@^21.0.2",
    "@supabase/supabase-js@^2.101.1",
    "websockets>=12",
    "bcrypt>=4.0",
    "httpx>=0.27",
    "zod@^4.3.6"
  ],
  "dataFlow": "User submits a repo URL + brief via Next.js dashboard → POST /api/build on FastAPI backend → create_session() spawns an async build task → Claude Agent SDK (SDK-only mode, TAO_USE_AGENT_SDK=1) executes a 5-phase pipeline (plan→generate→evaluate→test→ship) inside a cloned workspace → output streamed to browser via WebSocket /ws/build/{sid} and SSE /api/sessions/{sid}/logs → evaluated output committed and pushed to GitHub via fine-grained PAT → results logged to Supabase tables (gate_checks, claude_api_costs) → MCP server (pi-ceo-server.js) exposes analysis artifacts to Claude Desktop via stdio JSON-RPC 2.0 → autonomy poller (linear_todo_poller) polls Linear every 5 min and auto-creates sessions for Urgent/High Todo tickets → Pi-SEO scanner runs on cron (12 triggers in cron-triggers.json) scanning 10+ repos → findings triaged to Linear via TriageEngine → critical alerts sent to Telegram via marathon_watchdog.py → Telegram inbound messages processed by marathon_telegram_inbox.py and routed by marathon_watchdog.py.",
  "architectureNotes": "This is a Tiered Agent Orchestrator (TAO) — a three-layer Claude hierarchy (Opus 4.6 as planner, Sonnet 4.6 as generator/evaluator, Haiku 4.5 as worker) wrapped in a FastAPI backend, Next.js dashboard, and stdio MCP server. The system is split across three deployment environments: Railway (always-on FastAPI + autonomy poller + Pi-SEO crons), Vercel (Next.js dashboard), and a planned Mac Mini local brain (Ollama + n8n + Supabase). The critical architectural constraint is SDK-only execution: subprocess claude -p fallback paths were removed in Sprint 8 (RA-576), so TAO_USE_AGENT_SDK=1 is mandatory and misconfiguration raises ImportError at startup. State is ephemeral (in-memory _sessions dict) with atomic disk persistence via write-to-tmp-then-os.replace() pattern, backed by Supabase for cross-session observability."
}
```