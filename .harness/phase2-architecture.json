```json
{
  "techStack": [
    "Python 3.11+",
    "FastAPI",
    "Uvicorn",
    "WebSockets",
    "Next.js 16.2.x",
    "React 19",
    "TypeScript 5",
    "Tailwind CSS 3",
    "Node.js 20 (MCP server)",
    "@modelcontextprotocol/sdk",
    "@anthropic-ai/sdk",
    "@octokit/rest",
    "@supabase/supabase-js",
    "claude_agent_sdk (Python)",
    "xterm.js",
    "Supabase (PostgreSQL + pgvector)",
    "Docker",
    "Railway (backend deployment)",
    "Vercel (dashboard deployment)",
    "GitHub Actions (CI/CD)",
    "Telegram Bot API",
    "Linear API (GraphQL)",
    "bcrypt",
    "hatchling (build backend)"
  ],
  "pattern": "other",
  "entryPoints": [
    "app/server/main.py",
    "dashboard/app/",
    "mcp/pi-ceo-server.js",
    "scripts/marathon_watchdog.py",
    "scripts/marathon_telegram_inbox.py"
  ],
  "keyDependencies": [
    "fastapi>=0.111",
    "uvicorn[standard]>=0.30",
    "anthropic>=0.90",
    "claude-agent-sdk>=0.1",
    "httpx>=0.27",
    "next@^16.2.3",
    "react@^19.0.0",
    "@anthropic-ai/sdk@^0.36.3",
    "@octokit/rest@^21.0.2",
    "@supabase/supabase-js@^2.101.1",
    "@modelcontextprotocol/sdk@^1.29.0",
    "zod@^4.3.6",
    "bcrypt>=4.0",
    "pyyaml>=6"
  ],
  "dataFlow": "User submits a GitHub repo URL + brief via the Next.js dashboard (Vercel) → authenticated POST /api/build hits the FastAPI backend (Railway) → backend clones the repo into a sandboxed workspace, spawns a Claude Agent SDK session running a 5-phase pipeline (plan→generate→evaluate→test→ship) → live output streams to the browser via WebSocket (/ws/build/{sid}) or SSE (/api/sessions/{sid}/logs) → completed diffs are committed and pushed to GitHub; in parallel, a Linear autonomy poller (linear_todo_poller) fires build sessions from Linear Todo issues, a Pi-SEO scanner feeds findings through a triage engine that auto-creates Linear tickets, a marathon watchdog monitors health and pushes Telegram alerts, and an MCP server (mcp/pi-ceo-server.js) exposes harness state to Claude Desktop via stdio JSON-RPC 2.0.",
  "architectureNotes": "The system is a multi-layered autonomous engineering platform following a Tiered Agent Orchestrator (TAO) pattern: Opus 4.6 plans/decomposes, Sonnet 4.6 executes complex work, Haiku 4.5 handles discrete tasks. The deployment topology is deliberately split — Railway hosts the always-on FastAPI backend with background pollers (Linear autonomy, cron triggers, Pi-SEO scanner), Vercel hosts the Next.js dashboard, and GitHub Actions owns test-truth (ci.yml runs pytest + ruff + tsc + eslint + post-deploy smoke test). A known architectural debt documented in .harness/INCIDENT-2026-04-12.md is that scheduled-task rails historically ran inside ephemeral Cowork sandboxes (dying when the laptop sleeps), with the board deliberation recommending migration to GH Actions as scheduler + Railway as thin state store + Claude Agent SDK as headless executor."
}
```