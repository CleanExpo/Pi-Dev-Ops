```json
{
  "techStack": [
    "Python 3.11+",
    "FastAPI",
    "Uvicorn",
    "WebSockets",
    "Next.js 16",
    "React 19",
    "TypeScript",
    "Tailwind CSS",
    "Claude Max (Opus 4.6 / Sonnet 4.6 / Haiku 4.5)",
    "@anthropic-ai/sdk",
    "@octokit/rest",
    "Node.js 20",
    "Docker"
  ],
  "pattern": "other",
  "entryPoints": [
    "app/server/main.py",
    "dashboard/app/page.tsx",
    "dashboard/app/(main)/dashboard/page.tsx",
    "dashboard/app/api/analyze/route.ts",
    "mcp/pi-ceo-server.js",
    "app/run.ps1"
  ],
  "keyDependencies": [
    "fastapi>=0.111",
    "uvicorn[standard]>=0.30",
    "websockets>=12",
    "httpx>=0.27",
    "pyyaml>=6",
    "next@^16.2.2",
    "react@^19.0.0",
    "@anthropic-ai/sdk@^0.36.3",
    "@octokit/rest@^21.0.2",
    "tailwindcss@^3.4.17"
  ],
  "dataFlow": "Browser → Next.js dashboard (Vercel) → SSE stream via /api/analyze → Octokit fetches repo context → Claude CLI subprocess (claude -p) or SDK streams 8-phase analysis output → results pushed to GitHub branch via Octokit → PR created. Separately: Browser → FastAPI backend (localhost:7777) → POST /api/build → asyncio task → git clone → claude -p subprocess → WebSocket stream → git push. MCP server reads .harness/ files and exposes them to Claude Desktop via stdio JSON-RPC.",
  "architectureNotes": "Dual-surface architecture: a FastAPI backend (localhost or Docker) handles agentic build sessions via WebSocket streaming, while a Next.js 16 app (Vercel) runs 8-phase repo analysis via SSE using either the claude CLI or Anthropic SDK. All AI execution is deliberately delegated to the claude CLI subprocess to run inside the Claude Max subscription at zero token cost — the Python src/tao/ orchestration engine is scaffolded but not yet wired into any execution path. A stdio JSON-RPC 2.0 MCP server bridges Claude Desktop to .harness/ analysis artifacts."
}
```