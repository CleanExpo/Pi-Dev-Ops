Reading additional input from stdin...
2026-08-01T06:24:27.774916Z ERROR codex_core::session::session: failed to load skill C:\Users\Disaster Recovery 4\.agents\skills\seo-content\SKILL.md: missing YAML frontmatter delimited by ---
2026-08-01T06:24:27.774983Z ERROR codex_core::session::session: failed to load skill C:\Users\Disaster Recovery 4\.agents\skills\seo-images\SKILL.md: missing YAML frontmatter delimited by ---
2026-08-01T06:24:27.774994Z ERROR codex_core::session::session: failed to load skill C:\Users\Disaster Recovery 4\.agents\skills\seo-programmatic\SKILL.md: missing YAML frontmatter delimited by ---
2026-08-01T06:24:27.775002Z ERROR codex_core::session::session: failed to load skill C:\Users\Disaster Recovery 4\.agents\skills\seo-schema\SKILL.md: missing YAML frontmatter delimited by ---
2026-08-01T06:24:27.775201Z ERROR codex_core::session::session: failed to load skill C:\Users\Disaster Recovery 4\.agents\skills\seo-technical\SKILL.md: missing YAML frontmatter delimited by ---
2026-08-01T06:24:27.775211Z ERROR codex_core::session::session: failed to load skill C:\Users\Disaster Recovery 4\.codex\plugins\cache\openai-curated\build-web-data-visualization\11c74d6b\skills\accessibility-and-inclusive-visualization\SKILL.md: invalid name: exceeds maximum length of 64 characters
2026-08-01T06:24:27.775216Z ERROR codex_core::session::session: failed to load skill C:\Users\Disaster Recovery 4\.codex\plugins\cache\openai-curated\build-web-data-visualization\11c74d6b\skills\dashboards-and-real-time-visualization\SKILL.md: invalid name: exceeds maximum length of 64 characters
2026-08-01T06:24:27.775220Z ERROR codex_core::session::session: failed to load skill C:\Users\Disaster Recovery 4\.codex\plugins\cache\openai-curated\build-web-data-visualization\11c74d6b\skills\geospatial-and-cartographic-visualization\SKILL.md: invalid name: exceeds maximum length of 64 characters
2026-08-01T06:24:27.775224Z ERROR codex_core::session::session: failed to load skill C:\Users\Disaster Recovery 4\.codex\plugins\cache\openai-curated\build-web-data-visualization\11c74d6b\skills\grammar-of-graphics-and-declarative-visualization\SKILL.md: invalid name: exceeds maximum length of 64 characters
2026-08-01T06:24:27.775227Z ERROR codex_core::session::session: failed to load skill C:\Users\Disaster Recovery 4\.codex\plugins\cache\openai-curated\build-web-data-visualization\11c74d6b\skills\scrollytelling-and-parallax-data-visualization\SKILL.md: invalid name: exceeds maximum length of 64 characters
2026-08-01T06:24:27.775231Z ERROR codex_core::session::session: failed to load skill C:\Users\Disaster Recovery 4\.codex\plugins\cache\openai-curated\build-web-data-visualization\11c74d6b\skills\statistical-and-uncertainty-visualization\SKILL.md: invalid name: exceeds maximum length of 64 characters
2026-08-01T06:24:27.775234Z ERROR codex_core::session::session: failed to load skill C:\Users\Disaster Recovery 4\.codex\plugins\cache\openai-curated\build-web-data-visualization\11c74d6b\skills\typescript-data-visualization-engineering\SKILL.md: invalid name: exceeds maximum length of 64 characters
2026-08-01T06:24:27.775238Z ERROR codex_core::session::session: failed to load skill C:\Users\Disaster Recovery 4\.codex\plugins\cache\openai-curated\build-web-data-visualization\11c74d6b\skills\uml-and-software-architecture-visualization\SKILL.md: invalid name: exceeds maximum length of 64 characters
2026-08-01T06:24:27.775241Z ERROR codex_core::session::session: failed to load skill C:\Users\Disaster Recovery 4\.codex\plugins\cache\openai-curated\heygen\11c74d6b\skills\heygen-avatar\SKILL.md: invalid description: exceeds maximum length of 1024 characters
2026-08-01T06:24:27.775245Z ERROR codex_core::session::session: failed to load skill C:\Users\Disaster Recovery 4\.codex\plugins\cache\openai-curated\heygen\11c74d6b\skills\heygen-video\SKILL.md: invalid description: exceeds maximum length of 1024 characters
OpenAI Codex v0.134.0
--------
workdir: D:\Pi-Dev-Ops
model: gpt-5.5
provider: openai
approval: never
sandbox: workspace-write [workdir, /tmp, $TMPDIR]
reasoning effort: high
reasoning summaries: none
session id: 019fbbfe-cb17-7be3-8200-653ed4fcc5ef
--------
user
Read D:\Pi-Dev-Ops\.harness\cc-01-review-brief.md and follow it exactly. Read the diff file it names. Output ONLY '## Standards', '## Spec', then the VERDICT line.
2026-08-01T06:24:28.659900Z ERROR rmcp::transport::worker: worker quit with fatal: Transport channel closed, when AuthRequired(AuthRequiredError { www_authenticate_header: "Bearer realm=\"OAuth\", resource_metadata=\"https://mcp.linear.app/.well-known/oauth-protected-resource/mcp\", error=\"invalid_token\", error_description=\"Missing or invalid access token\"" })
hook: SessionStart
hook: SessionStart Failed
hook: UserPromptSubmit
hook: UserPromptSubmit Completed
exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -LiteralPath 'D:\\Pi-Dev-Ops\\.harness\\cc-01-review-brief.md'" in D:\Pi-Dev-Ops
 succeeded in 327ms:
# Review brief â€” capability 1 of 6, hermes-control-panel port (REVISED SPEC)

You are reviewing a code change. **Flag findings. Do not fix anything. Do not write code.**

## Inputs (these four only)

1. **This spec** (below)
2. **The loop** (below)
3. **The diff**: `D:\Pi-Dev-Ops\.harness\cc-01.diff` â€” read it
4. **The test**: `dashboard/__tests__/command-centre-readonly.test.ts` and `command-centre-provenance.json`, both in the diff

## Spec â€” revised 2026-08-01 by founder ruling

A capability is being ported between two Next.js apps.

**Source:** `Authority-Site/apps/web/src/app/(founder)/founder/command-centre/hermes-control-panel/`
**Target:** `Pi-Dev-Ops/dashboard/app/(main)/command-centre/hermes/`

**The governing instruction is: port faithfully, including existing behaviour.** This migration moves behaviour between apps. It does not improve it. A difference from the source is a defect *whether or not the source's own behaviour is ideal*.

- **R1. Behaviour matches the source.** Rendered output, data, and controls are as the source produces them.
  - **Known and intentional:** the page renders the first 8 of 13 registry modules with a `+N more` line. **This cap is in the source** (`DECK_LIST_CAP`) and is preserved deliberately. It is logged as KI-001 and will be fixed as separate work with its own review. **Do not report the cap as a defect of this port.** Do report it if the port's cap *differs* from the source's.
- **R2. No new network surface.** The port introduces no network/DB/execution construct the source file did not already contain.
- **R3. No new npm dependencies.**
- **R4. Typechecks and builds.**
- **R5. Import paths correctly rewritten for the target's `@/*` â†’ dashboard-root alias.**
- **R6. No new write path, paid-API call, or production-host reach relative to the source.**

### What is deliberately NOT claimed

**This spec does not claim the page makes no network call, and you should not evaluate it as if it did.**

That is an unbounded negative â€” there is always another path (dynamic import, `require`, `WebSocket`, `sendBeacon`, server actions, transitive side effects), so no evidence could ever satisfy it. Three prior attempts were correctly failed for exactly this reason, and the finding was right each time: **the spec was the defect, not the code.**

The claim now under review is bounded and diff-relative: **"introduces no network surface the source did not have."** Judge that claim. If you believe it is the wrong claim to be making, say so as a separate remark â€” but assess the work against the claim actually made.

## Loop

```
npx tsc --noEmit                                            -> exit 0
npm run build                                               -> exit 0, route /command-centre/hermes present
npx vitest run __tests__/command-centre-readonly.test.ts     -> 15 passed
```

## Test â€” what it does and its proven capability

Walks the transitive import graph from each capability page, then for each ported file compares the **count** of tracked constructs against its declared source baseline. The port fails if any count **increases**. Tracked: `fetch`, http clients, `WebSocket`/`EventSource`/`XMLHttpRequest`/`sendBeacon`, dynamic `import(`, `require(`, remote-host literals, DB writes, DB client construction, MCP client, `"use server"`, paid API key names.

Baselines are declared per-file in `command-centre-provenance.json`. **A file with no declared origin fails the test**, so nothing can enter the capability surface without a stated baseline.

**Proven capable, not assumed:** a real `fetch('https://evil.example.com')` was planted into the ported page; the suite failed 2 tests with `network: fetch increased vs baseline`. Removing it returned all 15 to green. The check can return non-null.

**Stated limits:** the comparison runs only where the Authority-Site checkout exists; in CI it warns `[BASELINE UNAVAILABLE]` and the comparison does not run. Counting is token-based, so it bounds *added* constructs, not obfuscated ones.

## Prior rounds

Attempts 1â€“3 were failed under the **old** spec, which demanded absolute read-only proof. The founder has since changed the spec. This is **attempt 1 under the revised spec**, not attempt 4 â€” the requirement changed, not the standard of evidence.

Judge this on its own merits. Do not pass it because prior rounds were corrected.

## What to report â€” two axes, kept separate

### Axis 1 â€” Standards
TypeScript/React/Next.js App Router practice in the diff. Cite the hunk. Distinguish hard violations from judgement calls. Skip what the compiler or a linter already enforces. Under 400 words.

### Axis 2 â€” Spec
Against R1â€“R6 as written above:
- (a) requirements not met or only partly met
- (b) behaviour introduced that was not asked for
- (c) requirements implemented incorrectly
- (d) whether the test genuinely supports the **diff-relative** claim, or whether the claim can be false while the test passes

Quote the diff for each finding. Under 400 words.

## Verdict

End with exactly one line:

`VERDICT: PASS` or `VERDICT: FAIL â€” <one-line reason>`

hook: PostToolUse
hook: PostToolUse Completed
exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -LiteralPath 'D:\\Pi-Dev-Ops\\.harness\\cc-01.diff'" in D:\Pi-Dev-Ops
 succeeded in 442ms:
diff --git a/.harness/known-issues.md b/.harness/known-issues.md
new file mode 100644
index 00000000..cd6711b0
--- /dev/null
+++ b/.harness/known-issues.md
@@ -0,0 +1,33 @@
+# Known issues â€” command centre migration
+
+Carried deliberately. Each is a decision, not an oversight. Fixing any of these is
+its own work with its own cross-model review â€” never folded into a port.
+
+---
+
+## KI-001 Â· hermes-control-panel renders 8 of 13 modules
+
+**Status:** open Â· **Raised:** 2026-08-01 Â· **Ruled by:** founder
+**Capability:** 1 â€” hermes-control-panel
+
+The page renders only the first `DECK_LIST_CAP = 8` of 13 registry modules, followed by a
+`+N more` line.
+
+**The cap is in the source, not introduced by the port.** `DECK_LIST_CAP` comes from the
+source's own `DeckDetails`. So the Authority-Site page has always shown a capped list while
+its header describes it as one that *"mirrors the Hermes web admin module list"*. The source
+does not satisfy its own header, and never has.
+
+**Ruling:** port faithfully, keep the cap. The migration's job is to move behaviour, not to
+change it. Un-capping is a behaviour change and needs its own spec, its own build and its own
+review.
+
+**How it surfaced:** cross-vendor review (Codex gpt-5.5) of capability 1, attempt 3 â€” it read
+the header as a requirement and correctly found the rendered output did not meet it. The
+finding was true; the spec was wrong to imply the source was uncapped.
+
+**Fix later:** decide whether 13 modules should render in full, paginate, or stay capped with
+clearer wording. Then change the header to match whatever is chosen, so the contract and the
+code agree.
+
+---
diff --git a/dashboard/__tests__/command-centre-provenance.json b/dashboard/__tests__/command-centre-provenance.json
new file mode 100644
index 00000000..1636d61d
--- /dev/null
+++ b/dashboard/__tests__/command-centre-provenance.json
@@ -0,0 +1,16 @@
+{
+  "_comment": "Provenance map for the command-centre migration. Every ported source file must appear here with the Authority-Site file it came from. The diff-relative conformance test compares each port against its named baseline: the port may not introduce a network/DB/execution construct the baseline did not already contain. An unlisted file fails the test, so a new file cannot enter the capability surface without a declared origin.",
+  "_baseline_root": "D:/Authority-Site/apps/web/src",
+  "_ruling": "2026-08-01 founder: port faithfully including existing behaviour. The claim under review is 'introduces no network surface the source did not have' â€” NOT absolute read-only proof. See adversarial-review spec-writing standard.",
+  "files": {
+    "app/(main)/command-centre/hermes/page.tsx": "app/(founder)/founder/command-centre/hermes-control-panel/page.tsx",
+    "components/command-centre/DeckDetails.tsx": "components/command-centre/DeckDetails.tsx",
+    "lib/command-centre/control-panel.ts": "lib/operator-gateway/control-panel.ts",
+    "components/command-centre/command-deck.module.css": "app/(founder)/founder/command-centre/command-deck.module.css",
+    "components/command-centre/deck-details.module.css": "components/command-centre/deck-details.module.css"
+  },
+  "_baseline_verified_by_hand": {
+    "_note": "Capabilities whose baseline safety properties are load-bearing require a hand-established baseline BEFORE porting, recorded here. Capability 6 (operator-gateway) is the one that needs it â€” its whole safety story is 'no live runner, no real execute button'. 'No new surface' is worthless against an unexamined baseline.",
+    "operator-gateway": "NOT YET ESTABLISHED â€” blocks capability 6"
+  }
+}
diff --git a/dashboard/__tests__/command-centre-readonly.test.ts b/dashboard/__tests__/command-centre-readonly.test.ts
new file mode 100644
index 00000000..d303545b
--- /dev/null
+++ b/dashboard/__tests__/command-centre-readonly.test.ts
@@ -0,0 +1,142 @@
+/**
+ * command-centre-readonly.test.ts â€” diff-relative conformance for ported capabilities.
+ *
+ * THE CLAIM THIS TEST CHECKS, and the one it deliberately does NOT:
+ *
+ *   CHECKS:      "this port introduces no network/DB/execution construct that the
+ *                 named source baseline did not already contain."
+ *   DOES NOT:    "this page makes no network call."
+ *
+ * The second is an unbounded negative. Three bounded attempts at capability 1 were
+ * all correctly failed by cross-vendor review because the spec asked for it: there
+ * is always another path (dynamic import, require, WebSocket, sendBeacon, server
+ * action, transitive side effect). The code was fine every time; the spec was the
+ * defect. Founder ruling 2026-08-01: take the diff-relative framing.
+ *
+ * A diff-relative claim is decidable â€” compare against a baseline, answer yes or no.
+ * It is also only as strong as that baseline, which is why capabilities whose safety
+ * properties are load-bearing (operator-gateway) require a hand-established baseline
+ * recorded in the provenance file before they may be ported.
+ */
+import { describe, expect, it } from "vitest";
+import { existsSync, readFileSync, readdirSync, statSync } from "node:fs";
+import { dirname, join, resolve } from "node:path";
+
+const ROOT = resolve(__dirname, "..");
+const PROV = JSON.parse(
+  readFileSync(join(__dirname, "command-centre-provenance.json"), "utf8")
+) as {
+  _baseline_root: string;
+  files: Record<string, string>;
+};
+const BASELINE = PROV._baseline_root;
+const baselineAvailable = existsSync(BASELINE);
+
+/** Constructs whose COUNT must not increase relative to the baseline. */
+const TRACKED: Array<{ re: RegExp; rule: string }> = [
+  { re: /\bfetch\s*\(/g, rule: "network: fetch" },
+  { re: /\b(axios|got|node-fetch|undici)\b/g, rule: "network: http client" },
+  { re: /\b(WebSocket|EventSource|XMLHttpRequest|sendBeacon)\b/g, rule: "network: streaming/beacon" },
+  { re: /\bimport\s*\(/g, rule: "dynamic import" },
+  { re: /\brequire\s*\(/g, rule: "require()" },
+  { re: /https?:\/\//g, rule: "remote host literal" },
+  { re: /\.(insert|update|upsert|delete)\s*\(/g, rule: "database write" },
+  { re: /createServerClient|createClient\s*\(/g, rule: "database client" },
+  { re: /\bMcpClient\b|modelcontextprotocol/g, rule: "MCP client" },
+  { re: /"use server"|'use server'/g, rule: "server action" },
+  { re: /ANTHROPIC_API_KEY|OPENAI_API_KEY|STRIPE_SECRET/g, rule: "paid API key" },
+];
+
+const strip = (s: string) =>
+  s.replace(/\/\*[\s\S]*?\*\//g, "").replace(/^\s*\/\/.*$/gm, "");
+
+const count = (src: string, re: RegExp) => (strip(src).match(new RegExp(re.source, "g")) || []).length;
+
+function resolveSpec(spec: string, from: string): string | null {
+  let base: string;
+  if (spec.startsWith("@/")) base = join(ROOT, spec.slice(2));
+  else if (spec.startsWith(".")) base = resolve(dirname(from), spec);
+  else return null;
+  for (const c of [`${base}.ts`, `${base}.tsx`, join(base, "index.ts"), join(base, "index.tsx"), base]) {
+    if (existsSync(c) && statSync(c).isFile()) return c;
+  }
+  return null;
+}
+
+/** Everything reachable from the capability entry pages, following imports. */
+function importGraph(): string[] {
+  const entries: string[] = [];
+  (function walk(d: string) {
+    if (!existsSync(d)) return;
+    for (const e of readdirSync(d)) {
+      const p = join(d, e);
+      if (statSync(p).isDirectory()) walk(p);
+      else if (e === "page.tsx") entries.push(p);
+    }
+  })(join(ROOT, "app/(main)/command-centre"));
+
+  const seen = new Set<string>();
+  const queue = [...entries];
+  while (queue.length) {
+    const f = queue.pop()!;
+    if (seen.has(f) || !existsSync(f)) continue;
+    seen.add(f);
+    for (const m of readFileSync(f, "utf8").matchAll(/from\s+['"]([^'"]+)['"]/g)) {
+      const r = resolveSpec(m[1], f);
+      if (r && !seen.has(r)) queue.push(r);
+    }
+  }
+  return [...seen];
+}
+
+const rel = (f: string) => f.slice(ROOT.length + 1).replace(/\\/g, "/");
+
+describe("command-centre: no new surface vs source baseline", () => {
+  const files = importGraph().map(rel);
+
+  it("the import graph reaches beyond the capability directory (positive control)", () => {
+    expect(files.length).toBeGreaterThan(1);
+    expect(files.some((f) => f.startsWith("components/command-centre"))).toBe(true);
+  });
+
+  it("every file in the capability surface has a declared origin", () => {
+    // A file with no provenance has no baseline, so "no new surface" is unprovable
+    // for it. Unlisted means fail, not skip.
+    const undeclared = files.filter((f) => !(f in PROV.files));
+    expect(undeclared, `no provenance entry for:\n${undeclared.join("\n")}`).toEqual([]);
+  });
+
+  it("reports plainly when the baseline is unreachable", () => {
+    // In CI the Authority-Site checkout is absent. That must read as "not verified
+    // here", never as "verified clean" â€” a skipped comparison is not a passing one.
+    if (!baselineAvailable) {
+      console.warn(
+        `[BASELINE UNAVAILABLE] ${BASELINE} not present. ` +
+          `Diff-relative comparison did NOT run in this environment. ` +
+          `It is authoritative only where the baseline checkout exists.`
+      );
+    }
+    expect(typeof baselineAvailable).toBe("boolean");
+  });
+
+  it("detects an increase when one exists (positive control)", () => {
+    const before = count("const a = 1", /\bfetch\s*\(/);
+    const after = count("const a = fetch('x')", /\bfetch\s*\(/);
+    expect(after).toBeGreaterThan(before);
+  });
+
+  for (const t of TRACKED) {
+    it.skipIf(!baselineAvailable)(`introduces no new â€” ${t.rule}`, () => {
+      const grew: string[] = [];
+      for (const [ported, source] of Object.entries(PROV.files)) {
+        const pPath = join(ROOT, ported);
+        const sPath = join(BASELINE, source);
+        if (!existsSync(pPath) || !existsSync(sPath)) continue;
+        const p = count(readFileSync(pPath, "utf8"), t.re);
+        const s = count(readFileSync(sPath, "utf8"), t.re);
+        if (p > s) grew.push(`${ported}: ${s} -> ${p}`);
+      }
+      expect(grew, `${t.rule} increased vs baseline:\n${grew.join("\n")}`).toEqual([]);
+    });
+  }
+});
diff --git a/dashboard/app/(main)/command-centre/hermes/page.tsx b/dashboard/app/(main)/command-centre/hermes/page.tsx
new file mode 100644
index 00000000..ce6069f0
--- /dev/null
+++ b/dashboard/app/(main)/command-centre/hermes/page.tsx
@@ -0,0 +1,219 @@
+// src/app/(founder)/founder/command-centre/hermes-control-panel/page.tsx
+//
+// Hermes v0.16 "Surface Release" â€” Control Panel (READ-ONLY foundation).
+// Mirrors the Hermes web admin module list inside the Unite-Group founder command-centre.
+// No external connections, no MCP, no remote gateway, no messaging-channel activation,
+// no secret values. Every external surface renders as inert (not connected / none enabled).
+// Auth enforced by the dashboard's (main) layout in the target app.
+//
+// Presentation: command-deck standard (summary-first, founder feedback 14/07/2026) â€”
+// OLED ground + tokens via the shared command-deck.module.css register, mono accents,
+// rounded-sm (2px). The inert/placeholder mirror sections collapse behind the shared
+// DeckDetails disclosure with honest one-line summaries. Data logic unchanged.
+
+
+import { getControlPanelView } from '@/lib/command-centre/control-panel'
+import { DeckDetails, DeckMoreLine, DECK_LIST_CAP } from '@/components/command-centre/DeckDetails'
+import deckStyles from '@/components/command-centre/command-deck.module.css'
+
+// Deck typeface â€” same face + variable the command-centre deck loads, so the
+// command-deck.module.css `--font-chakra` stack resolves on this sub-route too.
+
+// Deck token values (command-deck.module.css) for the inline-styled bits.
+const mono = 'ui-monospace, SFMono-Regular, monospace'
+const muted = '#a6afbc' // --deck-muted
+const okText = '#34d399' // --deck-cyan-text
+
+const wrap: React.CSSProperties = {
+  maxWidth: 1040,
+  margin: '0 auto',
+}
+// Banner fill is an alpha wash of the deck go LED (--deck-go).
+const banner: React.CSSProperties = {
+  background: 'rgba(45, 187, 87, 0.08)',
+  border: '1px solid rgba(45, 187, 87, 0.35)',
+  borderRadius: 2,
+  padding: '0.75rem 1rem',
+  marginBottom: '1.5rem',
+  fontSize: 14,
+}
+const th: React.CSSProperties = {
+  textAlign: 'left',
+  fontSize: 12,
+  textTransform: 'uppercase',
+  letterSpacing: '0.04em',
+  fontFamily: mono,
+  color: muted,
+  padding: '0.4rem 0.6rem',
+  borderBottom: '1px solid rgba(255, 255, 255, 0.10)', // --deck-line
+}
+const td: React.CSSProperties = {
+  padding: '0.5rem 0.6rem',
+  borderBottom: '1px solid rgba(255, 255, 255, 0.05)', // --deck-line-soft
+  fontSize: 14,
+  verticalAlign: 'top',
+}
+
+function riskStyle(risk: string): React.CSSProperties {
+  // Alpha washes of the deck LED fills; text = the AA --deck-*-text variants.
+  const map: Record<string, [string, string, string]> = {
+    none: ['rgba(45, 187, 87, 0.12)', '#34d399', 'rgba(45, 187, 87, 0.35)'],
+    low: ['rgba(244, 130, 15, 0.12)', '#f0a94c', 'rgba(244, 130, 15, 0.4)'],
+    high: ['rgba(229, 72, 77, 0.12)', '#f87171', 'rgba(229, 72, 77, 0.4)'],
+  }
+  const [bg, fg, bd] = map[risk] ?? map.none
+  return {
+    display: 'inline-block',
+    padding: '0.1rem 0.5rem',
+    borderRadius: 2, // deck standard: rounded-sm
+    fontSize: 12,
+    fontWeight: 600,
+    fontFamily: mono,
+    background: bg,
+    color: fg,
+    border: `1px solid ${bd}`,
+  }
+}
+
+export default function HermesControlPanelPage() {
+  const view = getControlPanelView()
+  const v = view.version
+  const posture = v.securityPosture
+
+  // Presentation-only summary counts for the disclosure headlines (honest,
+  // derived from the same read-only view â€” no data logic change).
+  const hardeningsOn = [
+    posture.secretRedaction,
+    posture.subprocessCredentialStripping,
+    posture.ssrfHardening,
+    posture.cveSecurityPinning,
+  ].filter((s) => s === 'on').length
+  const connectedSurfaces = [
+    view.liveConnections,
+    view.externalChannelsEnabled,
+    view.mcpConnected,
+    view.remoteGatewayConnected,
+  ].filter(Boolean).length
+  const shownModules = view.modules.slice(0, DECK_LIST_CAP)
+
+  return (
+    <div className={`${deckStyles.deck}`}>
+      <div style={wrap}>
+        <h1 style={{ fontSize: 24, marginBottom: '0.25rem' }}>Hermes Control Panel</h1>
+        <p style={{ color: muted, marginTop: 0, fontFamily: mono, fontSize: 13 }}>
+          Hermes Agent v{v.version} Â· {v.releaseName} ({v.release}) Â· config format {v.configFormat}
+        </p>
+
+        <div style={banner}>
+          <strong>Read-only foundation.</strong> {view.note}
+        </div>
+
+        <div style={{ display: 'grid', gap: '1rem' }}>
+          {/* â”€â”€ Security / version posture â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */}
+          <DeckDetails
+            title="Security & version posture"
+            stats={`design target (not live) Â· ${hardeningsOn}/4 hardenings on Â· leaner skill set ${posture.leanerSkillSet} Â· NVIDIA tap ${posture.nvidiaTap}`}
+            testId="hermes-security-posture"
+          >
+            {/* UNI-2394 â€” these posture values come from the static registry
+                (control-panel.ts, source 'static_registry'), not a live Hermes
+                probe. Label them honestly instead of presenting them as live. */}
+            <p style={{ color: muted, fontFamily: mono, fontSize: 12, marginTop: 0 }}>
+              Design target (not live) â€” values are the v{v.version} release posture from the static registry, not a live probe of a running Hermes instance.
+            </p>
+            <div style={{ display: 'flex', gap: '1.5rem', flexWrap: 'wrap', fontSize: 14 }}>
+              <span>Secret redaction: <b style={{ color: okText }}>{posture.secretRedaction}</b></span>
+              <span>Subprocess cred stripping: <b style={{ color: okText }}>{posture.subprocessCredentialStripping}</b></span>
+              <span>SSRF hardening: <b style={{ color: okText }}>{posture.ssrfHardening}</b></span>
+              <span>CVE pinning: <b style={{ color: okText }}>{posture.cveSecurityPinning}</b></span>
+              <span>Leaner skill set: <b>{posture.leanerSkillSet}</b></span>
+              <span>NVIDIA tap: <b>{posture.nvidiaTap}</b></span>
+            </div>
+          </DeckDetails>
+
+          {/* â”€â”€ Connection invariants â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */}
+          <DeckDetails
+            title="Connection state"
+            stats={
+              connectedSurfaces === 0
+                ? 'not connected â€” all external surfaces inert (by design)'
+                : `${connectedSurfaces}/4 external surfaces connected`
+            }
+            testId="hermes-connection-state"
+          >
+            <div style={{ display: 'flex', gap: '2rem', flexWrap: 'wrap', fontSize: 14 }}>
+              <span>Live connections: <b style={{ color: okText }}>{view.liveConnections ? 'yes' : 'no'}</b></span>
+              <span>External channels enabled: <b style={{ color: okText }}>{view.externalChannelsEnabled ? 'yes' : 'no'}</b></span>
+              <span>MCP connected: <b style={{ color: okText }}>{view.mcpConnected ? 'yes' : 'no'}</b></span>
+              <span>Remote gateway connected: <b style={{ color: okText }}>{view.remoteGatewayConnected ? 'yes' : 'no'}</b></span>
+              <span>Credentials exposed: <b style={{ color: okText }}>{view.credentialsExposed ? 'yes' : 'no'}</b></span>
+            </div>
+          </DeckDetails>
+
+          {/* â”€â”€ Modules â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */}
+          <DeckDetails
+            title={`Surface-Release modules (${view.moduleCount})`}
+            stats={`${view.modulesImplementableNow} now Â· ${view.modulesRequiringLaterApproval} need later approval Â· ${view.highRiskGatedCount} high-risk gated`}
+            testId="hermes-modules"
+          >
+            <table style={{ width: '100%', borderCollapse: 'collapse' }}>
+              <thead>
+                <tr>
+                  <th style={th}>Module</th>
+                  <th style={th}>Hermes feature</th>
+                  <th style={th}>State</th>
+                  <th style={th}>Ext. risk</th>
+                  <th style={th}>Later approval</th>
+                </tr>
+              </thead>
+              <tbody>
+                {shownModules.map((m) => (
+                  <tr key={m.moduleId}>
+                    <td style={td}>
+                      <div style={{ fontWeight: 600 }}>{m.title}</div>
+                      <div style={{ color: muted, fontSize: 12 }}>{m.note}</div>
+                    </td>
+                    <td style={td}>{m.hermesFeature}</td>
+                    <td style={td}>{m.state}</td>
+                    <td style={td}>
+                      <span style={riskStyle(m.externalActionRisk)}>{m.externalActionRisk}</span>
+                    </td>
+                    <td style={td}>{m.requiresLaterApproval ? 'yes' : 'no'}</td>
+                  </tr>
+                ))}
+              </tbody>
+            </table>
+            <DeckMoreLine total={view.modules.length} shown={shownModules.length} />
+          </DeckDetails>
+
+          {/* â”€â”€ Credential boundary (status only) â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */}
+          <DeckDetails
+            title="Credential boundary"
+            stats={`${view.credentialBoundaries.length} boundaries Â· status only â€” no secret values`}
+            testId="hermes-credential-boundary"
+          >
+            <p style={{ color: muted, fontSize: 13, marginTop: 0 }}>
+              Boundary names and status only â€” no secret values are ever displayed.
+            </p>
+            <table style={{ width: '100%', borderCollapse: 'collapse' }}>
+              <thead>
+                <tr>
+                  <th style={th}>Boundary</th>
+                  <th style={th}>Status</th>
+                </tr>
+              </thead>
+              <tbody>
+                {view.credentialBoundaries.map((c) => (
+                  <tr key={c.boundary}>
+                    <td style={td}>{c.boundary}</td>
+                    <td style={td}>{c.status}</td>
+                  </tr>
+                ))}
+              </tbody>
+            </table>
+          </DeckDetails>
+        </div>
+      </div>
+    </div>
+  )
+}
diff --git a/dashboard/components/command-centre/DeckDetails.tsx b/dashboard/components/command-centre/DeckDetails.tsx
new file mode 100644
index 00000000..5394421c
--- /dev/null
+++ b/dashboard/components/command-centre/DeckDetails.tsx
@@ -0,0 +1,72 @@
+// src/components/command-centre/DeckDetails.tsx
+//
+// Shared progressive-disclosure primitive for the command deck (founder
+// feedback 14/07/2026: "a lot of exposed data that can be cleaned up").
+// Summary row stays visible â€” title, one to three headline numbers, and a
+// status badge slot â€” while the dense detail (identifier lists, rosters,
+// forms) collapses behind a native <details> chevron. Pure presentation:
+// no data fetching, no new dependencies, deck tokens only (OLED ground,
+// mono accents, rounded-sm). Works in both server and client tiles.
+//
+// Note on identifiers: this page is founder-only behind the (founder)
+// layout auth, so moving emails/ids/hostnames into the collapsed layer is
+// visual de-clutter, not a security boundary â€” the expanded detail keeps
+// them in full.
+
+import styles from './deck-details.module.css'
+
+export function DeckDetails({
+  title,
+  stats,
+  badge,
+  defaultOpen = false,
+  testId,
+  children,
+}: {
+  /** Always-visible tile title. */
+  title: string
+  /** 1â€“3 headline numbers / status words, e.g. "3 connected Â· 1 needs reauth". */
+  stats?: React.ReactNode
+  /** Status badge slot (usually a SourceBadge). Sits at the right edge. */
+  badge?: React.ReactNode
+  /** Render expanded on first paint (uncontrolled after that). */
+  defaultOpen?: boolean
+  testId?: string
+  children: React.ReactNode
+}) {
+  return (
+    <details className={styles.deckDetails} open={defaultOpen || undefined} data-testid={testId}>
+      <summary className={styles.deckSummary}>
+        <span aria-hidden className={styles.chev} />
+        <span className={styles.title}>{title}</span>
+        {stats != null && <span className={styles.stats}>{stats}</span>}
+        {badge != null && <span className={styles.badge}>{badge}</span>}
+      </summary>
+      <div className={styles.body}>{children}</div>
+    </details>
+  )
+}
+
+/**
+ * Honest "+N more" line for lists capped inside a disclosure. Renders
+ * nothing when the list is fully shown, so callers can emit it
+ * unconditionally after a `.slice(0, cap)`.
+ */
+export function DeckMoreLine({ total, shown }: { total: number; shown: number }) {
+  if (total <= shown) return null
+  return <p className={styles.moreLine}>+{total - shown} more not shown</p>
+}
+
+/** Default row cap for lists rendered inside a DeckDetails disclosure. */
+export const DECK_LIST_CAP = 8
+
+/** Row cap for full founder pages (summary-first cleanup, founder 14/07/2026). */
+
+/**
+ * The deck-details CSS falls back to dark-deck inks (#f0f3f7 text) when the
+ * --deck-* / --cc-* tokens are absent â€” illegible on the founder app's light
+ * (paper) theme. Founder pages outside the command deck spread this onto a
+ * wrapping element so the primitive maps onto the app's light token set.
+ * (DeckDetails lives under components/command-centre/; imported here rather
+ * than duplicated â€” a later refactor can relocate it.)
+ */
diff --git a/dashboard/components/command-centre/command-deck.module.css b/dashboard/components/command-centre/command-deck.module.css
new file mode 100644
index 00000000..d7021480
--- /dev/null
+++ b/dashboard/components/command-centre/command-deck.module.css
@@ -0,0 +1,618 @@
+/* Command Deck â€” flight-deck console aesthetic for the Nexus Command Centre.
+   Scoped CSS module (no runtime style injection â€” CSP/nonce safe).
+   Signal palette: cyan (primary) / go-green / caution-amber / abort-red. */
+
+.deck {
+  /* UNI-2339 slice 2 â€” the deck ground migrates onto the ratified Gun Metal
+     canvas (shell.module.css register). Every tile consumes colour via these
+     tokens (tiles imported unchanged), so the flip happens here. All TEXT
+     tokens computed against the darkest panel they land on (--deck-panel
+     #1c2230) and the bare canvas (#0e1014) â€” ratios noted per token. */
+  --deck-cyan: #2dbb57;     /* primary accent â€” fills, LEDs, glows only */
+  --deck-go: #2dbb57;       /* go â€” fills/LEDs only */
+  --deck-amber: #f4820f;    /* caution â€” fills/LEDs only */
+  --deck-abort: #e5484d;    /* abort â€” fills/LEDs only */
+  --deck-glow: 45, 187, 87; /* RGB triplet for LED/glow shadows â€” candy green on the dark deck */
+  --deck-line: rgba(255, 255, 255, 0.10);
+  --deck-line-soft: rgba(255, 255, 255, 0.05);
+  --deck-panel: #1c2230;    /* canvas --surface-2 */
+  --deck-panel-hi: #232b3a; /* canvas --surface-3 */
+  --deck-text: #f0f3f7;     /* canvas --ink â†’ 14.28:1 on panel, 17.11:1 on canvas */
+  --deck-muted: #a6afbc;    /* canvas --ink-dim â†’ 7.18:1 on panel, 8.60:1 on canvas */
+  /* WCAG-AA text variants: the bright candy green/amber/red are kept for
+     fills/LEDs; TEXT consumers get light-on-dark variants that clear 4.5:1
+     on --deck-panel (#1c2230). */
+  --deck-cyan-text: #34d399;  /* 8.27:1 on panel, 9.90:1 on canvas */
+  --deck-amber-text: #f0a94c; /* 7.94:1 on panel, 9.51:1 on canvas */
+  --deck-abort-text: #f87171; /* 5.75:1 on panel, 6.88:1 on canvas */
+
+  /* Aliases so panels consolidated from the retired command-center page (which
+     use the --cc-* namespace) resolve their colours on this deck. */
+  --cc-bg: var(--deck-panel);
+  --cc-bg-soft: #141820;    /* canvas --surface-1 */
+  --cc-grid: var(--deck-line);
+  --cc-ink: var(--deck-text);
+  --cc-ink-dim: var(--deck-muted);
+  --cc-ink-hush: #8b96a5;   /* hushed but AA: 5.30:1 on panel, 5.93:1 on bg-soft */
+  /* --cc-signal stays the bright candy green: consolidated cc-* panels use it
+     for fills/dots/LEDs/borders (ActivityRow, HermesControlPanel backgrounds,
+     WorkPacketLane borders, ActivityLog dot, DegradedDataBanner borderLeft).
+     TEXT consumers use --cc-signal-text (AA-passing on the dark grounds). */
+  --cc-signal: var(--deck-cyan);
+  --cc-signal-text: var(--deck-cyan-text);
+  --cc-pulse-duration: 2.4s;
+
+  /* Tile inline-style tokens â€” the five queue/health/evidence tiles style via
+     inline `style={{}}` (class overrides can't reach them), so they consume
+     these vars with their old literals as fallbacks. Deck values = those
+     literals exactly; the daylight block re-inks them for paper. */
+  --tile-ink: #e6f7ff;
+  --tile-ink-dim: #9bb0c1;
+  --tile-ink-hush: #6f879b;
+  --tile-card-bg: rgba(0, 0, 0, 0.25);
+  --tile-card-line: rgba(255, 255, 255, 0.08);
+  --tile-amber-txt: #fb923c;
+  --tile-green-txt: #34d399;
+  --tile-red-txt: #d02f35;
+
+  position: relative;
+  min-height: 100%;
+  padding: 1.75rem 1.75rem 3rem;
+  color: var(--deck-text);
+  font-family: var(--font-chakra), var(--font-geist-sans), system-ui, sans-serif;
+  background-color: #0e1014;
+  background-image:
+    radial-gradient(120% 80% at 50% -10%, rgba(179, 0, 0, 0.07), transparent 60%),
+    radial-gradient(100% 70% at 100% 0%, rgba(255, 255, 255, 0.03), transparent 55%);
+  overflow: hidden;
+}
+
+/* Top ambient light (was the candy gloss sheen â€” canvas = matte, faint) */
+.deck::after {
+  content: "";
+  position: absolute;
+  inset: 0;
+  pointer-events: none;
+  background: linear-gradient(180deg, rgba(255, 255, 255, 0.04), transparent 28%);
+  z-index: 0;
+}
+
+.deck > * { position: relative; z-index: 1; }
+
+/* â”€â”€ Status strip â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
+.statusStrip {
+  display: flex;
+  align-items: center;
+  justify-content: space-between;
+  gap: 1.5rem;
+  flex-wrap: wrap;
+  padding: 0.9rem 1.1rem;
+  border: 1px solid var(--deck-line);
+  border-radius: 12px;
+  background: linear-gradient(180deg, var(--deck-panel-hi), var(--deck-panel));
+  box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.06), 0 8px 30px rgba(0, 0, 0, 0.45);
+}
+
+.brand { display: flex; align-items: center; gap: 0.75rem; }
+
+.brandMark {
+  width: 34px; height: 34px;
+  display: grid; place-items: center;
+  border: 1px solid var(--deck-cyan);
+  border-radius: 10px;
+  color: var(--deck-cyan-text);
+  font-weight: 700;
+  letter-spacing: 0.05em;
+  box-shadow: 0 0 18px rgba(var(--deck-glow), 0.35), inset 0 0 10px rgba(var(--deck-glow), 0.12);
+}
+
+.brandText { display: flex; flex-direction: column; line-height: 1.05; }
+.brandTitle {
+  font-weight: 600;
+  font-size: 0.95rem;
+  letter-spacing: 0.22em;
+  text-transform: uppercase;
+  color: var(--deck-text);
+}
+.brandSub {
+  font-family: var(--font-geist-mono), monospace;
+  font-size: 0.62rem;
+  letter-spacing: 0.28em;
+  text-transform: uppercase;
+  color: var(--deck-muted);
+}
+
+.clock {
+  font-family: var(--font-geist-mono), monospace;
+  font-size: 1.35rem;
+  font-weight: 500;
+  letter-spacing: 0.08em;
+  color: var(--deck-cyan-text);
+  text-shadow: 0 0 14px rgba(var(--deck-glow), 0.45);
+  font-variant-numeric: tabular-nums;
+}
+.clockLabel {
+  font-family: var(--font-geist-mono), monospace;
+  font-size: 0.55rem;
+  letter-spacing: 0.3em;
+  color: var(--deck-muted);
+  text-transform: uppercase;
+}
+
+.metrics { display: flex; align-items: stretch; gap: 1.4rem; }
+.metric { display: flex; flex-direction: column; gap: 0.15rem; min-width: 56px; }
+.metricValue {
+  font-family: var(--font-geist-mono), monospace;
+  font-size: 1.5rem;
+  font-weight: 600;
+  line-height: 1;
+  font-variant-numeric: tabular-nums;
+  color: var(--deck-text);
+}
+.metricValue[data-tone="go"] { color: var(--deck-cyan-text); }
+.metricValue[data-tone="amber"] { color: var(--deck-amber-text); }
+.metricLabel {
+  font-family: var(--font-geist-mono), monospace;
+  font-size: 0.55rem;
+  letter-spacing: 0.24em;
+  text-transform: uppercase;
+  color: var(--deck-muted);
+}
+
+.sys { display: flex; align-items: center; gap: 0.5rem; }
+.sysText {
+  font-family: var(--font-geist-mono), monospace;
+  font-size: 0.68rem;
+  letter-spacing: 0.18em;
+  text-transform: uppercase;
+  color: var(--deck-cyan-text);
+}
+
+.kbd {
+  font-family: var(--font-geist-mono), monospace;
+  font-size: 0.62rem;
+  letter-spacing: 0.1em;
+  color: var(--deck-muted);
+  border: 1px solid var(--deck-line);
+  border-radius: 10px;
+  padding: 0.28rem 0.5rem;
+  background: rgba(var(--deck-glow), 0.05);
+  white-space: nowrap;
+}
+.kbd b { color: var(--deck-text); font-weight: 600; }
+
+/* Section labels (HUD) removed â€” UNI-2339 slice 2 migrated every section
+   head to the canvas register (shell.module.css .glassSectionHead). */
+
+/* â”€â”€ Panel grid â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
+.panelGrid {
+  display: grid;
+  grid-template-columns: repeat(auto-fill, minmax(290px, 1fr));
+  gap: 0.9rem;
+}
+
+.panel {
+  position: relative;
+  display: flex; flex-direction: column; gap: 0.75rem;
+  padding: 0.95rem 1rem 1rem;
+  border: 1px solid var(--deck-line);
+  border-left: 3px solid var(--swatch, var(--deck-cyan));
+  border-radius: 14px;
+  background: linear-gradient(165deg, var(--deck-panel-hi) 0%, var(--deck-panel) 100%);
+  box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.06), 0 6px 18px rgba(0, 0, 0, 0.35);
+  transition: transform 0.18s ease, box-shadow 0.18s ease, border-color 0.18s ease;
+}
+/* Instrument bezel corner ticks */
+.panel::before, .panel::after {
+  content: "";
+  position: absolute;
+  width: 9px; height: 9px;
+  border: 1px solid var(--deck-line);
+  pointer-events: none;
+}
+.panel::before { top: 6px; right: 6px; border-left: 0; border-bottom: 0; }
+.panel::after { bottom: 6px; left: 6px; border-right: 0; border-top: 0; }
+.panel:hover {
+  transform: translateY(-2px);
+  border-color: rgba(var(--deck-glow), 0.4);
+  box-shadow: 0 10px 30px rgba(45, 120, 70, 0.16), 0 0 0 1px rgba(var(--deck-glow), 0.12);
+}
+
+.panelHead { display: flex; align-items: center; gap: 0.55rem; }
+.pname {
+  font-size: 0.92rem;
+  font-weight: 600;
+  letter-spacing: 0.04em;
+  color: var(--deck-text);
+  flex: 1; min-width: 0;
+  overflow: hidden; text-overflow: ellipsis; white-space: nowrap;
+}
+.ppurpose {
+  font-family: var(--font-geist-sans), system-ui, sans-serif;
+  font-size: 0.74rem;
+  line-height: 1.5;
+  color: var(--deck-muted);
+  min-height: 2.2em;
+}
+
+.led {
+  width: 8px; height: 8px; border-radius: 50%;
+  flex-shrink: 0;
+  background: var(--deck-muted);
+  box-shadow: 0 0 0 2px rgba(255, 255, 255, 0.12);
+}
+.led[data-state="active"] {
+  background: var(--deck-go);
+  box-shadow: 0 0 10px var(--deck-go), 0 0 0 2px rgba(255, 255, 255, 0.12);
+  animation: pulse 2.4s ease-in-out infinite;
+}
+.led[data-state="stub"] { background: var(--deck-amber); box-shadow: 0 0 8px var(--deck-amber); }
+
+.statusTag {
+  font-family: var(--font-geist-mono), monospace;
+  font-size: 0.55rem;
+  letter-spacing: 0.18em;
+  text-transform: uppercase;
+  color: var(--deck-muted);
+}
+
+.readouts {
+  display: flex; flex-direction: column; gap: 0.4rem;
+  padding-top: 0.6rem;
+  border-top: 1px solid var(--deck-line-soft);
+}
+.readout {
+  display: flex; align-items: center; gap: 0.5rem;
+  font-family: var(--font-geist-mono), monospace;
+  font-size: 0.66rem;
+  letter-spacing: 0.04em;
+  color: var(--deck-muted);
+}
+.readoutKey { color: var(--deck-muted); min-width: 58px; text-transform: uppercase; letter-spacing: 0.12em; font-size: 0.56rem; }
+.readoutVal { color: var(--deck-muted); overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
+
+.plink {
+  display: inline-flex; align-items: center; gap: 0.4rem;
+  font-family: var(--font-geist-mono), monospace;
+  font-size: 0.66rem;
+  letter-spacing: 0.04em;
+  color: var(--deck-cyan-text);
+  text-decoration: none;
+  width: fit-content;
+  transition: text-shadow 0.15s ease;
+}
+.plink:hover { text-shadow: 0 0 10px rgba(var(--deck-glow), 0.5); text-decoration: underline; }
+
+/* â”€â”€ Project integration manifests â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
+.integrationGrid {
+  display: grid;
+  grid-template-columns: repeat(auto-fill, minmax(min(100%, 340px), 1fr));
+  gap: 0.9rem;
+}
+
+.integrationSummary {
+  display: grid;
+  grid-template-columns: repeat(4, minmax(0, 1fr));
+  gap: 0.35rem;
+  padding: 0.55rem;
+  border: 1px solid var(--deck-line-soft);
+  border-radius: 10px;
+  background: rgba(var(--deck-glow), 0.05);
+}
+
+.integrationSummary span {
+  display: flex;
+  flex-direction: column;
+  gap: 0.1rem;
+  min-width: 0;
+  font-family: var(--font-geist-mono), monospace;
+  font-size: 0.54rem;
+  letter-spacing: 0.12em;
+  text-transform: uppercase;
+  color: var(--deck-muted);
+}
+
+.integrationSummary b {
+  color: var(--deck-text);
+  font-size: 0.9rem;
+  font-weight: 600;
+  line-height: 1;
+}
+
+.connectionList {
+  display: flex;
+  flex-direction: column;
+  gap: 0.38rem;
+  max-height: 260px;
+  overflow: auto;
+  padding-right: 0.1rem;
+}
+
+.connectionRow {
+  display: grid;
+  grid-template-columns: 8px minmax(0, 1fr) auto;
+  align-items: center;
+  gap: 0.45rem;
+  padding: 0.45rem 0.5rem;
+  border: 1px solid var(--deck-line-soft);
+  border-radius: 10px;
+  background: rgba(var(--deck-glow), 0.05);
+}
+
+.connectionName {
+  min-width: 0;
+  overflow: hidden;
+  text-overflow: ellipsis;
+  white-space: nowrap;
+  font-size: 0.68rem;
+  color: var(--deck-text);
+}
+
+.connectionState {
+  font-family: var(--font-geist-mono), monospace;
+  font-size: 0.52rem;
+  letter-spacing: 0.12em;
+  text-transform: uppercase;
+  color: var(--deck-muted);
+}
+
+.connectionAction {
+  grid-column: 2 / 4;
+  min-width: 0;
+  overflow: hidden;
+  text-overflow: ellipsis;
+  white-space: nowrap;
+  font-size: 0.62rem;
+  color: var(--deck-amber-text);
+}
+
+.integrationActions {
+  display: flex;
+  align-items: center;
+  justify-content: flex-end;
+  gap: 0.4rem;
+  min-width: min(100%, 320px);
+  flex-wrap: wrap;
+}
+
+.iconCommand,
+.queueCommand {
+  appearance: none;
+  display: inline-flex;
+  align-items: center;
+  justify-content: center;
+  gap: 0.42rem;
+  min-height: 32px;
+  border: 1px solid var(--deck-line);
+  border-radius: 10px;
+  color: #eaf3ec;
+  background: rgba(8, 15, 24, 0.82);
+  box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.08);
+  cursor: pointer;
+  transition: border-color 0.14s ease, color 0.14s ease, background 0.14s ease;
+}
+
+.iconCommand {
+  width: 34px;
+  padding: 0;
+}
+
+.queueCommand {
+  padding: 0 0.68rem;
+  font-family: var(--font-geist-mono), monospace;
+  font-size: 0.58rem;
+  letter-spacing: 0.1em;
+  text-transform: uppercase;
+  white-space: nowrap;
+}
+
+.iconCommand:hover,
+.queueCommand:hover {
+  border-color: rgba(var(--deck-glow), 0.5);
+  color: var(--deck-cyan);
+  background: rgba(13, 25, 38, 0.94);
+}
+
+.iconCommand:disabled,
+.queueCommand:disabled {
+  cursor: wait;
+  opacity: 0.58;
+}
+
+.integrationActionStatus {
+  min-width: 110px;
+  max-width: min(46vw, 220px);
+  overflow: hidden;
+  text-overflow: ellipsis;
+  white-space: nowrap;
+  font-family: var(--font-geist-mono), monospace;
+  font-size: 0.56rem;
+  letter-spacing: 0.1em;
+  text-transform: uppercase;
+  color: var(--deck-muted);
+  text-align: right;
+}
+
+/* â”€â”€ Capability bus (tools) â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
+.bus {
+  border: 1px solid var(--deck-line);
+  border-radius: 10px;
+  background: linear-gradient(180deg, var(--deck-panel-hi), var(--deck-panel));
+  padding: 1rem 1.1rem 1.1rem;
+}
+.busTop {
+  display: flex; align-items: center; justify-content: space-between;
+  gap: 1rem; flex-wrap: wrap;
+  padding-bottom: 0.85rem; margin-bottom: 0.9rem;
+  border-bottom: 1px solid var(--deck-line-soft);
+}
+.sources { display: flex; flex-wrap: wrap; gap: 0.4rem; }
+.sourceChip {
+  font-family: var(--font-geist-mono), monospace;
+  font-size: 0.6rem; letter-spacing: 0.06em;
+  color: var(--deck-muted);
+  border: 1px solid var(--deck-line);
+  border-radius: 10px;
+  padding: 0.22rem 0.5rem;
+  background: rgba(var(--deck-glow), 0.05);
+}
+.sourceChip b { color: var(--deck-cyan-text); font-weight: 600; }
+.stamp {
+  display: inline-flex; align-items: center; gap: 0.45rem;
+  font-family: var(--font-geist-mono), monospace;
+  font-size: 0.58rem; letter-spacing: 0.22em; text-transform: uppercase;
+  color: var(--deck-cyan-text);
+  border: 1px dashed rgba(52, 211, 153, 0.4);
+  border-radius: 10px; padding: 0.3rem 0.6rem;
+}
+
+.toolGrid {
+  display: grid;
+  grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
+  gap: 0.5rem;
+}
+.toolRow {
+  display: flex; align-items: flex-start; justify-content: space-between; gap: 0.6rem;
+  padding: 0.55rem 0.7rem;
+  border: 1px solid var(--deck-line-soft);
+  border-left: 2px solid var(--rail, var(--deck-line));
+  border-radius: 10px;
+  background: rgba(var(--deck-glow), 0.05);
+  transition: background 0.15s ease, border-color 0.15s ease;
+}
+.toolRow:hover { background: rgba(var(--deck-glow), 0.04); border-color: var(--deck-line); }
+.toolKey {
+  font-family: var(--font-geist-mono), monospace;
+  font-size: 0.7rem; color: var(--deck-text); font-weight: 500;
+  overflow: hidden; text-overflow: ellipsis; white-space: nowrap;
+}
+.toolDesc { font-size: 0.66rem; color: var(--deck-muted); line-height: 1.4; margin-top: 0.1rem; }
+.riskTag {
+  font-family: var(--font-geist-mono), monospace;
+  font-size: 0.52rem; letter-spacing: 0.12em; text-transform: uppercase;
+  color: var(--rail, var(--deck-muted));
+  white-space: nowrap;
+}
+.approval {
+  font-family: var(--font-geist-mono), monospace;
+  font-size: 0.5rem; letter-spacing: 0.1em; text-transform: uppercase;
+  color: var(--deck-amber-text); margin-top: 0.2rem; display: block; text-align: right;
+}
+
+/* â”€â”€ System detail expander (keeps the dense console out of the clean front) â”€â”€ */
+.systemDetail {
+  margin-top: 1.75rem;
+  border-top: 1px solid var(--deck-line);
+  padding-top: 0.75rem;
+}
+.systemSummary {
+  cursor: pointer;
+  list-style: none;
+  display: inline-flex;
+  align-items: center;
+  gap: 0.5rem;
+  font-size: 0.78rem;
+  font-weight: 600;
+  letter-spacing: 0.08em;
+  text-transform: uppercase;
+  color: var(--deck-muted);
+  padding: 0.5rem 0.25rem;
+  user-select: none;
+}
+.systemSummary::-webkit-details-marker { display: none; }
+.systemSummary::before {
+  content: "â–¸";
+  color: #2f9e44;
+}
+.systemDetail[open] .systemSummary::before { content: "â–¾"; }
+.systemSummary:hover { color: var(--deck-text); }
+
+/* â”€â”€ Reveal + pulse â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ */
+.reveal { opacity: 0; transform: translateY(8px); animation: rise 0.55s cubic-bezier(0.22, 1, 0.36, 1) forwards; }
+
+@keyframes rise { to { opacity: 1; transform: translateY(0); } }
+@keyframes pulse {
+  0%, 100% { opacity: 1; }
+  50% { opacity: 0.35; }
+}
+
+@media (prefers-reduced-motion: reduce) {
+  .reveal { animation: none; opacity: 1; transform: none; }
+  .led[data-state="active"] { animation: none; }
+  .deck::after { display: none; }
+}
+
+@media (max-width: 640px) {
+  .deck { padding: 1rem 1rem 2rem; }
+  .statusStrip { gap: 0.9rem; }
+  .metrics { gap: 1rem; }
+  .clock { font-size: 1.1rem; }
+}
+
+/* â”€â”€ Daylight register â€” Affirm-inspired fintech-trust feel: warm paper
+   ground, near-black ink, one indigo accent, soft shadows. Pattern source:
+   Mobbin â€” Affirm iOS (docs/design/mobbin-ui-library.md); the reference
+   informs hierarchy and tone, never pixels. Applied WITH .deck by
+   DeckThemeShell when the founder flips the Feel toggle; default stays the
+   dark deck. Contrast ratios noted per token against white (--deck-panel). */
+.deck.daylight {
+  --deck-cyan: #4a4af4;       /* indigo accent â€” fills, LEDs, glows only */
+  --deck-go: #0f9d58;
+  --deck-amber: #d97706;
+  --deck-abort: #dc2626;
+  --deck-glow: 74, 74, 244; /* RGB triplet for LED/glow shadows â€” indigo, matches --deck-cyan */
+  --deck-line: rgba(22, 24, 29, 0.12);
+  --deck-line-soft: rgba(22, 24, 29, 0.06);
+  --deck-panel: #ffffff;
+  --deck-panel-hi: #f4f1eb;
+  --deck-text: #16181d;       /* 16.6:1 on white */
+  --deck-muted: #5c6470;      /* 5.9:1 on white */
+  --deck-cyan-text: #3d3ddb;  /* 6.9:1 on white */
+  --deck-amber-text: #92510a; /* 6.1:1 on white */
+  --deck-abort-text: #b91c1c; /* 6.3:1 on white */
+
+  --cc-bg-soft: #f7f5f1;
+  --cc-ink-hush: #6b7280;     /* 4.8:1 on white */
+
+  --tile-ink: #16181d;
+  --tile-ink-dim: #5c6470;        /* 5.9:1 on white */
+  --tile-ink-hush: #6b7280;       /* 4.8:1 on white */
+  --tile-card-bg: rgba(22, 24, 29, 0.03);
+  --tile-card-line: rgba(22, 24, 29, 0.12);
+  --tile-amber-txt: #92510a;      /* 6.1:1 on white */
+  --tile-green-txt: #0b7a44;
+  --tile-red-txt: #b91c1c;        /* 6.3:1 on white */
+
+  color: var(--deck-text);
+  font-family: var(--font-geist-sans), system-ui, sans-serif;
+  background-color: #faf9f7;
+  background-image: none;
+}
+.deck.daylight::after { background: none; }
+.deck.daylight .statusStrip {
+  box-shadow: 0 1px 2px rgba(22, 24, 29, 0.05), 0 8px 24px -18px rgba(22, 24, 29, 0.18);
+}
+.deck.daylight .brandMark { box-shadow: none; }
+
+/* Feel toggle â€” the only chrome DeckThemeShell adds. Fixed so it never
+   collides with deck content; pill shape per the daylight reference. */
+.feelToggle {
+  position: fixed;
+  right: 1.1rem;
+  bottom: 1.1rem;
+  z-index: 40;
+  padding: 0.4rem 1rem;
+  border: 1px solid var(--deck-line);
+  border-radius: 999px;
+  background: var(--deck-panel);
+  color: var(--deck-muted);
+  font: inherit;
+  font-size: 0.72rem;
+  letter-spacing: 0.08em;
+  text-transform: uppercase;
+  cursor: pointer;
+}
+.feelToggle:hover {
+  color: var(--deck-text);
+  border-color: var(--deck-cyan);
+}
diff --git a/dashboard/components/command-centre/deck-details.module.css b/dashboard/components/command-centre/deck-details.module.css
new file mode 100644
index 00000000..c37799f1
--- /dev/null
+++ b/dashboard/components/command-centre/deck-details.module.css
@@ -0,0 +1,80 @@
+/* deck-details.module.css â€” styling for the DeckDetails disclosure primitive.
+   Matches the command-deck aesthetic: OLED-adjacent ground handled by the
+   parent tile, mono accents, rounded-sm (2px), --deck-* token vocabulary
+   (falls back to the --cc-* aliases the deck already maps onto them). */
+
+.deckDetails {
+  border: 1px solid var(--deck-line, var(--cc-grid, rgba(255, 255, 255, 0.1)));
+  border-radius: 2px;
+  background: transparent;
+}
+
+.deckSummary {
+  cursor: pointer;
+  list-style: none;
+  display: flex;
+  align-items: center;
+  gap: 0.6rem;
+  padding: 0.55rem 0.7rem;
+  user-select: none;
+  min-width: 0;
+}
+.deckSummary::-webkit-details-marker {
+  display: none;
+}
+.deckSummary:hover .title {
+  color: var(--deck-text, var(--cc-ink, #f0f3f7));
+}
+
+.chev {
+  flex: 0 0 auto;
+  font-family: ui-monospace, SFMono-Regular, monospace;
+  font-size: 0.7rem;
+  color: var(--deck-cyan-text, var(--cc-signal-text, #34d399));
+}
+.chev::before {
+  content: 'â–¸';
+}
+.deckDetails[open] > .deckSummary .chev::before {
+  content: 'â–¾';
+}
+
+.title {
+  color: var(--deck-text, var(--cc-ink, #f0f3f7));
+  font-size: 0.82rem;
+  font-weight: 700;
+  white-space: nowrap;
+  overflow: hidden;
+  text-overflow: ellipsis;
+}
+
+.stats {
+  color: var(--deck-muted, var(--cc-ink-dim, #a6afbc));
+  font-family: ui-monospace, SFMono-Regular, monospace;
+  font-size: 0.7rem;
+  letter-spacing: 0.03em;
+  white-space: nowrap;
+  overflow: hidden;
+  text-overflow: ellipsis;
+  min-width: 0;
+}
+
+.badge {
+  margin-left: auto;
+  flex: 0 0 auto;
+  display: inline-flex;
+  align-items: center;
+}
+
+.body {
+  border-top: 1px solid var(--deck-line-soft, rgba(255, 255, 255, 0.05));
+  padding: 0.6rem 0.7rem;
+}
+
+.moreLine {
+  margin: 0.4rem 0 0;
+  color: var(--deck-muted, var(--cc-ink-dim, #a6afbc));
+  font-family: ui-monospace, SFMono-Regular, monospace;
+  font-size: 0.7rem;
+  letter-spacing: 0.03em;
+}
diff --git a/dashboard/lib/command-centre/control-panel.ts b/dashboard/lib/command-centre/control-panel.ts
new file mode 100644
index 00000000..0b698979
--- /dev/null
+++ b/dashboard/lib/command-centre/control-panel.ts
@@ -0,0 +1,264 @@
+/**
+ * Hermes v0.16 "Surface Release" â€” Control Panel capability registry (static, read-only).
+ *
+ * Maps the Hermes Agent v0.16.0 Surface-Release feature areas to Unite-Group Control
+ * Panel modules as a READ-ONLY status surface. This file performs NO external calls, opens
+ * NO connections, reads NO secrets, and activates NOTHING. Every high-risk surface (remote
+ * gateway, MCP catalog, messaging channels, hooks/webhooks) is represented as inert:
+ * connected = false / enabled = false / status 'design_only'. Live wiring of any module is
+ * a separate, explicitly-gated Board decision.
+ *
+ * Safety invariants enforced at the type/data level:
+ *  - no module is 'active' for an external surface (externalActionRisk modules stay gated);
+ *  - the credential boundary carries STATUS STRINGS ONLY â€” never a secret value;
+ *  - externalChannelsEnabled / mcpConnected / remoteGatewayConnected are literal false.
+ */
+
+export type ModuleState =
+  | 'read_only' // surfaced, no action possible
+  | 'planned' // designed, not built live
+  | 'design_only' // stubbed; live wiring is a later gate
+  | 'available_not_installed' // capability exists but not installed/connected (operator action)
+  | 'not_connected' // external surface deliberately not connected
+  | 'none_enabled' // external surface deliberately has nothing enabled
+
+export type ExternalActionRisk = 'none' | 'low' | 'high'
+
+export interface HermesControlPanelModule {
+  moduleId: string
+  title: string
+  hermesFeature: string
+  state: ModuleState
+  externalActionRisk: ExternalActionRisk
+  implementableNow: boolean
+  requiresLaterApproval: boolean
+  note: string
+}
+
+export const HERMES_VERSION = '0.16.0'
+export const HERMES_RELEASE = '2026.6.5'
+export const HERMES_RELEASE_NAME = 'The Surface Release'
+export const HERMES_CONFIG_FORMAT = 27
+
+export interface HermesVersionStatus {
+  source: 'static_registry'
+  version: string
+  release: string
+  releaseName: string
+  configFormat: number
+  securityPosture: {
+    secretRedaction: 'on'
+    subprocessCredentialStripping: 'on'
+    ssrfHardening: 'on'
+    cveSecurityPinning: 'on'
+    leanerSkillSet: 'adopted'
+    nvidiaTap: 'available_not_installed'
+  }
+}
+
+export function getHermesVersionStatus(): HermesVersionStatus {
+  return {
+    source: 'static_registry',
+    version: HERMES_VERSION,
+    release: HERMES_RELEASE,
+    releaseName: HERMES_RELEASE_NAME,
+    configFormat: HERMES_CONFIG_FORMAT,
+    securityPosture: {
+      secretRedaction: 'on',
+      subprocessCredentialStripping: 'on',
+      ssrfHardening: 'on',
+      cveSecurityPinning: 'on',
+      leanerSkillSet: 'adopted',
+      nvidiaTap: 'available_not_installed',
+    },
+  }
+}
+
+export const HERMES_CONTROL_PANEL_MODULES: readonly HermesControlPanelModule[] = [
+  {
+    moduleId: 'desktop_session_status',
+    title: 'Desktop session status',
+    hermesFeature: 'Hermes Desktop app',
+    state: 'read_only',
+    externalActionRisk: 'none',
+    implementableNow: true,
+    requiresLaterApproval: false,
+    note: 'Derived from the hermes_local lane (active, local_runtime). No live presence control.',
+  },
+  {
+    moduleId: 'remote_gateway_registry',
+    title: 'Remote gateway registry',
+    hermesFeature: 'Remote Hermes gateway + gateway list',
+    state: 'design_only',
+    externalActionRisk: 'high',
+    implementableNow: true,
+    requiresLaterApproval: true,
+    note: '0 registered workers, not connected. Connecting a remote gateway is a later named-grant gate.',
+  },
+  {
+    moduleId: 'web_admin_console',
+    title: 'Web admin console',
+    hermesFeature: 'Web dashboard admin panel',
+    state: 'read_only',
+    externalActionRisk: 'none',
+    implementableNow: true,
+    requiresLaterApproval: false,
+    note: 'This founder Control Panel page mirrors the Hermes web admin module list.',
+  },
+  {
+    moduleId: 'mcp_catalog_manager',
+    title: 'MCP catalog manager',
+    hermesFeature: 'MCP catalog admin',
+    state: 'design_only',
+    externalActionRisk: 'high',
+    implementableNow: true,
+    requiresLaterApproval: true,
+    note: 'Catalogue stub, 0 servers, not connected. Connecting an MCP server is a later gate.',
+  },
+  {
+    moduleId: 'messaging_channels_manager',
+    title: 'Messaging channels manager',
+    hermesFeature: 'Channels page',
+    state: 'none_enabled',
+    externalActionRisk: 'high',
+    implementableNow: true,
+    requiresLaterApproval: true,
+    note: 'No external channels enabled. Enabling any channel is a later gate.',
+  },
+  {
+    moduleId: 'credential_boundary_dashboard',
+    title: 'Credential boundary dashboard',
+    hermesFeature: 'Credentials management + subprocess credential stripping',
+    state: 'read_only',
+    externalActionRisk: 'none',
+    implementableNow: true,
+    requiresLaterApproval: false,
+    note: 'Boundary names + status strings only; NEVER secret values. Reflects BLOCKED-OP sandbox state textually.',
+  },
+  {
+    moduleId: 'hooks_webhooks_manager',
+    title: 'Hooks / webhooks manager',
+    hermesFeature: 'Webhooks / hooks',
+    state: 'none_enabled',
+    externalActionRisk: 'high',
+    implementableNow: true,
+    requiresLaterApproval: true,
+    note: '0 webhooks, 0 hooks. Creating/enabling a live hook is a later gate.',
+  },
+  {
+    moduleId: 'memory_second_brain_sync',
+    title: 'Memory / 2nd-Brain sync panel',
+    hermesFeature: 'Memory configuration',
+    state: 'read_only',
+    externalActionRisk: 'none',
+    implementableNow: true,
+    requiresLaterApproval: false,
+    note: 'Built-in memory; 2nd-Brain (.agentic_nexus) sync is local_only. External memory provider = new-vendor gate.',
+  },
+  {
+    moduleId: 'model_lane_picker',
+    title: 'Model / lane picker',
+    hermesFeature: 'Fuzzy model picker',
+    state: 'read_only',
+    externalActionRisk: 'none',
+    implementableNow: true,
+    requiresLaterApproval: false,
+    note: 'Reuses the operator lane registry. Switching a live operator model is a later gate.',
+  },
+  {
+    moduleId: 'skill_hygiene_panel',
+    title: 'Skill hygiene panel',
+    hermesFeature: 'Leaner skill set + curator',
+    state: 'read_only',
+    externalActionRisk: 'none',
+    implementableNow: true,
+    requiresLaterApproval: false,
+    note: 'Leaner skill set adopted; curator governs agent-created skills. Read-only posture.',
+  },
+  {
+    moduleId: 'trusted_skills_tap_registry',
+    title: 'Trusted skills tap registry',
+    hermesFeature: 'Trusted NVIDIA skills tap',
+    state: 'available_not_installed',
+    externalActionRisk: 'low',
+    implementableNow: true,
+    requiresLaterApproval: true,
+    note: 'Default taps only; NVIDIA trusted tap available but NOT installed (no auto-install).',
+  },
+  {
+    moduleId: 'undo_recovery_panel',
+    title: 'Undo / recovery panel',
+    hermesFeature: '/undo [N] + checkpoints',
+    state: 'read_only',
+    externalActionRisk: 'none',
+    implementableNow: true,
+    requiresLaterApproval: false,
+    note: 'Surfaces undo + checkpoint availability. Invoking recovery from the panel is a later gate.',
+  },
+  {
+    moduleId: 'security_version_compliance',
+    title: 'Security / version compliance panel',
+    hermesFeature: 'Security hardening, SSRF, CVE pinning, Debug Share',
+    state: 'read_only',
+    externalActionRisk: 'none',
+    implementableNow: true,
+    requiresLaterApproval: false,
+    note: 'Shows version 0.16.0, config format 27, and security posture flags. Display only.',
+  },
+] as const
+
+export function getControlPanelModules(): readonly HermesControlPanelModule[] {
+  return HERMES_CONTROL_PANEL_MODULES
+}
+
+/** Credential boundary â€” STATUS STRINGS ONLY. Never carries a secret value. */
+export interface CredentialBoundary {
+  boundary: string
+  status: string
+}
+
+export const CREDENTIAL_BOUNDARIES: readonly CredentialBoundary[] = [
+  { boundary: 'op_sandbox', status: 'BLOCKED-OP (1Password CLI auth not green)' },
+  { boundary: 'prod_secrets', status: 'not_read' },
+  { boundary: 'api_keys', status: 'none (no-API-key operator gateway principle)' },
+] as const
+
+export interface HermesControlPanelView {
+  source: 'static_registry'
+  liveConnections: false
+  externalChannelsEnabled: false
+  mcpConnected: false
+  remoteGatewayConnected: false
+  credentialsExposed: false
+  version: HermesVersionStatus
+  moduleCount: number
+  modulesImplementableNow: number
+  modulesRequiringLaterApproval: number
+  highRiskGatedCount: number
+  modules: HermesControlPanelModule[]
+  credentialBoundaries: CredentialBoundary[]
+  note: string
+}
+
+/**
+ * Read-only Control Panel view. No external connections, no secret values, nothing active.
+ */
+export function getControlPanelView(): HermesControlPanelView {
+  const modules = getControlPanelModules()
+  return {
+    source: 'static_registry',
+    liveConnections: false,
+    externalChannelsEnabled: false,
+    mcpConnected: false,
+    remoteGatewayConnected: false,
+    credentialsExposed: false,
+    version: getHermesVersionStatus(),
+    moduleCount: modules.length,
+    modulesImplementableNow: modules.filter((m) => m.implementableNow).length,
+    modulesRequiringLaterApproval: modules.filter((m) => m.requiresLaterApproval).length,
+    highRiskGatedCount: modules.filter((m) => m.externalActionRisk === 'high').length,
+    modules: [...modules],
+    credentialBoundaries: [...CREDENTIAL_BOUNDARIES],
+    note: 'Hermes v0.16 Surface-Release Control Panel â€” read-only foundation. No live connections, no external channels, no MCP, no remote gateway, no secret values.',
+  }
+}
diff --git a/skills/adversarial-review/SKILL.md b/skills/adversarial-review/SKILL.md
new file mode 100644
index 00000000..9762928d
--- /dev/null
+++ b/skills/adversarial-review/SKILL.md
@@ -0,0 +1,92 @@
+---
+name: adversarial-review
+description: Review a proposed fix with a different model in a fresh session that has no access to the builder's reasoning. Two axes, standards and spec, run as independent sub-agents. It flags; it never fixes. A fail returns to `propose-fix`. Use after `propose-fix`, before anything is applied.
+---
+
+# adversarial-review
+
+**The point is separation of model and context, not roleplay.** The reviewer is a sub-agent given a narrow brief and a fixed input set. It is not a persona, has no name, and is not asked to act like a senior engineer. Do not write character instructions.
+
+The value comes from what the reviewer **cannot see**: the builder's reasoning, its hypotheses, its rejected approaches, and its confidence. A reviewer that inherits the builder's context inherits its blind spots and will agree with them fluently.
+
+Dual-axis structure adapted from `code-review` (mattpocock/skills).
+
+## Spec-writing standard â€” read before writing the spec
+
+**Never write a spec item that asks a reviewer to prove an unbounded negative.**
+
+"Proves this page makes no network call" is unbounded. There is always another path â€” a dynamic import, `require`, `WebSocket`, `EventSource`, `sendBeacon`, a server action, a transitive side-effect import. A competent reviewer will keep finding them, correctly, forever. The builder cannot win and the reviewer is not wrong.
+
+**Write bounded, diff-relative claims instead.**
+
+| Unbounded â€” do not write | Bounded â€” write this |
+|---|---|
+| "makes no network call" | "introduces no network surface the source did not have" |
+| "is read-only" | "adds no write path absent from the baseline" |
+| "has no side effects" | "the diff adds no side-effect construct not already present" |
+
+A diff-relative claim is decidable: compare the change against a named baseline and the answer is yes or no. An absolute claim is not.
+
+**A diff-relative claim is only as strong as its baseline.** Where the baseline's own safety properties are load-bearing â€” an execution surface, a payment path, an approval gate â€” establish the baseline **by hand, once, before the port**, and cite that record in the spec. Otherwise "no new surface" inherits whatever the source already had, unexamined.
+
+*Earned 2026-08-01: three bounded attempts at capability 1 of the command-centre migration were all correctly failed by cross-vendor review, because the spec asked for absolute read-only proof. The code was fine every time. The spec was the defect.*
+
+## Phase 1 â€” Assemble the input set
+
+The reviewer receives **exactly these four things** and nothing else:
+
+1. **The spec** â€” the originating issue, requirement, or stated intent
+2. **The failing loop** â€” the command from `prove-the-failure` and its red output
+3. **The diff** â€” the proposed change
+4. **The test** â€” the regression test and its observed failure
+
+**Do not include:** the diagnosis narrative, the ranked hypotheses, the rejected approaches, the attempt number, or any statement of how confident the builder is. Those are exactly the contaminants.
+
+Use a different model from the one that produced the fix. Same model, fresh session, is a weaker control and must be recorded as such.
+
+## Phase 2 â€” Two independent sub-agents
+
+Send one message with two `Agent` calls. They do not see each other's output.
+
+**Standards sub-agent brief:**
+> Review this diff against the repository's documented coding standards, then against the baseline smells. Cite the source for each documented violation and quote the hunk for each smell. Distinguish hard violations from judgement calls. Skip anything tooling already enforces. Under 400 words.
+
+**Spec sub-agent brief:**
+> Review this diff against the spec and the failing loop. Report: (a) requirements missing or incomplete; (b) behaviour introduced that was not asked for; (c) requirements implemented incorrectly; (d) whether the test would actually catch the failure the loop demonstrates, or merely passes alongside it. Quote the spec for each. Under 400 words.
+
+Item (d) is the one that catches the expensive mistake: a test that passes for the wrong reason and locks in a fix that does not work.
+
+## Phase 3 â€” Consolidate
+
+Present findings under `## Standards` and `## Spec` without merging or reordering. The axes stay independent â€” merging them lets a weak finding on one borrow credibility from the other.
+
+Close with one line: count per axis, and the single most serious finding in each.
+
+## Verdict
+
+| Verdict | Meaning | Next |
+|---|---|---|
+| **PASS** | No hard violations; no missing or incorrect requirements; test genuinely catches the failure | `verify` |
+| **FAIL** | Any hard violation, missing requirement, unasked-for behaviour, or a test that does not catch the failure | back to `propose-fix` |
+
+A FAIL consumes an attempt from `propose-fix`'s bound of three.
+
+**Reviewer silence, timeout, or crash is not a pass.** Convergence requires an explicit, evidence-backed verdict. If the reviewer did not return one, the review did not happen.
+
+## Completion
+
+- [ ] Reviewer model named, and confirmed different from the builder's
+- [ ] Input set limited to the four items â€” confirmed, not assumed
+- [ ] Both sub-agents returned, or the missing one is recorded as a gap
+- [ ] Findings presented per-axis, unmerged
+- [ ] Explicit verdict recorded
+
+## Stop conditions
+
+**The reviewer flags; it does not fix.** A reviewer that returns a diff has exceeded its brief â€” discard the diff and keep the finding.
+
+**Do not argue with a FAIL.** Return to `propose-fix` and change the approach. Re-running the same review hoping for a different verdict is the reasoning-erosion pattern the fence exists to prevent.
+
+## Next
+
+`verify` on PASS. `propose-fix` on FAIL, attempt count incremented.
diff --git a/skills/classify/SKILL.md b/skills/classify/SKILL.md
new file mode 100644
index 00000000..3036bf3e
--- /dev/null
+++ b/skills/classify/SKILL.md
@@ -0,0 +1,84 @@
+---
+name: classify
+description: The gate in the self-healing chain. Decides whether a diagnosed failure may proceed to an automated fix, by asking two questions â€” is this a known pattern in incident memory, and how wide is the blast radius. Known and low-radius may proceed; novel or high-radius stops and reports with the diagnosis attached. Also holds the chain's circuit breaker. Use after `diagnose`, before any repair is designed.
+---
+
+# classify
+
+**This is the gate. Its default answer is stop.**
+
+Two questions decide it, and both must pass. Either one failing halts the chain and produces a report instead of a repair.
+
+## Phase 0 â€” Circuit breaker, checked first
+
+Before classifying anything, count remediations completed in the trailing window.
+
+**If remediations in the last 24 hours â‰¥ 3, the chain halts entirely.** Not this failure â€” the whole loop. Write the breaker trip to `incident-memory`, notify the founder, and stop.
+
+A system repairing itself repeatedly is not healthy, it is failing repeatedly and hiding it behind successful repairs. The breaker exists to make that visible rather than smooth.
+
+The breaker is reset by a human, never by the chain.
+
+## Phase 1 â€” Known or novel
+
+Query `incident-memory` for the diagnosed cause.
+
+**Known** requires all of:
+- [ ] A prior entry with the **same cause**, not merely the same symptom
+- [ ] That entry's fix was **verified green**, not merely applied
+- [ ] Its adversarial review **passed**
+
+Same symptom with a different cause is **novel**. This is the most common misclassification and the most expensive: it produces a confident fix aimed at last month's problem.
+
+**Novel** if the diagnosis is unproven. An unproven cause cannot match a known pattern, because you do not yet know what it is.
+
+## Phase 2 â€” Blast radius, assessed explicitly
+
+Answer both in writing. Not "low" â€” the actual answer.
+
+**What else changes if this fix runs?**
+Files, callers, shared modules, schema, config, env, scheduled jobs, other repos.
+
+**What could that trigger?**
+Downstream consumers, data migrations, cache invalidation, permission changes, anything that fires on deploy.
+
+**High blast radius** if any of:
+- Touches a production host, branch, or database on the fence's list
+- Spends money or provisions a paid resource
+- Changes a schema, a migration, or a permission
+- Alters shared code with more than one caller outside its own module
+- Modifies CI, deploy config, or a scheduled workflow
+- Changes anything on the fence's self-modification list
+
+**Low blast radius** requires: single module, no callers outside it, no schema, no config, no production surface, fully reversible by `git revert`.
+
+Unsure is **high**. The word "just" appearing in your own reasoning ("it just changes one line") is a signal to re-check, not to proceed.
+
+## Decision
+
+| Known | Blast radius | Outcome |
+|---|---|---|
+| Yes | Low | **Proceed** to `propose-fix` |
+| Yes | High | **Stop.** Report with diagnosis attached |
+| No | Low | **Stop.** Report with diagnosis attached |
+| No | High | **Stop.** Report with diagnosis attached |
+
+Three of four outcomes are stop. That is correct and deliberate.
+
+## Completion
+
+- [ ] Breaker checked and not tripped
+- [ ] Known/novel decided, with the matching entry cited or its absence stated
+- [ ] Both blast-radius questions answered in writing
+- [ ] Decision recorded with its reason
+- [ ] If stopped: the report carries the full diagnosis, not a summary of it
+
+## Stop conditions
+
+**Stopping is the normal outcome, not a failure of the chain.** A stop with an attached diagnosis is more valuable than an automated fix, because a human reads a diagnosis in a minute and reviews a diff in twenty.
+
+Never widen "known" to make a fix eligible. If you find yourself arguing that a different cause is close enough, the answer is novel.
+
+## Next
+
+`propose-fix` if proceeding. Otherwise `incident-memory`, then report and stop.
diff --git a/skills/contain/SKILL.md b/skills/contain/SKILL.md
new file mode 100644
index 00000000..70d67fec
--- /dev/null
+++ b/skills/contain/SKILL.md
@@ -0,0 +1,48 @@
+---
+name: contain
+description: Reach a safe state before diagnosing. Halt the affected job, revert to last known good, or stop the loop â€” containment, never a fix. Records what was contained and what was deliberately left alone. Use immediately after `prove-the-failure`, or any time a failure is actively recurring and the bleeding must stop first.
+---
+
+# contain
+
+**Containment is not repair.** The goal is to stop the failure recurring while you still understand nothing about its cause. A fix applied here is a guess applied at the worst possible moment.
+
+Resist the pull to fix. You have a red loop and no diagnosis. Anything that looks like a repair right now is a coincidence waiting to be mistaken for understanding.
+
+## Phase 1 â€” Choose the smallest sufficient action
+
+In ascending order of blast radius. Take the first that reaches safety.
+
+1. **Do nothing** â€” the failure is not recurring and nothing is degrading. Record this as the containment decision. It is a real choice and often the right one.
+2. **Stop the loop** â€” disable the schedule, pause the cron, unqueue the job.
+3. **Halt the affected job** â€” cancel the in-flight run only.
+4. **Revert to last known good** â€” roll back the deployment or the commit that introduced it.
+5. **Isolate the surface** â€” take the affected route, feature flag, or integration out of the path.
+
+**Prefer reversible containment over thorough containment.** Disabling a schedule is trivially undone. A revert is not.
+
+## Phase 2 â€” Record the boundary
+
+Write both halves. The second half is the one people skip and the one that matters later.
+
+- **Contained:** what you stopped, how, and how to undo it
+- **Deliberately left alone:** what you could have stopped and chose not to, with the reason
+
+The "left alone" list is what a reviewer checks when the failure recurs somewhere adjacent. Without it, the next responder cannot tell the difference between a considered decision and an oversight.
+
+## Completion
+
+- [ ] The failure is no longer actively recurring, **or** "not recurring" is recorded as the finding
+- [ ] The containment action is named, with its exact undo
+- [ ] The deliberately-left-alone list is written, with reasons
+- [ ] No repair was attempted
+
+## Stop conditions
+
+**Containment that spends money or mutates production stops for a human.** Reverting a production deploy, disabling a live integration, and rolling back a database are all gated â€” propose them, do not perform them. Disabling a schedule in a repo, cancelling a CI run, and stopping a local loop are inside the fence.
+
+**If the only sufficient containment is a gated action,** stop and report: the symptom, the red loop, the proposed containment, and its undo. A human decides. Do not downgrade to a weaker containment just to stay inside the fence â€” say plainly that the safe action is gated.
+
+## Next
+
+`diagnose` â€” root cause, against the loop.
diff --git a/skills/diagnose/SKILL.md b/skills/diagnose/SKILL.md
new file mode 100644
index 00000000..e5d74687
--- /dev/null
+++ b/skills/diagnose/SKILL.md
@@ -0,0 +1,68 @@
+---
+name: diagnose
+description: Find root cause by running the red loop from `prove-the-failure` and changing one variable at a time â€” never by reading the code and reasoning about it. Outputs a written cause with file and line evidence. Use after containment, before any repair is designed.
+---
+
+# diagnose
+
+**Diagnose against the loop, not against a reading of the code.** A cause derived from reading is a hypothesis wearing a conclusion's clothes. A cause is proven when you can turn the loop red and green at will by changing the thing you claim is responsible.
+
+## Phase 1 â€” Confirm the loop is measuring the right failure
+
+Run it. Observe.
+
+- [ ] It produces the reported failure mode, not a nearby different one
+- [ ] It reproduces consistently, or at the pinned rate
+- [ ] The exact symptom is captured â€” error text, wrong value, timing
+
+If the loop is red for a different reason than the report, you have found a second failure. Stop and split them.
+
+## Phase 2 â€” Minimise
+
+Shrink to the smallest scenario still going red. Remove inputs, callers, config, data, and steps one at a time, rerunning after each cut. Keep only what is load-bearing.
+
+Done when removing any remaining element turns the loop green.
+
+## Phase 3 â€” Hypothesise, then rank
+
+Generate 3â€“5 hypotheses **before testing any**. Each must be falsifiable and predict a direction:
+
+> "If X is the cause, then changing Y turns the loop green, and changing Z makes it worse."
+
+Rank them. Cheap-and-likely first.
+
+## Phase 4 â€” Instrument, one variable at a time
+
+Each probe tests exactly one ranked prediction.
+
+1. **Debugger or REPL** where available â€” one breakpoint beats ten logs.
+2. **Targeted logs** at the boundary that distinguishes two hypotheses.
+3. Never "log everything and grep".
+
+Tag every debug log uniquely (`[DEBUG-a4f2]`) so cleanup is one grep.
+
+## Phase 5 â€” Prove it
+
+The cause is proven when you can **turn the loop red and green on demand** by toggling the claimed cause, and no other change is required.
+
+Correlation is not enough. If the loop goes green when you change two things, you have not finished.
+
+## Completion â€” a written cause
+
+- [ ] **Cause stated in one sentence**, in plain language
+- [ ] **File and line evidence** â€” the exact location, quoted
+- [ ] **Toggle demonstrated** â€” loop goes red and green by changing that thing alone
+- [ ] **Rejected hypotheses listed**, each with what ruled it out
+- [ ] **All debug instrumentation removed**
+
+The rejected list matters. It is the cheapest thing to write now and the most expensive to reconstruct later, and `classify` uses it to judge novelty.
+
+## Stop conditions
+
+**No red loop, no diagnosis.** If `prove-the-failure` did not complete, go back. Do not proceed on a described failure.
+
+**If the cause cannot be proven by toggle,** say so and report the strongest hypothesis as *unproven*, with what would prove it. An unproven cause is a valid outcome â€” it goes to `incident-memory` and it forces `classify` to treat the failure as novel.
+
+## Next
+
+`classify` â€” known or novel, and how wide the blast radius is.
diff --git a/skills/immunise/SKILL.md b/skills/immunise/SKILL.md
new file mode 100644
index 00000000..e448819a
--- /dev/null
+++ b/skills/immunise/SKILL.md
@@ -0,0 +1,51 @@
+---
+name: immunise
+description: Add the check, test, or workflow guard that stops this failure recurring. May propose a fence change; may never apply one. Use after `verify` passes and its cooldown has elapsed.
+---
+
+# immunise
+
+**A fix repairs this instance. An immunisation makes the class impossible.** Without this step the chain is a treadmill: the same failure returns, gets diagnosed again, and the cost is paid every time.
+
+Ask one question: **what would have caught this before it shipped?**
+
+## Phase 1 â€” Choose the cheapest guard that would have caught it
+
+In ascending order of cost. Take the first that would actually have fired.
+
+1. **A test** â€” the regression test from `propose-fix` may already be sufficient. If so, say so and stop; that is a complete immunisation.
+2. **A lint or type rule** â€” catches the class at author time.
+3. **A CI check** â€” catches it before merge.
+4. **A workflow guard** â€” a required check, a branch protection rule, a job precondition.
+5. **A fence predicate** â€” catches it at execution, for the class the fence exists to stop.
+
+Test the guard against the failure. **A guard that would not have fired is not an immunisation**, however sensible it looks. Prove it: apply the guard to the pre-fix state and watch it catch the failure.
+
+## Phase 2 â€” The fence carve-out
+
+**`immunise` may propose a fence change. It may never apply one.**
+
+Fence rule files â€” `fence.json`, the classifier, the hook registration, `denials.jsonl`, `HARD_STOP`, and the constitution corpus â€” are on the self-modification list. Touching them **freezes the agent on the first attempt**, not the second. There is no bounded-retry allowance here and no "the fix requires it" exception.
+
+This is not friction to be worked around. A self-healing chain that can widen its own fence is not fenced â€” it is a system that repairs its way out of its constraints, one justified step at a time. The whole point of the chain is that it stops at the same two gates as everything else.
+
+So the output for a fence-class immunisation is **a proposed diff to `fence.json`, in the report, unapplied.** A human applies it. Say plainly that this is the recommendation and why the chain cannot perform it.
+
+## Completion
+
+- [ ] Guard chosen, at the cheapest level that would have fired
+- [ ] **Guard proven** â€” applied to the pre-fix state and observed catching the failure
+- [ ] Guard applied, **or** proposed-and-unapplied if it touches the fence or a gated surface
+- [ ] If the regression test was sufficient, that is stated rather than padded with a redundant second guard
+
+## Stop conditions
+
+**No fence application. Ever. First attempt is terminal.**
+
+**A guard that touches CI, deploy config, or branch protection is a production surface** â€” propose it, do not apply it.
+
+**If no guard would have caught this,** say so. That is an honest and important finding: it means the class is currently undetectable, which is architectural information worth more than a guard that gives false comfort.
+
+## Next
+
+`incident-memory` â€” write it down so `classify` can read it.
diff --git a/skills/incident-memory/SKILL.md b/skills/incident-memory/SKILL.md
new file mode 100644
index 00000000..51c15995
--- /dev/null
+++ b/skills/incident-memory/SKILL.md
@@ -0,0 +1,80 @@
+---
+name: incident-memory
+description: Write symptom, cause, fix, reviewer verdict and outcome to a searchable store â€” successes and failures alike. This is the store `classify` reads to decide known versus novel. Use as the final step of every self-healing chain, including chains that stopped early or gave up.
+---
+
+# incident-memory
+
+**Every chain writes here. Including the ones that failed.**
+
+A store containing only successes teaches `classify` that everything is known and everything works. The failures â€” the unproven causes, the exhausted attempt bounds, the loops that could not be built â€” are what make the known/novel judgement honest.
+
+Store: `.harness/incidents.jsonl`, append-only, one JSON object per line.
+
+## Phase 1 â€” Write the record
+
+```jsonc
+{
+  "id": "INC-<UTC timestamp>",
+  "ts": "2026-08-01T03:40:00Z",
+  "symptom": "what was observed, in the reporter's words",
+  "loop": "the exact command from prove-the-failure",
+  "loop_built": true,
+  "contained": "what was stopped, and its undo",
+  "left_alone": "what was deliberately not stopped, and why",
+  "cause": "one sentence, plain language",
+  "cause_proven": true,
+  "evidence": "file:line",
+  "rejected": ["hypotheses ruled out, and what ruled them out"],
+  "classification": "known|novel",
+  "blast_radius": "low|high",
+  "blast_reasoning": "what else changes, what that could trigger",
+  "attempts": 1,
+  "approaches": ["how each attempt differed in kind"],
+  "diff": "path to the proposed diff, or null",
+  "reviewer_model": "which model reviewed",
+  "reviewer_verdict": "PASS|FAIL|ABSENT",
+  "reviewer_findings": ["what it flagged"],
+  "verified": true,
+  "immunisation": "the guard added, or the fence change proposed-unapplied, or none-possible",
+  "outcome": "fixed|gave-up|stopped-at-gate|breaker-tripped",
+  "gated_on": "what a human must decide, if anything"
+}
+```
+
+**`outcome` is the load-bearing field.** Its four values are all legitimate:
+
+- `fixed` â€” verified green, immunised
+- `gave-up` â€” attempt bound exhausted; the diagnosis is the deliverable
+- `stopped-at-gate` â€” `classify` said novel or high-radius, or a gated action was required
+- `breaker-tripped` â€” the chain halted; a human was called
+
+## Phase 2 â€” Make it findable
+
+`classify` matches on **cause**, not symptom. Write the cause so a later match is possible:
+
+- State the mechanism, not the manifestation. *"Workflow pushes directly to a protected branch"* not *"the drain job failed"*.
+- Name the surface concretely â€” repo, file, job, database.
+- Avoid words that only make sense with this incident's context in mind.
+
+Search is `grep` over the JSONL. Do not build an index; the file is small and will stay small, and an index is a second thing that can be wrong.
+
+## Completion
+
+- [ ] Record appended, valid JSON on one line
+- [ ] `cause` written as a mechanism, matchable by a future chain
+- [ ] `outcome` set to one of the four values
+- [ ] Failures recorded with the same care as successes
+- [ ] `gated_on` populated whenever a human must act
+
+## Stop conditions
+
+**Never rewrite or delete a prior record.** Append-only. A correction is a new record referencing the old `id`.
+
+**Never mark `outcome: fixed` without `verified: true`.** Applied is not verified; verified means `verify` ran and pasted both outputs.
+
+**This file is not on the self-modification list, but treat it as evidence.** An agent editing its own incident history to make a failure look known is the same class of act as clearing its own denials â€” if you find yourself wanting to adjust a past record so the current fix qualifies, that is the signal to stop.
+
+## Next
+
+End of chain. If `outcome` is anything other than `fixed`, the report goes to the founder with the diagnosis attached.
diff --git a/skills/nexus/SKILL.md b/skills/nexus/SKILL.md
index d7551d1f..e38f79f7 100644
--- a/skills/nexus/SKILL.md
+++ b/skills/nexus/SKILL.md
@@ -77,7 +77,9 @@ subagent to find credible sources that *contradict* it). If margot is unreachabl
 is **zero**. The routing menu lists up to ~10 specialists; deploy only the ones a stated need
 demands, default cap â‰¤3, escalating only on demonstrated breadth. Wrap each dispatch in the
 Nexus Prompt at its calibrated tier (nexus tier ladder in the playbook); cross-domain work â†’
-`specialist-council` for its `{verdict, must_fix, suggestions}` contract. A dispatched
+`specialist-council` for its `{verdict, must_fix, suggestions}` contract. Spec-shaped goals
+route to the `/spm` self-leveling bench instead (playbook G4 menu) â€” it brings its own
+fan-out; do not double-bench. A dispatched
 specialist must never re-enter this orchestrator; dispatch depth is capped at 1; the whole gate
 is bound to `TAO_HARD_STOP` / `TAO_MAX_COST_USD`.
 - Completion: each dispatched specialist earned its slot against a named need, or none were.
diff --git a/skills/nexus/references/orchestration-playbook.md b/skills/nexus/references/orchestration-playbook.md
index fb10a603..5aaf1534 100644
--- a/skills/nexus/references/orchestration-playbook.md
+++ b/skills/nexus/references/orchestration-playbook.md
@@ -104,6 +104,7 @@ what G3 already ran.
 | Strategic / go-no-go decision | `ceo-board` (deliberation), `boardroom` (multi-model triangulation), `judge` |
 | Knowledge base / project brain adoption (index a repo or bot's knowledge tree, retrieval standard, drift gates) | `second-brain-adopt` â€” preflights hands-off/autogit gates, applies the brain-1 adoption kit, ships via PR lane |
 | Cross-domain (â‰¥3 of the above) | `specialist-council` â€” one call returns `{verdict, must_fix, suggestions}` |
+| Spec-shaped goal (what exactly should be built â€” plan/spec/scope) | `spm` â€” its **self-leveling MOA bench** brings its own fan-out (0â€“8 seats sized by its F/I/N/X/S rubric, `spm/references/leveling.md`); do **not** dispatch additional G4 specialists for the same need (no double-benching) |
 | Finance / growth / infra / support metrics | senior agents CFO / CMO / CTO / CS (Pi-Dev-Ops swarm) |
 
 Dispatch mechanics: fan out as parallel `Agent` calls in one message (or a `Workflow` for a
@@ -135,9 +136,11 @@ token is unprovisioned) â€” do NOT claim unattended resilience until those close
 
 ### Recursion + cost guard (mandatory when G4 opens)
 A dispatched specialist must never re-enter this orchestrator (no nexus-inside-nexus). Dispatch
-depth is capped at 1 level. Bind the whole gate to the estate kill-switches: honour
-`TAO_HARD_STOP` (the `~/.claude/HARD_STOP` file) and `TAO_MAX_COST_USD`; if a fan-out would
-exceed the cost ceiling, narrow the bench first.
+depth is capped at 1 level. When the G4 pick is `spm`, nexus pulls `Skill(spm)` inline and the
+bench's seats count as nexus's depth-1 dispatches â€” seats are terminal leaf agents (they never
+invoke skills or spawn agents; guard text in `spm/references/moa-board.md`). Bind the whole gate
+to the estate kill-switches: honour `TAO_HARD_STOP` (the `~/.claude/HARD_STOP` file) and
+`TAO_MAX_COST_USD`; if a fan-out would exceed the cost ceiling, narrow the bench first.
 
 ## G3 â€” Deep-research integrity bar
 The failure mode is confident consensus-mush wearing a bibliography. Guard it:
diff --git a/skills/orchestrator/SKILL.md b/skills/orchestrator/SKILL.md
new file mode 100644
index 00000000..39f3e100
--- /dev/null
+++ b/skills/orchestrator/SKILL.md
@@ -0,0 +1,140 @@
+---
+name: orchestrator
+description: Continuity spine from idea to ship. Runs an OUTER loop that pushes work hub-to-spoke out to estate projects, and an INNER loop that audits itself against the constitution â€” trimming cache, flagging hallucinations, tightening code generation. Does NOT do the work; it routes, gates and carries state. Pulls a skill only when a stage needs it and releases it immediately, to protect context. The constitution is a hard gate checked before every self-modifying action. Stops for exactly two things â€” spending money and touching production. A denied action is terminal. Use when running a project end-to-end, resuming a stalled build, or dispatching work across multiple estate repos.
+owner_role: Orchestrator
+status: wave-1
+---
+
+# orchestrator â€” the thread that survives the context window
+
+**It does not write the code.** It decides what happens next, who does it, whether that is allowed, and
+whether the previous stage actually finished. Every other skill is labour; this is continuity.
+
+The problem it solves: work in this estate dies at handoffs. A session ends, a context compacts, an agent
+finishes a stage and the thread is gone â€” the estate's own constitution calls this its dominant failure
+mode. The orchestrator holds the thread by refusing to keep it in context at all.
+
+> **State lives on the tracker. Context is a scratchpad that is expected to be lost.**
+
+## The fence (read first)
+
+Two stops, and nothing else: **spending money** and **touching production**.
+Full predicates: [`docs/orchestrator/01-decision-rights.md`](../../docs/orchestrator/01-decision-rights.md).
+
+- Inside the fence, act. Do not ask permission for reversible work â€” paralysis by approval is a named
+  constitutional failure mode (Â§6.43), equal in weight to self-authorisation.
+- **A denied action is terminal.** Not deferred, not appealable. Denial is matched by fingerprint *before*
+  reasoning begins; quality of argument is not an input. Only the founder clears one, and clearing creates
+  a new record rather than reviving the dead one.
+- The fence is enforced at the `PreToolUse` hook, not by this prompt. **If that hook is not registered,
+  this skill is running unfenced â€” say so, out loud, before starting.**
+
+## Outer loop â€” hub to spoke
+
+Pi-Dev-Ops is the hub. Estate projects are spokes: `Authority-Site`, `CARSI`, `RestoreAssist`,
+`Synthex`, `Disaster-Recovery`, `DR-NRPG`, `ITR-Button`.
+
+```
+for each spoke with open work:
+    1. READ    the spoke's tracker state (Linear) â€” never the spoke's context
+    2. CLASSIFY the next stage from the artifact that exists, not from memory
+    3. GATE     the stage against the fence
+    4. DISPATCH a sub-agent scoped to exactly that stage
+    5. RECEIVE  its artifact; verify the artifact exists before marking the stage closed
+    6. WRITE    state back to the tracker
+    7. RELEASE  every skill and file the stage pulled
+```
+
+**Step 5 is the one that matters.** A stage is closed by the existence of its artifact, never by an
+agent's report that it finished. Per `proof-discipline`: a claim is not evidence. If the artifact is
+absent, the stage is open regardless of what the sub-agent said.
+
+**Authority does not travel with the work.** Â§6.21/Â§6.22: a sub-agent inherits objective, constraints,
+autonomy level and non-delegables â€” and never more authority than the orchestrator holds. Permission from
+one spoke does not carry to another.
+
+**Spokes are independent.** Work them in parallel where they do not share a substrate; a blocked spoke
+must never block a clear one. When a spoke stalls on a gate, record the gate and move to the next spoke â€”
+do not idle waiting.
+
+## Inner loop â€” self-audit
+
+Runs on cadence, between outer-loop passes.
+Full spec: [`docs/orchestrator/03-inner-loop.md`](../../docs/orchestrator/03-inner-loop.md).
+
+1. **Trim** â€” evict stale cache, dead branches, superseded artifacts, duplicated skills
+2. **Flag** â€” scan its own recent output for claims asserted without a tool result behind them
+3. **Tighten** â€” feed shipped-vs-reverted outcomes back into how it generates code
+4. **Log** â€” every self-modification appended to `.harness/self-mods.jsonl`, board-reviewable
+
+Self-fixes execute freely inside the fence. A self-fix that would touch money or production stops and
+asks â€” including any edit to `fence.json`, the classifier, or the hook itself (Â§6.2: no system expands its
+own permissions).
+
+## Skill checkout / check-in
+
+**Pull a skill when a stage needs it. Release it when the stage ends.** Never pre-load the catalogue.
+
+| Stage | Checks out |
+|---|---|
+| idea | `nexus-recall`, `brainstorming` |
+| research | `nexus-recall`, `source-ingest`, `firecrawl-*` |
+| scope | `spm`, `grill-me` |
+| plan | `engineering-requirements` (the 17-seat bench), `judge` |
+| build | domain skills only â€” the narrowest that fit |
+| test | `qa-lead`, `proof-discipline` |
+| ship | `pr-release-gate`, `task-completion-gate` |
+
+Release is not optional bookkeeping â€” it is the mechanism that lets one orchestrator run a seven-stage
+project without ever holding seven stages in context. A skill still loaded after its stage is a leak.
+
+**Route through `skills/index.md`; never invoke a sub-skill directly.** Entry points dispatch their own
+children.
+
+## The continuity spine
+
+`idea â†’ research â†’ scope â†’ plan â†’ build â†’ test â†’ ship`
+
+Full spec: [`docs/orchestrator/04-continuity-spine.md`](../../docs/orchestrator/04-continuity-spine.md).
+
+Each stage hands the next a **written artifact**, not a memory. **A stage cannot open until the prior
+stage has left its artifact** â€” that precondition is the thread. The plan parks on Linear as the map.
+
+At ship, the **coverage check**: every requirement from the original idea is marked `built`, `partial`, or
+`missing`. Silence is not coverage. A requirement nobody can find is `missing`, not "presumably done".
+
+## Constitution gate
+
+Checked before every self-modifying action, against the compiled `fence.json` â€” **not** against the
+10,859-line prose corpus. Prose cannot gate; see
+[`docs/orchestrator/00-constitution-boundary-audit.md`](../../docs/orchestrator/00-constitution-boundary-audit.md),
+which found **0 of 16 boundaries currently both unambiguous and hard-stop**.
+
+Six boundaries compile today and are the gate's initial content: Â§6.6 non-delegables (16 enumerated
+prohibitions), Â§6.21 delegation ceiling, Â§6.40 compression manifest, Â§6.42 fingerprint, Â§6.35 log states,
+and KILL.
+
+Fail closed. A missing, unparseable or erroring fence is a **stopped** orchestrator, never an open one.
+
+## What this skill must never do
+
+- **Do the work.** If it is writing implementation code, it has stopped orchestrating. Dispatch.
+- **Trust a report over an artifact.** Â§6.35 distinguishes `attempted` from `completed` for this reason.
+- **Carry state in context.** Anything not on the tracker is assumed lost at the next compaction.
+- **Re-litigate a denial.** See the fence.
+- **Expand its own authority**, or edit the fence that binds it.
+- **Treat silence as approval** (Â§6.25) â€” an unanswered gate is blocked, not permitted.
+
+## Anti-duplication
+
+This composes; it does not replace. Decision engine â†’ `swarm/nexus/policy.py`. Tiers â†’ `autonomy-ladder`.
+Halt â†’ `kill-switch-binding` / `swarm/kill_switch.py`. Cross-tool steps â†’ `dispatcher-core`. Audit rows â†’
+`audit-emit`. Ship gate â†’ `pr-release-gate`. Evidence standard â†’ `proof-discipline`.
+
+It adds one thing none of them has: **the thread between stages, held outside context.**
+
+## Known gap â€” state honestly at every run
+
+The `PreToolUse` hook does not exist yet, and on phill-desktop the `PermissionRequest` hook returns an
+unconditional `allow`. Until both are fixed, this orchestrator's fence is **honour-system**, and it must
+say so rather than imply enforcement it does not have.
diff --git a/skills/propose-fix/SKILL.md b/skills/propose-fix/SKILL.md
new file mode 100644
index 00000000..b1d85e47
--- /dev/null
+++ b/skills/propose-fix/SKILL.md
@@ -0,0 +1,77 @@
+---
+name: propose-fix
+description: Produce the repair as a diff plus a regression test, never applied directly. Bounded to three attempts, each materially different, then it stops and reports the diagnosis rather than the error. Use only after `classify` returns proceed.
+---
+
+# propose-fix
+
+**The output is a proposal, not a change.** A diff and a test, both reviewable, neither applied. Application happens after `adversarial-review` passes and `verify` runs â€” not here.
+
+## Phase 1 â€” Regression test first
+
+Write the test before the fix, and only if a **correct seam** exists.
+
+A correct seam exercises the real failure pattern as it occurs at the call site. A shallow seam that mocks past the bug produces false confidence and is worse than no test.
+
+If no correct seam exists, that finding is itself the important output. Record it and propose the architectural change that would create one â€” do not fake a test against a seam that cannot fail.
+
+1. Turn the minimised repro from `diagnose` into a failing test.
+2. **Watch it fail.** Paste the output.
+3. Confirm it fails for the diagnosed reason, not an adjacent one.
+
+## Phase 2 â€” Smallest sufficient diff
+
+The fix addresses the **proven cause**, not the symptom and not the surrounding code you would like to improve.
+
+- No refactoring alongside the fix. Two changes in one diff cannot be reviewed or reverted independently.
+- No opportunistic cleanup, however tempting.
+- No new abstraction for a single call site.
+
+Every changed line traces to the diagnosed cause. If a line does not, remove it.
+
+## Phase 3 â€” Attempt bound
+
+**Three attempts maximum.** Each must try a **materially different approach** â€” not the same idea with adjusted parameters.
+
+| Attempt | Requirement |
+|---|---|
+| 1 | The direct fix at the diagnosed cause |
+| 2 | A different mechanism or a different layer â€” must be describable as "instead of X, do Y" |
+| 3 | Last, materially different again |
+
+Retrying the same approach with a tweaked value is **not** a second attempt. If you cannot describe how attempt N differs in kind from Nâˆ’1, you have not made one.
+
+After three, **stop and report the diagnosis, not the error.** The diagnosis is the durable finding; the failed attempts are context. "Three approaches failed, here is the proven cause and why each failed" is a good outcome and is logged as such.
+
+**Giving up is a valid, logged outcome.** It goes to `incident-memory` with the same weight as a success.
+
+## The distinction from R3 â€” read this before retrying anything
+
+These are different rules and conflating them breaks both.
+
+| | Retrying a **denied** action | Retrying a **failed repair** |
+|---|---|---|
+| What happened | The fence refused it â€” money, production, or self-modification | A proposed fix did not work |
+| Rule | **Terminal.** Never retry, never route around it. A second attempt freezes the agent. | **Permitted, bounded.** Up to three materially different attempts. |
+| Why | A refusal is a decision. Persistence must not erode it. | A failure is information. Iteration is how it gets used. |
+| Who clears it | Founder only, creating a new record | Nobody â€” the bound is self-enforcing |
+
+The rules do not overlap. **A denied action was never attempted; a failed repair was attempted and did not work.** If a fence denial occurs during a repair attempt, that denial is terminal and the attempt bound does not license retrying it â€” stop and report.
+
+## Completion
+
+- [ ] Regression test written, **run, and observed failing** (output pasted)
+- [ ] Diff produced, smallest sufficient, every line traced to the cause
+- [ ] Nothing applied â€” this is a proposal
+- [ ] Attempt number recorded, and how it differs in kind from the previous
+- [ ] If bound exhausted: diagnosis reported, not the error
+
+## Stop conditions
+
+**Nothing is applied here.** If a step requires writing to the working tree beyond a scratch branch, stop.
+
+**A fix that spends money or mutates production is proposed only.** It never proceeds automatically regardless of what `classify` returned.
+
+## Next
+
+`adversarial-review` â€” a different model, a fresh session, no access to this reasoning.
diff --git a/skills/prove-the-failure/SKILL.md b/skills/prove-the-failure/SKILL.md
new file mode 100644
index 00000000..52e50600
--- /dev/null
+++ b/skills/prove-the-failure/SKILL.md
@@ -0,0 +1,63 @@
+---
+name: prove-the-failure
+description: Build a tight, red-capable feedback loop that drives the actual failure path and asserts the exact symptom, before any hypothesis is formed. One command, already run once, deterministic, fast, agent-runnable. Use at the start of any self-healing chain, or whenever something is broken/failing/slow and you are tempted to read the code and guess. First link in the self-healing chain; hands off to `contain`.
+---
+
+# prove-the-failure
+
+**This is the core skill. Everything downstream follows mechanically.** With a tight pass/fail signal targeting *this specific failure*, the cause becomes findable. Without one, code inspection is guessing with extra steps.
+
+Adapted from `diagnosing-bugs` Phase 1 (mattpocock/skills). The discipline is his; the gate is ours.
+
+Invest heavily here. Be aggressive. Refuse to give up early.
+
+## Phase 1 â€” Construct the loop
+
+Try roughly in this order. Stop at the first that goes red.
+
+1. **Failing test** at the right seam (unit, integration, e2e).
+2. **Replay the real artifact** â€” the failing CI log, webhook payload, or event that proves the failure already happened.
+3. **CLI invocation** with fixture input, diffing output against known-good.
+4. **HTTP script** against a running dev server.
+5. **Headless browser script** â€” drives UI, asserts DOM/console/network.
+6. **Throwaway harness** â€” minimal subset of the system, dependencies mocked.
+7. **Differential loop** â€” same input through old vs new, diff the outputs.
+8. **Bisection harness** â€” automate the state check, hand it to `git bisect run`.
+
+For a failure that has already occurred in production or CI, strategy 2 is usually fastest and is always the most honest: the artifact is evidence, not a simulation.
+
+## Phase 2 â€” Tighten it
+
+Treat the loop as a product.
+
+- **Faster?** Cache setup, skip unrelated init, narrow scope.
+- **Clearer signal?** Assert the exact symptom, not "didn't crash".
+- **Deterministic?** Pin time, seed RNG, isolate filesystem, freeze network.
+
+A 30-second flaky loop barely helps. A 2-second deterministic loop is a superpower.
+
+**Non-deterministic failures:** target a higher reproduction rate, not a clean repro. Loop the trigger 100Ã—, parallelise, narrow the timing window. A 50% failure is debuggable; 1% is not.
+
+## Completion â€” tight, red-capable loop
+
+Succeeds when you have **one command** that you have **already run at least once**, and you paste both the invocation and its output.
+
+- [ ] **Red-capable** â€” drives the actual failure path and asserts the specific symptom
+- [ ] **Deterministic** â€” same result across runs (or a pinned high rate)
+- [ ] **Fast** â€” seconds, not minutes
+- [ ] **Agent-runnable** â€” no human in the loop
+- [ ] **Pasted** â€” invocation and output are in the record, not described
+
+## Stop conditions
+
+**If a hypothesis is formed before this command exists, stop.** That is the failure this skill prevents. No red-capable command means no `diagnose`.
+
+**If you cannot build a loop,** stop and state exactly what you tried. Request one of: access to a reproducing environment, captured artifacts (CI log, HAR, payload, core dump), or permission for temporary instrumentation. Do not hypothesise without a loop. "I could not build a loop" is a valid, logged outcome â€” it goes to `incident-memory` like any other.
+
+## Gates
+
+Building a loop is read-and-local work and needs no approval. But a loop that reaches a production host, spends credits, or writes to a production database **is itself a gated action** â€” reproduce against a preview branch, a fixture, or the captured artifact instead. If the only possible loop touches production, stop and report that; do not build it.
+
+## Next
+
+`contain` â€” reach a safe state before diagnosing.
diff --git a/skills/self-healing-chain.md b/skills/self-healing-chain.md
new file mode 100644
index 00000000..02a97a61
--- /dev/null
+++ b/skills/self-healing-chain.md
@@ -0,0 +1,34 @@
+# The self-healing chain
+
+Nine single-purpose skills. Each is reachable on its own; the chain is the default order, not a requirement.
+
+```
+prove-the-failure â†’ contain â†’ diagnose â†’ classify â”€â”¬â†’ propose-fix â†’ adversarial-review â†’ verify â†’ immunise â†’ incident-memory
+                                                    â””â†’ (stop, report with diagnosis) â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â†’ incident-memory
+```
+
+`classify` is the gate. Three of its four outcomes are stop. That is the design.
+
+## Chain-wide constraints
+
+**Two gates, the same two as everything else.** No step spends money or mutates production without the founder. The chain has no special standing â€” a self-healing action is an action.
+
+**`immunise` may propose a fence change; it may never apply one.** Fence rule files are on the self-modification list and freeze the agent on the *first* attempt. There is no bounded-retry allowance and no "the fix requires it" exception. A chain that can widen its own fence is not fenced.
+
+**Bounded retries: three attempts, each materially different.** Then stop and report the diagnosis rather than the error. Giving up is a valid, logged outcome with the same standing as a fix.
+
+**R3 is a different rule and must not be conflated.** Retrying a *denied* action is terminal â€” a second attempt freezes the agent. Retrying a *failed repair* is permitted within the bound of three. A denied action was never attempted; a failed repair was attempted and did not work. If a fence denial occurs mid-repair, the denial is terminal and the attempt bound does not license retrying it. This distinction is restated in full inside `propose-fix`, deliberately, so an agent reading only that skill still gets it right.
+
+**Circuit breaker over the whole chain.** If completed remediations in the trailing 24 hours reach three, the chain halts entirely â€” not just the current failure â€” writes the trip to incident memory, and calls the founder. Reset is human-only. A system repairing itself repeatedly is failing repeatedly with the evidence smoothed over.
+
+**Never spec an unbounded negative.** A spec item that asks a reviewer to prove something never happens â€” no network call, no write, no side effect â€” cannot be satisfied, because there is always one more path. Write bounded, diff-relative claims: *"introduces no X the source did not have"*. Where the baseline's own safety properties are load-bearing, establish that baseline by hand before the port and cite it. Full rule in `adversarial-review`.
+
+**No expert personas.** Sub-agents are task-execution tools with narrow briefs and word limits. `adversarial-review` gets its value from what the reviewer *cannot see* â€” the builder's reasoning â€” not from pretending to be a senior engineer. Separation of model and context is the mechanism; roleplay is noise.
+
+## Lineage
+
+`prove-the-failure` is `diagnosing-bugs` Phase 1 (mattpocock/skills), made into a hard gate. `adversarial-review`'s dual-axis structure is from `code-review` (same repo). The phase-gate form, imperative voice, and checkbox completion criteria follow that house style throughout.
+
+## Status
+
+Built, not installed. Not wired to any trigger. The fence remains in shadow.
diff --git a/skills/specialist-council/SKILL.md b/skills/specialist-council/SKILL.md
index 46085adb..af2ffd2e 100644
--- a/skills/specialist-council/SKILL.md
+++ b/skills/specialist-council/SKILL.md
@@ -104,8 +104,11 @@ communication layer:
 ## Integration
 - **Orchestrators:** add a `wave 0: consult` entry to the wave-plan/production-brief
   before production waves, invoking this skill on the brief.
-- **/spm:** the specialist-board's cross-domain pass calls this to get real
-  specialist input into the spec, not just persona role-play.
+- **/spm:** the MOA bench (`spm/references/moa-board.md`, board_version 1.0+) embeds this
+  skill's consult contract and cooperation gate **in-memory** rather than invoking the skill
+  â€” spm's read-only constraint forbids the `council/{job_id}/` writes. The contract is
+  adopted verbatim there (plus `slug`/`escalate` fields); this skill remains the SSOT for
+  the contract's semantics.
 - **dispatcher-core:** expose as a `skill.specialist-council` step type.
 
 ## Anti-duplication
diff --git a/skills/spm/SKILL.md b/skills/spm/SKILL.md
index 0611cccb..2c6c775f 100644
--- a/skills/spm/SKILL.md
+++ b/skills/spm/SKILL.md
@@ -1,6 +1,6 @@
 ---
 name: spm
-description: Senior Project Manager command (/spm). Use before implementation to turn a rough task, feature, bug, idea, ticket, PR, or repo area into a decision-grade spec.md â€” via read-only project inspection, a 15+ year specialist board, judge-style challenge, verification + stress-test planning, and goal-ready acceptance criteria. Read-only: produces the spec, never the build.
+description: Senior Project Manager command (/spm). Use before implementation to turn a rough task, feature, bug, idea, ticket, PR, or repo area into a decision-grade spec.md â€” via read-only project inspection, a self-leveling MOA specialist bench (0â€“8 seats sized to task complexity), judge-style challenge, verification + stress-test planning, and goal-ready acceptance criteria. Read-only: produces the spec, never the build.
 owner_role: Tier-Architect (senior project manager; spec author, not builder)
 status: active
 automation: manual
@@ -42,13 +42,27 @@ the full spec. Still `No spec. No build.` â€” it produces the micro-spec, not th
 1. Understand the user request (`$ARGUMENTS`; if empty, ask what to plan).
 2. Inspect current project state (read-only: `git branch`/`status`/`log`/`diff`, README, CLAUDE.md, AGENTS.md, `.judge/`, `.session-handoff/`, `.resume-from-handoff/`, `.spm/`, `skills/`, `scripts/`, `tests/`, `.harness/`, relevant `app/`/`dashboard/`/`mcp/`/`src/`).
 3. Review existing capabilities (do not rebuild what exists).
-4. Apply 15+ year specialist perspectives (see `.spm/agent-board.md`): Product Manager, Software Architect, UX/UI Reviewer, Security Reviewer, QA/Test Lead, Devil's Advocate / Judge. Use subagents where helpful.
-5. Apply judge-style pushback (score out of 100; REJECT / REDUCE SCOPE / APPROVE EXPERIMENT / APPROVE BUILD). **Hard line: APPROVE BUILD requires a real 100/100 â€” every mandatory criterion satisfied. Below 100 is never a build authorisation; iterate to a real 100 or report the honest ceiling.**
+4. Convene the **self-leveling MOA bench**: score the 5-axis rubric (F/I/N/X/S) from step-2 recon â†’ tier T0â€“T3 per `references/leveling.md` â†’ seat the bench from `references/moa-board.md` (a project-local `.spm/agent-board.md` overrides the roster) â†’ dispatch seats as **parallel read-only subagents in one message**, each wrapped in `~/.claude/skills/nexus/references/NEXUS_PROMPT.md` at its calibrated tier + effort â†’ collect consult contracts, measure divergence, ramp up/down (max 2 rounds) â†’ synthesize into spec Â§7. **T0 = zero seats. Never role-play a board you didn't convene** â€” a board that wasn't dispatched is reported as "T0/inline", not simulated.
+5. Apply judge-style pushback (score out of 100; REJECT / REDUCE SCOPE / APPROVE EXPERIMENT / APPROVE BUILD). At T2+ the Â§8 judge challenge **is the devils-advocate-judge seat's contract** â€” its `must_fix` items become mandatory 100/100 criteria; at T0/T1 run the judge rubric inline. **Hard line: APPROVE BUILD requires a real 100/100 â€” every mandatory criterion satisfied. Below 100 is never a build authorisation; iterate to a real 100 or report the honest ceiling.** A security-seat `fail` at confidence â‰¥0.8 blocks 100/100 regardless of consensus.
 6. Define scope, risks, UX, security, testing, and acceptance criteria.
-7. Produce a high-quality SPM Spec (see `.spm/spec-template.md`).
-8. Generate the exact `/goal` command to implement the spec (see `.spm/goal-template.md`).
+7. Produce a high-quality SPM Spec (template: `.spm/spec-template.md` if the project ships one, else the section list under Required output).
+8. Generate the exact `/goal` command to implement the spec (template: `.spm/goal-template.md` if present, else spec Â§16 conventions). The spec's verification plan (Â§13â€“14) must satisfy `references/sandbox-policy.md` â€” isolation named, prod untouched.
 9. Prepare a session-handoff seed so the next terminal can resume cleanly.
 
+## Bench guards
+
+- Seats are **leaf agents**: read-only, no Skill/Agent/Task/Workflow calls, no file writes â€”
+  the guard text in `references/moa-board.md` goes verbatim into every seat brief.
+- Depth: under `/nexus`, bench seats are nexus's depth-1 dispatches; standalone `/spm`,
+  seats are depth 1. Seats are terminal either way.
+- Honour `~/.claude/HARD_STOP` (checked before dispatch and between rounds) and
+  `TAO_MAX_COST_USD` â€” narrow the bench before breaching it; the disconfirming seat is
+  never dropped.
+- Operator override: "no board" / "bench=T0" (or any explicit tier pin) in `$ARGUMENTS`
+  pins the tier â€” honored without argument, logged in Â§7.
+- Â§7 always records: tier + axis scores, seats convened, per-seat verdict+confidence,
+  divergence numbers, ramp decisions, `board_version`. Every run leaves a receipt.
+
 ## Evidence policy
 
 Prefer first-source evidence (repo source > tests/logs/schemas/CI > official docs/SDK/changelogs
diff --git a/skills/spm/references/leveling.md b/skills/spm/references/leveling.md
new file mode 100644
index 00000000..75617cb7
--- /dev/null
+++ b/skills/spm/references/leveling.md
@@ -0,0 +1,117 @@
+# Bench leveling â€” the self-leveling ramp for /spm's MOA board
+
+`leveling_version: 1.0` Â· calibrated 2026-07-10 Â· change-control: agent-workflow 5-clean-runs
+
+This file defines how `/spm` sizes its specialist bench to the task. It is consumed at
+workflow step 4, **after** step-2 recon (the rubric needs recon data). The output of this
+file's procedure is a tier (T0â€“T3) and, via `moa-board.md`, a seated bench. The axis scores
+and tier are always printed in spec Â§7 â€” every run leaves a receipt for why the bench was
+the size it was. The Â§7 receipt always cites both `leveling_version` and `board_version`
+(reading moa-board.md's version header at T0 is not "opening the roster" â€” seats stay
+closed). Windows recon note for step 2: scope Glob/Grep to subdirectories with the `path`
+parameter and keep patterns flat â€” repo-root or subdirectory-in-pattern searches can time
+out or miss on large repos.
+
+Reused primitives (do not reimplement elsewhere): the two-axis "higher wins" rule is
+autonomy-ladder's; ramp-up-on-failure is tier-evaluator's; the divergence gate adapts
+boardroom's Jaccard escalation to role-contracts; the cost guard is nexus G4's
+(`TAO_MAX_COST_USD`, `~/.claude/HARD_STOP`).
+
+## 1. Classification rubric
+
+Score five axes from step-2 recon. Each axis is 0/1/2 by **checkable** definition â€” if you
+cannot point at the evidence for a score, you are guessing; take the higher value.
+
+| Axis | 0 | 1 | 2 |
+|---|---|---|---|
+| **F â€” Footprint** (files-touched estimate) | â‰¤2 files / one component | 3â€“10 files or one subsystem | >10 files, cross-repo, or a new service |
+| **I â€” Irreversibility** | fully undoable (local edit, draft, doc) | costly to unwind (additive migration, public API addition, config with rollback) | prod deploy / publish / spend / destructive schema / secret rotation / vendor commitment |
+| **N â€” Novelty** | known pattern already in the repo | new pattern, familiar domain | new domain vocabulary/entities (Unknown-Unknowns dense) |
+| **X â€” Cross-domain count** (product/UX, architecture, security, data, infra/ops, compliance, marketing/content) | 1 domain | 2 domains | â‰¥3 domains |
+| **S â€” Security surface** | no auth/PII/money/untrusted input | touches an authed surface or parses external input | auth/authz change, PII/tenant isolation, payments, secrets, RLS |
+
+## 2. Tier mapping
+
+Sum the axes (0â€“10), then apply floors â€” floors beat sums:
+
+- **T0**: sum â‰¤1 AND I=0 AND S=0
+- **T1**: sum 2â€“3 AND Iâ‰¤1 AND Sâ‰¤1
+- **T2**: sum 4â€“6, **OR any single axis = 2** (a single maxed axis floors the tier at T2)
+- **T3**: sum â‰¥7, **OR I=2, OR S=2**, OR (N=2 AND X=2) â€” irreversibility and security auto-promote
+- **Tie-break**: unsure between two tiers â†’ take the higher (autonomy-ladder rule).
+- **Fast-lane interplay**: the fast-lane micro-spec is T0/T1 by construction â€” more than ~8
+  bullets already escalates to the full spec, which re-scores.
+- **Under /nexus**: G1's small/broad seeds the estimate (smallâ‰ˆT0/T1, broadâ‰ˆT2/T3); spm
+  re-scores with this rubric because it holds recon data G1 lacks.
+- **Operator override**: "no board" / "bench=T0" (or any explicit tier pin) in `$ARGUMENTS`
+  pins the tier. Log it in Â§7 as `operator override`. The override is honored without
+  argument â€” the receipt keeps it auditable.
+
+## 3. Tier table
+
+| Tier | Seats | Models / effort | Rounds | Verification depth | Spec form |
+|---|---|---|---|---|---|
+| **T0** | **0** â€” no Agent dispatch | n/a | 0 | judge rubric run inline by the spec author | micro-spec or direct answer; Â§7 = one line: `T0, no seats (Fâ€¦ Iâ€¦ Nâ€¦ Xâ€¦ Sâ€¦) Â· rounds 0 Â· board_version: X.Y Â· leveling_version: X.Y` |
+| **T1** | 1â€“2 (most-relevant lens; add the disconfirming seat only if seat 1 is not a clean `pass`) | Sonnet 5, low/medium effort | 1 pass | inline judge rubric | full or micro-spec |
+| **T2** | 3â€“4, **â‰¥1 mandatorily disconfirming (judge seat)** | Sonnet 5 default; security/architect seats Opus 4.8 | max 2 | divergence measured (Â§4); sandbox-policy compliance stated in Â§13â€“14 | full 19-section spec |
+| **T3** | 6â€“8 + **adversarial verify pass** (non-author model, opus-adversary style â€” a pass, not a seat) | synthesis/security/adversary Opus 4.8 at high effort; rest Sonnet 5 | max 2 | flip-test folded into Â§8; Â§13â€“14 **must name the container strategy** (sandbox-policy.md) | full spec, hard-floor rules active |
+
+## 4. Divergence measurement
+
+Adapted from boardroom's Jaccard gate â€” but measured over structured role-contracts, not
+model text. Requires every `must_fix` item to carry a short topic `slug` (see the contract
+in `moa-board.md`).
+
+- **`verdict_split`** = 1 âˆ’ (count of the modal verdict Ã· seats). Example: pass/pass/pass/fail â†’ 0.25.
+- **`fix_overlap`** = mean pairwise Jaccard similarity over the seats' `must_fix` slug sets â€”
+  **canonicalized first**: map each slug to its topic (merge synonyms/aliases across seats)
+  before computing Jaccard, and print both numbers (`lexical / canonical`) in Â§7. Seats slug
+  independently, so raw-lexical overlap systematically under-reads agreement.
+
+Both numbers are printed in Â§7.
+
+## 5. Ramp rules
+
+| Reading | Condition | Action |
+|---|---|---|
+| **Consensus** | split = 0, all `pass`, no must_fix | **RAMP DOWN**: early-exit, skip round 2, synthesize now |
+| **Unanimous criticism** | split = 0 with non-`pass` verdicts (any overlap) | fold ALL must_fix, synthesize, no round 2 â€” round 2 exists to resolve contradictions, not unanimity |
+| **Convergent criticism** | split â‰¤ 0.25 AND overlap â‰¥ 0.5 | fold the agreed must_fix into the spec; no round 2 |
+| **Divergent** | split â‰¥ 0.5, OR (overlap < 0.2 AND â‰¥1 non-pass) | **RAMP UP**: escalate synthesis to Opus 4.8; add the judge seat if absent; run round 2 (final); round-2 briefs carry peers' round-1 must_fix (cooperation gate) |
+| **Hard floor** | any `fail` with confidence â‰¥ 0.8 from the security seat, or on an irreversibility finding | the spec **cannot reach APPROVE BUILD 100/100** until the finding is resolved â€” regardless of the other seats' consensus |
+
+Additional ramp triggers:
+
+- **UP**: a seat returns `"escalate": true` with a named need; or mid-recon axis discovery
+  (e.g. a hidden schema migration turns I to 2) â€” re-tier upward and top up seats.
+- **DOWN**: consensus early-exit (above); or a smaller-than-estimated footprint re-tiers
+  down **max one level per run** (never T3â†’T0 mid-flight).
+- **Cost guard**: bind the bench to `TAO_MAX_COST_USD`; if a fan-out would exceed it,
+  narrow in this order: drop domain-specialist duplicates â†’ drop ops-cost-realist â†’
+  drop ux-reviewer (if surface is internal) â†’ reduce round cap to 1. **The disconfirming
+  seat is never dropped.** Log every narrowing in Â§7.
+- **Hard caps**: 2 rounds, always. Check `~/.claude/HARD_STOP` before the first dispatch
+  and between rounds â€” if present, halt and report.
+
+## 6. Worked examples
+
+**T0 â€” "rename `getUser` to `fetchUser` in one file".** F=0 (1 file), I=0, N=0, X=0, S=0.
+Sum 0 â†’ T0. Zero seats. Â§7: `T0, no seats (F0 I0 N0 X0 S0)`. Micro-spec, done.
+
+**T2 â€” "add CSV export to the reports page".** F=1 (4 files: route, component, lib, test),
+I=0 (feature-flagged, additive), N=0 (export pattern exists), X=1 (product + data), S=1
+(authed surface). Sum 3â€¦ but recon shows the export touches a shared query builder â†’ F=1
+holds, S=1 holds; a re-check finds the download endpoint parses user-supplied column lists
+â†’ S stays 1. Sum 3 = T1? Recon flags the column-list parsing as injection-adjacent â†’
+tie-break rule: take the higher â†’ **T2**. Seats: product-manager, architect,
+security-reviewer, devils-advocate-judge. Round 1: 3 pass, security `needs-work`
+(must_fix: `csv-injection`, `column-allowlist`). split 0.25, overlap n/aâ†’ convergent
+criticism path: fold must_fix, no round 2. Spec Â§15 gains two mandatory criteria.
+
+**T3 â€” "move tenant isolation from app-layer checks to RLS".** I=2 (destructive schema +
+policy change), S=2 (tenant isolation). Auto-promote â†’ **T3** regardless of sum. Seats (7):
+product-manager, architect (Opus), security-reviewer (Opus), qa-verification-lead,
+devils-advocate-judge (Opus), domain-specialist(data), ops-cost-realist. Security seat
+returns `fail` @0.9 (must_fix: `rls-bypass-service-role`) â†’ hard floor: APPROVE BUILD
+blocked; spec reports the honest ceiling with the unresolved finding at the top. Â§13â€“14
+name the shadow-DB container strategy per sandbox-policy.md.
diff --git a/skills/spm/references/moa-board.md b/skills/spm/references/moa-board.md
new file mode 100644
index 00000000..af37ac5b
--- /dev/null
+++ b/skills/spm/references/moa-board.md
@@ -0,0 +1,126 @@
+# MOA board â€” the /spm specialist bench roster
+
+`board_version: 1.0` Â· `contract_v: 1` Â· calibrated 2026-07-10
+Change-control: roster/threshold changes ride agent-workflow's promotion ladder â€” 5 clean
+hand-run executions before a change is considered calibrated. Every emitted spec prints the
+`board_version` used in Â§7.
+
+**Precedence:** a project-local `.spm/agent-board.md` overrides this file when present
+(spm step 2 already inspects `.spm/`); log `project board override active` in Â§7. This
+file is the global default â€” projects inherit it automatically and customize by copying it
+into `.spm/` and editing. spm never writes either file.
+
+Tier selection, divergence math, and ramp rules live in `leveling.md` (read it first â€”
+T0 never opens this roster).
+
+## Consult contract (per seat, returned as ONE JSON block in the seat's final text)
+
+Adopted verbatim from `specialist-council`'s consult-response contract (credited), plus two
+fields: `slug` on each must_fix item (for divergence math) and `escalate` (the seat's only
+way to ask for more than its brief).
+
+```json
+{
+  "specialist": "security-reviewer",
+  "verdict": "pass | needs-work | fail",
+  "must_fix": [
+    {"slug": "csv-injection", "issue": "...", "why": "...", "evidence": "file:line or doc Â§"}
+  ],
+  "suggestions": [{"change": "...", "impact": "high | medium | low"}],
+  "confidence": 0.0,
+  "escalate": false,
+  "escalation_need": null
+}
+```
+
+Rules: `must_fix` items become **mandatory 100/100 criteria** in spec Â§15 unless explicitly
+retired by evidence in a later round. `confidence` is the seat's own calibration (0â€“1).
+A malformed contract gets ONE re-ask, then the seat abstains (excluded from divergence
+math, noted in Â§7). Seat failures are reported, never swallowed (boardroom rule).
+
+## The 8 seats
+
+Each seat is defined with agent-workflow's 5-part contract (name Â· soul Â· job Â· keys Â· stop).
+
+| # | Seat | Lens (soul) | Default model | Seated when |
+|---|---|---|---|---|
+| 1 | `product-manager` | value, scope, appetite; ruthless about what NOT to build. Does NOT design solutions | Sonnet 5 | feature-shaped work, T1+ |
+| 2 | `architect` | boundaries, complexity, data flow; folds in council-of-logic's Turing (complexity) + Von Neumann (architecture) checks | Sonnet 5 â†’ Opus 4.8 at T3 | Fâ‰¥1 or Nâ‰¥1 |
+| 3 | `ux-reviewer` | flows, empty/error states, friction; the user's advocate | Sonnet 5 | any user-facing surface |
+| 4 | `security-reviewer` | auth/authz, PII, secrets, tenancy, RLS, untrusted input | **Opus 4.8 always â€” never cost-downgraded** | Sâ‰¥1; mandatory at S=2 |
+| 5 | `qa-verification-lead` | test + verification plan; proof-discipline embedded ({proven \| observed \| assumed} claim classes); owns the sandbox-policy check (Â§13â€“14) | Sonnet 5 â†’ Opus 4.8 at T3 | T2+ |
+| 6 | `devils-advocate-judge` | **the designated disconfirming seat** â€” argues against the build; judge's 7-lens rubric; returns a score /100 with its contract | Opus 4.8 | mandatory T2+; conditional at T1 (only if seat 1 isn't a clean pass) |
+| 7 | `domain-specialist` | parametric slot â€” filled per task shape from the nexus G4 menu vocabulary (seo / geo-optimization / eeat / marketing-orchestrator / remotion / data / readiness / â€¦) | Sonnet 5 | Xâ‰¥1 with a non-engineering domain in play |
+| 8 | `ops-cost-realist` | deploy, rollback, monitoring, run-cost; enforces sandbox-policy in the verification plan | Sonnet 5 | Iâ‰¥1 or an infra/ops domain |
+
+Seat-count by tier: T1 seats {most-relevant one of 1â€“3, + 6 conditionally} Â· T2 seats
+{3â€“4 of the above by relevance, 6 always} Â· T3 {6â€“8, with 2/4/5/6 required}. At T3 the
+adversarial verify is a **pass over the synthesis** (opus-adversary style, non-author
+model), not a ninth seat.
+
+Keys (inputs a seat receives): the task statement, spec-in-progress excerpts relevant to
+its lens, step-2 recon summary, axis scores, and â€” in round 2 â€” peers' round-1 must_fix
+items (cooperation gate: a round-2 contract must cite and address at least one peer
+objection). Stop (when a seat is done): exactly one contract JSON returned; no follow-up
+turns.
+
+## Dispatch mechanics
+
+- Seats run as **parallel `Agent` calls in ONE message** â€” never serially, and **including
+  the judge seat** (the disconfirming seat is part of the bench, never a follow-up dispatch).
+  Agent types: `Explore` (recon-flavored lenses), `Plan` (architecture/product lenses), or
+  `general-purpose`. All read-only briefs.
+- Wrap every seat brief in the Nexus Prompt (`~/.claude/skills/nexus/references/NEXUS_PROMPT.md`),
+  `{TASK}` = the seat brief, at the seat's calibrated model tier and effort (Model- and
+  Effort-calibration blocks are in the wrapper). **Strip the wrapper's Delegation section
+  when wrapping leaf seats** â€” it contradicts the leaf-agent guard, and the guard wins.
+  Request the seat's model tier via the Agent tool's `model` parameter (sonnet/opus per the
+  roster); note in Â§7 that the requested tier is the intent â€” actual serving model is not
+  observable from seat output. Never append show-your-reasoning instructions (autonomy
+  contract â€” refusal trap).
+- Contracts must return to the dispatching context. If a seat's return routes elsewhere
+  (e.g. a resumed seat notifying a parent coordinator), treat the externally-relayed
+  contract as evidence only after verbatim capture, and note the relay in Â§7.
+- Reconcile condensed contracts; **never paste raw seat output into the spec**. Â§7 carries:
+  tier + axis scores, seats convened, per-seat verdict+confidence, divergence numbers,
+  ramp decisions, board_version.
+- Synthesis is centralized (one decision-maker â€” the spec author), per specialist-council's
+  coordination rule. The bench consults; the SPM decides; the judge seat's must_fix
+  constrains.
+
+## Guardrails
+
+**Leaf-agent guard â€” include verbatim in every seat brief:**
+
+> You are a leaf agent â€” one consulting seat on a read-only spec board. You MUST NOT:
+> invoke the Skill tool for any skill (not `nexus` â€” it is `disable-model-invocation` and
+> errors; not `spm`, `specialist-council`, or `boardroom`); dispatch subagents via
+> Agent/Task/Workflow; write, edit, create, move, or delete any file anywhere (including
+> scratch/temp); run any state-changing command. You MAY: Read, Grep, Glob, and read-only
+> Bash (`git status/log/diff`, `ls`). Return exactly one consult-response JSON block as
+> text, then stop. If you need something outside your brief, set `"escalate": true` with
+> the named need â€” do not fetch it yourself by spawning anything.
+
+- **Depth accounting**: under `/nexus`, nexus pulls `Skill(spm)` inline, so bench seats are
+  nexus's depth-1 dispatches (cap respected). Standalone `/spm` is depth 0; seats are
+  depth 1. Either way seats are terminal.
+- **Cooperation gate** (round 2 only): each seat must cite and address â‰¥1 peer objection
+  before its round-2 input is accepted.
+- **Diversity over redundancy**: seats are distinct domain lenses, never N copies; the
+  disconfirming seat (judge) is mandatory at T2+ and survives all cost narrowing.
+- **Honest degradation**: all seats fail â†’ run the old inline persona pass, label the spec
+  Â§7 `bench degraded â€” inline persona pass`, and say so in the final recommendation. Opus
+  unavailable for the security seat â†’ run it on Sonnet, mark Â§12 `reduced assurance`, and
+  block APPROVE BUILD 100/100.
+- **Kill-switches**: honour `~/.claude/HARD_STOP` and `TAO_MAX_COST_USD` per leveling.md Â§5.
+
+## Task-shape â†’ seat mapping (mirrors the nexus G4 menu)
+
+| Task shape | Seats emphasized |
+|---|---|
+| Code / refactor / architecture | architect, qa-verification-lead, judge (+ security if Sâ‰¥1) |
+| New domain vocabulary | architect(N-focus), domain-specialist, judge |
+| Outward-facing / market / content | product-manager, domain-specialist (seo/geo/eeat/marketing), ux-reviewer, judge |
+| Ship / go-live readiness | qa-verification-lead, ops-cost-realist, security-reviewer, judge |
+| Strategic / go-no-go | product-manager, judge (+ boardroom is nexus's tool, NOT a seat here â€” do not double-bench) |
+| Data / schema / migration | architect, security-reviewer, qa-verification-lead, ops-cost-realist, judge |
diff --git a/skills/spm/references/sandbox-policy.md b/skills/spm/references/sandbox-policy.md
new file mode 100644
index 00000000..0801bed9
--- /dev/null
+++ b/skills/spm/references/sandbox-policy.md
@@ -0,0 +1,45 @@
+# Sandbox policy â€” verification isolation for /spm specs
+
+`policy_version: 1.0` Â· calibrated 2026-07-10
+
+This policy governs the **spec that /spm emits**, not spm itself (spm is read-only and
+executes nothing). It binds the verification plan (Â§13) and loop/stress testing (Â§14) that
+the emitted `/goal` will execute: **verification runs in isolation â€” containers, sandboxes,
+shadow resources â€” never against production.**
+
+## Mandate by tier
+
+- **T2+**: Â§13â€“14 must state where verification runs and why that environment cannot touch
+  prod (distinct URL/port/credentials, ephemeral lifecycle).
+- **T3**: Â§13â€“14 **must name the container strategy explicitly** (which image/compose file/
+  branch-DB mechanism), including the guard that makes a prod connection impossible
+  (e.g. "globalSetup hard-fails unless DATABASE_URL port = 5499").
+- Any verification step that must touch prod (e.g. a live smoke after deploy) is
+  autonomy-ladder **L3** â†’ it becomes a human-gated acceptance criterion in Â§15, never an
+  autonomous step.
+
+## Decision table â€” pick by project shape
+
+| Project shape | Isolation strategy |
+|---|---|
+| Has Dockerfile / docker-compose | Ephemeral compose env (dedicated `*.test.yml`, non-default ports, tmpfs data, healthchecks; `--wait` then run; `down -v` after) |
+| Node service, no compose | testcontainers (or a dedicated test compose added by the spec) for pg/redis/queues; unit layer mocks stay as-is |
+| DB migration involved | Shadow/branch database (Supabase branch, throwaway container DB, or `db push` into an ephemeral instance); NEVER `migrate`/`db push` against a live URL; migration files land create-only, apply is a human gate |
+| Frontend / web app | Preview deploy (never the prod alias) + Playwright against the preview URL; visual/e2e evidence attached |
+| CLI / library | Temp-dir sandbox (`$TMP`-scoped fixtures), no writes outside the sandbox path |
+| External APIs (social, payment, LLM) | Recorded fixtures / mock server in tests; live-key smoke only as an L3 human-gated criterion |
+
+## Escape hatch â€” when no sandbox is possible
+
+If genuinely no isolation exists for a step (rare), the spec must say so explicitly and:
+1. classify every claim that step would have proven as `observed, not proven`
+   (proof-discipline classes), never `proven`;
+2. gate the affected acceptance criteria on a human-run check in Â§15;
+3. have the judge seat treat the gap as a standing must_fix â€” APPROVE BUILD 100/100
+   requires either the sandbox or the explicit human gate, never silence.
+
+## Receipt
+
+Â§13 states, in one line each: environment name, isolation mechanism, prod-impossibility
+guard, teardown. The qa-verification-lead seat owns checking this at T2+; ops-cost-realist
+checks teardown/cost.
diff --git a/skills/verify/SKILL.md b/skills/verify/SKILL.md
new file mode 100644
index 00000000..0366aa1f
--- /dev/null
+++ b/skills/verify/SKILL.md
@@ -0,0 +1,59 @@
+---
+name: verify
+description: Run the loop from `prove-the-failure` and confirm red goes green, then hold a cooldown before any further action is permitted. Use after `adversarial-review` returns PASS, before `immunise`.
+---
+
+# verify
+
+**Green is a measurement, not a belief.** The only evidence that counts is the same loop that proved the failure, run again, now passing â€” and passing *because of the fix*, not alongside it.
+
+## Phase 1 â€” Prove the loop still catches the failure
+
+Before trusting green, confirm the loop can still go red. Revert the fix, run the loop, watch it fail.
+
+- [ ] Loop is red **without** the fix
+- [ ] Loop is green **with** the fix
+- [ ] No other change is present in the working tree
+
+A loop that is green with and without the fix proves nothing. This is the positive control, and skipping it is how a fix that changed nothing gets marked verified.
+
+## Phase 2 â€” Run the full check
+
+- [ ] The original loop, unmodified, now passes
+- [ ] The regression test passes
+- [ ] The regression test still fails when the fix is reverted
+- [ ] Existing tests around the change still pass
+- [ ] No debug instrumentation remains
+
+## Phase 3 â€” Cooldown
+
+**No further action for the cooldown window.** Not `immunise`, not another repair, not a related cleanup.
+
+Default: **30 minutes**, or one full cycle of the affected scheduled job, whichever is longer.
+
+The cooldown exists because a fix that breaks something adjacent usually reveals it on the next scheduled run, not immediately. Chaining straight into the next action means the breakage lands inside the next repair and the two become indistinguishable.
+
+During cooldown: watch. Do not act.
+
+- [ ] Cooldown elapsed
+- [ ] Affected job ran at least once, or its next run is confirmed scheduled
+- [ ] No new failure appeared in the window
+
+## Completion
+
+- [ ] Red-without-fix demonstrated (output pasted)
+- [ ] Green-with-fix demonstrated (output pasted)
+- [ ] Regression test verified in both directions
+- [ ] Cooldown observed and clean
+
+Paste both outputs. A claim of green without its output is not a verification, and this chain treats it as an unverified claim regardless of how confident the wording is.
+
+## Stop conditions
+
+**If the loop is still red, the fix failed.** Return to `propose-fix`, consuming one attempt from its bound of three. Do not adjust the loop to make it pass â€” modifying the loop to accommodate the fix inverts the entire chain.
+
+**If a new failure appears during cooldown,** stop. Do not repair it inside this chain. Start a new chain at `prove-the-failure`, and record the adjacency â€” two failures in one window is a circuit-breaker signal.
+
+## Next
+
+`immunise` â€” stop it recurring.

hook: PostToolUse
hook: PostToolUse Completed
exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Select-String -LiteralPath 'D:\\Pi-Dev-Ops\\.harness\\cc-01.diff' -Pattern 'diff --git a/dashboard/app/\\(main\\)/command-centre/hermes/page.tsx' -Context 0,260" in D:\Pi-Dev-Ops
exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Select-String -LiteralPath 'D:\\Pi-Dev-Ops\\.harness\\cc-01.diff' -Pattern '"'^diff --git|''^@@'"'" in D:\Pi-Dev-Ops
exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Select-String -LiteralPath 'D:\\Pi-Dev-Ops\\.harness\\cc-01.diff' -Pattern 'diff --git a/dashboard/components/command-centre/DeckDetails.tsx' -Context 0,220" in D:\Pi-Dev-Ops
 succeeded in 400ms:

.harness\cc-01.diff:1:diff --git a/.harness/known-issues.md b/.harness/known-issues.md
.harness\cc-01.diff:6:@@ -0,0 +1,33 @@
.harness\cc-01.diff:40:diff --git a/dashboard/__tests__/command-centre-provenance.json 
b/dashboard/__tests__/command-centre-provenance.json
.harness\cc-01.diff:45:@@ -0,0 +1,16 @@
.harness\cc-01.diff:62:diff --git a/dashboard/__tests__/command-centre-readonly.test.ts 
b/dashboard/__tests__/command-centre-readonly.test.ts
.harness\cc-01.diff:67:@@ -0,0 +1,142 @@
.harness\cc-01.diff:210:diff --git a/dashboard/app/(main)/command-centre/hermes/page.tsx 
b/dashboard/app/(main)/command-centre/hermes/page.tsx
.harness\cc-01.diff:215:@@ -0,0 +1,219 @@
.harness\cc-01.diff:435:diff --git a/dashboard/components/command-centre/DeckDetails.tsx 
b/dashboard/components/command-centre/DeckDetails.tsx
.harness\cc-01.diff:440:@@ -0,0 +1,72 @@
.harness\cc-01.diff:513:diff --git a/dashboard/components/command-centre/command-deck.module.css 
b/dashboard/components/command-centre/command-deck.module.css
.harness\cc-01.diff:518:@@ -0,0 +1,618 @@
.harness\cc-01.diff:1137:diff --git a/dashboard/components/command-centre/deck-details.module.css 
b/dashboard/components/command-centre/deck-details.module.css
.harness\cc-01.diff:1142:@@ -0,0 +1,80 @@
.harness\cc-01.diff:1223:diff --git a/dashboard/lib/command-centre/control-panel.ts 
b/dashboard/lib/command-centre/control-panel.ts
.harness\cc-01.diff:1228:@@ -0,0 +1,264 @@
.harness\cc-01.diff:1493:diff --git a/skills/adversarial-review/SKILL.md b/skills/adversarial-review/SKILL.md
.harness\cc-01.diff:1498:@@ -0,0 +1,92 @@
.harness\cc-01.diff:1591:diff --git a/skills/classify/SKILL.md b/skills/classify/SKILL.md
.harness\cc-01.diff:1596:@@ -0,0 +1,84 @@
.harness\cc-01.diff:1681:diff --git a/skills/contain/SKILL.md b/skills/contain/SKILL.md
.harness\cc-01.diff:1686:@@ -0,0 +1,48 @@
.harness\cc-01.diff:1735:diff --git a/skills/diagnose/SKILL.md b/skills/diagnose/SKILL.md
.harness\cc-01.diff:1740:@@ -0,0 +1,68 @@
.harness\cc-01.diff:1809:diff --git a/skills/immunise/SKILL.md b/skills/immunise/SKILL.md
.harness\cc-01.diff:1814:@@ -0,0 +1,51 @@
.harness\cc-01.diff:1866:diff --git a/skills/incident-memory/SKILL.md b/skills/incident-memory/SKILL.md
.harness\cc-01.diff:1871:@@ -0,0 +1,80 @@
.harness\cc-01.diff:1952:diff --git a/skills/nexus/SKILL.md b/skills/nexus/SKILL.md
.harness\cc-01.diff:1956:@@ -77,7 +77,9 @@ subagent to find credible sources that *contradict* it). If margot is 
unreachabl
.harness\cc-01.diff:1967:diff --git a/skills/nexus/references/orchestration-playbook.md 
b/skills/nexus/references/orchestration-playbook.md
.harness\cc-01.diff:1971:@@ -104,6 +104,7 @@ what G3 already ran.
.harness\cc-01.diff:1979:@@ -135,9 +136,11 @@ token is unprovisioned) — do NOT claim unattended resilience until those 
close
.harness\cc-01.diff:1994:diff --git a/skills/orchestrator/SKILL.md b/skills/orchestrator/SKILL.md
.harness\cc-01.diff:1999:@@ -0,0 +1,140 @@
.harness\cc-01.diff:2140:diff --git a/skills/propose-fix/SKILL.md b/skills/propose-fix/SKILL.md
.harness\cc-01.diff:2145:@@ -0,0 +1,77 @@
.harness\cc-01.diff:2223:diff --git a/skills/prove-the-failure/SKILL.md b/skills/prove-the-failure/SKILL.md
.harness\cc-01.diff:2228:@@ -0,0 +1,63 @@
.harness\cc-01.diff:2292:diff --git a/skills/self-healing-chain.md b/skills/self-healing-chain.md
.harness\cc-01.diff:2297:@@ -0,0 +1,34 @@
.harness\cc-01.diff:2332:diff --git a/skills/specialist-council/SKILL.md b/skills/specialist-council/SKILL.md
.harness\cc-01.diff:2336:@@ -104,8 +104,11 @@ communication layer:
.harness\cc-01.diff:2350:diff --git a/skills/spm/SKILL.md b/skills/spm/SKILL.md
.harness\cc-01.diff:2354:@@ -1,6 +1,6 @@
.harness\cc-01.diff:2362:@@ -42,13 +42,27 @@ the full spec. Still `No spec. No build.` — it produces the micro-spec, 
not th
.harness\cc-01.diff:2394:diff --git a/skills/spm/references/leveling.md b/skills/spm/references/leveling.md
.harness\cc-01.diff:2399:@@ -0,0 +1,117 @@
.harness\cc-01.diff:2517:diff --git a/skills/spm/references/moa-board.md b/skills/spm/references/moa-board.md
.harness\cc-01.diff:2522:@@ -0,0 +1,126 @@
.harness\cc-01.diff:2649:diff --git a/skills/spm/references/sandbox-policy.md b/skills/spm/references/sandbox-policy.md
.harness\cc-01.diff:2654:@@ -0,0 +1,45 @@
.harness\cc-01.diff:2700:diff --git a/skills/verify/SKILL.md b/skills/verify/SKILL.md
.harness\cc-01.diff:2705:@@ -0,0 +1,59 @@



 succeeded in 399ms:

> .harness\cc-01.diff:210:diff --git a/dashboard/app/(main)/command-centre/hermes/page.tsx 
b/dashboard/app/(main)/command-centre/hermes/page.tsx
  .harness\cc-01.diff:211:new file mode 100644
  .harness\cc-01.diff:212:index 00000000..ce6069f0
  .harness\cc-01.diff:213:--- /dev/null
  .harness\cc-01.diff:214:+++ b/dashboard/app/(main)/command-centre/hermes/page.tsx
  .harness\cc-01.diff:215:@@ -0,0 +1,219 @@
  .harness\cc-01.diff:216:+// src/app/(founder)/founder/command-centre/hermes-control-panel/page.tsx
  .harness\cc-01.diff:217:+//
  .harness\cc-01.diff:218:+// Hermes v0.16 "Surface Release" — Control Panel (READ-ONLY foundation).
  .harness\cc-01.diff:219:+// Mirrors the Hermes web admin module list inside the Unite-Group founder command-centre.
  .harness\cc-01.diff:220:+// No external connections, no MCP, no remote gateway, no messaging-channel activation,
  .harness\cc-01.diff:221:+// no secret values. Every external surface renders as inert (not connected / none enabled).
  .harness\cc-01.diff:222:+// Auth enforced by the dashboard's (main) layout in the target app.
  .harness\cc-01.diff:223:+//
  .harness\cc-01.diff:224:+// Presentation: command-deck standard (summary-first, founder feedback 14/07/2026) —
  .harness\cc-01.diff:225:+// OLED ground + tokens via the shared command-deck.module.css register, mono accents,
  .harness\cc-01.diff:226:+// rounded-sm (2px). The inert/placeholder mirror sections collapse behind the shared
  .harness\cc-01.diff:227:+// DeckDetails disclosure with honest one-line summaries. Data logic unchanged.
  .harness\cc-01.diff:228:+
  .harness\cc-01.diff:229:+
  .harness\cc-01.diff:230:+import { getControlPanelView } from '@/lib/command-centre/control-panel'
  .harness\cc-01.diff:231:+import { DeckDetails, DeckMoreLine, DECK_LIST_CAP } from 
'@/components/command-centre/DeckDetails'
  .harness\cc-01.diff:232:+import deckStyles from '@/components/command-centre/command-deck.module.css'
  .harness\cc-01.diff:233:+
  .harness\cc-01.diff:234:+// Deck typeface — same face + variable the command-centre deck loads, so the
  .harness\cc-01.diff:235:+// command-deck.module.css `--font-chakra` stack resolves on this sub-route too.
  .harness\cc-01.diff:236:+
  .harness\cc-01.diff:237:+// Deck token values (command-deck.module.css) for the inline-styled bits.
  .harness\cc-01.diff:238:+const mono = 'ui-monospace, SFMono-Regular, monospace'
  .harness\cc-01.diff:239:+const muted = '#a6afbc' // --deck-muted
  .harness\cc-01.diff:240:+const okText = '#34d399' // --deck-cyan-text
  .harness\cc-01.diff:241:+
  .harness\cc-01.diff:242:+const wrap: React.CSSProperties = {
  .harness\cc-01.diff:243:+  maxWidth: 1040,
  .harness\cc-01.diff:244:+  margin: '0 auto',
  .harness\cc-01.diff:245:+}
  .harness\cc-01.diff:246:+// Banner fill is an alpha wash of the deck go LED (--deck-go).
  .harness\cc-01.diff:247:+const banner: React.CSSProperties = {
  .harness\cc-01.diff:248:+  background: 'rgba(45, 187, 87, 0.08)',
  .harness\cc-01.diff:249:+  border: '1px solid rgba(45, 187, 87, 0.35)',
  .harness\cc-01.diff:250:+  borderRadius: 2,
  .harness\cc-01.diff:251:+  padding: '0.75rem 1rem',
  .harness\cc-01.diff:252:+  marginBottom: '1.5rem',
  .harness\cc-01.diff:253:+  fontSize: 14,
  .harness\cc-01.diff:254:+}
  .harness\cc-01.diff:255:+const th: React.CSSProperties = {
  .harness\cc-01.diff:256:+  textAlign: 'left',
  .harness\cc-01.diff:257:+  fontSize: 12,
  .harness\cc-01.diff:258:+  textTransform: 'uppercase',
  .harness\cc-01.diff:259:+  letterSpacing: '0.04em',
  .harness\cc-01.diff:260:+  fontFamily: mono,
  .harness\cc-01.diff:261:+  color: muted,
  .harness\cc-01.diff:262:+  padding: '0.4rem 0.6rem',
  .harness\cc-01.diff:263:+  borderBottom: '1px solid rgba(255, 255, 255, 0.10)', // --deck-line
  .harness\cc-01.diff:264:+}
  .harness\cc-01.diff:265:+const td: React.CSSProperties = {
  .harness\cc-01.diff:266:+  padding: '0.5rem 0.6rem',
  .harness\cc-01.diff:267:+  borderBottom: '1px solid rgba(255, 255, 255, 0.05)', // --deck-line-soft
  .harness\cc-01.diff:268:+  fontSize: 14,
  .harness\cc-01.diff:269:+  verticalAlign: 'top',
  .harness\cc-01.diff:270:+}
  .harness\cc-01.diff:271:+
  .harness\cc-01.diff:272:+function riskStyle(risk: string): React.CSSProperties {
  .harness\cc-01.diff:273:+  // Alpha washes of the deck LED fills; text = the AA --deck-*-text variants.
  .harness\cc-01.diff:274:+  const map: Record<string, [string, string, string]> = {
  .harness\cc-01.diff:275:+    none: ['rgba(45, 187, 87, 0.12)', '#34d399', 'rgba(45, 187, 87, 0.35)'],
  .harness\cc-01.diff:276:+    low: ['rgba(244, 130, 15, 0.12)', '#f0a94c', 'rgba(244, 130, 15, 0.4)'],
  .harness\cc-01.diff:277:+    high: ['rgba(229, 72, 77, 0.12)', '#f87171', 'rgba(229, 72, 77, 0.4)'],
  .harness\cc-01.diff:278:+  }
  .harness\cc-01.diff:279:+  const [bg, fg, bd] = map[risk] ?? map.none
  .harness\cc-01.diff:280:+  return {
  .harness\cc-01.diff:281:+    display: 'inline-block',
  .harness\cc-01.diff:282:+    padding: '0.1rem 0.5rem',
  .harness\cc-01.diff:283:+    borderRadius: 2, // deck standard: rounded-sm
  .harness\cc-01.diff:284:+    fontSize: 12,
  .harness\cc-01.diff:285:+    fontWeight: 600,
  .harness\cc-01.diff:286:+    fontFamily: mono,
  .harness\cc-01.diff:287:+    background: bg,
  .harness\cc-01.diff:288:+    color: fg,
  .harness\cc-01.diff:289:+    border: `1px solid ${bd}`,
  .harness\cc-01.diff:290:+  }
  .harness\cc-01.diff:291:+}
  .harness\cc-01.diff:292:+
  .harness\cc-01.diff:293:+export default function HermesControlPanelPage() {
  .harness\cc-01.diff:294:+  const view = getControlPanelView()
  .harness\cc-01.diff:295:+  const v = view.version
  .harness\cc-01.diff:296:+  const posture = v.securityPosture
  .harness\cc-01.diff:297:+
  .harness\cc-01.diff:298:+  // Presentation-only summary counts for the disclosure headlines (honest,
  .harness\cc-01.diff:299:+  // derived from the same read-only view — no data logic change).
  .harness\cc-01.diff:300:+  const hardeningsOn = [
  .harness\cc-01.diff:301:+    posture.secretRedaction,
  .harness\cc-01.diff:302:+    posture.subprocessCredentialStripping,
  .harness\cc-01.diff:303:+    posture.ssrfHardening,
  .harness\cc-01.diff:304:+    posture.cveSecurityPinning,
  .harness\cc-01.diff:305:+  ].filter((s) => s === 'on').length
  .harness\cc-01.diff:306:+  const connectedSurfaces = [
  .harness\cc-01.diff:307:+    view.liveConnections,
  .harness\cc-01.diff:308:+    view.externalChannelsEnabled,
  .harness\cc-01.diff:309:+    view.mcpConnected,
  .harness\cc-01.diff:310:+    view.remoteGatewayConnected,
  .harness\cc-01.diff:311:+  ].filter(Boolean).length
  .harness\cc-01.diff:312:+  const shownModules = view.modules.slice(0, DECK_LIST_CAP)
  .harness\cc-01.diff:313:+
  .harness\cc-01.diff:314:+  return (
  .harness\cc-01.diff:315:+    <div className={`${deckStyles.deck}`}>
  .harness\cc-01.diff:316:+      <div style={wrap}>
  .harness\cc-01.diff:317:+        <h1 style={{ fontSize: 24, marginBottom: '0.25rem' }}>Hermes Control Panel</h1>
  .harness\cc-01.diff:318:+        <p style={{ color: muted, marginTop: 0, fontFamily: mono, fontSize: 13 }}>
  .harness\cc-01.diff:319:+          Hermes Agent v{v.version} · {v.releaseName} ({v.release}) · config format 
{v.configFormat}
  .harness\cc-01.diff:320:+        </p>
  .harness\cc-01.diff:321:+
  .harness\cc-01.diff:322:+        <div style={banner}>
  .harness\cc-01.diff:323:+          <strong>Read-only foundation.</strong> {view.note}
  .harness\cc-01.diff:324:+        </div>
  .harness\cc-01.diff:325:+
  .harness\cc-01.diff:326:+        <div style={{ display: 'grid', gap: '1rem' }}>
  .harness\cc-01.diff:327:+          {/* ── Security / version posture ─────────────────────────────── */}
  .harness\cc-01.diff:328:+          <DeckDetails
  .harness\cc-01.diff:329:+            title="Security & version posture"
  .harness\cc-01.diff:330:+            stats={`design target (not live) · ${hardeningsOn}/4 hardenings on · leaner 
skill set ${posture.leanerSkillSet} · NVIDIA tap ${posture.nvidiaTap}`}
  .harness\cc-01.diff:331:+            testId="hermes-security-posture"
  .harness\cc-01.diff:332:+          >
  .harness\cc-01.diff:333:+            {/* UNI-2394 — these posture values come from the static registry
  .harness\cc-01.diff:334:+                (control-panel.ts, source 'static_registry'), not a live Hermes
  .harness\cc-01.diff:335:+                probe. Label them honestly instead of presenting them as live. */}
  .harness\cc-01.diff:336:+            <p style={{ color: muted, fontFamily: mono, fontSize: 12, marginTop: 0 }}>
  .harness\cc-01.diff:337:+              Design target (not live) — values are the v{v.version} release posture from 
the static registry, not a live probe of a running Hermes instance.
  .harness\cc-01.diff:338:+            </p>
  .harness\cc-01.diff:339:+            <div style={{ display: 'flex', gap: '1.5rem', flexWrap: 'wrap', fontSize: 14 }}>
  .harness\cc-01.diff:340:+              <span>Secret redaction: <b style={{ color: okText 
}}>{posture.secretRedaction}</b></span>
  .harness\cc-01.diff:341:+              <span>Subprocess cred stripping: <b style={{ color: okText 
}}>{posture.subprocessCredentialStripping}</b></span>
  .harness\cc-01.diff:342:+              <span>SSRF hardening: <b style={{ color: okText 
}}>{posture.ssrfHardening}</b></span>
  .harness\cc-01.diff:343:+              <span>CVE pinning: <b style={{ color: okText 
}}>{posture.cveSecurityPinning}</b></span>
  .harness\cc-01.diff:344:+              <span>Leaner skill set: <b>{posture.leanerSkillSet}</b></span>
  .harness\cc-01.diff:345:+              <span>NVIDIA tap: <b>{posture.nvidiaTap}</b></span>
  .harness\cc-01.diff:346:+            </div>
  .harness\cc-01.diff:347:+          </DeckDetails>
  .harness\cc-01.diff:348:+
  .harness\cc-01.diff:349:+          {/* ── Connection invariants ──────────────────────────────────── */}
  .harness\cc-01.diff:350:+          <DeckDetails
  .harness\cc-01.diff:351:+            title="Connection state"
  .harness\cc-01.diff:352:+            stats={
  .harness\cc-01.diff:353:+              connectedSurfaces === 0
  .harness\cc-01.diff:354:+                ? 'not connected — all external surfaces inert (by design)'
  .harness\cc-01.diff:355:+                : `${connectedSurfaces}/4 external surfaces connected`
  .harness\cc-01.diff:356:+            }
  .harness\cc-01.diff:357:+            testId="hermes-connection-state"
  .harness\cc-01.diff:358:+          >
  .harness\cc-01.diff:359:+            <div style={{ display: 'flex', gap: '2rem', flexWrap: 'wrap', fontSize: 14 }}>
  .harness\cc-01.diff:360:+              <span>Live connections: <b style={{ color: okText }}>{view.liveConnections ? 
'yes' : 'no'}</b></span>
  .harness\cc-01.diff:361:+              <span>External channels enabled: <b style={{ color: okText 
}}>{view.externalChannelsEnabled ? 'yes' : 'no'}</b></span>
  .harness\cc-01.diff:362:+              <span>MCP connected: <b style={{ color: okText }}>{view.mcpConnected ? 'yes' 
: 'no'}</b></span>
  .harness\cc-01.diff:363:+              <span>Remote gateway connected: <b style={{ color: okText 
}}>{view.remoteGatewayConnected ? 'yes' : 'no'}</b></span>
  .harness\cc-01.diff:364:+              <span>Credentials exposed: <b style={{ color: okText 
}}>{view.credentialsExposed ? 'yes' : 'no'}</b></span>
  .harness\cc-01.diff:365:+            </div>
  .harness\cc-01.diff:366:+          </DeckDetails>
  .harness\cc-01.diff:367:+
  .harness\cc-01.diff:368:+          {/* ── Modules ────────────────────────────────────────────────── */}
  .harness\cc-01.diff:369:+          <DeckDetails
  .harness\cc-01.diff:370:+            title={`Surface-Release modules (${view.moduleCount})`}
  .harness\cc-01.diff:371:+            stats={`${view.modulesImplementableNow} now · 
${view.modulesRequiringLaterApproval} need later approval · ${view.highRiskGatedCount} high-risk gated`}
  .harness\cc-01.diff:372:+            testId="hermes-modules"
  .harness\cc-01.diff:373:+          >
  .harness\cc-01.diff:374:+            <table style={{ width: '100%', borderCollapse: 'collapse' }}>
  .harness\cc-01.diff:375:+              <thead>
  .harness\cc-01.diff:376:+                <tr>
  .harness\cc-01.diff:377:+                  <th style={th}>Module</th>
  .harness\cc-01.diff:378:+                  <th style={th}>Hermes feature</th>
  .harness\cc-01.diff:379:+                  <th style={th}>State</th>
  .harness\cc-01.diff:380:+                  <th style={th}>Ext. risk</th>
  .harness\cc-01.diff:381:+                  <th style={th}>Later approval</th>
  .harness\cc-01.diff:382:+                </tr>
  .harness\cc-01.diff:383:+              </thead>
  .harness\cc-01.diff:384:+              <tbody>
  .harness\cc-01.diff:385:+                {shownModules.map((m) => (
  .harness\cc-01.diff:386:+                  <tr key={m.moduleId}>
  .harness\cc-01.diff:387:+                    <td style={td}>
  .harness\cc-01.diff:388:+                      <div style={{ fontWeight: 600 }}>{m.title}</div>
  .harness\cc-01.diff:389:+                      <div style={{ color: muted, fontSize: 12 }}>{m.note}</div>
  .harness\cc-01.diff:390:+                    </td>
  .harness\cc-01.diff:391:+                    <td style={td}>{m.hermesFeature}</td>
  .harness\cc-01.diff:392:+                    <td style={td}>{m.state}</td>
  .harness\cc-01.diff:393:+                    <td style={td}>
  .harness\cc-01.diff:394:+                      <span 
style={riskStyle(m.externalActionRisk)}>{m.externalActionRisk}</span>
  .harness\cc-01.diff:395:+                    </td>
  .harness\cc-01.diff:396:+                    <td style={td}>{m.requiresLaterApproval ? 'yes' : 'no'}</td>
  .harness\cc-01.diff:397:+                  </tr>
  .harness\cc-01.diff:398:+                ))}
  .harness\cc-01.diff:399:+              </tbody>
  .harness\cc-01.diff:400:+            </table>
  .harness\cc-01.diff:401:+            <DeckMoreLine total={view.modules.length} shown={shownModules.length} />
  .harness\cc-01.diff:402:+          </DeckDetails>
  .harness\cc-01.diff:403:+
  .harness\cc-01.diff:404:+          {/* ── Credential boundary (status only) ──────────────────────── */}
  .harness\cc-01.diff:405:+          <DeckDetails
  .harness\cc-01.diff:406:+            title="Credential boundary"
  .harness\cc-01.diff:407:+            stats={`${view.credentialBoundaries.length} boundaries · status only — no 
secret values`}
  .harness\cc-01.diff:408:+            testId="hermes-credential-boundary"
  .harness\cc-01.diff:409:+          >
  .harness\cc-01.diff:410:+            <p style={{ color: muted, fontSize: 13, marginTop: 0 }}>
  .harness\cc-01.diff:411:+              Boundary names and status only — no secret values are ever displayed.
  .harness\cc-01.diff:412:+            </p>
  .harness\cc-01.diff:413:+            <table style={{ width: '100%', borderCollapse: 'collapse' }}>
  .harness\cc-01.diff:414:+              <thead>
  .harness\cc-01.diff:415:+                <tr>
  .harness\cc-01.diff:416:+                  <th style={th}>Boundary</th>
  .harness\cc-01.diff:417:+                  <th style={th}>Status</th>
  .harness\cc-01.diff:418:+                </tr>
  .harness\cc-01.diff:419:+              </thead>
  .harness\cc-01.diff:420:+              <tbody>
  .harness\cc-01.diff:421:+                {view.credentialBoundaries.map((c) => (
  .harness\cc-01.diff:422:+                  <tr key={c.boundary}>
  .harness\cc-01.diff:423:+                    <td style={td}>{c.boundary}</td>
  .harness\cc-01.diff:424:+                    <td style={td}>{c.status}</td>
  .harness\cc-01.diff:425:+                  </tr>
  .harness\cc-01.diff:426:+                ))}
  .harness\cc-01.diff:427:+              </tbody>
  .harness\cc-01.diff:428:+            </table>
  .harness\cc-01.diff:429:+          </DeckDetails>
  .harness\cc-01.diff:430:+        </div>
  .harness\cc-01.diff:431:+      </div>
  .harness\cc-01.diff:432:+    </div>
  .harness\cc-01.diff:433:+  )
  .harness\cc-01.diff:434:+}
  .harness\cc-01.diff:435:diff --git a/dashboard/components/command-centre/DeckDetails.tsx 
b/dashboard/components/command-centre/DeckDetails.tsx
  .harness\cc-01.diff:436:new file mode 100644
  .harness\cc-01.diff:437:index 00000000..5394421c
  .harness\cc-01.diff:438:--- /dev/null
  .harness\cc-01.diff:439:+++ b/dashboard/components/command-centre/DeckDetails.tsx
  .harness\cc-01.diff:440:@@ -0,0 +1,72 @@
  .harness\cc-01.diff:441:+// src/components/command-centre/DeckDetails.tsx
  .harness\cc-01.diff:442:+//
  .harness\cc-01.diff:443:+// Shared progressive-disclosure primitive for the command deck (founder
  .harness\cc-01.diff:444:+// feedback 14/07/2026: "a lot of exposed data that can be cleaned up").
  .harness\cc-01.diff:445:+// Summary row stays visible — title, one to three headline numbers, and a
  .harness\cc-01.diff:446:+// status badge slot — while the dense detail (identifier lists, rosters,
  .harness\cc-01.diff:447:+// forms) collapses behind a native <details> chevron. Pure presentation:
  .harness\cc-01.diff:448:+// no data fetching, no new dependencies, deck tokens only (OLED ground,
  .harness\cc-01.diff:449:+// mono accents, rounded-sm). Works in both server and client tiles.
  .harness\cc-01.diff:450:+//
  .harness\cc-01.diff:451:+// Note on identifiers: this page is founder-only behind the (founder)
  .harness\cc-01.diff:452:+// layout auth, so moving emails/ids/hostnames into the collapsed layer is
  .harness\cc-01.diff:453:+// visual de-clutter, not a security boundary — the expanded detail keeps
  .harness\cc-01.diff:454:+// them in full.
  .harness\cc-01.diff:455:+
  .harness\cc-01.diff:456:+import styles from './deck-details.module.css'
  .harness\cc-01.diff:457:+
  .harness\cc-01.diff:458:+export function DeckDetails({
  .harness\cc-01.diff:459:+  title,
  .harness\cc-01.diff:460:+  stats,
  .harness\cc-01.diff:461:+  badge,
  .harness\cc-01.diff:462:+  defaultOpen = false,
  .harness\cc-01.diff:463:+  testId,
  .harness\cc-01.diff:464:+  children,
  .harness\cc-01.diff:465:+}: {
  .harness\cc-01.diff:466:+  /** Always-visible tile title. */
  .harness\cc-01.diff:467:+  title: string
  .harness\cc-01.diff:468:+  /** 1–3 headline numbers / status words, e.g. "3 connected · 1 needs reauth". */
  .harness\cc-01.diff:469:+  stats?: React.ReactNode
  .harness\cc-01.diff:470:+  /** Status badge slot (usually a SourceBadge). Sits at the right edge. */



hook: PostToolUse
hook: PostToolUse
 succeeded in 384ms:

> .harness\cc-01.diff:435:diff --git a/dashboard/components/command-centre/DeckDetails.tsx 
b/dashboard/components/command-centre/DeckDetails.tsx
  .harness\cc-01.diff:436:new file mode 100644
  .harness\cc-01.diff:437:index 00000000..5394421c
  .harness\cc-01.diff:438:--- /dev/null
  .harness\cc-01.diff:439:+++ b/dashboard/components/command-centre/DeckDetails.tsx
  .harness\cc-01.diff:440:@@ -0,0 +1,72 @@
  .harness\cc-01.diff:441:+// src/components/command-centre/DeckDetails.tsx
  .harness\cc-01.diff:442:+//
  .harness\cc-01.diff:443:+// Shared progressive-disclosure primitive for the command deck (founder
  .harness\cc-01.diff:444:+// feedback 14/07/2026: "a lot of exposed data that can be cleaned up").
  .harness\cc-01.diff:445:+// Summary row stays visible — title, one to three headline numbers, and a
  .harness\cc-01.diff:446:+// status badge slot — while the dense detail (identifier lists, rosters,
  .harness\cc-01.diff:447:+// forms) collapses behind a native <details> chevron. Pure presentation:
  .harness\cc-01.diff:448:+// no data fetching, no new dependencies, deck tokens only (OLED ground,
  .harness\cc-01.diff:449:+// mono accents, rounded-sm). Works in both server and client tiles.
  .harness\cc-01.diff:450:+//
  .harness\cc-01.diff:451:+// Note on identifiers: this page is founder-only behind the (founder)
  .harness\cc-01.diff:452:+// layout auth, so moving emails/ids/hostnames into the collapsed layer is
  .harness\cc-01.diff:453:+// visual de-clutter, not a security boundary — the expanded detail keeps
  .harness\cc-01.diff:454:+// them in full.
  .harness\cc-01.diff:455:+
  .harness\cc-01.diff:456:+import styles from './deck-details.module.css'
  .harness\cc-01.diff:457:+
  .harness\cc-01.diff:458:+export function DeckDetails({
  .harness\cc-01.diff:459:+  title,
  .harness\cc-01.diff:460:+  stats,
  .harness\cc-01.diff:461:+  badge,
  .harness\cc-01.diff:462:+  defaultOpen = false,
  .harness\cc-01.diff:463:+  testId,
  .harness\cc-01.diff:464:+  children,
  .harness\cc-01.diff:465:+}: {
  .harness\cc-01.diff:466:+  /** Always-visible tile title. */
  .harness\cc-01.diff:467:+  title: string
  .harness\cc-01.diff:468:+  /** 1–3 headline numbers / status words, e.g. "3 connected · 1 needs reauth". */
  .harness\cc-01.diff:469:+  stats?: React.ReactNode
  .harness\cc-01.diff:470:+  /** Status badge slot (usually a SourceBadge). Sits at the right edge. */
  .harness\cc-01.diff:471:+  badge?: React.ReactNode
  .harness\cc-01.diff:472:+  /** Render expanded on first paint (uncontrolled after that). */
  .harness\cc-01.diff:473:+  defaultOpen?: boolean
  .harness\cc-01.diff:474:+  testId?: string
  .harness\cc-01.diff:475:+  children: React.ReactNode
  .harness\cc-01.diff:476:+}) {
  .harness\cc-01.diff:477:+  return (
  .harness\cc-01.diff:478:+    <details className={styles.deckDetails} open={defaultOpen || undefined} 
data-testid={testId}>
  .harness\cc-01.diff:479:+      <summary className={styles.deckSummary}>
  .harness\cc-01.diff:480:+        <span aria-hidden className={styles.chev} />
  .harness\cc-01.diff:481:+        <span className={styles.title}>{title}</span>
  .harness\cc-01.diff:482:+        {stats != null && <span className={styles.stats}>{stats}</span>}
  .harness\cc-01.diff:483:+        {badge != null && <span className={styles.badge}>{badge}</span>}
  .harness\cc-01.diff:484:+      </summary>
  .harness\cc-01.diff:485:+      <div className={styles.body}>{children}</div>
  .harness\cc-01.diff:486:+    </details>
  .harness\cc-01.diff:487:+  )
  .harness\cc-01.diff:488:+}
  .harness\cc-01.diff:489:+
  .harness\cc-01.diff:490:+/**
  .harness\cc-01.diff:491:+ * Honest "+N more" line for lists capped inside a disclosure. Renders
  .harness\cc-01.diff:492:+ * nothing when the list is fully shown, so callers can emit it
  .harness\cc-01.diff:493:+ * unconditionally after a `.slice(0, cap)`.
  .harness\cc-01.diff:494:+ */
  .harness\cc-01.diff:495:+export function DeckMoreLine({ total, shown }: { total: number; shown: number }) {
  .harness\cc-01.diff:496:+  if (total <= shown) return null
  .harness\cc-01.diff:497:+  return <p className={styles.moreLine}>+{total - shown} more not shown</p>
  .harness\cc-01.diff:498:+}
  .harness\cc-01.diff:499:+
  .harness\cc-01.diff:500:+/** Default row cap for lists rendered inside a DeckDetails disclosure. */
  .harness\cc-01.diff:501:+export const DECK_LIST_CAP = 8
  .harness\cc-01.diff:502:+
  .harness\cc-01.diff:503:+/** Row cap for full founder pages (summary-first cleanup, founder 14/07/2026). */
  .harness\cc-01.diff:504:+
  .harness\cc-01.diff:505:+/**
  .harness\cc-01.diff:506:+ * The deck-details CSS falls back to dark-deck inks (#f0f3f7 text) when the
  .harness\cc-01.diff:507:+ * --deck-* / --cc-* tokens are absent — illegible on the founder app's light
  .harness\cc-01.diff:508:+ * (paper) theme. Founder pages outside the command deck spread this onto a
  .harness\cc-01.diff:509:+ * wrapping element so the primitive maps onto the app's light token set.
  .harness\cc-01.diff:510:+ * (DeckDetails lives under components/command-centre/; imported here rather
  .harness\cc-01.diff:511:+ * than duplicated — a later refactor can relocate it.)
  .harness\cc-01.diff:512:+ */
  .harness\cc-01.diff:513:diff --git a/dashboard/components/command-centre/command-deck.module.css 
b/dashboard/components/command-centre/command-deck.module.css
  .harness\cc-01.diff:514:new file mode 100644
  .harness\cc-01.diff:515:index 00000000..d7021480
  .harness\cc-01.diff:516:--- /dev/null
  .harness\cc-01.diff:517:+++ b/dashboard/components/command-centre/command-deck.module.css
  .harness\cc-01.diff:518:@@ -0,0 +1,618 @@
  .harness\cc-01.diff:519:+/* Command Deck — flight-deck console aesthetic for the Nexus Command Centre.
  .harness\cc-01.diff:520:+   Scoped CSS module (no runtime style injection — CSP/nonce safe).
  .harness\cc-01.diff:521:+   Signal palette: cyan (primary) / go-green / caution-amber / abort-red. */
  .harness\cc-01.diff:522:+
  .harness\cc-01.diff:523:+.deck {
  .harness\cc-01.diff:524:+  /* UNI-2339 slice 2 — the deck ground migrates onto the ratified Gun Metal
  .harness\cc-01.diff:525:+     canvas (shell.module.css register). Every tile consumes colour via these
  .harness\cc-01.diff:526:+     tokens (tiles imported unchanged), so the flip happens here. All TEXT
  .harness\cc-01.diff:527:+     tokens computed against the darkest panel they land on (--deck-panel
  .harness\cc-01.diff:528:+     #1c2230) and the bare canvas (#0e1014) — ratios noted per token. */
  .harness\cc-01.diff:529:+  --deck-cyan: #2dbb57;     /* primary accent — fills, LEDs, glows only */
  .harness\cc-01.diff:530:+  --deck-go: #2dbb57;       /* go — fills/LEDs only */
  .harness\cc-01.diff:531:+  --deck-amber: #f4820f;    /* caution — fills/LEDs only */
  .harness\cc-01.diff:532:+  --deck-abort: #e5484d;    /* abort — fills/LEDs only */
  .harness\cc-01.diff:533:+  --deck-glow: 45, 187, 87; /* RGB triplet for LED/glow shadows — candy green on the dark 
deck */
  .harness\cc-01.diff:534:+  --deck-line: rgba(255, 255, 255, 0.10);
  .harness\cc-01.diff:535:+  --deck-line-soft: rgba(255, 255, 255, 0.05);
  .harness\cc-01.diff:536:+  --deck-panel: #1c2230;    /* canvas --surface-2 */
  .harness\cc-01.diff:537:+  --deck-panel-hi: #232b3a; /* canvas --surface-3 */
  .harness\cc-01.diff:538:+  --deck-text: #f0f3f7;     /* canvas --ink → 14.28:1 on panel, 17.11:1 on canvas */
  .harness\cc-01.diff:539:+  --deck-muted: #a6afbc;    /* canvas --ink-dim → 7.18:1 on panel, 8.60:1 on canvas */
  .harness\cc-01.diff:540:+  /* WCAG-AA text variants: the bright candy green/amber/red are kept for
  .harness\cc-01.diff:541:+     fills/LEDs; TEXT consumers get light-on-dark variants that clear 4.5:1
  .harness\cc-01.diff:542:+     on --deck-panel (#1c2230). */
  .harness\cc-01.diff:543:+  --deck-cyan-text: #34d399;  /* 8.27:1 on panel, 9.90:1 on canvas */
  .harness\cc-01.diff:544:+  --deck-amber-text: #f0a94c; /* 7.94:1 on panel, 9.51:1 on canvas */
  .harness\cc-01.diff:545:+  --deck-abort-text: #f87171; /* 5.75:1 on panel, 6.88:1 on canvas */
  .harness\cc-01.diff:546:+
  .harness\cc-01.diff:547:+  /* Aliases so panels consolidated from the retired command-center page (which
  .harness\cc-01.diff:548:+     use the --cc-* namespace) resolve their colours on this deck. */
  .harness\cc-01.diff:549:+  --cc-bg: var(--deck-panel);
  .harness\cc-01.diff:550:+  --cc-bg-soft: #141820;    /* canvas --surface-1 */
  .harness\cc-01.diff:551:+  --cc-grid: var(--deck-line);
  .harness\cc-01.diff:552:+  --cc-ink: var(--deck-text);
  .harness\cc-01.diff:553:+  --cc-ink-dim: var(--deck-muted);
  .harness\cc-01.diff:554:+  --cc-ink-hush: #8b96a5;   /* hushed but AA: 5.30:1 on panel, 5.93:1 on bg-soft */
  .harness\cc-01.diff:555:+  /* --cc-signal stays the bright candy green: consolidated cc-* panels use it
  .harness\cc-01.diff:556:+     for fills/dots/LEDs/borders (ActivityRow, HermesControlPanel backgrounds,
  .harness\cc-01.diff:557:+     WorkPacketLane borders, ActivityLog dot, DegradedDataBanner borderLeft).
  .harness\cc-01.diff:558:+     TEXT consumers use --cc-signal-text (AA-passing on the dark grounds). */
  .harness\cc-01.diff:559:+  --cc-signal: var(--deck-cyan);
  .harness\cc-01.diff:560:+  --cc-signal-text: var(--deck-cyan-text);
  .harness\cc-01.diff:561:+  --cc-pulse-duration: 2.4s;
  .harness\cc-01.diff:562:+
  .harness\cc-01.diff:563:+  /* Tile inline-style tokens — the five queue/health/evidence tiles style via
  .harness\cc-01.diff:564:+     inline `style={{}}` (class overrides can't reach them), so they consume
  .harness\cc-01.diff:565:+     these vars with their old literals as fallbacks. Deck values = those
  .harness\cc-01.diff:566:+     literals exactly; the daylight block re-inks them for paper. */
  .harness\cc-01.diff:567:+  --tile-ink: #e6f7ff;
  .harness\cc-01.diff:568:+  --tile-ink-dim: #9bb0c1;
  .harness\cc-01.diff:569:+  --tile-ink-hush: #6f879b;
  .harness\cc-01.diff:570:+  --tile-card-bg: rgba(0, 0, 0, 0.25);
  .harness\cc-01.diff:571:+  --tile-card-line: rgba(255, 255, 255, 0.08);
  .harness\cc-01.diff:572:+  --tile-amber-txt: #fb923c;
  .harness\cc-01.diff:573:+  --tile-green-txt: #34d399;
  .harness\cc-01.diff:574:+  --tile-red-txt: #d02f35;
  .harness\cc-01.diff:575:+
  .harness\cc-01.diff:576:+  position: relative;
  .harness\cc-01.diff:577:+  min-height: 100%;
  .harness\cc-01.diff:578:+  padding: 1.75rem 1.75rem 3rem;
  .harness\cc-01.diff:579:+  color: var(--deck-text);
  .harness\cc-01.diff:580:+  font-family: var(--font-chakra), var(--font-geist-sans), system-ui, sans-serif;
  .harness\cc-01.diff:581:+  background-color: #0e1014;
  .harness\cc-01.diff:582:+  background-image:
  .harness\cc-01.diff:583:+    radial-gradient(120% 80% at 50% -10%, rgba(179, 0, 0, 0.07), transparent 60%),
  .harness\cc-01.diff:584:+    radial-gradient(100% 70% at 100% 0%, rgba(255, 255, 255, 0.03), transparent 55%);
  .harness\cc-01.diff:585:+  overflow: hidden;
  .harness\cc-01.diff:586:+}
  .harness\cc-01.diff:587:+
  .harness\cc-01.diff:588:+/* Top ambient light (was the candy gloss sheen — canvas = matte, faint) */
  .harness\cc-01.diff:589:+.deck::after {
  .harness\cc-01.diff:590:+  content: "";
  .harness\cc-01.diff:591:+  position: absolute;
  .harness\cc-01.diff:592:+  inset: 0;
  .harness\cc-01.diff:593:+  pointer-events: none;
  .harness\cc-01.diff:594:+  background: linear-gradient(180deg, rgba(255, 255, 255, 0.04), transparent 28%);
  .harness\cc-01.diff:595:+  z-index: 0;
  .harness\cc-01.diff:596:+}
  .harness\cc-01.diff:597:+
  .harness\cc-01.diff:598:+.deck > * { position: relative; z-index: 1; }
  .harness\cc-01.diff:599:+
  .harness\cc-01.diff:600:+/* ── Status strip ─────────────────────────────────────────────────────── */
  .harness\cc-01.diff:601:+.statusStrip {
  .harness\cc-01.diff:602:+  display: flex;
  .harness\cc-01.diff:603:+  align-items: center;
  .harness\cc-01.diff:604:+  justify-content: space-between;
  .harness\cc-01.diff:605:+  gap: 1.5rem;
  .harness\cc-01.diff:606:+  flex-wrap: wrap;
  .harness\cc-01.diff:607:+  padding: 0.9rem 1.1rem;
  .harness\cc-01.diff:608:+  border: 1px solid var(--deck-line);
  .harness\cc-01.diff:609:+  border-radius: 12px;
  .harness\cc-01.diff:610:+  background: linear-gradient(180deg, var(--deck-panel-hi), var(--deck-panel));
  .harness\cc-01.diff:611:+  box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.06), 0 8px 30px rgba(0, 0, 0, 0.45);
  .harness\cc-01.diff:612:+}
  .harness\cc-01.diff:613:+
  .harness\cc-01.diff:614:+.brand { display: flex; align-items: center; gap: 0.75rem; }
  .harness\cc-01.diff:615:+
  .harness\cc-01.diff:616:+.brandMark {
  .harness\cc-01.diff:617:+  width: 34px; height: 34px;
  .harness\cc-01.diff:618:+  display: grid; place-items: center;
  .harness\cc-01.diff:619:+  border: 1px solid var(--deck-cyan);
  .harness\cc-01.diff:620:+  border-radius: 10px;
  .harness\cc-01.diff:621:+  color: var(--deck-cyan-text);
  .harness\cc-01.diff:622:+  font-weight: 700;
  .harness\cc-01.diff:623:+  letter-spacing: 0.05em;
  .harness\cc-01.diff:624:+  box-shadow: 0 0 18px rgba(var(--deck-glow), 0.35), inset 0 0 10px rgba(var(--deck-glow), 
0.12);
  .harness\cc-01.diff:625:+}
  .harness\cc-01.diff:626:+
  .harness\cc-01.diff:627:+.brandText { display: flex; flex-direction: column; line-height: 1.05; }
  .harness\cc-01.diff:628:+.brandTitle {
  .harness\cc-01.diff:629:+  font-weight: 600;
  .harness\cc-01.diff:630:+  font-size: 0.95rem;
  .harness\cc-01.diff:631:+  letter-spacing: 0.22em;
  .harness\cc-01.diff:632:+  text-transform: uppercase;
  .harness\cc-01.diff:633:+  color: var(--deck-text);
  .harness\cc-01.diff:634:+}
  .harness\cc-01.diff:635:+.brandSub {
  .harness\cc-01.diff:636:+  font-family: var(--font-geist-mono), monospace;
  .harness\cc-01.diff:637:+  font-size: 0.62rem;
  .harness\cc-01.diff:638:+  letter-spacing: 0.28em;
  .harness\cc-01.diff:639:+  text-transform: uppercase;
  .harness\cc-01.diff:640:+  color: var(--deck-muted);
  .harness\cc-01.diff:641:+}
  .harness\cc-01.diff:642:+
  .harness\cc-01.diff:643:+.clock {
  .harness\cc-01.diff:644:+  font-family: var(--font-geist-mono), monospace;
  .harness\cc-01.diff:645:+  font-size: 1.35rem;
  .harness\cc-01.diff:646:+  font-weight: 500;
  .harness\cc-01.diff:647:+  letter-spacing: 0.08em;
  .harness\cc-01.diff:648:+  color: var(--deck-cyan-text);
  .harness\cc-01.diff:649:+  text-shadow: 0 0 14px rgba(var(--deck-glow), 0.45);
  .harness\cc-01.diff:650:+  font-variant-numeric: tabular-nums;
  .harness\cc-01.diff:651:+}
  .harness\cc-01.diff:652:+.clockLabel {
  .harness\cc-01.diff:653:+  font-family: var(--font-geist-mono), monospace;
  .harness\cc-01.diff:654:+  font-size: 0.55rem;
  .harness\cc-01.diff:655:+  letter-spacing: 0.3em;



hook: PostToolUse
hook: PostToolUse Completed
hook: PostToolUse Completed
hook: PostToolUse Completed
exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Select-String -LiteralPath 'D:\\Pi-Dev-Ops\\.harness\\cc-01.diff' -Pattern 'diff --git a/dashboard/lib/command-centre/control-panel.ts' -Context 0,300" in D:\Pi-Dev-Ops
exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Select-String -LiteralPath 'D:\\Pi-Dev-Ops\\.harness\\cc-01.diff' -Pattern 'diff --git a/dashboard/components/command-centre/deck-details.module.css' -Context 0,110" in D:\Pi-Dev-Ops
exec
"C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Select-String -LiteralPath 'D:\\Pi-Dev-Ops\\.harness\\cc-01.diff' -Pattern 'baselineAvailable|it.skipIf|importGraph|resolveSpec|matchAll' -Context 2,2" in D:\Pi-Dev-Ops
 succeeded in 384ms:

> .harness\cc-01.diff:1137:diff --git a/dashboard/components/command-centre/deck-details.module.css 
b/dashboard/components/command-centre/deck-details.module.css
  .harness\cc-01.diff:1138:new file mode 100644
  .harness\cc-01.diff:1139:index 00000000..c37799f1
  .harness\cc-01.diff:1140:--- /dev/null
  .harness\cc-01.diff:1141:+++ b/dashboard/components/command-centre/deck-details.module.css
  .harness\cc-01.diff:1142:@@ -0,0 +1,80 @@
  .harness\cc-01.diff:1143:+/* deck-details.module.css — styling for the DeckDetails disclosure primitive.
  .harness\cc-01.diff:1144:+   Matches the command-deck aesthetic: OLED-adjacent ground handled by the
  .harness\cc-01.diff:1145:+   parent tile, mono accents, rounded-sm (2px), --deck-* token vocabulary
  .harness\cc-01.diff:1146:+   (falls back to the --cc-* aliases the deck already maps onto them). */
  .harness\cc-01.diff:1147:+
  .harness\cc-01.diff:1148:+.deckDetails {
  .harness\cc-01.diff:1149:+  border: 1px solid var(--deck-line, var(--cc-grid, rgba(255, 255, 255, 0.1)));
  .harness\cc-01.diff:1150:+  border-radius: 2px;
  .harness\cc-01.diff:1151:+  background: transparent;
  .harness\cc-01.diff:1152:+}
  .harness\cc-01.diff:1153:+
  .harness\cc-01.diff:1154:+.deckSummary {
  .harness\cc-01.diff:1155:+  cursor: pointer;
  .harness\cc-01.diff:1156:+  list-style: none;
  .harness\cc-01.diff:1157:+  display: flex;
  .harness\cc-01.diff:1158:+  align-items: center;
  .harness\cc-01.diff:1159:+  gap: 0.6rem;
  .harness\cc-01.diff:1160:+  padding: 0.55rem 0.7rem;
  .harness\cc-01.diff:1161:+  user-select: none;
  .harness\cc-01.diff:1162:+  min-width: 0;
  .harness\cc-01.diff:1163:+}
  .harness\cc-01.diff:1164:+.deckSummary::-webkit-details-marker {
  .harness\cc-01.diff:1165:+  display: none;
  .harness\cc-01.diff:1166:+}
  .harness\cc-01.diff:1167:+.deckSummary:hover .title {
  .harness\cc-01.diff:1168:+  color: var(--deck-text, var(--cc-ink, #f0f3f7));
  .harness\cc-01.diff:1169:+}
  .harness\cc-01.diff:1170:+
  .harness\cc-01.diff:1171:+.chev {
  .harness\cc-01.diff:1172:+  flex: 0 0 auto;
  .harness\cc-01.diff:1173:+  font-family: ui-monospace, SFMono-Regular, monospace;
  .harness\cc-01.diff:1174:+  font-size: 0.7rem;
  .harness\cc-01.diff:1175:+  color: var(--deck-cyan-text, var(--cc-signal-text, #34d399));
  .harness\cc-01.diff:1176:+}
  .harness\cc-01.diff:1177:+.chev::before {
  .harness\cc-01.diff:1178:+  content: '▸';
  .harness\cc-01.diff:1179:+}
  .harness\cc-01.diff:1180:+.deckDetails[open] > .deckSummary .chev::before {
  .harness\cc-01.diff:1181:+  content: '▾';
  .harness\cc-01.diff:1182:+}
  .harness\cc-01.diff:1183:+
  .harness\cc-01.diff:1184:+.title {
  .harness\cc-01.diff:1185:+  color: var(--deck-text, var(--cc-ink, #f0f3f7));
  .harness\cc-01.diff:1186:+  font-size: 0.82rem;
  .harness\cc-01.diff:1187:+  font-weight: 700;
  .harness\cc-01.diff:1188:+  white-space: nowrap;
  .harness\cc-01.diff:1189:+  overflow: hidden;
  .harness\cc-01.diff:1190:+  text-overflow: ellipsis;
  .harness\cc-01.diff:1191:+}
  .harness\cc-01.diff:1192:+
  .harness\cc-01.diff:1193:+.stats {
  .harness\cc-01.diff:1194:+  color: var(--deck-muted, var(--cc-ink-dim, #a6afbc));
  .harness\cc-01.diff:1195:+  font-family: ui-monospace, SFMono-Regular, monospace;
  .harness\cc-01.diff:1196:+  font-size: 0.7rem;
  .harness\cc-01.diff:1197:+  letter-spacing: 0.03em;
  .harness\cc-01.diff:1198:+  white-space: nowrap;
  .harness\cc-01.diff:1199:+  overflow: hidden;
  .harness\cc-01.diff:1200:+  text-overflow: ellipsis;
  .harness\cc-01.diff:1201:+  min-width: 0;
  .harness\cc-01.diff:1202:+}
  .harness\cc-01.diff:1203:+
  .harness\cc-01.diff:1204:+.badge {
  .harness\cc-01.diff:1205:+  margin-left: auto;
  .harness\cc-01.diff:1206:+  flex: 0 0 auto;
  .harness\cc-01.diff:1207:+  display: inline-flex;
  .harness\cc-01.diff:1208:+  align-items: center;
  .harness\cc-01.diff:1209:+}
  .harness\cc-01.diff:1210:+
  .harness\cc-01.diff:1211:+.body {
  .harness\cc-01.diff:1212:+  border-top: 1px solid var(--deck-line-soft, rgba(255, 255, 255, 0.05));
  .harness\cc-01.diff:1213:+  padding: 0.6rem 0.7rem;
  .harness\cc-01.diff:1214:+}
  .harness\cc-01.diff:1215:+
  .harness\cc-01.diff:1216:+.moreLine {
  .harness\cc-01.diff:1217:+  margin: 0.4rem 0 0;
  .harness\cc-01.diff:1218:+  color: var(--deck-muted, var(--cc-ink-dim, #a6afbc));
  .harness\cc-01.diff:1219:+  font-family: ui-monospace, SFMono-Regular, monospace;
  .harness\cc-01.diff:1220:+  font-size: 0.7rem;
  .harness\cc-01.diff:1221:+  letter-spacing: 0.03em;
  .harness\cc-01.diff:1222:+}
  .harness\cc-01.diff:1223:diff --git a/dashboard/lib/command-centre/control-panel.ts 
b/dashboard/lib/command-centre/control-panel.ts
  .harness\cc-01.diff:1224:new file mode 100644
  .harness\cc-01.diff:1225:index 00000000..0b698979
  .harness\cc-01.diff:1226:--- /dev/null
  .harness\cc-01.diff:1227:+++ b/dashboard/lib/command-centre/control-panel.ts
  .harness\cc-01.diff:1228:@@ -0,0 +1,264 @@
  .harness\cc-01.diff:1229:+/**
  .harness\cc-01.diff:1230:+ * Hermes v0.16 "Surface Release" — Control Panel capability registry (static, read-only).
  .harness\cc-01.diff:1231:+ *
  .harness\cc-01.diff:1232:+ * Maps the Hermes Agent v0.16.0 Surface-Release feature areas to Unite-Group Control
  .harness\cc-01.diff:1233:+ * Panel modules as a READ-ONLY status surface. This file performs NO external calls, opens
  .harness\cc-01.diff:1234:+ * NO connections, reads NO secrets, and activates NOTHING. Every high-risk surface (remote
  .harness\cc-01.diff:1235:+ * gateway, MCP catalog, messaging channels, hooks/webhooks) is represented as inert:
  .harness\cc-01.diff:1236:+ * connected = false / enabled = false / status 'design_only'. Live wiring of any module is
  .harness\cc-01.diff:1237:+ * a separate, explicitly-gated Board decision.
  .harness\cc-01.diff:1238:+ *
  .harness\cc-01.diff:1239:+ * Safety invariants enforced at the type/data level:
  .harness\cc-01.diff:1240:+ *  - no module is 'active' for an external surface (externalActionRisk modules stay 
gated);
  .harness\cc-01.diff:1241:+ *  - the credential boundary carries STATUS STRINGS ONLY — never a secret value;
  .harness\cc-01.diff:1242:+ *  - externalChannelsEnabled / mcpConnected / remoteGatewayConnected are literal false.
  .harness\cc-01.diff:1243:+ */
  .harness\cc-01.diff:1244:+
  .harness\cc-01.diff:1245:+export type ModuleState =
  .harness\cc-01.diff:1246:+  | 'read_only' // surfaced, no action possible
  .harness\cc-01.diff:1247:+  | 'planned' // designed, not built live



hook: PostToolUse
 succeeded in 408ms:

> .harness\cc-01.diff:1223:diff --git a/dashboard/lib/command-centre/control-panel.ts 
b/dashboard/lib/command-centre/control-panel.ts
  .harness\cc-01.diff:1224:new file mode 100644
  .harness\cc-01.diff:1225:index 00000000..0b698979
  .harness\cc-01.diff:1226:--- /dev/null
  .harness\cc-01.diff:1227:+++ b/dashboard/lib/command-centre/control-panel.ts
  .harness\cc-01.diff:1228:@@ -0,0 +1,264 @@
  .harness\cc-01.diff:1229:+/**
  .harness\cc-01.diff:1230:+ * Hermes v0.16 "Surface Release" — Control Panel capability registry (static, read-only).
  .harness\cc-01.diff:1231:+ *
  .harness\cc-01.diff:1232:+ * Maps the Hermes Agent v0.16.0 Surface-Release feature areas to Unite-Group Control
  .harness\cc-01.diff:1233:+ * Panel modules as a READ-ONLY status surface. This file performs NO external calls, opens
  .harness\cc-01.diff:1234:+ * NO connections, reads NO secrets, and activates NOTHING. Every high-risk surface (remote
  .harness\cc-01.diff:1235:+ * gateway, MCP catalog, messaging channels, hooks/webhooks) is represented as inert:
  .harness\cc-01.diff:1236:+ * connected = false / enabled = false / status 'design_only'. Live wiring of any module is
  .harness\cc-01.diff:1237:+ * a separate, explicitly-gated Board decision.
  .harness\cc-01.diff:1238:+ *
  .harness\cc-01.diff:1239:+ * Safety invariants enforced at the type/data level:
  .harness\cc-01.diff:1240:+ *  - no module is 'active' for an external surface (externalActionRisk modules stay 
gated);
  .harness\cc-01.diff:1241:+ *  - the credential boundary carries STATUS STRINGS ONLY — never a secret value;
  .harness\cc-01.diff:1242:+ *  - externalChannelsEnabled / mcpConnected / remoteGatewayConnected are literal false.
  .harness\cc-01.diff:1243:+ */
  .harness\cc-01.diff:1244:+
  .harness\cc-01.diff:1245:+export type ModuleState =
  .harness\cc-01.diff:1246:+  | 'read_only' // surfaced, no action possible
  .harness\cc-01.diff:1247:+  | 'planned' // designed, not built live
  .harness\cc-01.diff:1248:+  | 'design_only' // stubbed; live wiring is a later gate
  .harness\cc-01.diff:1249:+  | 'available_not_installed' // capability exists but not installed/connected (operator 
action)
  .harness\cc-01.diff:1250:+  | 'not_connected' // external surface deliberately not connected
  .harness\cc-01.diff:1251:+  | 'none_enabled' // external surface deliberately has nothing enabled
  .harness\cc-01.diff:1252:+
  .harness\cc-01.diff:1253:+export type ExternalActionRisk = 'none' | 'low' | 'high'
  .harness\cc-01.diff:1254:+
  .harness\cc-01.diff:1255:+export interface HermesControlPanelModule {
  .harness\cc-01.diff:1256:+  moduleId: string
  .harness\cc-01.diff:1257:+  title: string
  .harness\cc-01.diff:1258:+  hermesFeature: string
  .harness\cc-01.diff:1259:+  state: ModuleState
  .harness\cc-01.diff:1260:+  externalActionRisk: ExternalActionRisk
  .harness\cc-01.diff:1261:+  implementableNow: boolean
  .harness\cc-01.diff:1262:+  requiresLaterApproval: boolean
  .harness\cc-01.diff:1263:+  note: string
  .harness\cc-01.diff:1264:+}
  .harness\cc-01.diff:1265:+
  .harness\cc-01.diff:1266:+export const HERMES_VERSION = '0.16.0'
  .harness\cc-01.diff:1267:+export const HERMES_RELEASE = '2026.6.5'
  .harness\cc-01.diff:1268:+export const HERMES_RELEASE_NAME = 'The Surface Release'
  .harness\cc-01.diff:1269:+export const HERMES_CONFIG_FORMAT = 27
  .harness\cc-01.diff:1270:+
  .harness\cc-01.diff:1271:+export interface HermesVersionStatus {
  .harness\cc-01.diff:1272:+  source: 'static_registry'
  .harness\cc-01.diff:1273:+  version: string
  .harness\cc-01.diff:1274:+  release: string
  .harness\cc-01.diff:1275:+  releaseName: string
  .harness\cc-01.diff:1276:+  configFormat: number
  .harness\cc-01.diff:1277:+  securityPosture: {
  .harness\cc-01.diff:1278:+    secretRedaction: 'on'
  .harness\cc-01.diff:1279:+    subprocessCredentialStripping: 'on'
  .harness\cc-01.diff:1280:+    ssrfHardening: 'on'
  .harness\cc-01.diff:1281:+    cveSecurityPinning: 'on'
  .harness\cc-01.diff:1282:+    leanerSkillSet: 'adopted'
  .harness\cc-01.diff:1283:+    nvidiaTap: 'available_not_installed'
  .harness\cc-01.diff:1284:+  }
  .harness\cc-01.diff:1285:+}
  .harness\cc-01.diff:1286:+
  .harness\cc-01.diff:1287:+export function getHermesVersionStatus(): HermesVersionStatus {
  .harness\cc-01.diff:1288:+  return {
  .harness\cc-01.diff:1289:+    source: 'static_registry',
  .harness\cc-01.diff:1290:+    version: HERMES_VERSION,
  .harness\cc-01.diff:1291:+    release: HERMES_RELEASE,
  .harness\cc-01.diff:1292:+    releaseName: HERMES_RELEASE_NAME,
  .harness\cc-01.diff:1293:+    configFormat: HERMES_CONFIG_FORMAT,
  .harness\cc-01.diff:1294:+    securityPosture: {
  .harness\cc-01.diff:1295:+      secretRedaction: 'on',
  .harness\cc-01.diff:1296:+      subprocessCredentialStripping: 'on',
  .harness\cc-01.diff:1297:+      ssrfHardening: 'on',
  .harness\cc-01.diff:1298:+      cveSecurityPinning: 'on',
  .harness\cc-01.diff:1299:+      leanerSkillSet: 'adopted',
  .harness\cc-01.diff:1300:+      nvidiaTap: 'available_not_installed',
  .harness\cc-01.diff:1301:+    },
  .harness\cc-01.diff:1302:+  }
  .harness\cc-01.diff:1303:+}
  .harness\cc-01.diff:1304:+
  .harness\cc-01.diff:1305:+export const HERMES_CONTROL_PANEL_MODULES: readonly HermesControlPanelModule[] = [
  .harness\cc-01.diff:1306:+  {
  .harness\cc-01.diff:1307:+    moduleId: 'desktop_session_status',
  .harness\cc-01.diff:1308:+    title: 'Desktop session status',
  .harness\cc-01.diff:1309:+    hermesFeature: 'Hermes Desktop app',
  .harness\cc-01.diff:1310:+    state: 'read_only',
  .harness\cc-01.diff:1311:+    externalActionRisk: 'none',
  .harness\cc-01.diff:1312:+    implementableNow: true,
  .harness\cc-01.diff:1313:+    requiresLaterApproval: false,
  .harness\cc-01.diff:1314:+    note: 'Derived from the hermes_local lane (active, local_runtime). No live presence 
control.',
  .harness\cc-01.diff:1315:+  },
  .harness\cc-01.diff:1316:+  {
  .harness\cc-01.diff:1317:+    moduleId: 'remote_gateway_registry',
  .harness\cc-01.diff:1318:+    title: 'Remote gateway registry',
  .harness\cc-01.diff:1319:+    hermesFeature: 'Remote Hermes gateway + gateway list',
  .harness\cc-01.diff:1320:+    state: 'design_only',
  .harness\cc-01.diff:1321:+    externalActionRisk: 'high',
  .harness\cc-01.diff:1322:+    implementableNow: true,
  .harness\cc-01.diff:1323:+    requiresLaterApproval: true,
  .harness\cc-01.diff:1324:+    note: '0 registered workers, not connected. Connecting a remote gateway is a later 
named-grant gate.',
  .harness\cc-01.diff:1325:+  },
  .harness\cc-01.diff:1326:+  {
  .harness\cc-01.diff:1327:+    moduleId: 'web_admin_console',
  .harness\cc-01.diff:1328:+    title: 'Web admin console',
  .harness\cc-01.diff:1329:+    hermesFeature: 'Web dashboard admin panel',
  .harness\cc-01.diff:1330:+    state: 'read_only',
  .harness\cc-01.diff:1331:+    externalActionRisk: 'none',
  .harness\cc-01.diff:1332:+    implementableNow: true,
  .harness\cc-01.diff:1333:+    requiresLaterApproval: false,
  .harness\cc-01.diff:1334:+    note: 'This founder Control Panel page mirrors the Hermes web admin module list.',
  .harness\cc-01.diff:1335:+  },
  .harness\cc-01.diff:1336:+  {
  .harness\cc-01.diff:1337:+    moduleId: 'mcp_catalog_manager',
  .harness\cc-01.diff:1338:+    title: 'MCP catalog manager',
  .harness\cc-01.diff:1339:+    hermesFeature: 'MCP catalog admin',
  .harness\cc-01.diff:1340:+    state: 'design_only',
  .harness\cc-01.diff:1341:+    externalActionRisk: 'high',
  .harness\cc-01.diff:1342:+    implementableNow: true,
  .harness\cc-01.diff:1343:+    requiresLaterApproval: true,
  .harness\cc-01.diff:1344:+    note: 'Catalogue stub, 0 servers, not connected. Connecting an MCP server is a later 
gate.',
  .harness\cc-01.diff:1345:+  },
  .harness\cc-01.diff:1346:+  {
  .harness\cc-01.diff:1347:+    moduleId: 'messaging_channels_manager',
  .harness\cc-01.diff:1348:+    title: 'Messaging channels manager',
  .harness\cc-01.diff:1349:+    hermesFeature: 'Channels page',
  .harness\cc-01.diff:1350:+    state: 'none_enabled',
  .harness\cc-01.diff:1351:+    externalActionRisk: 'high',
  .harness\cc-01.diff:1352:+    implementableNow: true,
  .harness\cc-01.diff:1353:+    requiresLaterApproval: true,
  .harness\cc-01.diff:1354:+    note: 'No external channels enabled. Enabling any channel is a later gate.',
  .harness\cc-01.diff:1355:+  },
  .harness\cc-01.diff:1356:+  {
  .harness\cc-01.diff:1357:+    moduleId: 'credential_boundary_dashboard',
  .harness\cc-01.diff:1358:+    title: 'Credential boundary dashboard',
  .harness\cc-01.diff:1359:+    hermesFeature: 'Credentials management + subprocess credential stripping',
  .harness\cc-01.diff:1360:+    state: 'read_only',
  .harness\cc-01.diff:1361:+    externalActionRisk: 'none',
  .harness\cc-01.diff:1362:+    implementableNow: true,
  .harness\cc-01.diff:1363:+    requiresLaterApproval: false,
  .harness\cc-01.diff:1364:+    note: 'Boundary names + status strings only; NEVER secret values. Reflects BLOCKED-OP 
sandbox state textually.',
  .harness\cc-01.diff:1365:+  },
  .harness\cc-01.diff:1366:+  {
  .harness\cc-01.diff:1367:+    moduleId: 'hooks_webhooks_manager',
  .harness\cc-01.diff:1368:+    title: 'Hooks / webhooks manager',
  .harness\cc-01.diff:1369:+    hermesFeature: 'Webhooks / hooks',
  .harness\cc-01.diff:1370:+    state: 'none_enabled',
  .harness\cc-01.diff:1371:+    externalActionRisk: 'high',
  .harness\cc-01.diff:1372:+    implementableNow: true,
  .harness\cc-01.diff:1373:+    requiresLaterApproval: true,
  .harness\cc-01.diff:1374:+    note: '0 webhooks, 0 hooks. Creating/enabling a live hook is a later gate.',
  .harness\cc-01.diff:1375:+  },
  .harness\cc-01.diff:1376:+  {
  .harness\cc-01.diff:1377:+    moduleId: 'memory_second_brain_sync',
  .harness\cc-01.diff:1378:+    title: 'Memory / 2nd-Brain sync panel',
  .harness\cc-01.diff:1379:+    hermesFeature: 'Memory configuration',
  .harness\cc-01.diff:1380:+    state: 'read_only',
  .harness\cc-01.diff:1381:+    externalActionRisk: 'none',
  .harness\cc-01.diff:1382:+    implementableNow: true,
  .harness\cc-01.diff:1383:+    requiresLaterApproval: false,
  .harness\cc-01.diff:1384:+    note: 'Built-in memory; 2nd-Brain (.agentic_nexus) sync is local_only. External memory 
provider = new-vendor gate.',
  .harness\cc-01.diff:1385:+  },
  .harness\cc-01.diff:1386:+  {
  .harness\cc-01.diff:1387:+    moduleId: 'model_lane_picker',
  .harness\cc-01.diff:1388:+    title: 'Model / lane picker',
  .harness\cc-01.diff:1389:+    hermesFeature: 'Fuzzy model picker',
  .harness\cc-01.diff:1390:+    state: 'read_only',
  .harness\cc-01.diff:1391:+    externalActionRisk: 'none',
  .harness\cc-01.diff:1392:+    implementableNow: true,
  .harness\cc-01.diff:1393:+    requiresLaterApproval: false,
  .harness\cc-01.diff:1394:+    note: 'Reuses the operator lane registry. Switching a live operator model is a later 
gate.',
  .harness\cc-01.diff:1395:+  },
  .harness\cc-01.diff:1396:+  {
  .harness\cc-01.diff:1397:+    moduleId: 'skill_hygiene_panel',
  .harness\cc-01.diff:1398:+    title: 'Skill hygiene panel',
  .harness\cc-01.diff:1399:+    hermesFeature: 'Leaner skill set + curator',
  .harness\cc-01.diff:1400:+    state: 'read_only',
  .harness\cc-01.diff:1401:+    externalActionRisk: 'none',
  .harness\cc-01.diff:1402:+    implementableNow: true,
  .harness\cc-01.diff:1403:+    requiresLaterApproval: false,
  .harness\cc-01.diff:1404:+    note: 'Leaner skill set adopted; curator governs agent-created skills. Read-only 
posture.',
  .harness\cc-01.diff:1405:+  },
  .harness\cc-01.diff:1406:+  {
  .harness\cc-01.diff:1407:+    moduleId: 'trusted_skills_tap_registry',
  .harness\cc-01.diff:1408:+    title: 'Trusted skills tap registry',
  .harness\cc-01.diff:1409:+    hermesFeature: 'Trusted NVIDIA skills tap',
  .harness\cc-01.diff:1410:+    state: 'available_not_installed',
  .harness\cc-01.diff:1411:+    externalActionRisk: 'low',
  .harness\cc-01.diff:1412:+    implementableNow: true,
  .harness\cc-01.diff:1413:+    requiresLaterApproval: true,
  .harness\cc-01.diff:1414:+    note: 'Default taps only; NVIDIA trusted tap available but NOT installed (no 
auto-install).',
  .harness\cc-01.diff:1415:+  },
  .harness\cc-01.diff:1416:+  {
  .harness\cc-01.diff:1417:+    moduleId: 'undo_recovery_panel',
  .harness\cc-01.diff:1418:+    title: 'Undo / recovery panel',
  .harness\cc-01.diff:1419:+    hermesFeature: '/undo [N] + checkpoints',
  .harness\cc-01.diff:1420:+    state: 'read_only',
  .harness\cc-01.diff:1421:+    externalActionRisk: 'none',
  .harness\cc-01.diff:1422:+    implementableNow: true,
  .harness\cc-01.diff:1423:+    requiresLaterApproval: false,
  .harness\cc-01.diff:1424:+    note: 'Surfaces undo + checkpoint availability. Invoking recovery from the panel is a 
later gate.',
  .harness\cc-01.diff:1425:+  },
  .harness\cc-01.diff:1426:+  {
  .harness\cc-01.diff:1427:+    moduleId: 'security_version_compliance',
  .harness\cc-01.diff:1428:+    title: 'Security / version compliance panel',
  .harness\cc-01.diff:1429:+    hermesFeature: 'Security hardening, SSRF, CVE pinning, Debug Share',
  .harness\cc-01.diff:1430:+    state: 'read_only',
  .harness\cc-01.diff:1431:+    externalActionRisk: 'none',
  .harness\cc-01.diff:1432:+    implementableNow: true,
  .harness\cc-01.diff:1433:+    requiresLaterApproval: false,
  .harness\cc-01.diff:1434:+    note: 'Shows version 0.16.0, config format 27, and security posture flags. Display 
only.',
  .harness\cc-01.diff:1435:+  },
  .harness\cc-01.diff:1436:+] as const
  .harness\cc-01.diff:1437:+
  .harness\cc-01.diff:1438:+export function getControlPanelModules(): readonly HermesControlPanelModule[] {
  .harness\cc-01.diff:1439:+  return HERMES_CONTROL_PANEL_MODULES
  .harness\cc-01.diff:1440:+}
  .harness\cc-01.diff:1441:+
  .harness\cc-01.diff:1442:+/** Credential boundary — STATUS STRINGS ONLY. Never carries a secret value. */
  .harness\cc-01.diff:1443:+export interface CredentialBoundary {
  .harness\cc-01.diff:1444:+  boundary: string
  .harness\cc-01.diff:1445:+  status: string
  .harness\cc-01.diff:1446:+}
  .harness\cc-01.diff:1447:+
  .harness\cc-01.diff:1448:+export const CREDENTIAL_BOUNDARIES: readonly CredentialBoundary[] = [
  .harness\cc-01.diff:1449:+  { boundary: 'op_sandbox', status: 'BLOCKED-OP (1Password CLI auth not green)' },
  .harness\cc-01.diff:1450:+  { boundary: 'prod_secrets', status: 'not_read' },
  .harness\cc-01.diff:1451:+  { boundary: 'api_keys', status: 'none (no-API-key operator gateway principle)' },
  .harness\cc-01.diff:1452:+] as const
  .harness\cc-01.diff:1453:+
  .harness\cc-01.diff:1454:+export interface HermesControlPanelView {
  .harness\cc-01.diff:1455:+  source: 'static_registry'
  .harness\cc-01.diff:1456:+  liveConnections: false
  .harness\cc-01.diff:1457:+  externalChannelsEnabled: false
  .harness\cc-01.diff:1458:+  mcpConnected: false
  .harness\cc-01.diff:1459:+  remoteGatewayConnected: false
  .harness\cc-01.diff:1460:+  credentialsExposed: false
  .harness\cc-01.diff:1461:+  version: HermesVersionStatus
  .harness\cc-01.diff:1462:+  moduleCount: number
  .harness\cc-01.diff:1463:+  modulesImplementableNow: number
  .harness\cc-01.diff:1464:+  modulesRequiringLaterApproval: number
  .harness\cc-01.diff:1465:+  highRiskGatedCount: number
  .harness\cc-01.diff:1466:+  modules: HermesControlPanelModule[]
  .harness\cc-01.diff:1467:+  credentialBoundaries: CredentialBoundary[]
  .harness\cc-01.diff:1468:+  note: string
  .harness\cc-01.diff:1469:+}
  .harness\cc-01.diff:1470:+
  .harness\cc-01.diff:1471:+/**
  .harness\cc-01.diff:1472:+ * Read-only Control Panel view. No external connections, no secret values, nothing active.
  .harness\cc-01.diff:1473:+ */
  .harness\cc-01.diff:1474:+export function getControlPanelView(): HermesControlPanelView {
  .harness\cc-01.diff:1475:+  const modules = getControlPanelModules()
  .harness\cc-01.diff:1476:+  return {
  .harness\cc-01.diff:1477:+    source: 'static_registry',
  .harness\cc-01.diff:1478:+    liveConnections: false,
  .harness\cc-01.diff:1479:+    externalChannelsEnabled: false,
  .harness\cc-01.diff:1480:+    mcpConnected: false,
  .harness\cc-01.diff:1481:+    remoteGatewayConnected: false,
  .harness\cc-01.diff:1482:+    credentialsExposed: false,
  .harness\cc-01.diff:1483:+    version: getHermesVersionStatus(),
  .harness\cc-01.diff:1484:+    moduleCount: modules.length,
  .harness\cc-01.diff:1485:+    modulesImplementableNow: modules.filter((m) => m.implementableNow).length,
  .harness\cc-01.diff:1486:+    modulesRequiringLaterApproval: modules.filter((m) => m.requiresLaterApproval).length,
  .harness\cc-01.diff:1487:+    highRiskGatedCount: modules.filter((m) => m.externalActionRisk === 'high').length,
  .harness\cc-01.diff:1488:+    modules: [...modules],
  .harness\cc-01.diff:1489:+    credentialBoundaries: [...CREDENTIAL_BOUNDARIES],
  .harness\cc-01.diff:1490:+    note: 'Hermes v0.16 Surface-Release Control Panel — read-only foundation. No live 
connections, no external channels, no MCP, no remote gateway, no secret values.',
  .harness\cc-01.diff:1491:+  }
  .harness\cc-01.diff:1492:+}
  .harness\cc-01.diff:1493:diff --git a/skills/adversarial-review/SKILL.md b/skills/adversarial-review/SKILL.md
  .harness\cc-01.diff:1494:new file mode 100644
  .harness\cc-01.diff:1495:index 00000000..9762928d
  .harness\cc-01.diff:1496:--- /dev/null
  .harness\cc-01.diff:1497:+++ b/skills/adversarial-review/SKILL.md
  .harness\cc-01.diff:1498:@@ -0,0 +1,92 @@
  .harness\cc-01.diff:1499:+---
  .harness\cc-01.diff:1500:+name: adversarial-review
  .harness\cc-01.diff:1501:+description: Review a proposed fix with a different model in a fresh session that has no 
access to the builder's reasoning. Two axes, standards and spec, run as independent sub-agents. It flags; it never 
fixes. A fail returns to `propose-fix`. Use after `propose-fix`, before anything is applied.
  .harness\cc-01.diff:1502:+---
  .harness\cc-01.diff:1503:+
  .harness\cc-01.diff:1504:+# adversarial-review
  .harness\cc-01.diff:1505:+
  .harness\cc-01.diff:1506:+**The point is separation of model and context, not roleplay.** The reviewer is a 
sub-agent given a narrow brief and a fixed input set. It is not a persona, has no name, and is not asked to act like a 
senior engineer. Do not write character instructions.
  .harness\cc-01.diff:1507:+
  .harness\cc-01.diff:1508:+The value comes from what the reviewer **cannot see**: the builder's reasoning, its 
hypotheses, its rejected approaches, and its confidence. A reviewer that inherits the builder's context inherits its 
blind spots and will agree with them fluently.
  .harness\cc-01.diff:1509:+
  .harness\cc-01.diff:1510:+Dual-axis structure adapted from `code-review` (mattpocock/skills).
  .harness\cc-01.diff:1511:+
  .harness\cc-01.diff:1512:+## Spec-writing standard — read before writing the spec
  .harness\cc-01.diff:1513:+
  .harness\cc-01.diff:1514:+**Never write a spec item that asks a reviewer to prove an unbounded negative.**
  .harness\cc-01.diff:1515:+
  .harness\cc-01.diff:1516:+"Proves this page makes no network call" is unbounded. There is always another path — a 
dynamic import, `require`, `WebSocket`, `EventSource`, `sendBeacon`, a server action, a transitive side-effect import. 
A competent reviewer will keep finding them, correctly, forever. The builder cannot win and the reviewer is not wrong.
  .harness\cc-01.diff:1517:+
  .harness\cc-01.diff:1518:+**Write bounded, diff-relative claims instead.**
  .harness\cc-01.diff:1519:+
  .harness\cc-01.diff:1520:+| Unbounded — do not write | Bounded — write this |
  .harness\cc-01.diff:1521:+|---|---|
  .harness\cc-01.diff:1522:+| "makes no network call" | "introduces no network surface the source did not have" |
  .harness\cc-01.diff:1523:+| "is read-only" | "adds no write path absent from the baseline" |



 succeeded in 377ms:

  .harness\cc-01.diff:98:+};
  .harness\cc-01.diff:99:+const BASELINE = PROV._baseline_root;
> .harness\cc-01.diff:100:+const baselineAvailable = existsSync(BASELINE);
  .harness\cc-01.diff:101:+
  .harness\cc-01.diff:102:+/** Constructs whose COUNT must not increase relative to the baseline. */
  .harness\cc-01.diff:120:+const count = (src: string, re: RegExp) => (strip(src).match(new RegExp(re.source, "g")) || 
[]).length;
  .harness\cc-01.diff:121:+
> .harness\cc-01.diff:122:+function resolveSpec(spec: string, from: string): string | null {
  .harness\cc-01.diff:123:+  let base: string;
  .harness\cc-01.diff:124:+  if (spec.startsWith("@/")) base = join(ROOT, spec.slice(2));
  .harness\cc-01.diff:132:+
  .harness\cc-01.diff:133:+/** Everything reachable from the capability entry pages, following imports. */
> .harness\cc-01.diff:134:+function importGraph(): string[] {
  .harness\cc-01.diff:135:+  const entries: string[] = [];
  .harness\cc-01.diff:136:+  (function walk(d: string) {
  .harness\cc-01.diff:149:+    if (seen.has(f) || !existsSync(f)) continue;
  .harness\cc-01.diff:150:+    seen.add(f);
> .harness\cc-01.diff:151:+    for (const m of readFileSync(f, "utf8").matchAll(/from\s+['"]([^'"]+)['"]/g)) {
> .harness\cc-01.diff:152:+      const r = resolveSpec(m[1], f);
  .harness\cc-01.diff:153:+      if (r && !seen.has(r)) queue.push(r);
  .harness\cc-01.diff:154:+    }
  .harness\cc-01.diff:160:+
  .harness\cc-01.diff:161:+describe("command-centre: no new surface vs source baseline", () => {
> .harness\cc-01.diff:162:+  const files = importGraph().map(rel);
  .harness\cc-01.diff:163:+
  .harness\cc-01.diff:164:+  it("the import graph reaches beyond the capability directory (positive control)", () => {
  .harness\cc-01.diff:177:+    // In CI the Authority-Site checkout is absent. That must read as "not verified
  .harness\cc-01.diff:178:+    // here", never as "verified clean" — a skipped comparison is not a passing one.
> .harness\cc-01.diff:179:+    if (!baselineAvailable) {
  .harness\cc-01.diff:180:+      console.warn(
  .harness\cc-01.diff:181:+        `[BASELINE UNAVAILABLE] ${BASELINE} not present. ` +
  .harness\cc-01.diff:184:+      );
  .harness\cc-01.diff:185:+    }
> .harness\cc-01.diff:186:+    expect(typeof baselineAvailable).toBe("boolean");
  .harness\cc-01.diff:187:+  });
  .harness\cc-01.diff:188:+
  .harness\cc-01.diff:194:+
  .harness\cc-01.diff:195:+  for (const t of TRACKED) {
> .harness\cc-01.diff:196:+    it.skipIf(!baselineAvailable)(`introduces no new — ${t.rule}`, () => {
  .harness\cc-01.diff:197:+      const grew: string[] = [];
  .harness\cc-01.diff:198:+      for (const [ported, source] of Object.entries(PROV.files)) {



hook: PostToolUse
hook: PostToolUse
hook: PostToolUse Completed
hook: PostToolUse Completed
hook: PostToolUse Completed
codex
## Standards
No hard TypeScript/React violations found in the ported TS/TSX hunks. The App Router page uses dashboard-root `@/` imports as required: `@/lib/command-centre/control-panel` and `@/components/command-centre/DeckDetails`.

Judgement call: `DeckDetails.tsx` leaves a dangling source comment after `DECK_LIST_CAP = 8`: “Row cap for full founder pages...” followed by a light-theme note, with no exported symbol attached. It is harmless at runtime but reads like an incomplete port artifact.

## Spec
FAIL: the diff introduces substantial unrelated behaviour outside the requested Hermes port. The spec is for `dashboard/app/(main)/command-centre/hermes/`, but the diff also adds/modifies global workflow skill surfaces such as `skills/adversarial-review/SKILL.md`, `skills/orchestrator/SKILL.md`, `skills/spm/references/leveling.md`, and `skills/verify/SKILL.md`. That is behaviour introduced that was not asked for.

The test supports the bounded “no new network surface vs source baseline” claim only partially. It skips the baseline comparison when `D:/Authority-Site/apps/web/src` is absent via `it.skipIf(!baselineAvailable)`, so the suite can pass without checking R2/R6 in that environment. The import graph also only follows `from ...` imports: `matchAll(/from\s+['"]([^'"]+)['"]/g)`, so a side-effect import could evade provenance and construct counting while still affecting the page.

VERDICT: FAIL — diff includes unrelated global skill changes and the conformance test can pass without proving the diff-relative claim in baseline-unavailable environments.
hook: Stop
hook: Stop Failed
tokens used
61,743
## Standards
No hard TypeScript/React violations found in the ported TS/TSX hunks. The App Router page uses dashboard-root `@/` imports as required: `@/lib/command-centre/control-panel` and `@/components/command-centre/DeckDetails`.

Judgement call: `DeckDetails.tsx` leaves a dangling source comment after `DECK_LIST_CAP = 8`: “Row cap for full founder pages...” followed by a light-theme note, with no exported symbol attached. It is harmless at runtime but reads like an incomplete port artifact.

## Spec
FAIL: the diff introduces substantial unrelated behaviour outside the requested Hermes port. The spec is for `dashboard/app/(main)/command-centre/hermes/`, but the diff also adds/modifies global workflow skill surfaces such as `skills/adversarial-review/SKILL.md`, `skills/orchestrator/SKILL.md`, `skills/spm/references/leveling.md`, and `skills/verify/SKILL.md`. That is behaviour introduced that was not asked for.

The test supports the bounded “no new network surface vs source baseline” claim only partially. It skips the baseline comparison when `D:/Authority-Site/apps/web/src` is absent via `it.skipIf(!baselineAvailable)`, so the suite can pass without checking R2/R6 in that environment. The import graph also only follows `from ...` imports: `matchAll(/from\s+['"]([^'"]+)['"]/g)`, so a side-effect import could evade provenance and construct counting while still affecting the page.

VERDICT: FAIL — diff includes unrelated global skill changes and the conformance test can pass without proving the diff-relative claim in baseline-unavailable environments.
2026-08-01T06:25:29.672359Z ERROR rmcp::transport::streamable_http_client: fail to delete session: unexpected server response: DELETE returned HTTP 400 session_id="bef5570a-c51c-4abc-b97c-54cd5ea84acc"
