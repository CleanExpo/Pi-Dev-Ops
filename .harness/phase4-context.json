```json
{
  "projectPurpose": "Pi-CEO is a private autonomous engineering platform that converts a GitHub repo URL and a plain-English brief into a Claude Code execution session — cloning, building, evaluating, and pushing changes back without human intervention. It exists to give a non-technical solo founder (Phill) zero-touch engineering leverage across a multi-business portfolio by encoding DevOps methodology into an AI orchestration layer.",
  "targetUsers": [
    "Phill McGurk — solo non-technical founder managing 3+ revenue businesses (CCW-CRM, RestoreAssist, DR-NRPG) who cannot write code but needs continuous security remediation, feature delivery, and portfolio monitoring",
    "Claude Desktop / Cowork users connecting via MCP for harness reads and Linear operations",
    "The autonomous system itself — the Linear poller, cron engine, and watchdog are consumers of the same API surface"
  ],
  "businessLogic": "The core rules: (1) Every build is a 5-phase pipeline — clone → plan → generate → evaluate → push — with a quality gate (score ≥ threshold) that can block or warn before pushing. (2) Linear is the task queue — Urgent/High Todo issues are auto-picked by the autonomy poller every 5 minutes; issues only move in-progress when a session starts and close only when a session completes. (3) Permissions are pre-granted at three layers (settings.json bypassPermissions, ClaudeAgentOptions, --dangerously-skip-permissions) — no interactive prompts during autonomous runs. (4) The watchdog never auto-promotes ideas to Linear, never pushes to main autonomously, never touches secrets — a strict 9-item 'never' list enforced by the escalation contract. (5) A ZTE (Zero Touch Engineering) score of 75 points measures leverage across 12 dimensions; it is the primary internal health metric (being superseded by Business Velocity Index). (6) SDK-only execution as of Sprint 8 — TAO_USE_AGENT_SDK=0 raises ImportError at startup by design. (7) Observability writes are fire-and-forget to Supabase — they must never block the build pipeline.",
  "currentState": "beta",
  "bigThree": {
    "model": "Appropriate tier architecture: Opus 4.6 for planning/orchestration, Sonnet 4.6 for generation and evaluation, Haiku 4.5 for fast discrete tasks. The confidence-weighted evaluator gate (config.yaml evaluator_confidence block) is a sophisticated improvement over a flat threshold. The autonomy budget anchors (5 tiers from 10min/haiku to 240min/opus) show thoughtful cost-capability tradeoffs. Key gap: the evaluator grades on completeness/correctness/conciseness/format but cannot detect intent drift — a high-scoring session could fix the wrong thing. The planned two-agent review pattern (writer + reviewer with separate system prompts) from the board deliberation is not yet implemented.",
    "prompt": "CLAUDE.md is the strongest context document — concrete conventions, explicit forbidden patterns, SDK architecture table, known failure modes. The skill registry (28 SKILL.md files) + PITER classifier + ADW templates represent real prompt engineering discipline. Weaknesses: the evaluator output format contract is rigid ASCII (COMPLETENESS: X/10) which is brittle to model phrasing variations. The brief-to-session pipeline has no structured input validation beyond URL format — a vague brief gets a vague build. The .harness/agents/ spec files (planner.md, generator.md, evaluator.md) are well-structured but the generator spec references 'ADW workflow steps for the intent' without a current working link to what those steps are in practice.",
    "context": "The harness directory is well-designed as a context compression layer — lessons.jsonl, per-project charters, board meeting minutes, and scan results all feed the right context at session start. Critical gap identified in the incident post-mortem and TurboQuant assessment: there is no per-project RAG memory layer yet. Every session starts with whatever fits in the context window rather than retrieved relevant chunks. The .harness/anthropic-docs/ files are stored as raw scraped HTML with JavaScript rendering artifacts — they are nearly unreadable and waste tokens. The Telegram inbox design (ideas-from-phone, human promotion gate) correctly enforces human-in-the-loop for scope expansion."
  },
  "keyInsights": [
    "The system reports ZTE 73/75 while the portfolio it manages has zero measurable improvement — the incident doc and CEO memo (2026-04-13) explicitly acknowledge this: ZTE measures capability existence, not whether capabilities are working. The scoring system is optimizing for the wrong metric.",
    "The first autonomous overnight run (2026-04-11→12) produced zero commits because the push credentials gap was known but undocumented as a hard dependency — CLAUDE.md and PERMISSIONS.md both exist but neither surfaced 'git push requires human with SSH key' as a blocking constraint before the marathon started.",
    "The .harness/anthropic-docs/ files (api-reference.md, claude-code-overview.md, etc.) contain raw Next.js hydration JavaScript and CSS instead of cleaned documentation content — these are stored as fetched HTML that wasn't post-processed. Any session that loads these for context is consuming thousands of tokens of noise.",
    "The MCP server (mcp/pi-ceo-server.js v3.0.0) depends on @modelcontextprotocol/sdk ^1.29.0 and zod ^4.3.6, but the board meetings consistently report 'get_zte_score returns No ZTE data found' — the MCP tools are partially broken and board meetings have been running on fallback harness file reads for multiple sprints.",
    "The SDK migration (RA-576) removed the subprocess fallback entirely with TAO_USE_AGENT_SDK=0 raising ImportError by design — this is a deliberate loud-failure pattern but it means there is no fallback if the SDK has an outage or breaking change. The sdk-version-policy.md acknowledges this risk but the quarterly fallback dryrun only tests the direct Anthropic SDK path, not a claude -p subprocess path.",
    "The gap audit (2026-04-12-gap-audit.json) found 3 critical discrepancies: Linear two-way sync (webhook.py verifies inbound but never calls back to Linear to update issue state), the SSE streaming endpoint claimed in the spec but showing only WebSocket in visible code, and sessions.py pipeline completeness unverifiable due to truncation. These were filed as RA-665 through RA-671 but it is unclear if they were resolved.",
    "The cron-triggers.json last_fired_at timestamps use Unix epoch but the board-meeting documents reference AEST timestamps — there is an implicit timezone handling assumption that could cause cron debounce failures on Railway deploys (noted as a known bug in the SWOT analysis: bogus-future-timestamp debounce).",
    "The Supabase integration has two separate URL configurations in .env.example — NEXT_PUBLIC_SUPABASE_URL appears twice with different placeholder values (zbryrmxmgfmslqzizsto.supabase.co and https://your-project.supabase.co). This suggests the env file was merged without deduplication and could cause silent misconfiguration."
  ],
  "deploymentTopology": "Split deployment: FastAPI backend on Railway (always-on, single worker, port 8080, Docker from python:3.12-slim with Node.js 20 for claude CLI), Next.js dashboard on Vercel (rootDirectory: dashboard). MCP server runs locally on Phill's Mac via Claude Desktop stdio. Railway handles the autonomy poller, cron engine, Pi-SEO scanner, and all background tasks. Supabase (cloud) is the persistence layer for gate checks, alert escalations, heartbeat logs, and triage events. The Mac Mini running Ollama + n8n is planned (Phase 0-2 approved) but not yet deployed. No redundancy on Railway — single dyno, in-memory session dict, disk-based persistence at app/data/. Secrets in Railway env vars. GitHub Actions runs CI (pytest + ruff + tsc + eslint + build + secrets scan + prod smoke test after 90s settle delay).",
  "dataModels": [
    "BuildSession — core execution unit with id, repo_url, brief, model, status (9 states), output_lines[], evaluator_score, evaluator_status, last_completed_phase, linear_issue_id, scope, budget_minutes; persisted to app/logs/sessions/{sid}.json",
    "PipelineState — Ship Chain artifact tracking spec.md → plan.md → build session → test results → ship log; persisted to .harness/pipeline/{pipeline_id}/",
    "LinearIssue — work queue entry with team/project IDs hardcoded (RestoreAssist team a8a52f07, Pi-Dev-Ops project f45212be); autonomy poller filters Urgent/High Todo issues and transitions them",
    "CronTrigger — scheduled job definition with type (scan/monitor/analyse_lessons/intel_refresh/board_meeting/fallback_dryrun/scout/feedback_loop), weekday/hour/minute, last_fired_at; stored in .harness/cron-triggers.json",
    "ScanResult — Pi-SEO finding container with scan_type, findings[], health_score per project; written to .harness/scan-results/{project-id}/{date}-{scan_type}.json",
    "MonitorDigest — portfolio health snapshot with regression detection, cross-repo systemic issues, weighted health score; written to .harness/monitor-digests/{YYYYMMDD-HHMM}.json with 30-file retention",
    "Lesson — knowledge base entry in .harness/lessons.jsonl with source, category, lesson text, severity; injected into session context via _get_lesson_context() per intent classification",
    "TelegramMessage — inbound message schema in .harness/telegram-inbox/{update_id}.json with route classification (fix/brief/idea/note), processed flag, received_at; drained by watchdog every 30 minutes"
  ]
}
```