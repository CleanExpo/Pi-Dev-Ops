```json
{
  "projectPurpose": "Pi CEO is a private Zero Touch Engineering platform that converts a GitHub repo URL and plain-English brief into an autonomous Claude Code execution session — running build pipelines, quality gates, and portfolio security scans without human intervention. It exists to let a non-technical solo founder (Phill) operate multiple software businesses as if he had a full engineering team on retainer.",
  "targetUsers": [
    "Phill McGurk — non-technical solo founder managing 3+ revenue-generating software portfolios (CCW-CRM, RestoreAssist, DR-NRPG) who explicitly states he is not a coding engineer",
    "Claude Desktop / Cowork users accessing analysis outputs via the MCP server",
    "Future: portfolio operators as a potential B2B SaaS product (Moonshot framing from board deliberation)"
  ],
  "businessLogic": "Core rules: (1) TAO tier routing — Opus for planning, Sonnet for generation/evaluation, Haiku for fast tasks — with cost optimisation via Claude Max zero-API-cost path. (2) Evaluator gate — 4-dimension scoring (completeness/correctness/conciseness/format) must meet threshold (default 7/10) before git push; below-threshold triggers retry up to TAO_EVALUATOR_MAX_RETRIES. (3) Autonomy poller polls Linear every 5min for Urgent/High Todo issues, transitions to In Progress, fires build sessions, and closes tickets on success — gated by TAO_AUTONOMY_ENABLED and requires LINEAR_API_KEY to arm. (4) Human-in-the-loop gates: watchdog NEVER auto-creates Linear issues from phone ideas, never merges branches, never rotates secrets, never amplifies its own scope (9 hardcoded prohibitions in ESCALATION.md). (5) Pi-SEO scanner runs 4x/day against 11 repos, triages findings into Linear by severity, and monitors portfolio health — gated by PI_SEO_ACTIVE=1. (6) ZTE scoring (currently 73/75) measures capability presence not execution quality — a known gap the board identified as the core measurement problem. (7) Permission pre-grant at 3 layers (settings.json bypassPermissions, ClaudeAgentOptions, --dangerously-skip-permissions flag) must be in place before any autonomous work begins.",
  "currentState": "alpha",
  "bigThree": {
    "model": "Tier routing is appropriate: Opus for decomposition, Sonnet for generation and evaluation, Haiku for discrete tasks. SDK-only path (RA-576) is the right call — subprocess fallback was a maintenance liability. The evaluator uses 4-dimension scoring which is well-structured but the threshold (7/10) is coarse; the confidence-weighted gate in config.yaml (auto_ship at 9.5/90%) is the right evolution. The 14B local model (Qwen 3 / Llama 3.3 8B via Ollama) proposed in the Enhanced board deliberation is correctly scoped as a triage classifier, not a decision engine — that framing is sound. Critical gap: triage accuracy of the local model has never been measured against Pi-CEO's specific classification logic.",
    "prompt": "The PITER classification + ADW workflow templates injected per intent is a strong prompt architecture. The evaluator output format contract (COMPLETENESS: X/10 — reason, parseable by regex) is well-specified. The per-session lesson injection (relevant entries from lessons.jsonl filtered by intent) is a correct RAG-lite pattern. Gaps: (1) The anthropic-docs files stored in .harness/ are raw HTML scrapes with thousands of tokens of JavaScript/CSS noise — the context benefit is near-zero and the token cost is real. (2) System prompts for the autonomy poller and scanner agents are not visible in the provided files — unclear whether they carry charter context or operate stateless. (3) The board meeting agent generates high-quality strategic output (evidenced by the deliberation files) but the prompt structure for those sessions is implicit, not documented.",
    "context": "The .harness/ directory is the right pattern for persistent context — lessons.jsonl, config.yaml, per-project charters, and cron state all compound correctly. The TURBOQUANT-ASSESSMENT.md correctly identifies that the real problem is relevance selection (RAG) not compression (KV quantisation). Critical failures: (1) The anthropic-docs/ files (.harness/anthropic-docs/*.md) contain raw Next.js SSR output — thousands of tokens of CSS variables, font-face declarations, and React serialisation noise with ~200 tokens of actual API content. This is the worst context hygiene failure in the system. (2) spec.md was 2-days stale at the time of the board meeting files — the system's self-model was running on stale data while claiming 73/75 ZTE. (3) The ZTE score itself measures capability presence (does the feature exist?) not operational health (is it working?) — the board correctly identified this as a vanity metric. (4) The overnight failure post-mortem shows the watchdog was verifying tests in an environment it didn't own — a classic context-domain mismatch."
  },
  "keyInsights": [
    "The system has a measurement integrity problem: ZTE 73/75 coexists with zero autonomous commits overnight, 10 urgent Linear tickets, and RestoreAssist down 76h+. The score measures feature existence not operational health — the board's BVI proposal (Business Velocity Index) is the correct correction but has not been implemented.",
    "The anthropic-docs stored in .harness/ are raw HTML scrapes (see .harness/anthropic-docs/*.md) containing thousands of tokens of CSS, JavaScript, and React serialisation noise. The actual API documentation content is <5% of each file. These files are injected into context but provide near-zero signal — this is a significant token waste on every board meeting cycle.",
    "The autonomy architecture has a topology flaw documented in the incident report: scheduled tasks run inside ephemeral Cowork sandboxes that die when the Mac sleeps. The Railway FastAPI server IS always-on but the job orchestration (watchdog, Pi-SEO scans, Telegram polling) was running in the wrong environment. This is not a bug — it is the correct expected output of the architecture as designed.",
    "Permission pre-grant at 3 layers (settings.json defaultMode bypassPermissions, ClaudeAgentOptions permission_mode, --dangerously-skip-permissions subprocess flag) means any compromised dependency or prompt injection has zero friction to execute arbitrary shell commands. The .claude/settings.json allows Bash(rm:*), Bash(curl:*), Bash(wget:*), Write(*) — the deny list blocks rm -rf / and sudo but not targeted file deletion or data exfiltration to allowed domains.",
    "The SDK migration (RA-576) removed the subprocess fallback entirely — TAO_USE_AGENT_SDK=0 now raises ImportError at startup. This is described as 'deliberate loud failure' but creates a hard dependency on claude_agent_sdk availability with no graceful degradation path. The quarterly fallback dry-run (RA-634) tests the Anthropic Python SDK path, not the claude CLI, suggesting two different fallback concepts are conflated in the risk register.",
    "The gap audit (board-meetings/2026-04-12-gap-audit.json) identifies that webhook.py has zero outbound Linear API calls despite the spec claiming bidirectional Linear sync is complete — 3 critical and 4 high discrepancies between spec claims and reality. The evaluator that scored these sessions apparently passed them despite the two-way sync being unimplemented.",
    "The ZTE score has been at or near ceiling (60/60 then 73/75) for multiple sprint cycles while the portfolio it manages (CCW: 1 critical + 196 high findings; RestoreAssist: 25 high findings; DR-NRPG: 2 critical + 130 high findings) shows no measurable improvement trend. The scoring system rewards building capabilities, not exercising them.",
    "The MCP server (mcp/pi-ceo-server.js v3.0.0) exposes list_harness_files, get_zte_score, and other tools but multiple board meetings document these tools returning stale or missing data (ZTE tool returning 'No ZTE data found', feature_list.json missing). The board meetings that generate strategic recommendations are running on broken instrumentation."
  ],
  "deploymentTopology": "Split deployment: (1) FastAPI backend on Railway (always-on, auto-deploys on main push, single worker, port 8080, Docker image with Node.js 20 + claude CLI installed) with in-memory session state and file-based persistence to app/logs/ — no database for sessions. (2) Next.js dashboard on Vercel (rootDirectory: dashboard, auto-deploy). (3) MCP server as stdio JSON-RPC process on Phill's local machine via Claude Desktop config. (4) Supabase (PostgreSQL) for observability tables (gate_checks, alert_escalations, heartbeat_log, triage_log, workflow_runs, claude_api_costs) — fire-and-forget writes. (5) Mac Mini M4 Pro running Ollama (Qwen 3 14B / Llama 3.3 8B) + n8n Docker + Open WebUI as the proposed always-on local intelligence layer (Phase 0-2 of Enhanced roadmap, approved but not yet deployed). Key constraint: session state is in-memory dict (_sessions) — Railway restart loses all running sessions. Max 3 concurrent sessions (TAO_MAX_SESSIONS). No horizontal scaling possible with current architecture.",
  "dataModels": [
    "BuildSession — core session entity: id, repo_url, brief, model, status (created/cloning/building/evaluating/complete/failed/killed/interrupted), output_lines[], evaluator_score, evaluator_status, last_completed_phase, linear_issue_id, scope, budget_minutes — persisted as JSON to app/logs/.sessions/{id}.json",
    "TaskSpec — decomposed work unit from Planner: description, context, expected_output, tier assignment, token budget",
    "LessonEntry — knowledge retention: source, category, lesson text, severity, timestamp — append-only JSONL at .harness/lessons.jsonl (38 entries as of incident date)",
    "ScanResult — Pi-SEO finding: project_id, scan_type, findings[], health_score, timestamp — stored as .harness/scan-results/{project}/{date}-{scan_type}.json",
    "MonitorDigest — portfolio health snapshot: project scores, regression deltas, systemic patterns, alert list — stored as .harness/monitor-digests/{YYYYMMDD-HHMM}.json, pruned at 30 files",
    "TelegramMessage — inbound mobile message: update_id, chat_id, text, processed, route, route_result — stored as .harness/telegram-inbox/{update_id:012}.json with .offset tracking",
    "CronTrigger — scheduled task definition: id, type (scan/monitor/analyse_lessons/intel_refresh/board_meeting/fallback_dryrun/scout/feedback_loop), timing fields, enabled, last_fired_at — stored in .harness/cron-triggers.json (16 active triggers)",
    "PipelineState — Ship Chain pipeline: pipeline_id, spec_md, plan_md, session_id, test_results, ship_log, phase checkpoints — persisted to .harness/pipeline/{id}/ directory",
    "ZTEScore — maturity assessment: 12 leverage points each 0-5, total /60 (old) or /75 (new), band classification — stored in .harness/leverage-audit.md and .harness/zte-v2-score.json"
  ]
}
```