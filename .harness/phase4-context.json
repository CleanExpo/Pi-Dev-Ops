```json
{
  "projectPurpose": "Pi CEO is a private solo DevOps tool that turns a GitHub repo URL and plain-English brief into an autonomous Claude Code execution session — cloning, analyzing via 8 structured phases, streaming live output to browser, and pushing results back to GitHub. It eliminates per-token API cost by running entirely within Claude Max subscription.",
  "targetUsers": [
    "Solo developer / founder (single user, private tool)",
    "Engineering team leads reviewing codebases via MCP in Claude Desktop",
    "Board/stakeholders consuming generated board notes and sprint plans"
  ],
  "businessLogic": "Dual execution modes: CLI (Claude Max, zero cost) via `claude -p` subprocess OR SDK (Anthropic API key, billed). 8 analysis phases run sequentially against fetched repo context: Inventory → Architecture → Code Quality → Context → Gap Analysis → Enhancement Plan → Executive Summary → Commit & Push. Each phase outputs structured JSON parsed into AnalysisResult. ZTE maturity scored 12–60 across 12 Leverage Points (bands: Manual/Assisted/Autonomous/ZTE). Session security enforced via HMAC-signed tokens, HttpOnly cookies, 30 req/min rate limit. Results persisted to `.harness/` on analysis branch, PR auto-created. MCP server exposes 6 tools to Claude Desktop reading from `.harness/`.",
  "currentState": "alpha",
  "keyInsights": [
    "The core intelligence deliberately bypasses Python orchestration — shells out to `claude -p` CLI to run inside Claude Max context (zero token cost). src/tao/ Python engine is stub scaffolding only, never called by the web server.",
    "Two separate UIs exist: app/static/index.html (minimal backend UI at :7777, handles build sessions) and dashboard/ Next.js app (rich analysis UI on Vercel). They are NOT connected — dashboard talks to GitHub directly, backend talks to local Claude CLI.",
    "Leverage audit baseline is 35/60 (Assisted band), just 1 point below Autonomous threshold. Weakest: Error Recovery, Session Continuity, Quality Gating, Trigger Automation, Knowledge Retention — all scored 2/5.",
    "Session state is purely in-memory (_sessions dict in sessions.py) — lost on restart. No persistence. This is the single highest-impact technical debt item (P1-A fix is a simple JSON file write).",
    "Rate limiter has an unbounded memory leak: _req_log in auth.py accumulates IP entries indefinitely. No background cleanup task exists.",
    "dashboard/.env.example only exposes NEXT_PUBLIC_API_URL but dashboard/README.md requires ANTHROPIC_API_KEY, GITHUB_TOKEN, TELEGRAM_BOT_TOKEN — the example is incomplete for onboarding.",
    "sessions.py line ~180 has a cosmetic bug: `if 'af' in dir()` should be `if 'af' in locals()` — `dir()` returns class attributes, not local variables. The file count in the summary block may silently fail.",
    "MCP server reads .harness/ files from local filesystem — it cannot access analysis results produced by Vercel dashboard (which writes to GitHub). There is a disconnect: dashboard → GitHub → .harness/ (not synced locally unless git pulled).",
    "next.config.ts CSP allows `unsafe-eval` in script-src which weakens XSS protection — inconsistent with the stated security posture.",
    "The `ceo-mode` skill exists in skills/ but is missing from the tao-skills master index in _deploy.py, creating a discrepancy in the 23-skill count documentation."
  ]
}
```