```json
{
  "projectPurpose": "Pi-CEO is a private agentic engineering platform that converts a GitHub repo URL and plain-English brief into an autonomous Claude Code execution session — cloning, building, evaluating, and pushing changes with zero human involvement. It exists to give a solo non-technical founder (Phill) a ZTE (Zero Touch Engineering) operating system that maintains, improves, and monitors a portfolio of 11 business repos 24/7.",
  "targetUsers": [
    "Phill McGurk — solo portfolio operator, non-engineer, managing CCW-CRM, RestoreAssist, and DR-NRPG repos with real paying users and compliance obligations",
    "Pi-CEO itself as an autonomous agent — the system is designed to operate on its own repos as first customer",
    "Future: portfolio operators running multiple client codebases who want autonomous maintenance without hiring engineering staff"
  ],
  "businessLogic": "The system encodes five interlocking rules: (1) sessions are tier-routed — Opus plans, Sonnet builds, Haiku does fast tasks — and model selection is automatic from config.yaml; (2) every build is gated by a 4-dimension evaluator (completeness/correctness/conciseness/format) with a configurable threshold, and the SDK-only path (RA-576) means there is no subprocess fallback — misconfiguration fails loudly at startup; (3) autonomy is armed only when both TAO_AUTONOMY_ENABLED=1 AND LINEAR_API_KEY are set — the /health endpoint exposes 'autonomy.armed' specifically to detect the silent-failure mode; (4) no autonomous action ever pushes to main, rotates secrets, modifies Railway env vars, or amplifies its own scope (ESCALATION.md rule 7) — human gates are enforced at promotion, merge, and deploy; (5) the Pi-SEO scanner runs 4x/day against 11 repos, triages findings into Linear automatically, and the portfolio health drives the sprint backlog — the system's own work queue is externally sourced from real business health data.",
  "currentState": "beta",
  "bigThree": {
    "model": "Model tier selection is appropriate: Opus for planning/decomposition where reasoning depth matters, Sonnet for generation and evaluation where speed-quality balance is needed, Haiku available for fast discrete tasks. The evaluator runs Sonnet in parallel (config.yaml 'parallel: true') which is correct for a quality gate. The critical gap: the SDK migration (RA-576) removed subprocess fallbacks entirely, making TAO_USE_AGENT_SDK=1 mandatory — a correct architectural decision that eliminated drift but creates a hard dependency on claude_agent_sdk stability. The local Qwen 3 14B plan for triage routing (BOARD-DELIBERATION-ENHANCED) is appropriate — classification/routing at 14B is proven, but tool-calling reliability on Ollama is flagged as an open research question in the harness.",
    "prompt": "Prompt structure is well-engineered: PITER classification (intent: feature/bug/chore/spike/hotfix) selects the right ADW workflow template before any generation begins; skills are injected per-intent via skills_for_intent(); lesson context from lessons.jsonl is injected dynamically. The evaluator output format is strictly contracted (COMPLETENESS/CORRECTNESS/CONCISENESS/FORMAT lines with exact regex) and deviations cause evaluator_status='error' — this is correct. The board meeting personas are sophisticated multi-perspective deliberation (9 board members with named dissent tracking) but this exists only in .harness docs, not yet wired into the autonomous pipeline as of Cycle 23. The main prompt weakness: spec.md was documented as stale (last updated 2026-04-10, cycle 23 reports it as a blocking gap via RA-675 and RA-685) — the primary context document the system uses to understand itself was not being kept current.",
    "context": "Context management is the system's most mature capability. Per-session: lessons.jsonl entries relevant to the session's intent are injected at brief time. Per-project: business charters (.harness/business-charters/) encode domain knowledge, security posture, and roadmap. The .harness/ tree is the explicit memory layer — config.yaml, leverage-audit.md, sprint_plan.md, feature_list.json all feed board meetings via MCP tools. Critical gap: the anthropic-docs/ directory stores raw HTML-scraped Next.js RSC payloads (thousands of lines of __next_f.push([1,...]) JavaScript) rather than parsed markdown — the fetched content is effectively noise and cannot be used as context. The .offset tracking in telegram-inbox/ and the 7-day GC policy show correct stateful context management for the async human loop. The harness watchdog correctly identified that verification logic must not run in the environment being verified — the signed test-status file pattern from ARCHITECTURE-V2 is the right solution, not yet implemented."
  },
  "keyInsights": [
    "The ZTE score (73-75/75 reported, earlier 60/60) is a vanity metric — it measures capability presence, not operational correctness. The board deliberation explicitly documents this: autonomy.py was dead, RestoreAssist was down 76h+, and the overnight marathon produced zero commits while the score showed 60/60. The CEO memo (2026-04-13) correctly identifies this and proposes replacing ZTE with Business Velocity Index.",
    "The overnight marathon failure (2026-04-12) was an architecture failure, not a bug: all scheduled tasks ran inside ephemeral Cowork sandboxes that died when the Mac slept, 7 commits were unpushed so Railway ran stale code, and the watchdog false-positive (CRITICAL: tests red) was caused by missing 'anthropic>=0.90' in the sandbox — the watchdog ran pytest in an environment it didn't own. The self-referential trap: the monitoring system verified itself.",
    "The .harness/anthropic-docs/ files are storing raw Next.js RSC streaming responses (self.__next_f.push([1,...]) arrays with CSS, font-face declarations, and React hydration payloads) — not usable documentation. The intel_refresh cron (RA-635) is fetching the wrong artifact. This means the 'Anthropic Intelligence' context injection is providing noise rather than signal.",
    "There are two Dockerfiles: root-level Dockerfile (sets WORKDIR /pi-ceo, copies src/, .harness/, skills/) and app/Dockerfile (sets WORKDIR /app, does NOT copy skills or harness). The app/Dockerfile is the older version used for Railway before the root Dockerfile superseded it. This creates a deployment ambiguity: railway.toml likely references one, but both exist in the repo.",
    "The MCP server (mcp/pi-ceo-server.js version 3.0.0) has 21 tools including Linear issue creation and harness file reads, but the board meeting minutes consistently show 'LINEAR UNAVAILABLE (401)' across multiple cycles. The MCP credentials are in Claude Desktop config (%APPDATA%\\Claude\\claude_desktop_config.json), not in Railway env — meaning the MCP tools only work when Claude Desktop is running on Phill's Mac, not from the autonomous Railway-hosted pipeline.",
    "The app/requirements.txt includes 'claude-agent-sdk>=0.1' but pyproject.toml does not — meaning the Railway Docker build (which uses requirements.txt) has the SDK, but any pure pyproject.toml install path does not. This asymmetry could cause environment drift.",
    "The system's ESCALATION.md rule 7 explicitly prohibits autonomous creation of scheduled tasks that amplify the agent's own scope — but the autonomy poller (autonomy.py) does exactly this at the Linear ticket level: it picks up Todo issues and fires build sessions without human approval per-ticket. The guardrail is at the merge level (PRs, no direct push), not at the execution level.",
    "The 'do-while poller pattern' fix (10-second startup delay replacing sleep-first 5-minute delay) was committed as commit e611b1c but never pushed to origin/main — this was the root cause of the Railway poller never firing on the night of 2026-04-11. The fix existed in code but Railway was running the pre-fix version.",
    "Supabase is used in two incompatible ways: @supabase/ssr in the Next.js dashboard (client-side, anon key) and supabase_log.py in the FastAPI backend (service role key, fire-and-forget writes). The .env.example has NEXT_PUBLIC_SUPABASE_URL defined twice with different placeholder values, which would cause confusion in Vercel environment setup."
  ],
  "deploymentTopology": "Split deployment: FastAPI backend on Railway (single worker, port 8080, Docker container from root Dockerfile) with always-on autonomy poller as asyncio background task; Next.js dashboard on Vercel (rootDirectory: 'dashboard', builds from dashboard/ subdirectory); MCP server runs locally via Node.js stdio on Phill's Mac (Claude Desktop integration only). Railway handles GitHub/Linear webhooks, cron dispatch via internal cron_loop, Pi-SEO scans, and session execution. The backend is intentionally single-worker (uvicorn --workers 1) to avoid session state conflicts since _sessions is an in-memory dict. Workspaces are ephemeral on Railway (no persistent volume configured in visible files). Supabase PostgreSQL is the only persistent state store beyond the filesystem. The system has no load balancing, no horizontal scaling path, and no persistent workspace volume — by design for a solo operator.",
  "dataModels": [
    "BuildSession — core execution unit: session ID, repo_url, brief, status (created/cloning/building/evaluating/complete/failed/killed/interrupted), output_lines list, evaluator_score float, last_completed_phase string for resume, linear_issue_id for two-way sync, budget_minutes for TAO_AUTONOMY_BUDGET",
    "Linear Issue — external source of truth for work queue: ticket ID (RA-NNN), priority (Urgent/High/Normal), status (Todo/In Progress/Done), team ID, optional repo: label for webhook routing",
    "BuildSession persistence JSON — written atomically via os.replace() to app/logs/.sessions/{sid}.json after every state change, loaded by restore_sessions() on startup",
    "lessons.jsonl — append-only JSONL of evaluator learnings: source, category, lesson text, severity, timestamp — injected into briefs via _get_lesson_context()",
    "Pi-SEO ScanResult — per-project scan output: scan_type (security/code_quality/dependencies/deployment_health), findings list with severity, health_score 0-100, written to .harness/scan-results/{project-id}/{date}-{scan_type}.json",
    "MonitorDigest — portfolio health snapshot: project health scores, regression deltas, systemic cross-repo patterns, written to .harness/monitor-digests/{YYYYMMDD-HHMM}.json, pruned after 30 days",
    "Telegram inbox message — .harness/telegram-inbox/{update_id}.json: update_id, text, processed bool, route string (fix/brief/idea/note), received_at, processed_at",
    "PipelineState — Ship Chain artifact: pipeline_id, spec_md path, plan_md path, session_id, ship_log dict with shipped bool — persisted to .harness/pipeline/{pipeline_id}/",
    "Supabase gate_checks table — quality gate results per /ship phase: pipeline_id, score, passed bool, timestamp",
    "Supabase alert_escalations table — critical alert tracking: alert_key, acked bool, escalation_count, last_paged_at"
  ]
}
```