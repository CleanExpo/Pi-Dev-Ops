```json
{
  "projectPurpose": "Pi CEO is a Zero Touch Engineering (ZTE) platform for a solo technical founder that converts GitHub repo URLs and plain-English briefs into autonomous Claude Code execution sessions — cloning, implementing, evaluating, and pushing changes while managing a portfolio of 11 business repos. It exists to eliminate the founder as a coding bottleneck across multiple businesses simultaneously.",
  "targetUsers": [
    "Phill (sole user) — non-coder founder operating 3+ businesses (CCW-CRM, RestoreAssist, DR-NRPG) who needs autonomous overnight code maintenance without engineering staff",
    "Claude Desktop / Cowork sessions — MCP consumers that read analysis outputs and trigger builds via 21 exposed tools"
  ],
  "businessLogic": "The system encodes a tiered autonomy contract: Opus plans, Sonnet builds, Sonnet evaluates — each session gated by a 4-dimension evaluator score threshold (default 7/10) before any git push. The Linear poller (5-min Railway cron) is the primary trigger: Todo tickets become sessions, sessions become PRs, PRs close tickets. A hard escalation contract governs what the agent may never do autonomously (rotate secrets, force-push, create scheduled tasks, merge to main). The ZTE score (73/75 at Sprint 8) measures capability existence, not execution health — a known gap the system is actively correcting by introducing a Business Velocity Index. Human-in-the-loop is preserved at exactly two points: PR merge approval, and Linear idea promotion from the Telegram inbox.",
  "currentState": "beta",
  "bigThree": {
    "model": "Tiered model selection (Opus 4.6 planner, Sonnet 4.6 generator/evaluator, Haiku 4.5 for fast tasks) is appropriate and well-reasoned. The local Qwen 3 14B routing layer (in planning via RA-637–652) is architecturally sound for triage at Q4_K_M on 24GB unified memory. Primary risk: the evaluator grades code quality (completeness/correctness/conciseness/format) but cannot assess commercial intent-drift — a gap explicitly named in ARCHITECTURE-V2.md and the board deliberation. No alternative model is better suited; the gap is structural, not model-selection.",
    "prompt": "Prompts are structured via PITER classification → ADW template selection → skill injection from 28 SKILL.md files — a layered, methodical approach. The evaluator output format is strictly contracted (.harness/contracts/eval-contract.md) and machine-parseable. The board meeting prompt pipeline (6-phase: STATUS→LINEAR→SWOT→RECOMMENDATIONS→MINUTES→GAP AUDIT) is well-decomposed. Key weakness: the generator brief is augmented by lessons.jsonl context injection but the lessons themselves have no quality gate — low-signal lessons dilute the context. The Telegram inbox route classification (fix/brief/idea/note) is under-specified for ambiguous inputs and falls back to 'note' silently.",
    "context": "Context surfacing is the system's most mature capability: per-session lesson injection filtered by intent (skills_for_intent()), per-project charter memory, CLAUDE.md house rules, and a RAG memory architecture planned for Sprint 10. Critical noise problem identified: .harness/anthropic-docs/ files contain raw Next.js hydration JavaScript (self.__next_f.push([...]) blobs) interleaved with the actual API documentation — these files are stored as scraped HTML-to-markdown artifacts and will pollute any context window they enter. The triage-cache.json at 353KB with no rotation is a compounding context quality risk. The ZTE score reported as 60/60 or 73/75 inconsistently across board meeting files indicates the self-assessment context is unreliable — confirmed by RA-675 (stale spec.md) and the CEO memo noting 'Pi-CEO's self-assessment cannot be trusted.'"
  },
  "keyInsights": [
    "The system achieved ZTE 73/75 (capability score) while the portfolio health remained flat at ~62/100 with zero improvement trend across 23 cycles — the metric was optimising for the wrong thing. The CEO memo from 2026-04-13 explicitly introduces a Business Velocity Index to replace ZTE as the primary metric.",
    "The first overnight autonomous run (2026-04-11→12) produced zero commits because 7 commits sat unpushed on local main — the Cowork sandbox has no git push credentials. Every 'autonomous' scheduled task was actually conditional on Phill's Mac being awake and his laptop having GitHub auth. This is documented in INCIDENT-2026-04-12.md as the root cause.",
    "The anthropic-docs/ files in .harness/ are raw browser-rendered HTML scraped and stored as markdown — they contain thousands of lines of JavaScript (self.__next_f.push, CSS variable declarations, font-face rules) rather than clean documentation text. Any session that ingests these files gets noise-dominated context.",
    "The ZTE score appears as both 60/60 and 73/75 in different board meeting files — the 60/60 is the old 12-point leverage audit ceiling, and 73/75 is the newer expanded scoring from Sprint 8. The MCP tool get_zte_score was returning 'No ZTE data found' for multiple consecutive board cycles, meaning board meetings were running blind on health metrics.",
    "The SDK migration (RA-576) removed the subprocess fallback entirely — TAO_USE_AGENT_SDK=0 now raises ImportError at startup. This is a deliberate loud failure design, but it means the quarterly fallback dryrun (fallback-dryrun-quarterly cron trigger) tests a different code path (direct Anthropic SDK, not claude CLI) rather than being a true rollback option.",
    "The Pi-SEO scanner infrastructure is fully built (scanner, triage engine, 3 skill layers, 10 repos registered, cron rotation, auto-PR) but has never been activated against real repos — zero baseline scan data exists despite being listed as complete since Sprint 6. The scanner found findings in dry-runs but the portfolio health numbers in charters (CCW: 1 critical + 196 high, RestoreAssist: 25 high, DR-NRPG: 766 high + 2 critical) are from a single 2026-04-10 scan, not continuous monitoring.",
    "The Managed Agents API PoC (MANAGED_AGENTS_v4_FINAL.md) failed 0/14 cycles due to a 400 schema rejection on config.setup_commands — the migration to claude_agent_sdk was the correct decision, but the PoC evidence files still exist in .harness/poc-metrics/ suggesting cleanup was deferred.",
    "session persistence uses write-to-.tmp-then-os.replace() (CLAUDE.md line) but _sessions is in-memory — a Railway redeploy loses all running session state. The last_fired_at timestamps in cron-triggers.json also reset on redeploy, causing the bogus-future-timestamp debounce bug that silently suppresses scans after every deploy."
  ],
  "deploymentTopology": "Split across three surfaces: (1) Railway — always-on FastAPI backend (app/server/main.py, uvicorn single worker, port 8080), autonomy poller as asyncio background task, 12 cron triggers via cron.py, Supabase writes for observability. (2) Vercel — Next.js 16 dashboard (dashboard/), static + SSR, connects to Railway backend via NEXT_PUBLIC_API_URL. (3) Local Mac — MCP server (mcp/pi-ceo-server.js) via stdio JSON-RPC for Claude Desktop integration, Telegram inbox polling scripts, marathon watchdog (Cowork-dependent, not truly always-on). Docker image installs Node.js + @anthropic-ai/claude-code CLI alongside Python. No horizontal scaling — TAO_MAX_SESSIONS=3 concurrent sessions, single Railway dyno. Supabase (zbryrmxmgfmslqzizsto.supabase.co) as the persistence layer for gate_checks, alert_escalations, heartbeat_log, triage_log, workflow_runs, claude_api_costs.",
  "dataModels": [
    "BuildSession — core session entity with id, repo_url, brief, model, status (created/cloning/building/evaluating/complete/failed/killed/interrupted), output_lines[], evaluator_score, last_completed_phase, linear_issue_id, budget_minutes, scope contract",
    "CronTrigger — scheduled job definition with id, type (scan/monitor/analyse_lessons/intel_refresh/board_meeting/fallback_dryrun/scout/feedback_loop), priority_filter, hour/minute/weekday, last_fired_at — persisted to .harness/cron-triggers.json",
    "ScanResult — Pi-SEO finding container with scan_type, findings[], health_score — written to .harness/scan-results/{project-id}/{date}-{scan_type}.json",
    "MonitorDigest — portfolio health snapshot with regression detection, cross-repo patterns, weighted health score — written to .harness/monitor-digests/{YYYYMMDD-HHMM}.json",
    "TelegramInboxMessage — inbound mobile command with update_id, text, route (fix/brief/idea/note), route_result — written to .harness/telegram-inbox/{update_id}.json",
    "Lesson — engineering learning entry with source, category, lesson, severity — appended to .harness/lessons.jsonl, injected into sessions via skills_for_intent()",
    "PipelineState — Ship Chain artifact with pipeline_id, spec.md, plan.md, test results, ship_log — persisted to .harness/pipeline/{pipeline_id}/",
    "SDKMetrics — per-invocation SDK telemetry with success, latency, tokens, cost_usd — written to .harness/agent-sdk-metrics/YYYY-MM-DD.jsonl"
  ]
}
```