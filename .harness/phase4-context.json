```json
{
  "projectPurpose": "Pi CEO is a private autonomous engineering platform that converts a GitHub repo URL and plain-English brief into a fully executed Claude Code session — cloning, building, evaluating, and pushing code changes — with zero API cost via Claude Max subscription. It acts as a 'Second in Charge' for a solo founder's software portfolio.",
  "targetUsers": [
    "Phill (founder/sole user — non-technical, operates via Telegram and Linear)",
    "Portfolio businesses: CCW-CRM, RestoreAssist, DR-NRPG (downstream beneficiaries)"
  ],
  "businessLogic": "TAO (Tiered Agent Orchestrator) model: Opus plans, Sonnet builds, Sonnet evaluates via a BLOCKING quality gate (threshold 7/10, configurable). Sessions are triggered by GitHub webhooks, Linear webhooks, cron-triggers.json (16 entries), Telegram commands, or manual API calls. The autonomy poller (`app/server/autonomy.py`) polls Linear every 5 min for Urgent/High Todo tickets and auto-creates build sessions. SDK-only execution path as of Sprint 8 (RA-576) — subprocess fallback removed, TAO_USE_AGENT_SDK=0 raises ImportError at startup. Pi-SEO scanner monitors 10+ repos on 4x-daily cron, triage engine auto-creates Linear tickets from critical/high findings. All Supabase writes are fire-and-forget via `supabase_log.py`. Human-in-the-loop enforced on: PR merges, secret rotation, Linear issue creation from Telegram ideas, and any action on the 9-item NEVER-DO list in ESCALATION.md.",
  "currentState": "alpha",
  "keyInsights": [
    "ZTE score is self-reported at 73/75 but the Cycle 23 CEO Memo explicitly states this is a measurement problem — the system optimises for capability existence (ZTE) not capability effectiveness (Business Velocity Index). Portfolio health stuck at 62/100 with zero improvement trend.",
    "Critical topology failure documented in INCIDENT-2026-04-12.md: scheduled tasks ran inside ephemeral Cowork sandboxes that died when the Mac slept. Railway is the only truly always-on component. 7 commits sat unpushed, Railway ran stale code, zero Linear tickets closed overnight.",
    "SDK-only architecture is declared complete (RA-576) but the subprocess fallback removal is ONLY in sessions.py — board_meeting.py:241 and pipeline.py still have subprocess calls that must splice CLAUDE_EXTRA_FLAGS per PERMISSIONS.md.",
    "Main.py already implements the SSE streaming endpoint (`GET /api/sessions/{sid}/logs`) that the gap audit (2026-04-12-gap-audit.json) flagged as missing — the gap audit used truncated source files and filed false critical tickets (RA-665, RA-666, RA-667).",
    "MCP server (mcp/pi-ceo-server.js v3.0.0) exposes 21 tools including Linear CRUD, but the Linear API key has returned 401 errors across multiple consecutive board meeting cycles — autonomous triage is silently broken.",
    "The `triage-cache.json` file is 353KB with no rotation strategy, flagged across 3+ board meeting cycles with no action — compounding disk/startup risk on Railway ephemeral filesystem.",
    "Autonomy poller had a sleep-first bootstrap bug (5-min cold-start dead zone) documented in lessons and commit e611b1c — fix exists but was in unpushed commits at time of incident.",
    "Per-project memory layer (RAG over per-project markdown context) is architecturally scoped in TURBOQUANT-ASSESSMENT.md but not implemented — every build session starts without business-specific context for CCW/RestoreAssist/DR-NRPG.",
    "Security posture: CCW has 1 critical + 196 high findings, DR-NRPG has 2 critical + 4 critical npm CVEs — Pi-CEO Standard declares these blocking gates before feature work, but no autonomous session has yet touched either repo.",
    "Two Dockerfiles exist (`Dockerfile` at root and `app/Dockerfile`) with divergent WORKDIR and CMD configurations — root Dockerfile is Railway-targeted, app/Dockerfile is standalone, creating potential confusion on deploy path."
  ]
}
```