```json
{
  "projectPurpose": "Pi-CEO is a solo-founder DevOps automation platform that converts GitHub repo URLs and plain-English briefs into autonomous Claude Code execution sessions, closing the loop from Linear ticket → code generation → evaluation → PR — targeting a single non-technical founder (Phill) who operates a portfolio of 11 Australian SaaS/restoration-industry projects.",
  "targetUsers": [
    "Phill (sole founder, Unite Group) — non-technical portfolio operator running CCW-CRM, RestoreAssist, DR-NRPG and 8 other repos who needs overnight autonomous engineering without coding himself",
    "Pi-CEO autonomous agents — the system is its own primary user via the Linear autonomy poller, board meeting crons, and Pi-SEO scanner loops"
  ],
  "businessLogic": "The system encodes five interlocking business rules: (1) TAO tier routing — Opus plans, Sonnet builds, Haiku drafts; tier selection is cost-gated via BudgetTracker anchors in config.yaml; (2) evaluator gate — every generated diff is scored on Completeness/Correctness/Conciseness/Format; OVERALL < threshold logs a warning but does not block ship (non-blocking Phase 1), retry loop planned for Phase 2; (3) autonomy kill switches — TAO_AUTONOMY_ENABLED=0 stops Linear polling, TAO_USE_FALLBACK=1 routes to direct Anthropic SDK, PI_SEO_ACTIVE=0 halts scan crons; all are one env-var flips; (4) human-in-the-loop escalation contract (ESCALATION.md) — the watchdog self-heals when safe, escalates exactly one Telegram message with a strict envelope format when human judgment is required, and never autonomously executes the nine prohibited actions (secret rotation, force-push, Linear task creation that amplifies agent scope, etc.); (5) ZTE scoring as the internal quality metric — 75-point leverage audit drives sprint prioritisation, but the system is actively migrating to a Business Velocity Index (BVI) based on portfolio health deltas after recognising that ZTE measures capability existence, not business outcome delivery.",
  "currentState": "alpha",
  "bigThree": {
    "model": "The three-tier model selection (Opus/Sonnet/Haiku) is appropriate for cost/quality routing and is correctly implemented. The evaluator uses Sonnet for structured scoring — correct choice. The local-model layer (Qwen 3 14B on Mac Mini) is approved but undeployed; routing logic between local triage and cloud frontier is unbuilt. Critical gap: the evaluator gate is explicitly non-blocking ('below-threshold score logs warning but does not block git push — for now' in eval-contract.md line 23); this means the quality gate is an observer, not a gate, directly contradicting the harness claim of 'BLOCKING evaluator gate'. The SDK-only migration (RA-576) is complete in config but the canary rollout (RA-574) has never been activated — 0% of production traffic touches the SDK path across 23 board cycles.",
    "prompt": "Agent specifications in .harness/agents/ are well-structured with explicit input/output contracts, timeout constraints, and output format requirements. ESCALATION.md encodes the envelope format for Telegram with exact field ordering — good. CLAUDE.md is unusually detailed and functional. Key weakness: the board meeting prompts don't include the BVI baseline metric yet (explicitly deferred to Cycle 24), so every board meeting for 23 cycles has reported ZTE 60-73/75 while portfolio health stagnated at 62/100. The Pi-CEO Standard (PI-CEO-STANDARD.md) encodes precise exit criteria per pillar — this is the most operationally useful prompt artifact in the repo. The anthropic-docs files stored in .harness/anthropic-docs/ are raw Next.js server-rendered HTML with thousands of lines of CSS and JS bundle references — completely unprocessable by the agent as-is; the fetch pipeline stores noise, not signal.",
    "context": "The harness architecture (.harness/ directory as the agent's persistent memory) is the system's strongest design decision — board minutes, scan results, lessons.jsonl, and charters all serve as durable context across sessions. Critical failure: the overnight incident (INCIDENT-2026-04-12.md) revealed that 7 commits sat unpushed for 9+ hours because the Cowork sandbox has no push credentials and the planned Railway poller never received the fix — the context the agent needed (that push credentials don't exist in the sandbox) was not surfaced at task-start. The .harness/anthropic-docs/ fetched documentation is stored as raw HTML (confirmed in the actual files above) rather than extracted markdown — this means the intelligence refresh cron (intel-refresh-monday) writes gigabytes of JavaScript bundle noise that cannot be usefully injected into agent context. lessons.jsonl at 38 entries is the system's best compounding asset but has no retrieval layer — lessons are injected by intent category but there is no semantic search against them."
  },
  "keyInsights": [
    "The system reports ZTE 73/75 ('Zero Touch Band') across 23 board cycles while the portfolio health sits at 62/100 with zero improvement trend — the gap audit (board-minutes 2026-04-12) filed 7 Linear tickets (RA-665 to RA-671) for critical/high discrepancies between spec claims and code reality, including the Linear two-way sync being entirely unimplemented despite being listed as Complete in spec.md",
    "The SDK canary (TAO_USE_AGENT_SDK_CANARY_RATE=0.10) has been Priority 1 in every board meeting for 3+ consecutive cycles and has never been set — the Railway env var has not been changed; this means 100% of production traffic still runs through the subprocess path that RA-576 was supposed to eliminate, making the 'SDK-only' claim in CLAUDE.md false",
    "The .harness/anthropic-docs/ intelligence refresh stores raw Next.js server-rendered HTML (confirmed by reading the actual file contents above) — the files contain thousands of lines of CSS variables, font-face declarations, and JS bundle references with ~200 words of actual API documentation buried inside; the intel_refresh cron is writing noise, not intelligence",
    "The overnight autonomy failure root cause was not a bug — it was a topology mismatch: 'autonomous' was implemented as Cowork scheduled tasks that only run when Phill's Mac is open, while the only truly always-on component (Railway FastAPI) never received the fix commits because the sandbox had no push credentials; the V2 architecture document exists but the Day 1 action (git push origin main) was still pending as of the board meeting",
    "The evaluator gate is non-blocking by design ('below-threshold score logs warning but does not block git push — for now' in eval-contract.md) yet CLAUDE.md and every board meeting report it as a 'BLOCKING gate' — the ZTE Leverage Point 8 (Quality Gating) scored 5/5 is based on a claim that contradicts the actual implementation",
    "triage-cache.json at 353KB with no pruning strategy has been flagged in every board meeting for multiple cycles without any ticket being created to address it — it is the canonical example of the system identifying its own technical debt without acting on it",
    "The gap audit (2026-04-12-gap-audit.json) found that the SSE log streaming endpoint documented in spec.md does not exist in main.py — the only real-time delivery mechanism is WebSocket at /ws/build/{sid}; this is a silent spec lie that would cause frontend integration failures",
    "The business-charters directory was created during a marathon session on 2026-04-11 that produced zero commits — the charters exist only in the Cowork sandbox and have never been pushed to origin/main; they represent work that cannot be seen by Railway or any other always-on component"
  ],
  "deploymentTopology": "Split topology: FastAPI backend (Python 3.12, uvicorn, single worker) deployed to Railway on port 8080, auto-detected via RAILWAY_ENVIRONMENT env var; Next.js 16 dashboard deployed to Vercel with rootDirectory=dashboard; MCP server (Node.js stdio JSON-RPC) runs locally on Phill's Mac via Claude Desktop config. Railway is the only truly always-on component. The autonomy poller (linear_todo_poller), Pi-SEO scan crons (cron_loop), and escalation watchdog all run as asyncio background tasks inside the single Railway FastAPI process — no separate workers, no queue. Sessions use ephemeral workspace directories (app/workspaces/) that are non-persistent across Railway restarts. Supabase (PostgreSQL + pgvector) provides the only durable cross-restart storage for gate_checks, alert_escalations, and heartbeat_log. The Mac Mini Enhanced architecture (Ollama + n8n + Supabase local) is approved in board deliberation but has not been deployed — RA-637 through RA-652 are open tickets.",
  "dataModels": [
    "BuildSession — in-memory Python dataclass tracking session lifecycle (created/cloning/building/evaluating/complete/failed/killed/interrupted), output_lines[], evaluator_score, last_completed_phase for resume; persisted to app/logs/.sessions/{sid}.json via atomic os.replace() writes",
    "PipelineState — dataclass for the Ship Chain 6-phase pipeline (spec→plan→build→test→review→ship), persisted to .harness/pipeline/{pipeline_id}/ as individual phase artifact files",
    "LinearIssue — implicit model derived from Linear GraphQL API; issues drive the autonomy poller via todo_issues query filtered by Urgent/High priority; no ORM, raw HTTP",
    "ScanResult — Pi-SEO scanner output per project per scan_type, written to .harness/scan-results/{project-id}/{date}-{scan_type}.json; consumed by MonitorDigest and TriageEngine",
    "MonitorDigest — JSON snapshot of portfolio health delta, regression detection, systemic cross-repo patterns; written to .harness/monitor-digests/{YYYYMMDD-HHMM}.json; pruned to 30 files max",
    "Lesson — JSONL entry in .harness/lessons.jsonl with source/category/severity/lesson fields; 38 entries as of last board meeting; injected into build sessions by intent category via _get_lesson_context()",
    "TelegramInboxMessage — JSON file per message in .harness/telegram-inbox/{update_id:012}.json with processed/route/route_result fields; offset tracked in .harness/telegram-inbox/.offset",
    "CronTrigger — JSON entry in .harness/cron-triggers.json with type/hour/minute/weekday/last_fired_at; 16 active triggers; loaded at Railway startup by cron_loop background task"
  ]
}
```