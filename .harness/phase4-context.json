```json
{
  "projectPurpose": "Pi-CEO is a private agentic engineering platform that converts a GitHub repo URL and plain-English brief into an autonomous Claude Code execution session — cloning, building, evaluating, and pushing results — while serving as the 'Second in Charge' for a portfolio of 11 businesses owned by one non-technical founder (Phill).",
  "targetUsers": [
    "Phill McGurk — solo non-technical founder managing 3-4 active businesses (CCW-CRM, RestoreAssist, DR-NRPG), who wants zero-touch overnight engineering without reading code",
    "Claude Desktop / Cowork sessions — acting as the MCP client for 21 tools that surface analysis results, sprint plans, and ZTE scores",
    "Future: portfolio operators running multiple non-technical businesses who want autonomous code quality maintenance"
  ],
  "businessLogic": "The system encodes a Zero Touch Engineering (ZTE) scoring model (75-point scale across 12 leverage dimensions) as the primary quality gate. Business rules: (1) Linear tickets in 'Todo' state with Urgent/High priority auto-trigger build sessions via the autonomy poller every 5 minutes — this is the core revenue-creation loop; (2) the evaluator gate (default threshold 7/10) must pass before any git push — quality is enforced structurally not optionally; (3) human-in-the-loop is preserved for merges (no auto-merge to main) and for idea promotion from Telegram inbox to Linear — rule 7 in ESCALATION.md explicitly blocks autonomous scope amplification; (4) Pi-SEO scans 11 repos 4x/day against the Pi-CEO Standard (7 pillars: security/quality/dependencies/deployment/observability/docs/autonomy-ready) and auto-files Linear issues for regressions; (5) the watchdog escalates to founder via Telegram using a strict envelope format — glance-decide-reply from a phone lock screen is the UX contract.",
  "currentState": "beta",
  "bigThree": {
    "model": "The 3-tier model (Opus 4.6 for planning, Sonnet 4.6 for generation/evaluation, Haiku 4.5 for fast tasks) is appropriate and cost-optimised for Claude Max subscription (zero API cost). The evaluator uses 4-dimension structured scoring which is correctly structured for a grading task. However, the ZTE self-improvement loop (lesson-pattern analyser) is unbuilt — lessons accumulate in lessons.jsonl but no model extracts actionable patterns back into CLAUDE.md or skills. The Pi-SEO scanner uses pattern matching rather than semantic analysis, which produces enormous false-positive counts (5583 print() findings in CCW) that degrade signal quality. Better alternative: a lightweight semantic classifier on top of pattern hits to separate noise from real risk.",
    "prompt": "The skills system (28 SKILL.md files across 4 layers) is the most sophisticated prompt engineering in the codebase — encoding methodology as reusable context rather than ad-hoc instructions. PITER classification and ADW workflow templates are well-structured. The evaluator prompt (4 dimensions with strict output format) is correctly specified. Critical gap: the .harness/anthropic-docs/ files are stored as raw scraped HTML with massive CSS/JS noise (the files in context are 95% JavaScript Next.js hydration payloads, not actual documentation content) — this pollutes every session that reads them. The per-project charter system (PI-CEO-STANDARD.md + project charters) is excellent structured context but is only manually maintained, not automatically injected into relevant sessions.",
    "context": "The context architecture has a fundamental topology problem documented in the incident report: the right context (lessons, charters, scan results) lives in .harness/ but the system that needs it (Railway autonomy poller) can't reliably access it because commits aren't pushed. The lessons injection via _get_lesson_context() is sound in principle but the lessons.jsonl has only 18-38 entries — too sparse for meaningful retrieval. The Telegram bidirectional loop is correctly designed with human-gated idea promotion. The biggest context gap: no per-project RAG memory layer — every session starts cold without business context for the repo it's working on. The TURBOQUANT-ASSESSMENT.md correctly identifies this and proposes a memory/ folder + retrieval step, but it hasn't been built."
  },
  "keyInsights": [
    "The ZTE score (73/75 reported) is a vanity metric as noted in the board deliberations — it measures capability existence, not capability execution. The system scored 73/75 while delivering zero overnight commits, which proves the metric is measuring the wrong thing. The Business Velocity Index (BVI) proposed in the Cycle 23 CEO memo is the correct metric but hasn't been implemented.",
    "The autonomy architecture has a fundamental single-point-of-failure that recurs: the self-start bootstrap delay in linear_todo_poller means Railway needs 5+ minutes after deploy before it polls Linear. Commit e611b1c fixes this to 10 seconds but was unpushed for multiple sprints — the fix existing in the codebase and not being deployed is a pattern that will repeat without a deploy-from-tested-artifact discipline.",
    "The .harness/anthropic-docs/ files (api-reference.md, claude-code-cli-reference.md, etc.) contain almost no actual documentation — they're raw scraped HTML with thousands of lines of CSS variables, font-face declarations, and Next.js hydration JavaScript. Any session reading these files is consuming ~95% noise. This corrupts the intel-refresh value proposition entirely.",
    "The cron-triggers.json uses Unix timestamps for last_fired_at but the timestamps (e.g. 1775952090) correspond to year 2026 in Unix time — these are plausible, but the scheduler has a documented bug where bogus-future timestamps in last_fired_at suppress the next scheduled scan window on Railway redeploy, silently preventing all scans.",
    "The gap audit (board-meetings/2026-04-12-gap-audit.json) identified that Linear two-way sync (writing build outcomes back to Linear tickets) is not implemented despite being claimed in the spec — sessions build and push but never close the Linear ticket autonomously. This means the autonomy poller's core loop (Todo → In Progress → Done) only works for the first transition.",
    "The .claude/settings.json uses defaultMode: bypassPermissions which grants the agent unlimited file system access including rm, mv, cp on all paths. Combined with the allowlist including rm:*, this means a malicious or confused session could delete arbitrary files with no sandbox boundary. The deny list blocks sudo and rm -rf / but not targeted file deletion.",
    "The Supabase integration (RA-651, RA-633) uses fire-and-forget writes ('observability failures must never block the build pipeline') — this is correct for non-critical metrics but means the gate_checks table (quality gate results) can silently fail to record, making the audit trail unreliable for debugging failed sessions.",
    "Two Dockerfiles exist (root Dockerfile and app/Dockerfile) with different working directories, different source paths, and different CMD instructions — the root one copies src/ (TAO engine) but the app/Dockerfile doesn't. This creates a divergence between local development builds and Railway production builds that will cause silent failures when TAO engine features are used."
  ],
  "deploymentTopology": "Split deployment: FastAPI backend on Railway (always-on, auto-deploys on main push, 1 uvicorn worker, port 8080, Docker container with Node.js + claude CLI installed); Next.js dashboard on Vercel (serverless, rootDirectory: dashboard); MCP server runs locally via stdio on Claude Desktop (node mcp/pi-ceo-server.js). Backend has WebSocket streaming for live build output, SSE endpoint for log replay, and background tasks for GC, cron, and autonomy polling. Supabase (PostgreSQL + pgvector) for observability tables. No persistent volumes — workspaces are ephemeral on Railway. Session state persisted to disk JSON with atomic os.replace() writes. Max 3 concurrent sessions. Dependencies: Linear API (ticket management), GitHub API (webhooks + push), Telegram Bot API (bidirectional comms), Anthropic API (fallback path only — primary is claude CLI).",
  "dataModels": [
    "BuildSession — core session object with id, repo_url, brief, model, status lifecycle, output_lines[], evaluator_score, phase checkpoints, linear_issue_id; persisted as JSON to app/logs/sessions/{sid}.json",
    "LinearIssue — ticket entity driving the autonomy loop; Todo+Urgent/High issues are the trigger for build sessions; the system reads but doesn't reliably write back (gap audit finding)",
    "lessons.jsonl — append-only JSONL knowledge base with source, category, lesson, severity fields; injected into sessions via _get_lesson_context() by intent classification",
    "ScanResult — Pi-SEO finding with scan_type, findings[], health_score per project; written to .harness/scan-results/{project}/{date}-{type}.json; drives Linear ticket creation via TriageEngine",
    "MonitorDigest — portfolio health snapshot with regression detection, cross-repo patterns, weighted health scores; written to .harness/monitor-digests/{datetime}.json",
    "PipelineState — Ship Chain state with spec/plan/test/ship phases, artifact paths, gate scores; persisted to .harness/pipeline/{pipeline_id}/",
    "CronTrigger — scheduled job definition with type (scan/monitor/analyse_lessons/intel_refresh/board_meeting), schedule fields, last_fired_at; stored in .harness/cron-triggers.json and loaded into memory on startup"
  ]
}
```