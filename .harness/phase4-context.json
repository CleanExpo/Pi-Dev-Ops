```json
{
  "projectPurpose": "Pi CEO is a private agentic engineering platform that converts a GitHub repo URL and a plain-English brief into an autonomous Claude Code execution session — cloning, building, evaluating, and pushing changes back to GitHub. It exists to give a non-technical solo founder (Phill) Zero Touch Engineering across a portfolio of 11 production repositories without writing code himself.",
  "targetUsers": [
    "Phill (Unite Group founder) — non-technical solo operator managing 3 revenue-generating products (CCW-CRM, RestoreAssist, DR-NRPG) who needs overnight autonomous engineering without hiring developers",
    "Claude Desktop / Cowork users connecting via MCP server for analysis, board notes, and sprint planning",
    "Telegram mobile user (Phill) sending commands and receiving alerts while away from his desk"
  ],
  "businessLogic": "The core rules: (1) Every build session flows through a 5-phase pipeline — clone → PITER-classify intent → generate via Claude Agent SDK → evaluate with 4-dimension scoring gate → push PR. (2) The evaluator is a BLOCKING gate at configurable threshold (default 7/10) with up to 2 retries with critique injection. (3) Autonomy poller polls Linear every 5 minutes for Urgent/High Todo issues, transitions them In Progress, and fires sessions — but requires LINEAR_API_KEY to be armed. (4) ZTE (Zero Touch Engineering) score across 12 leverage points drives sprint planning; 73/75 is current state but the system self-reports inaccurately (stale spec.md, false-positive watchdog alerts). (5) Human is never bypassed for: secret rotation, git push --force, Railway env changes, PR merges, Linear issue creation from phone ideas — these are hardcoded escalations. (6) Pi-SEO scans 10+ repos 4x/day; findings above threshold auto-create Linear tickets via triage engine. (7) The watchdog can self-heal known patterns but must escalate anything with a 'needs_founder' flag via a strict Telegram envelope format.",
  "currentState": "beta",
  "bigThree": {
    "model": "Appropriate tier selection: Opus 4.6 for planning/decomposition, Sonnet 4.6 for generation and evaluation, Haiku 4.5 for fast discrete tasks. SDK-only since RA-576 (subprocess fallback removed). The confidence-weighted evaluator gate in config.yaml (auto_ship at 9.5/90%, flag below 60% confidence) is well-designed. The PITER intent classifier + ADW workflow templates are the right pattern for structured prompt routing. Key gap: no per-project memory layer exists yet — every session starts cold with only lessons.jsonl injection; the RAG memory layer described in TURBOQUANT-ASSESSMENT.md is planned but unbuilt, meaning agents lack charter/context without manual prompt engineering per session.",
    "prompt": "The 5-phase pipeline has structured contracts (.harness/contracts/build-contract.md, eval-contract.md) with strict output formats parseable by sessions.py. The evaluator output format (COMPLETENESS/CORRECTNESS/CONCISENESS/FORMAT/OVERALL) is machine-parseable and well-specified. The 28 skills in /skills/ and agent specs in .harness/agents/ encode methodology. Critical weakness: the board meeting prompts operate blind when LINEAR_API_KEY is expired (401 for 3+ consecutive cycles) and fall back to stale harness files — the prompt has no reliable truth source. The ESCALATION.md envelope format for Telegram is correctly minimized for phone lock-screen readability. The watchdog ran pytest in an environment missing 'anthropic>=0.90', producing false CRITICAL alerts — the prompt/environment boundary is broken.",
    "context": "The .harness/ directory is the context layer — lessons.jsonl (38 entries auto-appended on low evaluator scores), config.yaml, spec.md, cron-triggers.json, agent-sdk-metrics/ JSONL. Context injection via _get_lesson_context() per intent is correct. Critical failures: (1) spec.md was 2+ days stale during Sprint 9 analysis — the system's self-model was wrong; (2) ZTE score reported as 60/60 in board meetings while autonomy.py was dead and RestoreAssist was down 76h+ — score measures capability existence not operational health; (3) The anthropic-docs files in .harness/anthropic-docs/ are raw HTML/JS from Mintlify's SSR output — not compressed, not parsed, not usable as context (the fetcher saved rendered Next.js page source instead of clean markdown); (4) triage-cache.json at 353KB with no pruning is growing unboundedly."
  },
  "keyInsights": [
    "The ZTE 60/60 score is a capability-completeness metric, not an operational health metric — the system achieved perfect scores while being operationally broken (dead autonomy poller, stale spec, false-positive watchdog, Linear auth expired). The CEO Memo (2026-04-13) correctly identifies this and proposes a Business Velocity Index replacement, but it hasn't been implemented yet.",
    "The overnight marathon failure (2026-04-11→12) revealed the fundamental topology bug: 'autonomous' tasks ran in Cowork sandboxes that died when the Mac slept. Only Railway-hosted components (the FastAPI server) were truly always-on. The architecture documents this thoroughly but the fix (GH Actions as scheduler + Railway as thin state store) is still in planning as of the latest harness state.",
    "The .harness/anthropic-docs/ files contain raw Mintlify SSR HTML/JavaScript (Next.js self.__next_f.push serialization), not readable documentation. The weekly anthropic-intel-refresh cron fetches URLs but saves rendered page source. This means the 'intelligence refresh' produces zero usable context — the entire observability feature is broken silently.",
    "The claude_agent_sdk dependency in app/requirements.txt references 'claude-agent-sdk>=0.1' which is a non-public/internal package name — the actual Anthropic SDK is 'anthropic>=0.90'. This suggests the SDK migration (RA-576) may have a dependency naming issue that could cause deployment failures on fresh Railway containers.",
    "The Linear API auth (LINEAR_API_KEY) failed for 3+ consecutive board meeting cycles, yet no automated remediation exists. The watchdog's escalation contract lists 9 things it will never do autonomously — but key rotation is #1 on that list. The system cannot self-heal its own primary integration failure.",
    "Pi-SEO scanning produces finding counts that got WORSE during the autonomous marathon (30→37 critical, 6433→10155 high) because the scans ran but the fixits never committed — the scanner generates findings faster than the build pipeline resolves them. This creates an autonomy treadmill unless the build pipeline has priority routing to match scan cadence.",
    "The .claude/settings.json uses 'defaultMode: bypassPermissions' — meaning any Claude session in this repo has full filesystem read/write/delete, git operations, curl, and npm without confirmation. Combined with the hardcoded paths to /Users/phill-mac/Pi-CEO/Pi-Dev-Ops in WorktreeCreate hooks, this is a developer machine, not a sandboxed environment.",
    "Sessions are in-memory only (_sessions dict in sessions.py) — a Railway restart loses all in-flight session state. The CLAUDE.md explicitly documents 'write-to-.tmp-then-os.replace()' for atomic writes, but the persistence requirement notes this risk. The autonomy poller creating sessions without durable queuing means Railway redeploys during active builds silently lose work."
  ],
  "deploymentTopology": "Split deployment: FastAPI backend on Railway (always-on, single worker, PORT=8080, auto-detected via RAILWAY_ENVIRONMENT env var); Next.js dashboard on Vercel (rootDirectory: dashboard, edge CDN); MCP server as local stdio process on developer Mac via Claude Desktop config. Backend uses in-memory session state with JSON file persistence to app/logs/.sessions/. Supabase PostgreSQL for observability tables (gate_checks, alert_escalations, heartbeat_log, triage_log). No database for sessions — deliberate ephemeral design. Scaling: TAO_MAX_SESSIONS=3 concurrent Claude sessions hard cap; rate limiting 30 req/min per IP. Docker image installs Node.js + @anthropic-ai/claude-code CLI alongside Python 3.12. The autonomy poller, cron loop, GC loop, and claude_cli_poll all run as asyncio background tasks in the single-worker Uvicorn process.",
  "dataModels": [
    "BuildSession — in-memory + JSON: session_id, repo_url, brief, model, status (9 states), output_lines[], evaluator_score, last_completed_phase, linear_issue_id, budget_minutes, scope dict",
    "CronTrigger — .harness/cron-triggers.json: id, type (scan/monitor/scout/intel_refresh/board_meeting/fallback_dryrun/feedback_loop), schedule fields, last_fired_at, enabled",
    "Lesson — .harness/lessons.jsonl: source, category, lesson text, severity, timestamp — auto-appended when evaluator scores a dimension below threshold",
    "ScanResult — .harness/scan-results/{project}/{date}-{scan_type}.json: scan_type, findings[], health_score — output of Pi-SEO ProjectScanner",
    "MonitorDigest — .harness/monitor-digests/{YYYYMMDD-HHMM}.json: portfolio health scores, regression deltas, systemic cross-repo patterns, alert list",
    "TelegramInboxMessage — .harness/telegram-inbox/{update_id}.json: update_id, chat_id, text, processed, route, route_result — bidirectional phone command loop",
    "PipelineState — .harness/pipeline/{pipeline_id}/: spec.md, plan.md, test results, ship_log — 6-phase Ship Chain artifact store",
    "AgentSDKMetric — .harness/agent-sdk-metrics/YYYY-MM-DD.jsonl: session_id, phase, model, success, latency_s, tokens, cost_usd — every SDK invocation"
  ]
}
```