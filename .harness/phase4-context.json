```json
{
  "projectPurpose": "Pi CEO is a Zero Touch Engineering platform that converts a GitHub repo URL and plain-English brief into an autonomous Claude Code execution session — cloning, building, evaluating, and pushing changes back to GitHub with no human in the loop. It exists to let a non-technical founder (Phill) operate a portfolio of 11 software projects without writing code or managing engineers.",
  "targetUsers": [
    "Phill McGurk — solo non-technical portfolio operator managing CCW-CRM, RestoreAssist, DR-NRPG, and 8 other repos under Unite Group, using this as his primary engineering leverage",
    "Future: other non-technical founders with multi-repo portfolios (the 'Founder OS' productisation framing in the board memos)"
  ],
  "businessLogic": "The core rules: (1) Linear ticket in Todo state → autonomy poller picks it up within 5min → Claude Agent SDK runs a 5-phase pipeline (Plan→Generate→Evaluate→Test→Ship) → PR opened, ticket closed. (2) Evaluator is a quality gate — sessions below threshold are retried up to 2x before failing. (3) The watchdog escalates CRITICAL to Telegram only when a founder decision is needed; self-healable issues are silently remediated. (4) Ideas from Telegram are never auto-promoted to Linear — human must bless each one (ESCALATION.md Rule 7). (5) SDK-only mode as of Sprint 8 — TAO_USE_AGENT_SDK=0 raises ImportError at startup by design. (6) ZTE scoring measures capability existence across 12 leverage points; BVI (Business Velocity Index) is the emerging replacement metric measuring actual business outcomes.",
  "currentState": "beta",
  "bigThree": {
    "model": "Tiered model selection is well-reasoned: Opus for planning (complex decomposition), Sonnet for generation and evaluation (speed/capability balance), Haiku for fast discrete tasks. The AUTONOMY_BUDGET single-knob pattern (config.yaml lines 50-60) correctly scales model tier with time budget. Critical gap: the evaluator grades code quality but not functional correctness — the CEO memo at .harness/board-meetings/2026-04-13-ceo-memo-board-minutes.md explicitly flags 'the evaluator gate measures code quality, not functional correctness.' A 73/75 ZTE score coexisting with 0 portfolio projects improved is direct evidence the model selection is optimising the wrong objective.",
    "prompt": "The PITER intent classifier + ADW workflow templates provide structured context injection, which is above average for agentic systems. However, the prompts are not visible in the provided context (they live in skills/ directory not included). The evaluator contract (.harness/contracts/eval-contract.md) is tight — strict parseable output format, integer scores, explicit fail thresholds. The board meeting persona system (.harness/BOARD-DELIBERATION-2026-04-12.md) demonstrates sophisticated multi-perspective prompting with adversarial 'Contrarian' and domain-expert 'Custom Oracle' roles. Key gap: no evidence of per-project context injection from the proposed memory/ RAG layer — sessions start without project-specific charter context despite the TURBOQUANT-ASSESSMENT.md correctly identifying this as the primary need.",
    "context": "The .harness/ directory is the context layer — lessons.jsonl, business charters, board minutes, scan results, agent specs all feed into sessions. The lesson injection pattern (_get_lesson_context() injecting relevant lessons per intent) is the strongest context mechanism. Critical failure: the overnight incident (.harness/INCIDENT-2026-04-12.md) was caused by stale context — Railway ran pre-marathon code because 7 commits were never pushed. The watchdog ran tests in an ephemeral sandbox that lacked anthropic>=0.90, producing a false CRITICAL from a context-environment mismatch. The right context (test results) was being read from the wrong environment. The fix (GH Actions as authoritative test-truth source) correctly addresses context reliability, not just environment drift."
  },
  "keyInsights": [
    "The ZTE scoring system is a vanity metric trap — 73/75 ZTE with zero portfolio improvement trend (0 CRITICALs resolved, 0 projects improved per the 2026-04-13 CEO memo) means the system has been optimising for capability existence rather than capability execution. The board explicitly identified this and proposed replacing ZTE as the primary metric with Business Velocity Index.",
    "The autonomous rails had a fundamental topology failure: 'autonomous' actually meant 'runs when Phill's Mac is open.' Seven commits sat unpushed on local main while Railway ran stale code — the architecture had a mandatory human step (git push) disguised as a one-liner. The V2 architecture correctly identifies Railway+GH Actions as the only surfaces that are truly always-on.",
    "The SDK migration (RA-576) removed the subprocess fallback but TAO_USE_AGENT_SDK=0 now raises ImportError at startup — this is deliberately loud but means any misconfiguration creates a complete service outage rather than graceful degradation. This is a sharp edge in the beta that production systems typically avoid.",
    "The .harness/anthropic-docs/ files contain raw JavaScript/CSS from Anthropic's documentation sites, not extracted content — the intel refresh is scraping raw HTML and storing it unparsed. This means the 'Anthropic Intelligence' context layer is essentially unusable noise rather than actionable signal.",
    "The gap audit (.harness/board-meetings/2026-04-12-gap-audit.json) identified that Linear two-way sync (state updates flowing back from build sessions to Linear tickets) was marked 'Complete' in spec.md but not implemented in webhook.py — the spec-to-code drift is a systemic problem, not a one-off. Three critical discrepancies found in a single audit cycle.",
    "The Telegram inbox architecture (.harness/ideas-from-phone/README.md) has a deliberate human-in-the-loop gate for idea promotion, but the watchdog's false-positive CRITICAL alert pattern means the escalation channel loses trust over time. A watchdog that cries wolf is worse than no watchdog — the incident doc explicitly names this.",
    "The Supabase integration is split between cloud (NEXT_PUBLIC_SUPABASE_URL in env.example pointing to a real project URL zbryrmxmgfmslqzizsto.supabase.co) and app logic, but the service role key is not set in .env.example — all Supabase writes from the server side are fire-and-forget with no error surfacing, making observability failures invisible by design.",
    "The multi-board-persona system in BOARD-DELIBERATION-2026-04-12.md demonstrates that the most valuable strategic output comes from adversarial prompting (Contrarian challenged 3 core assumptions, Moonshot proposed collapsing 4-week migration to 4-day GH Actions deployment). This pattern is not yet automated — it requires a manual 'convene the board' invocation rather than running on a schedule."
  ],
  "deploymentTopology": "Three-surface architecture: (1) Railway hosts the FastAPI backend (app/server/main.py) on port 8080, single Uvicorn worker, Docker image from Dockerfile at repo root. Railway auto-restarts on deploy. Autonomy poller, cron triggers, and Pi-SEO scanner run as asyncio background tasks within the FastAPI process. (2) Vercel hosts the Next.js dashboard (dashboard/), rootDirectory set in vercel.json. (3) Local Mac Mini (or developer machine) runs the MCP server (mcp/pi-ceo-server.js) for Claude Desktop integration. Sessions create temporary workspace directories in app/workspaces/ on Railway's ephemeral disk. Session state persists to app/logs/.sessions/ JSON files. Supabase cloud handles structured observability data. No Redis, no queue, no external task runner — all scheduling is in-process APScheduler/asyncio. Single point of failure: Railway container restart loses all in-memory _sessions dict (mitigated by disk persistence via restore_sessions()).",
  "dataModels": [
    "BuildSession — core entity: session_id, repo_url, brief, model, status (lifecycle FSM), output_lines (streaming log), evaluator_score, last_completed_phase (for resume), linear_issue_id (for two-way sync). In-memory dict _sessions + JSON persistence to disk.",
    "LinearIssue — polled from Linear API every 5min by autonomy poller: issue_id, title, priority, state, labels (repo:<url> label extracts target repo). Drives autonomous session creation.",
    "ScanResult — output of Pi-SEO scanner per project per scan_type: scan_type, findings (list with severity/title/file/line), health_score. Persisted to .harness/scan-results/{project}/{date}-{type}.json.",
    "MonitorDigest — cross-cycle delta analysis: previous vs current health scores, regressions detected, systemic cross-repo patterns, alert list. Persisted to .harness/monitor-digests/{YYYYMMDD-HHMM}.json.",
    "TelegramMessage — inbound from Phill's phone: update_id (filename key), text, route (fix/brief/idea/note), processed flag, route_result. Persisted to .harness/telegram-inbox/{update_id:012}.json.",
    "Lesson — auto-appended when evaluator scores a dimension below threshold: source, category, lesson text, severity. Append-only JSONL at .harness/lessons.jsonl. 38 entries as of latest harness state.",
    "PipelineState — Ship Chain artifact: pipeline_id, spec.md, plan.md, test results, ship_log. Persisted to .harness/pipeline/{pipeline_id}/.",
    "CronTrigger — scheduled work item: id, type (scan/monitor/analyse_lessons/intel_refresh/board_meeting/scout/feedback_loop), schedule fields, last_fired_at. Persisted to .harness/cron-triggers.json."
  ]
}
```