```json
{
  "projectPurpose": "Pi-CEO is a Zero Touch Engineering (ZTE) platform that converts a GitHub repo URL and a plain-English brief into a fully autonomous Claude Code execution session — cloning, building, evaluating, and pushing code — while simultaneously acting as a portfolio operator managing security health, Linear issue tracking, and autonomous remediation across 11 business repos owned by a solo non-technical founder.",
  "targetUsers": [
    "Phill McGurk — solo non-technical founder managing a portfolio of 3+ revenue-generating codebases (CCW-CRM, RestoreAssist, DR-NRPG) who needs autonomous overnight engineering without writing code or supervising runs",
    "The Pi-CEO system itself — as a self-consuming tool where the platform works on its own Linear backlog via the autonomy poller"
  ],
  "businessLogic": "The core rules: (1) A Linear ticket tagged Urgent/High in Todo state is the atomic unit of work — the autonomy poller (5-min Railway cron) picks it up, transitions it In Progress, runs a 5-phase SDK pipeline (plan→generate→evaluate→test→ship), and closes it. (2) The evaluator gate is blocking: OVERALL score < TAO_EVALUATOR_THRESHOLD blocks ship, triggers retry up to max_retries, auto-appends failure patterns to lessons.jsonl for future context injection. (3) Human-in-the-loop is enforced at two hard gates: ideas from Telegram NEVER auto-promote to Linear (ESCALATION.md rule 7), and autonomous sessions open PRs — they never merge to main directly for the first 14 days. (4) ZTE Score (currently self-reported 73/75) measures capability existence not capability execution — the system has a documented measurement problem where 60/60 ZTE coexisted with zero overnight commits. (5) Pi-SEO scans 4x/day across 11 repos and auto-files Linear tickets from critical/high findings; security findings are a blocking gate before feature work on revenue repos. (6) All observability writes are fire-and-forget to Supabase — observability failures must never block the build pipeline.",
  "currentState": "alpha",
  "bigThree": {
    "model": "Model tier selection is appropriate: Opus 4.6 for planning/decomposition (complex reasoning), Sonnet 4.6 for generation/evaluation (speed+capability balance), Haiku 4.5 available for fast discrete tasks. The tiered approach correctly matches cost to complexity. However, the evaluator uses the same model family as the generator (both Sonnet), which creates a risk of correlated evaluation bias — the evaluator may not catch errors the generator makes systematically. The Custom Oracle in BOARD-DELIBERATION correctly identifies the 'two-agent rule' (separate writer/reviewer with different system prompts) as missing. The local Qwen 3 14B proposal for triage routing is sound for classification tasks but the board correctly identified tool-calling reliability issues at the 14B scale. The SDK migration from subprocess to claude_agent_sdk is complete and appropriate — subprocess was fragile and environment-dependent.",
    "prompt": "The prompt architecture has several structural strengths: PITER intent classification at brief entry, ADW workflow templates per intent type, skill injection via skills_for_intent(), and lesson context injection per intent from lessons.jsonl. The evaluator output format is strictly specified and parseable. However, three gaps are significant: (1) The evaluator prompt is not visible in the provided files — impossible to assess whether the four-dimension scoring rubric is actually operationalized correctly or just described in the contract markdown. (2) The self-improvement loop that should read lessons.jsonl and propose CLAUDE.md updates is documented as missing across multiple board meeting cycles — the feedback compresses INTO lessons.jsonl but never loops back OUT to improve prompts. (3) The board meeting prompts are documented as working but the gap audit (2026-04-12-gap-audit.json) found 3 critical discrepancies between spec claims and actual implementation, suggesting the analysis prompts are producing overconfident assessments.",
    "context": "Context surfacing has a documented systemic failure: the ZTE Score is calculated from harness files that go stale (spec.md was 2 days old across multiple cycles, CLAUDE.md reports 73/75 while board meetings were reporting 60/60). The lessons.jsonl corpus (38 entries) is injected per-intent but there is no retrieval step — all lessons are dumped into context regardless of relevance, which will degrade as the corpus grows. The per-project memory layer (proposed in TURBOQUANT-ASSESSMENT.md as a RAG system with project-specific charter folders) is correctly designed but not yet implemented — sessions start without project-specific context, which explains why the autonomous overnight run produced zero commits (agents lacked the push credentials, environment knowledge, and project context to execute). The anthropic-docs cache in .harness/anthropic-docs/ stores raw Next.js-rendered HTML rather than extracted content, making it nearly useless as a context source."
  },
  "keyInsights": [
    "The overnight failure (INCIDENT-2026-04-12.md) reveals a fundamental topology error: the system was designed as '24/7 autonomous' but all scheduling ran inside Cowork sandboxes that died when Phill's Mac slept — Railway was always-on but only received 7 unpushed commits. The architecture read as autonomous was actually 'autonomous while the Mac is open'. This is not fixed as of the last committed state.",
    "The ZTE Score (60/60 → 73/75 across board meeting history) is a vanity metric in its current form — it measures whether code implementing a capability exists, not whether the capability executes correctly in production. The INCIDENT document explicitly states 'The overnight failure was the correct, expected outcome of the architecture I handed over' — meaning the evaluator that gave passing scores was measuring code quality of scaffolding code, not operational correctness.",
    "The gap audit (2026-04-12-gap-audit.json) found that Linear two-way sync (state updates flowing back from build sessions to Linear issues) is NOT implemented — webhook.py has inbound parsing but zero outbound GraphQL mutations. The spec claims it is complete. This means the autonomy loop has a broken feedback mechanism: tickets may be created but session outcomes never update Linear state.",
    "app/server/main.py contains a complete, production-grade FastAPI implementation with SSE streaming (/api/sessions/{sid}/logs), WebSocket streaming, Telegram webhook (/webhook/telegram with /ack_alert), Linear/GitHub webhook handling, cron triggers, pipeline endpoints, and a detailed /health endpoint including autonomy polling state — far more sophisticated than the gap audit's truncated view suggested.",
    "The .harness/anthropic-docs/ directory stores raw Next.js hydration bundles (self.__next_f.push([1,...]) JavaScript) not extracted documentation text — any system reading these files for Anthropic API context is reading minified React state serialization, not useful reference material.",
    "Two separate Docker files exist (root Dockerfile and app/Dockerfile) with different base paths and WORKDIR configurations — the root one copies src/ and skills/ which app/Dockerfile does not, meaning deployments from these two contexts produce different runtime environments. Railway uses railway.toml which likely points to one of these but the discrepancy is a latent misconfiguration risk.",
    "The autonomy poller has a documented cold-start bug where it sleeps first before the initial poll, causing a 5-minute dead zone on every Railway deployment — this was identified in lessons.jsonl but the fix (startup_delay=10s then immediate first fetch) shows as shipped in CLAUDE.md (commit e611b1c) but the board meetings as of 2026-04-12 still list it as 'unpushed'. Whether this is actually deployed to Railway is ambiguous from the harness state.",
    "The MCP server (mcp/pi-ceo-server.js v3.0.0) exposes 21 tools including Linear operations, but board meetings from 2026-04-11 and 2026-04-12 both report LINEAR_API_KEY as 401 — meaning the primary board intelligence loop has been running blind for at least 48 hours before the last committed board meeting."
  ],
  "deploymentTopology": "Split topology: FastAPI backend on Railway (always-on, single worker, 8080, auto-restart on deploy), Next.js dashboard on Vercel (SSR, auto-deploy from main branch). MCP server runs as a local stdio process on Phill's Mac for Claude Desktop integration. Database: Supabase cloud PostgreSQL with pgvector for observability tables (gate_checks, alert_escalations, heartbeat_log, triage_log, workflow_runs, claude_api_costs). Build workspaces are ephemeral on Railway disk — cloned repos are created per-session and deleted post-run. Sessions state is in-memory dict (_sessions) with disk persistence via write-to-tmp-then-os.replace() atomic writes to app/logs/.sessions/. No shared state between Railway restarts beyond disk. Scaling: single worker only (--workers 1 in CMD), max 3 concurrent sessions (TAO_MAX_SESSIONS). The planned Mac Mini local brain (Ollama + n8n + Supabase) is in the Linear backlog (RA-637 through RA-652) but not yet deployed.",
  "dataModels": [
    "BuildSession — core execution unit with repo_url, brief, model, status (created/cloning/building/evaluating/complete/failed/killed/interrupted), output_lines list, evaluator_score, last_completed_phase for resume, linear_issue_id for ticket sync",
    "LinearIssue — external entity tracked by the autonomy poller; Urgent/High Todo issues are the trigger for autonomous session creation; team ID a8a52f07, project ID f45212be are hardcoded for Pi-Dev-Ops team",
    "ScanResult — per-repo Pi-SEO findings with scan_type (security/code_quality/dependencies/deployment_health), findings list, health_score; written to .harness/scan-results/{project}/{date}-{type}.json",
    "MonitorDigest — portfolio health snapshot saved to .harness/monitor-digests/{YYYYMMDD-HHMM}.json; includes regression detection, cross-repo systemic patterns, weighted portfolio health score",
    "TelegramInboxMessage — inbound phone message at .harness/telegram-inbox/{update_id}.json with route classification (fix/brief/idea/note) and processing state",
    "PipelineState — Ship Chain 6-phase artifact (spec.md → plan.md → build → test → review → ship) persisted to .harness/pipeline/{pipeline_id}/; enables phase-level resume and ship gate",
    "Lesson — append-only entry in .harness/lessons.jsonl with source, category, lesson text, severity; injected into build session context via skills_for_intent(); 38 entries as of last board meeting",
    "CronTrigger — scheduled task definition in .harness/cron-triggers.json with type (scan/monitor/analyse_lessons/intel_refresh/board_meeting/fallback_dryrun/scout/feedback_loop), schedule fields, last_fired_at; 15 active triggers"
  ]
}
```