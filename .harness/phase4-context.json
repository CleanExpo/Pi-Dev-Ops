```json
{
  "projectPurpose": "Pi-CEO is a Zero Touch Engineering (ZTE) platform that converts GitHub repo URLs and plain-English briefs into autonomous Claude Code execution sessions, managing a portfolio of 3 business codebases (CCW, RestoreAssist, DR-NRPG) through continuous scanning, triage, and self-directed remediation. It exists to let a non-technical solo founder operate multiple production codebases without writing code or managing engineering staff.",
  "targetUsers": [
    "Phill McGurk — solo non-technical founder managing 3+ production codebases who wants overnight autonomous code maintenance without an engineering team",
    "Future portfolio operators (productisation target) — non-technical founders with multiple repos needing automated security/quality remediation"
  ],
  "businessLogic": "The system encodes seven core rules: (1) Linear tickets are the single source of work truth — all autonomous sessions are triggered by Linear state, not by the system's own judgment; (2) nothing merges to main autonomously — all sessions open PRs for human review; (3) evaluator gate blocks quality below threshold (default 7/10) before any push; (4) the watchdog never escalates without a verified test-truth source (GH Actions, not sandbox) — false CRITICAL alerts destroy founder trust; (5) human-in-the-loop is mandatory for scope amplification — ideas are filed, not auto-promoted; (6) security findings above 'high' severity are Urgent priority and block feature work; (7) autonomy is a property of topology — anything that requires the Mac to be awake is not autonomous by definition.",
  "currentState": "beta",
  "bigThree": {
    "model": "Three-tier Claude model selection (Opus 4.6 for planning, Sonnet 4.6 for generation/evaluation, Haiku 4.5 for workers) is appropriate and cost-optimised for the TAO architecture. The confidence-weighted evaluator gate in config.yaml (auto_ship at 9.5/90%, flag below 60% confidence) is sophisticated. Key gap: the autonomy poller's 5-minute Linear poll interval is unnecessarily slow for a Railway-hosted service — a webhook-first + poll fallback pattern would reduce latency from 5 min to sub-second. Local Qwen 3 14B routing layer (Pi-CEO Enhanced) is architecturally sound but unproven — the board deliberation correctly identified that 14B models cannot make commercial judgment calls, only classify and route.",
    "prompt": "The PITER intent classifier + ADW template selection pattern (5 workflow templates mapped to intent types: feature/bug/chore/spike/hotfix) is well-structured. The evaluator output format contract (.harness/contracts/eval-contract.md) is precise and parseable. Critical weakness: the generator prompt has no per-project memory injection — every session starts cold without the project charter, recent scan findings, or house-style context. TURBOQUANT-ASSESSMENT.md correctly identifies this gap and proposes a RAG memory layer, but it hasn't been implemented. The 28 skill files encode methodology but their injection into sessions via skills_for_intent() has no compression — at scale this will bloat the context window.",
    "context": "Context management is the system's most significant architectural gap. Sessions receive skill context and lesson context (via _get_lesson_context()) but no project-specific memory — a session fixing a CCW security finding has no access to the CCW charter, recent scan deltas, or what was fixed last week. The .harness/ directory is the correct location for this state but the retrieval step doesn't exist yet. Board meetings are the best context surfacing in the system — they aggregate ZTE score, Linear state, SWOT, and sprint recommendations into a single document. However, the anthropic-docs stored in .harness/anthropic-docs/ are raw HTML dumps (Next.js RSC payloads) rather than clean markdown — the stored context is high-noise. The lessons.jsonl corpus (38 entries as of latest board meeting) is the most valuable compressing context layer and it's underutilised."
  },
  "keyInsights": [
    "The ZTE score (73/75 as of Sprint 8) is a vanity metric — it measures capability existence, not operational effectiveness. The system scored 60/60 while producing zero commits overnight and falsely alerting CRITICAL on green tests. The board correctly identified this and proposed a Business Velocity Index replacement, but it hasn't been implemented.",
    "The fundamental architecture failure in V1 was conflating 'scheduled task' with 'autonomous' — Cowork sandboxes that die when the Mac sleeps are not autonomous infrastructure. Railway is the only truly always-on component, but as of the incident report (2026-04-12), 7 commits including the self-start fix had never been pushed to origin/main.",
    "The MCP server (mcp/pi-ceo-server.js v3.0.0) exposes 21 tools but the board meetings consistently report tool failures — get_zte_score returns 'No ZTE data found', feature_list.json missing, Linear auth 401 for multiple consecutive cycles. The observability layer reports health while the system is operationally blind.",
    "The evaluator gate is non-blocking in its current implementation ('warns' but doesn't retry) — sessions.py shows evaluator_status='warned' proceeds to git push. Despite the contracts/eval-contract.md documenting a blocking gate, the actual code allows below-threshold output to ship. This is a spec-code divergence that the gap audit (2026-04-12-gap-audit.json) partially captured.",
    "The .harness/anthropic-docs/ files contain raw Next.js RSC serialised payloads (self.__next_f.push([1,...]) JavaScript) rather than clean documentation content — whoever wrote the fetch script captured the full HTML response including React hydration scripts. This makes the stored docs nearly useless for context injection.",
    "Pi-CEO Enhanced (local Mac Mini + Ollama + n8n) is approved and tickets are filed (RA-637 through RA-652) but the 14-day clean-operation gate hasn't been attempted — the board approved architecture that hasn't been implemented, and the gap between approval and execution is the platform's core problem.",
    "The two-dockerfile problem: root Dockerfile and app/Dockerfile are diverged — root copies src/tao/, skills/, .harness/ while app/Dockerfile does not. Railway uses the root Dockerfile, but local Docker builds from app/ will produce a different image without TAO skills context, creating environment drift that is invisible at startup."
  ],
  "deploymentTopology": "Split deployment: Next.js dashboard on Vercel (rootDirectory: 'dashboard'), FastAPI backend on Railway (single worker, port 8080, auto-detected via RAILWAY_ENVIRONMENT). Node.js MCP server runs locally via Claude Desktop config. Sessions are stateful in-memory dict (_sessions) with disk persistence to app/logs/.sessions/ — a Railway redeploy loses all active session state. Claude Code execution happens inside Railway container via claude_agent_sdk (TAO_USE_AGENT_SDK=1 required — subprocess fallback removed in Sprint 8/RA-576). Supabase (PostgreSQL + pgvector) for observability event logging. Telegram Bot API for bidirectional mobile control. No Redis, no queue — sessions are fire-and-forget asyncio tasks. Max 3 concurrent sessions (TAO_MAX_SESSIONS=3).",
  "dataModels": [
    "BuildSession — core session entity tracking lifecycle state (created→cloning→building→evaluating→complete/failed), output_lines buffer, evaluator_score, phase checkpoints for resume, linear_issue_id linkage",
    "PipelineState — 6-phase Ship Chain artifact (spec→plan→build→test→review→ship) with per-phase outputs and ship_log gate decision",
    "ScanResult — Pi-SEO output per repo per scan_type with findings list and health_score, persisted to .harness/scan-results/",
    "MonitorDigest — cross-repo portfolio health snapshot comparing current vs previous scan cycle, detecting regressions ≥5pt, persisted to .harness/monitor-digests/",
    "CronTrigger — scheduled task definition (id, type, hour/minute/weekday, last_fired_at) persisted to .harness/cron-triggers.json — resets on Railway redeploy",
    "LessonEntry — structured learning from evaluator outcomes (source, category, lesson, severity) persisted to .harness/lessons.jsonl, injected into future sessions via _get_lesson_context()",
    "TelegramMessage — inbound message schema with update_id, route classification, processed flag, stored in .harness/telegram-inbox/ with 7-day GC"
  ]
}
```