```json
{
  "projectPurpose": "Pi CEO is a private autonomous engineering platform that converts a GitHub repo URL and plain-English brief into a Claude Code execution session — cloning, building, evaluating, and pushing code with zero human intervention. It also functions as a portfolio operations tool, running continuous security scanning (Pi-SEO) across 11 business repos and autonomously triaging findings into Linear tickets.",
  "targetUsers": [
    "Phill (sole founder/operator, Unite Group)",
    "Future: portfolio operators with multiple client codebases"
  ],
  "businessLogic": "Three-tier agent architecture (Opus=planner, Sonnet=specialist, Haiku=worker) executes build sessions via Claude Agent SDK (SDK-only since Sprint 8/RA-576 — subprocess fallback removed). Sessions are triggered by: GitHub webhooks, Linear webhooks, cron triggers, or the autonomy poller (polls Linear every 5min for Urgent/High Todo issues). Each session runs a 5-phase pipeline (spec→plan→generate→evaluate→ship) with a blocking evaluator gate (threshold 7/10, max 2 retries). Pi-SEO scanner monitors 11 repos on a 4x/day cron, auto-creates Linear tickets for critical/high findings. Human-in-loop rules: no autonomous git push to main (PRs only for first 14 days per board decision), no Linear issue creation from Telegram ideas (human promotes manually), no secret rotation, no force-pushes. ZTE score self-reported as 73/75 but ops reality shows autonomy poller broken, LINEAR_API_KEY missing, commits unpushed — the self-reported score is a vanity metric. Supabase logs all gate_checks, alert_escalations, heartbeat_log, triage_log, workflow_runs, claude_api_costs.",
  "currentState": "alpha",
  "keyInsights": [
    "CRITICAL GAP — The overnight autonomous run (2026-04-11→12) produced zero commits: 7 commits sat unpushed on local main, Railway ran stale code, Cowork sandbox tasks died when the Mac slept. The autonomous rails are not actually autonomous — they are scheduled tasks on Phill's laptop.",
    "ARCHITECTURE MISMATCH — The system self-reports ZTE 73/75 but the incident doc (.harness/INCIDENT-2026-04-12.md) documents: 0 Linear tickets moved, 0 PRs opened, Pi-SEO findings increased 30→37 critical during the 'autonomous' run. The scoring methodology does not include operational health.",
    "SDK MIGRATION INCOMPLETE — CLAUDE.md states 'SDK-only execution confirmed (RA-576)' and subprocess fallback removed, but agent-sdk-metrics show all success=true entries have output_len=14 or 20 (stub values) and concurrent success=false entries with 'RuntimeError: Connection failed' and 'timeout after 1s'. The SDK is not making real calls in the metrics file.",
    "LINEAR API AUTH BROKEN — Three consecutive board meeting cycles (2026-04-11, 2026-04-12 twice) report 'LINEAR UNAVAILABLE (401)'. The autonomy poller depends on LINEAR_API_KEY to function. Board minutes acknowledge 'autonomy poller DISABLED' implicitly throughout.",
    "DEPENDENCY VERSION CONFLICT — app/requirements.txt lists 'claude-agent-sdk>=0.1' but pyproject.toml does not include this package. The Dockerfile installs from app/requirements.txt. This means the SDK dependency may not be installed in production, explaining the Connection failed errors in metrics.",
    "VERCEL BUILD EXPOSES 1 HIGH SEVERITY VULNERABILITY — build log .harness/build-logs/20260410T210734-dpl_BAvfh2mN.json explicitly shows '1 high severity vulnerability' in npm audit. The subsequent deploy fixed it (found 0 vulnerabilities) but it was shipped to production for at least one deployment window.",
    "GAP AUDIT FINDINGS (2026-04-12) — The formal gap audit filed 7 Linear tickets (RA-665 to RA-671) identifying: Linear two-way sync not implemented (webhook.py has no outbound Linear GraphQL calls), SSE streaming endpoint claimed but no GET /api/sessions/{sid}/logs route visible, Telegram webhook in Next.js not Python FastAPI contradicting CLAUDE.md claims. Three critical discrepancies.",
    "BOARD DELIBERATION OUTPUT — The 9-persona board deliberation on 2026-04-12 reached consensus: collapse 4-week V2 Railway migration to a 4-day GH-Actions-as-scheduler + Claude-SDK-as-executor deployment, PR-only for first 14 days, two-agent review pattern. The Pi-CEO Enhanced board approved Qwen 3 14B local model on Mac Mini M4 Pro with n8n + Supabase cloud as always-on intelligence layer.",
    "SSE STREAMING IMPLEMENTED — Despite gap audit claim, main.py lines 206-237 show GET /api/sessions/{sid}/logs as a StreamingResponse with text/event-stream and exponential polling. The gap audit was reviewing an older/truncated version of main.py. The endpoint exists and is functional.",
    "PERMISSION BYPASS IS TOTAL — .claude/settings.json has defaultMode='bypassPermissions' with Write(*), Edit(*), Read(*) blanket grants. Combined with Bash(rm:*) allowance, an autonomous session can delete arbitrary files. The only guards are behavioral (watchdog never-do list) not technical.",
    "HARNESS AS LIVING SYSTEM — The .harness/ directory functions as the system's memory, decision log, and operational state: 38+ lessons.jsonl entries, board minutes every 6h, incident post-mortems, SDK migration plans, business charters for 3 portfolio projects, Telegram inbox messages. This institutional knowledge is the most valuable artifact in the repo."
  ]
}
```