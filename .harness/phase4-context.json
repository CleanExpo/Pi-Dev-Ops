```json
{
  "projectPurpose": "Pi-CEO is a solo-founder DevOps automation platform that converts a GitHub repo URL and plain-English brief into an autonomous Claude Code execution session — cloning, building, evaluating, and pushing changes back to GitHub. It also monitors a portfolio of 11 business repos via Pi-SEO scanning, triages findings into Linear, and provides a Telegram-based remote control loop for the founder while away from the desk.",
  "targetUsers": [
    "Phill (sole founder/operator — Unite Group)",
    "Non-technical portfolio operators who need autonomous engineering execution",
    "Future: portfolio-operator-as-a-service customers (aspirational, not yet built)"
  ],
  "businessLogic": "Three-tier Claude orchestration (Opus=planner, Sonnet=generator/evaluator, Haiku=fast tasks). Build pipeline: clone → PITER intent classification → ADW template selection → Claude Agent SDK execution → 4-dimension evaluator gate (threshold 7/10) → git push → Linear ticket update. Pi-SEO scanner runs on 13 cron triggers (4x/day high-priority, 1x/day medium/low) across 11 repos, auto-creating Linear tickets for findings above severity thresholds. Autonomy poller polls Linear every 5 min for Urgent/High Todo issues and auto-creates sessions. Human-in-the-loop gates: PRs never auto-merged, ideas never auto-promoted to Linear (rule 7 in ESCALATION.md), autonomous push to main deferred until 14-day PR-only track proves clean. ZTE Score (73/75 at Sprint 8) measures capability completeness, but the pivot at Cycle 23 introduces Business Velocity Index (CRITICAL resolution speed + portfolio health delta + shipped features) as the primary metric.",
  "currentState": "alpha",
  "keyInsights": [
    "TOPOLOGY MISMATCH IS THE CORE FAILURE: The overnight failure (2026-04-11→12) produced zero commits because 7 commits sat unpushed on local main — Railway ran stale code. Scheduled tasks inside Cowork sandboxes stop when the Mac sleeps. The system was autonomous only while the founder was awake, which is the opposite of the design intent.",
    "ZTE SCORE IS A VANITY METRIC IN ITS CURRENT FORM: 73/75 ZTE coexists with 0 portfolio projects improved, RestoreAssist down 76h+, and 37 critical Pi-SEO findings growing. The score measures capability existence, not operational effectiveness — the CEO memo at Cycle 23 explicitly pivots away from it.",
    "SDK-ONLY MODE IS THE SOLE SUPPORTED PATH: As of Sprint 8/RA-576, the subprocess claude -p fallback has been removed from sessions.py. TAO_USE_AGENT_SDK=0 raises ImportError at startup — misconfiguration is intentionally loud. The API fallback (TAO_USE_FALLBACK=1 using direct Anthropic SDK) is a quarterly-tested contingency only.",
    "CRITICAL GAP — LINEAR TWO-WAY SYNC IS NOT IMPLEMENTED: The gap audit (2026-04-12-gap-audit.json) flags as CRITICAL that webhook.py has zero outbound Linear API calls — no issueUpdate GraphQL mutation, no state transitions flowing back. The autonomy loop claims to close Linear tickets but the actual write-back path is unverified in the audit.",
    "SELF-ASSESSMENT IS BROKEN: spec.md was last generated 2026-04-10 (Sprint 7), meaning the system's own intelligence layer describes a 2-sprint-stale architecture. RA-675/RA-685 exist to fix this, gated on Phill running python _deploy.py manually. The evaluator measures code quality, not functional correctness — the CEO memo explicitly warns Sprint 10 outputs require manual verification.",
    "ARCHITECTURE DECISION RECORDED: Board deliberation (2026-04-12) collapsed a 4-week V2 Railway migration into GH Actions as scheduler + Claude Agent SDK headless + Railway as thin state store only. Target: 4-day delivery to MVP, PR-only autonomy for first 14 days, direct-to-main gated on 3 clean merged tickets without human edits.",
    "MAC MINI LOCAL AI LAYER APPROVED: Pi-CEO Enhanced board approved Ollama + Qwen 3 14B (native, not Docker) + n8n (single Docker container) + Supabase cloud free tier, replacing 5-container original proposal. Qdrant dropped — pgvector sufficient. 14-day clean-operation gate before Phases 3-6 proceed. UPS mandatory prerequisite.",
    "SECURITY IS A BLOCKING GATE: RestoreAssist (325 findings) and CCW-CRM (4,877 findings including 1 critical SSRF vector and 196 high) operate in insurance/compliance contexts where a security incident is a commercial termination event. The CEO memo makes security health a blocking gate before new feature work on either project.",
    "PERMISSION MODEL IS PRE-GRANTED AT THREE LAYERS: .claude/settings.json (bypassPermissions + allowlist), ClaudeAgentOptions(permission_mode='bypassPermissions'), and --dangerously-skip-permissions via CLAUDE_EXTRA_FLAGS. Any new tool MUST be added to the allow list before shipping — a permission prompt at runtime is a regression.",
    "WATCHDOG FALSE-POSITIVE PATTERN: The marathon watchdog ran pytest in a fresh Cowork sandbox missing anthropic>=0.90, classified ModuleNotFoundError as 'tests red', and escalated CRITICAL. Test-truth must come from GH Actions (known environment), never from the watchdog sandbox. This is the architectural fix, not adding the dependency to the sandbox."
  ]
}
```