```json
{
  "projectPurpose": "Pi-CEO is a solo-founder DevOps automation platform that converts a GitHub repo URL and plain-English brief into an autonomous Claude Code execution session — cloning, building, evaluating, and pushing changes back to GitHub with zero per-token API cost on Claude Max. It simultaneously acts as an always-on portfolio manager that scans, triages, and works a backlog of security and quality issues across 11 business repos.",
  "targetUsers": [
    "Phill McGurk — solo non-technical founder operating a portfolio of 4 businesses (CCW-CRM, RestoreAssist, DR-NRPG, Pi-Dev-Ops)",
    "Future: portfolio operators running multiple repos who want autonomous code maintenance without engineering staff"
  ],
  "businessLogic": "TAO (Tiered Agent Orchestrator) runs 3 Claude tiers: Opus (planner/decomposer), Sonnet (generator/evaluator), Haiku (fast workers). A build session flows through 5-6 phases: PITER intent classification → ADW task decomposition → code generation via Claude Agent SDK → evaluator quality gate (threshold 7/10, blocking) → git push. The autonomy poller (app/server/autonomy.py) polls Linear every 5 minutes for Urgent/High Todo issues, creates sessions automatically, and closes tickets on completion. Pi-SEO scanner runs 4x/day against 11 repos, triages findings into Linear as Urgent/High/Medium issues, and the build pipeline works them down. Human-in-loop rules enforced by .harness/ESCALATION.md rule 7: agent never auto-promotes ideas to Linear, never merges its own PRs to main, never rotates secrets. ZTE (Zero Touch Engineering) score tracks 12 leverage points 0-75; current claimed score is 73/75. Business Velocity Index (BVI) is the emerging primary metric replacing ZTE. SDK-only execution mode since Sprint 8 (RA-576) — subprocess fallback removed, TAO_USE_AGENT_SDK=1 required or startup fails loudly.",
  "currentState": "WIP",
  "keyInsights": [
    "Architecture gap between claims and reality: .harness/board-meetings report ZTE 60/60 then 73/75 while INCIDENT-2026-04-12.md documents zero commits landed overnight, false-positive watchdog CRITICALs, and Pi-SEO findings worsening (30→37 critical, 6433→10155 high) — the ZTE metric measures capability existence, not functional operation",
    "Critical topology failure: autonomous rails were Cowork-sandbox-dependent (mac-sleep kills them) not Railway-hosted — commits sat unpushed because sandbox has no GitHub push credentials; the V2 architecture doc and board deliberation correctly identify Railway+GH Actions as the fix but as of the harness files this migration was still planned, not shipped",
    "SDK migration is complete (RA-576) but the canary rollout plan (sdk-phase2-rollout.md) shows Phase A was never activated — dual-path wired but 0% real traffic, three consecutive board cycles flagged this as Priority 1 without action",
    "Pi-SEO infrastructure is fully built (scanner, triage engine, 3 skill layers, cron rotation, 12 triggers in cron-triggers.json with last_fired_at values indicating it IS running) but gap-audit.json reveals critical gaps: Linear two-way sync is missing from webhook.py (no issueUpdate GraphQL calls), and the SSE streaming endpoint is claimed in spec but absent from main.py",
    "The .harness/ directory is the operational brain — 38+ entries in lessons.jsonl, board minutes, gap audits, business charters for 3 portfolio companies, escalation contracts, agent specs — this is a sophisticated self-documenting system but the docs themselves show a pattern of claiming features complete before they are verified working in production",
    "Supabase integration added in RA-651/RA-633 for gate_checks, alert_escalations, heartbeat_log — but SUPABASE_SERVICE_ROLE_KEY is not set in .env.example (blank), meaning all Supabase writes are likely silently failing in production",
    "The business charter model (PI-CEO-STANDARD.md + per-project charters) is architecturally sound — 7 pillars, clear exit criteria, automated enforcement via Pi-SEO — but all three portfolio projects (CCW, RestoreAssist, DR-NRPG) remain in RED on security with thousands of findings and no confirmed autonomous fix applied yet",
    "MCP server (mcp/pi-ceo-server.js v3.0.0) has 21 tools for Linear operations and harness reads, enabling Claude Desktop control — this is the human-in-loop interface that Phill uses daily, and it's the most mature component"
  ]
}
```