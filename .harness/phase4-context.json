```json
{
  "projectPurpose": "Pi-CEO is a Zero Touch Engineering (ZTE) autonomous DevOps platform that converts a GitHub repo URL and plain-English brief into a complete Claude-executed engineering session — cloning, building, evaluating, and pushing results. It exists to let a non-technical founder (Phill) operate a 10+ repo software portfolio without writing code or supervising every change.",
  "targetUsers": [
    "Phill McGurk — solo non-technical founder managing 4+ business repos (CCW-CRM, RestoreAssist, DR-NRPG, Pi-Dev-Ops) who explicitly cannot and will not write code",
    "Claude Desktop / Cowork users who need structured engineering intelligence via MCP tools",
    "Future: portfolio operators managing multiple software businesses who need autonomous maintenance rails"
  ],
  "businessLogic": "The core rules: (1) Every build session runs through a 5-phase pipeline — plan, generate, evaluate, test, ship — with Claude SDK as sole executor (subprocess fallback removed RA-576); (2) The evaluator is a BLOCKING gate at threshold 7/10 with up to 2 retries before failing; (3) The autonomy poller polls Linear every 5 minutes for Urgent/High Todo issues and auto-creates sessions — kill switch TAO_AUTONOMY_ENABLED=0; (4) Nothing merges to main autonomously — PRs open for human review; (5) Agents never rotate secrets, never push --force, never create issues that amplify their own scope (ESCALATION.md rule 7); (6) ZTE score (now 73/75) measures platform maturity across 12 leverage dimensions — the real KPI shifting to Business Velocity Index (tickets closed, portfolio health improved); (7) Pi-SEO scans 11 repos 4x/day and auto-files Linear issues for security regressions.",
  "currentState": "beta",
  "bigThree": {
    "model": "Model tier selection is appropriate: Opus 4.6 for planning/orchestration (complex reasoning), Sonnet 4.6 for generation and evaluation (speed/quality balance), Haiku 4.5 for discrete fast tasks. The ZTE autonomy budget system (config.yaml anchors) dynamically adjusts model tier, eval threshold, and timeout based on budget_minutes — this is sophisticated and correct. The evaluator running Sonnet rather than Opus is deliberate cost optimization that may undergrade complex outputs. The local-model-for-triage vision (Qwen 3 14B via Ollama) is architecturally sound for the routing layer but unimplemented — the Mac Mini Enhanced plan remains in planning documents only.",
    "prompt": "Prompts are structured through 28 SKILL.md files plus per-agent specification files (.harness/agents/*.md). The PITER intent classifier + ADW workflow templates inject structured context before every session. The evaluator output format contract is strict and parseable. Weaknesses: the spec.md is repeatedly noted as stale (last regenerated 2026-04-10 as of the most recent board minutes) which means every session starts with slightly incorrect architectural context. The self-improvement loop that would automatically update CLAUDE.md from lessons.jsonl patterns is listed as a gap in every board meeting for 3+ cycles but never built. The evaluator prompt grades code quality but cannot detect intent drift — a known gap per the Custom Oracle in the board deliberation.",
    "context": "Context surfacing is the most sophisticated part of the system. lessons.jsonl (38 entries) is injected per-intent via _get_lesson_context(). Business charters provide per-project memory. The .harness/ directory serves as a persistent state layer across sessions. Key gaps: (1) spec.md staleness — the primary context document lags reality by 2-3 days repeatedly; (2) No RAG retrieval layer exists yet — all context is flat-file injection, not similarity-ranked; (3) triage-cache.json at 353KB has no pruning, creating growing noise; (4) The harness/anthropic-docs/ files are raw scraped HTML with embedded JS/CSS rather than clean extracted text — these consume tokens without delivering signal value."
  },
  "keyInsights": [
    "The ZTE score (73/75) is a vanity metric that coexists with zero portfolio health improvement — the CEO memo (2026-04-13) explicitly acknowledges this and proposes replacing it with a Business Velocity Index. The system optimizes for capability existence, not capability utilization.",
    "The first overnight autonomous run (2026-04-11→12) produced zero commits because 7 commits were unpushed on local main — Railway ran stale code the entire time. The 'autonomous' rails were actually 'autonomous when Phill's Mac is open.' This is the single most important architectural truth in the entire codebase.",
    "The claude_agent_sdk SDK-only migration (RA-576) removed the subprocess fallback, making TAO_USE_AGENT_SDK=0 cause an ImportError at startup — this is a deliberate loud failure. However the canary rollout plan (sdk-phase2-rollout.md) shows Phase A was never activated, meaning the SDK path went straight from 0% to 100% production traffic without validation data.",
    "The .harness/anthropic-docs/ files contain raw Next.js RSC hydration payloads (self.__next_f.push([1,...]) strings) rather than cleaned documentation text. Every session that loads these files spends tokens on JavaScript framework noise, not API knowledge.",
    "The gap audit (2026-04-12-gap-audit.json) found 3 critical discrepancies: no Linear two-way sync (issues are never updated by the build pipeline), no SSE log streaming endpoint, and the Linear webhook handler path may not exist in main.py. These are foundational features the spec claims are complete.",
    "The system has a human-in-loop bypass that scales poorly: every idea from phone → .harness/ideas-from-phone/ must be manually promoted to Linear by Phill. With 16 tickets filed across 3 phases of Pi-CEO Enhanced plus ongoing portfolio work, this manual promotion bottleneck will become the constraint on throughput.",
    "The MCP server (version 3.0.0) exposes 21 tools but the board meetings show get_zte_score, get_feature_list, and executive-summary returning stale/missing data for multiple consecutive cycles. The MCP layer is the primary consumer of harness data but has no staleness validation — it serves whatever file exists regardless of age.",
    "The Supabase integration (gate_checks, alert_escalations, heartbeat_log tables) is fire-and-forget by design ('observability failures must never block the build pipeline') — this means there is no guaranteed audit trail for production events.",
    "The .claude/settings.json has defaultMode: bypassPermissions with Read(*), Write(*), Edit(*) — every Claude session running in this repo has unrestricted filesystem access. Combined with the autonomous poller creating sessions against external repos, the blast radius of a compromised brief is the entire Railway container filesystem."
  ],
  "deploymentTopology": "Split deployment: FastAPI backend on Railway (always-on, single worker, port 8080, Docker container with Ollama/Node.js for claude CLI); Next.js dashboard on Vercel (CDN-distributed, rootDirectory: dashboard); MCP server runs locally via stdio on Phill's Mac. Supabase (PostgreSQL + pgvector) provides persistent state for observability events. Sessions are in-memory dict (_sessions) on the Railway container — a server restart loses all running sessions. Git workspaces clone to app/workspaces/ on Railway ephemeral disk. No Redis, no queue, no shared state between scaling units — TAO_MAX_SESSIONS=3 enforced by the API layer. The Mac Mini Enhanced plan (Ollama + n8n + Open WebUI) exists as planning documents only — not deployed.",
  "dataModels": [
    "BuildSession — core entity tracking repo_url, brief, model, status lifecycle, output_lines, evaluator_score, phase checkpoints, linear_issue_id; persisted to app/logs/.sessions/{sid}.json",
    "CronTrigger — scheduled scan/monitor/intel_refresh/board_meeting entries in .harness/cron-triggers.json with last_fired_at timestamps; reloaded on Railway restart",
    "ScanResult — per-repo scan output in .harness/scan-results/{project}/{date}-{scan_type}.json containing findings[], health_score, scan_type",
    "MonitorDigest — cross-repo portfolio health snapshot in .harness/monitor-digests/{YYYYMMDD-HHMM}.json with regression detection and delta from previous cycle",
    "Lesson — JSONL entry in .harness/lessons.jsonl with source, category, lesson text, severity; injected into sessions by intent matching",
    "TelegramInboxMessage — raw inbound message in .harness/telegram-inbox/{update_id}.json with route classification (fix/brief/idea/note) and processing state",
    "PipelineState — Ship Chain artifact set: spec.md + plan.md + test results + ship-log stored in .harness/pipeline/{pipeline_id}/",
    "LinearIssue — remote state in Linear workspace (team RestoreAssist, project Pi-Dev-Ops); RA-xxx ticket format; autonomy poller reads Todo issues, gap audit creates findings tickets",
    "SDKMetrics — JSONL rows in .harness/agent-sdk-metrics/YYYY-MM-DD.jsonl tracking per-invocation latency, success/failure, token cost for canary analysis"
  ]
}
```