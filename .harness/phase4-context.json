```json
{
  "projectPurpose": "Pi CEO is a solo-founder DevOps automation platform that converts a GitHub repo URL and plain-English brief into an autonomous Claude Code execution session — cloning, building, evaluating, and pushing code changes back to GitHub. It exists because Phill McGurk (Unite Group) needs a non-technical founder's interface to maintain and improve a portfolio of 11 revenue-generating repositories without hiring engineers.",
  "targetUsers": [
    "Phill McGurk — solo non-technical founder managing 3 primary portfolio businesses (CCW-CRM, RestoreAssist, DR-NRPG) who explicitly states 'I am not a Coding Engineer and don't plan to become one'",
    "Portfolio operators running multiple SaaS/web products who need autonomous security remediation and feature delivery without a full engineering team",
    "Future productisation target: non-technical founders operating 3-10 repos who need a 'Founder OS' rather than a DevOps tool"
  ],
  "businessLogic": "The system encodes a Zero Touch Engineering (ZTE) scoring model (75-point scale across 12 leverage dimensions) as the primary quality gate — every architectural decision is justified by which ZTE point it improves. Build sessions follow a 5-phase pipeline (Plan→Generate→Evaluate→Test→Ship) where the evaluator uses a confidence-weighted threshold (7.0-9.5/10) to gate merges. Linear tickets are the canonical unit of work: the autonomy poller polls Linear every 5 minutes, transitions Urgent/High Todo items to In Progress, fires SDK sessions, and closes tickets on success. Human-in-the-loop is enforced at exactly one point: merging PRs — the agent opens PRs but never merges them. Escalation rules (ESCALATION.md rule 7) explicitly prohibit autonomous Linear issue creation from Telegram inbox messages, autonomous git pushes to main, and any action that amplifies the agent's own scope. The Pi-CEO Standard (7 pillars: Security, Code Quality, Dependencies, Deployment Health, Observability, Documentation, Autonomy-ready) is the business contract against which all 11 portfolio repos are measured.",
  "currentState": "beta",
  "bigThree": {
    "model": "Tiered model selection is appropriate: Opus 4.6 for planning/decomposition (complex reasoning), Sonnet 4.6 for generation and evaluation (cost/capability balance), Haiku 4.5 for discrete fast tasks. The evaluator running on Sonnet is correctly calibrated — it grades diffs, not generates code. The autonomy budget system (config.yaml anchors at 10/30/60/120/240 minutes) dynamically adjusts model tier and eval thresholds based on session scope, which is a sound routing strategy. Key gap: the local Qwen 3 14B layer proposed in the Enhanced architecture (for triage/routing) hasn't shipped — all routing still goes to frontier models at full cost. The TurboQuant dismissal is technically correct (wrong layer — KV cache compression doesn't help API consumers), and the proposed RAG memory layer is the right alternative but unbuilt.",
    "prompt": "The 28 SKILL.md files and PITER classification framework provide structured intent-to-template mapping, which is above average for agentic systems. The CLAUDE.md file is exceptionally detailed — it specifies model versions, SDK architecture decisions, code conventions, branch naming, and file size limits in one place. The evaluator prompt contract (COMPLETENESS/CORRECTNESS/CONCISENESS/FORMAT dimensions with strict output format for parsing) is well-structured. Key gaps: (1) The harness docs have a staleness problem — spec.md and feature_list.json were 2-3 days stale across multiple consecutive board meetings, meaning the context injected into sessions was inaccurate. (2) The per-project memory layer (memory/ccw/, memory/restoreassist/) is designed in TURBOQUANT-ASSESSMENT.md but not yet built — sessions start without project-specific charter context injected. (3) The anthropic-docs files stored in .harness/ are raw HTML/JS rendered output (not clean markdown), making them noisy context rather than clean signal.",
    "context": "The .harness/ directory is the right pattern — centralised state that survives session boundaries, with lessons.jsonl as a compounding knowledge base. The lessons injection per-intent (context_precision lever) is architecturally sound. However three critical context failures are documented: (1) The overnight marathon failure (INCIDENT-2026-04-12.md) proved the scheduled watchdog ran pytest in an environment missing 'anthropic>=0.90', producing false CRITICAL alerts — the system surfaced wrong context about its own health. (2) The /health endpoint reported 'ok' even when LINEAR_API_KEY was missing and the autonomy poller was silently skip-polling — health context was misleading. (3) The anthropic-docs stored as raw Next.js hydration blobs (not parsed text) means when these are injected as context they bring thousands of tokens of JavaScript noise rather than API documentation. The board meetings tool (MCP) had 3/5 tools returning stale or missing data for multiple consecutive cycles. The ZTE score self-reporting at 60/60 (later revised to 73/75) while RestoreAssist was down 76h+ and Pi-SEO findings were worsening is the canonical example of context that looks complete but is measuring the wrong thing."
  },
  "keyInsights": [
    "The ZTE scoring system is an internal capability audit, not a customer value metric — the system reached 73/75 ZTE while zero portfolio projects improved, exposing that optimising for ZTE score is not the same as delivering business outcomes. The CEO Memo (2026-04-13) explicitly acknowledges this and proposes replacing ZTE with a Business Velocity Index.",
    "The overnight marathon failure (2026-04-12) was an architecture failure not a code failure: all autonomous rails ran inside Cowork sandboxes that stopped when the Mac slept, but were presented as '24/7 autonomous' — the gap between claimed and actual autonomy was the core trust breach.",
    "There are two parallel architecture proposals (V2 in ARCHITECTURE-V2.md and 'Enhanced' in BOARD-DELIBERATION-ENHANCED) that are partially overlapping and neither is fully implemented — the system is in an architectural transition state where Railway, GH Actions, Mac Mini local AI, and Cowork scheduled tasks are all in-flight simultaneously.",
    "The cron-triggers.json last_fired_at timestamps (e.g. 1775952090 = ~2026-04-xx) suggest the Pi-SEO scans have been firing successfully in production, but the autonomy poller (linear_todo_poller) had a known 5-minute cold-start delay bug (sleep-first pattern) that was fixed in commit e611b1c but that commit was never pushed to origin — Railway was running pre-fix code.",
    "The MCP server (mcp/pi-ceo-server.js v3.0.0) exposes 21 tools including direct Linear issue creation, but the ESCALATION.md explicitly prohibits autonomous Linear issue creation from Telegram — this creates a permission surface that the guard rails depend on never being called autonomously, not on being architecturally prevented.",
    "The .harness/anthropic-docs/ files contain raw Next.js server-side rendered HTML blobs (thousands of lines of JavaScript) rather than clean documentation — these files are loaded as context by the intel_refresh cron but would inject CSS variables, font declarations, and React hydration code as 'documentation' context.",
    "The evaluator gate is currently non-blocking for below-threshold scores (config: 'below-threshold score logs warning but does not block git push') — the quality gate described as a 'BLOCKING evaluator gate' in ZTE scores is actually only a warning gate in production code at sessions.py.",
    "Railway was chosen as the always-on infrastructure, but the .env.example contains a hardcoded Supabase URL (zbryrmxmgfmslqzizsto.supabase.co) and Telegram bot token in the PREFLIGHT checklist (8630069375:AAH5CDnCm5ApdFsdfFz1KwNGEFDtgy9yvDA) — both are committed to what appears to be a private but not secret repository."
  ],
  "deploymentTopology": "Three-layer topology in transition: (1) Railway — FastAPI backend (port 8080) with single uvicorn worker, auto-deployed from main branch via railway.toml, serves the API, autonomy poller, cron dispatcher, and Pi-SEO scanner; (2) Vercel — Next.js dashboard deployed via rootDirectory:dashboard, communicates with Railway backend cross-origin using SameSite=None cookies; (3) Local Mac Mini (proposed, partially implemented) — Ollama + Qwen 3 14B for triage, n8n for workflow orchestration, Supabase cloud free tier for vector memory. MCP server runs as a stdio process on the developer's local machine (Claude Desktop integration). GitHub Actions handles CI with three jobs: pytest+ruff, tsc+eslint+build, and smoke-prod (post-deploy gate against Railway). Sessions are ephemeral in-memory dicts with disk persistence to app/logs/.sessions/ — server restarts lose in-flight sessions. Max 3 concurrent sessions (TAO_MAX_SESSIONS). The system has no database replication, no multi-region deployment, and no load balancing.",
  "dataModels": [
    "BuildSession — core entity tracking a single autonomous coding run: id, repo_url, brief, model, status (8 states), output_lines[], evaluator_score, last_completed_phase, process handle, linear_issue_id; persisted atomically via os.replace() to .sessions/{sid}.json",
    "LinearTicket — external state machine: Todo→InProgress→Done transitions drive the autonomy loop; ticket ID extracted from PR descriptions and session metadata; team/project IDs hardcoded in CLAUDE.md",
    "ScanResult — Pi-SEO output per project per scan_type: findings[], health_score, scan_type; stored in .harness/scan-results/{project}/{date}-{type}.json; drives triage_log Supabase writes",
    "MonitorDigest — aggregated portfolio health snapshot: per-project health scores, regression deltas, systemic cross-repo issues; stored in .harness/monitor-digests/{YYYYMMDD-HHMM}.json; 30-file retention",
    "Lesson — compounding knowledge entry: source, category, lesson text, severity; appended to .harness/lessons.jsonl; injected into session prompts via _get_lesson_context() filtered by intent",
    "PipelineState — Ship Chain state machine tracking a 6-phase idea-to-ship flow: pipeline_id, spec.md, plan.md, session_id, test_results, ship_log; persisted to .harness/pipeline/{id}/",
    "TelegramInboxMessage — inbound message schema: update_id, chat_id, text, processed bool, route (fix/brief/idea/note), route_result; stored in .harness/telegram-inbox/{update_id}.json with .offset cursor",
    "CronTrigger — scheduled task definition: id, type (scan/monitor/analyse_lessons/intel_refresh/board_meeting/scout/feedback_loop), weekday/hour/minute, enabled, last_fired_at; stored in .harness/cron-triggers.json"
  ]
}
```