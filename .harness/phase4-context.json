```json
{
  "projectPurpose": "Pi CEO is a private solo-developer agentic engineering platform that converts a plain-English brief + GitHub repo URL into an autonomous Claude Code execution session — cloning, building, evaluating, and pushing code with zero human intervention and zero API cost via Claude Max subscription.",
  "targetUsers": [
    "Solo developer (Phill / Unite Group) running autonomous engineering sessions",
    "Claude Desktop / Cowork users consuming MCP tools",
    "CI/CD systems triggering builds via GitHub/Linear webhooks"
  ],
  "businessLogic": "Core pipeline: POST /api/build → PITER intent classifier (hotfix→bug→chore→spike→feature priority) → ADW template selection → skill + lesson context injection → `claude -p` subprocess (generator, 2-attempt retry) → blocking evaluator gate (4-dimension scoring, threshold=8/10, max 2 retries with critique injection) → auto-learn failures to lessons.jsonl → git push (3-attempt backoff). Sessions are memory-backed with atomic JSON persistence; max 3 concurrent. Fan-out via /api/build/parallel decomposes briefs into N parallel workers with opus-tier escalation on failure. Webhooks (GitHub HMAC + Linear) and cron triggers provide zero-touch automation. ZTE score: 60/60 across 12 leverage points.",
  "currentState": "production-ready",
  "keyInsights": [
    "Zero API cost architecture: all Claude inference goes through `claude -p` subprocess using Claude Max subscription — calling anthropic.Anthropic() directly would incur per-token charges; this is a load-bearing architectural constraint, not an implementation detail",
    "The TAO engine in src/tao/ is largely scaffolding — BudgetTracker accumulates tokens but has no cost_usd() method, src/tao/agents/__init__.py has an AgentDispatcher but the real orchestration happens in sessions.py via claude subprocess; the engine does not call Claude API directly",
    "CLAUDE.md (app/server/brief.py CLAUDMD reference) is a near-empty stub — this is the highest single-leverage documentation gap since every future Claude session starts from it; lessons.jsonl has 20 entries doing the work CLAUDE.md should be doing",
    "Split deployment with a hard dependency gap: dashboard on Vercel calls backend at PI_CEO_URL, but `claude -p` subprocess is unavailable in Railway cloud environment (Claude Code CLI not authenticated); cloud deployments can smoke-test API routes but cannot execute real builds without ANTHROPIC_API_KEY SDK mode",
    "The evaluator threshold was raised from 7→8 in Sprint 6 (config.py EVALUATOR_THRESHOLD=8) but the eval-contract.md still documents threshold=7 — harness docs lag code by at least one sprint sprint across multiple files (spec.md, feature_list.json, sprint_plan.md all pre-date Sprint 5/6 changes)",
    "Security posture is solid for a solo tool but has a critical exposure: READY_TO_DEPLOY.md committed to git contains plaintext production secrets including TAO_PASSWORD, GITHUB_TOKEN (ghp_GFuX6sv...), and LINEAR_API_KEY (lin_api_DUkm...) — these must be rotated immediately",
    "The quality gate (_QUALITY_GATE constant in brief.py) is injected into every generator brief, mirroring the evaluator rubric — this closed-loop design (generator knows the rubric, evaluator checks against it) is the key architectural decision driving score improvements from ~7.5 to 8.5-9.5",
    "Three MCP tools return stale/missing data (get_zte_score reads leverage-audit.md correctly post-RA-469 fix, but feature_list.json reflects Sprint 3 only — F-027 and F-028 are listed pending but Sprints 4-6 features are absent from the JSON entirely)"
  ]
}
```