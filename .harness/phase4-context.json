```json
{
  "projectPurpose": "Pi CEO is a private autonomous engineering platform that converts a GitHub repo URL and plain-English brief into a full Claude Code execution session — cloning, building, evaluating, and pushing changes — serving as a 'second in charge' for a solo technical founder managing a portfolio of 11 production repos.",
  "targetUsers": [
    "Phill (sole founder, Unite Group) — non-coding operator who wants autonomous overnight engineering work across CCW-CRM, RestoreAssist, DR-NRPG and 8 other portfolio repos without writing code himself",
    "Future portfolio operators — the system is being architected toward a 'Founder OS' productisation path per board deliberation RA-686"
  ],
  "businessLogic": "The system encodes a strict ZTE (Zero Touch Engineering) scoring framework across 12 leverage points. Core rules: (1) every build follows a PITER intent classifier → ADW workflow template → tiered Claude model selection chain; (2) evaluator gates quality at 7/10 threshold with up to 2 retries before shipping; (3) autonomous scope is hard-bounded by ESCALATION.md rule 7 — the agent never amplifies its own scope without a human in the loop; (4) the autonomy poller watches Linear for Urgent/High Todo issues and auto-creates sessions, but only when LINEAR_API_KEY is present and TAO_AUTONOMY_ENABLED=1; (5) the watchdog sends Telegram alerts but the 9-item 'never autonomously' list explicitly blocks secret rotation, force-pushes, Railway env changes, and financial transfers; (6) ideas from Telegram are filed but never auto-promoted to Linear — human blesses each one.",
  "currentState": "beta",
  "bigThree": {
    "model": "Appropriate tiered model selection: Opus 4.6 for planning/orchestration, Sonnet 4.6 for generation and evaluation, Haiku 4.5 for discrete tasks. The SDK-only path (RA-576) is correct — subprocess fallback removed deliberately. Critical gap: the PoC of Anthropic's Managed Agents API (beta) failed 0/14 cycles due to auth and schema errors, so the team correctly pivoted to claude_agent_sdk. The evaluator using the same model tier as the generator (both Sonnet) is a weak point — a reviewer that shares the generator's biases will miss systematic errors; Opus as reviewer or a second independent Sonnet instance with a different system prompt would be stronger. The ZTE 73/75 score self-reported while autonomy.py was non-functional and RestoreAssist was down 76h+ is evidence the scoring model is measuring capability presence, not operational health — the CEO memo (2026-04-13) correctly identifies this as the primary measurement problem.",
    "prompt": "The skill library (28 SKILL.md files across 4 layers) and the PITER intent classifier are well-structured. The evaluator prompt enforces a strict parseable output format (COMPLETENESS/CORRECTNESS/CONCISENESS/FORMAT/OVERALL). Weak points: (1) the board meeting prompts consume Anthropic docs pages that are raw HTML/JS bundles with thousands of tokens of CSS and framework boilerplate — .harness/anthropic-docs/*.md files contain almost no extractable signal per token, severely degrading context quality; (2) the self-improvement loop is unbuilt — lessons.jsonl accumulates but no pattern extraction feeds back to CLAUDE.md or skills; (3) the complexity_tier and plan_discovery fields on BuildRequest exist but their prompting implementation is not visible in the provided code, suggesting they may be stubs.",
    "context": "The harness state management (.harness/ directory) is the strongest architectural decision — lessons, scan results, charters, board minutes, and agent specs are persisted and surfaced to agents. Critical failures: (1) the Anthropic docs fetcher saves raw rendered HTML pages as markdown — the actual useful content (API reference tables, parameter descriptions) is buried under megabytes of Next.js hydration JSON and CSS-in-JS, making these files almost useless as context; (2) sessions are in-memory dicts with disk persistence but the persistence path uses sys.path manipulation ('TAO engine imported by sessions.py via sys.path manipulation' per Dockerfile) which is fragile; (3) the per-project RAG memory layer described in TURBOQUANT-ASSESSMENT.md does not exist yet — context for CCW vs RestoreAssist vs DR-NRPG sessions is not differentiated; (4) the watchdog ran pytest in a sandbox missing anthropic>=0.90 and reported CRITICAL — the context surfaced to the watchdog (its own environment) was wrong, causing the false-positive that destroyed founder trust."
  },
  "keyInsights": [
    "The overnight failure (2026-04-11→12) was architectural, not a bug: scheduled tasks ran inside ephemeral Cowork sandboxes that died when the Mac slept, while Railway ran stale code because 7 commits were unpushed — the system was never autonomous, it was a glorified cron on Phill's laptop.",
    "ZTE 73/75 is a vanity metric in the current state: it measures capability presence, not operational health. The system scored 60/60 while autonomy.py had no LINEAR_API_KEY, producing a 'green' health check that lied about whether any work was actually being executed.",
    "The Anthropic docs fetcher (.harness/anthropic-docs/) saves raw Next.js-rendered HTML as markdown — these files are almost entirely CSS, JavaScript hydration JSON, and font-face declarations with ~2% useful API reference content, consuming context budget for near-zero signal.",
    "The claude_agent_sdk is referenced throughout CLAUDE.md, sessions.py comments, and sdk-migration-plan.md but the actual package is listed in requirements.txt as 'claude-agent-sdk>=0.1' — a package that does not appear in the official Anthropic PyPI index, suggesting this may be a placeholder or internal package that could silently fail on a clean Railway deploy.",
    "The MCP server (mcp/pi-ceo-server.js v3.0.0) exposes 21 tools including linear_create_issue and linear_update_issue, but the gap audit (2026-04-12-gap-audit.json) confirms as CRITICAL that webhook.py has zero outbound Linear API calls — the two-way sync that closes the autonomous loop does not exist in the Python backend.",
    "The business model depends entirely on one person (Phill) as both the sole operator and the sole decision-maker — ESCALATION.md, PERMISSIONS.md, and the idea inbox all route exclusively to him with no backup escalation path, creating a single point of human failure that the architecture explicitly preserves.",
    "The .claude/settings.json has defaultMode: bypassPermissions with Read(*), Write(*), Edit(*) allowed — combined with WebFetch on api.linear.app and api.anthropic.com — this is a maximally permissive agent configuration on a private repo containing real business charters and production credentials patterns.",
    "Three board meeting cycles in a row (2026-04-11, 2026-04-12 morning, 2026-04-12 evening) all listed the same Priority 1 action (activate SDK Canary Phase A) and it was never executed — the autonomous board meetings are generating correct recommendations but nothing in the architecture converts board recommendations into Linear tickets automatically, so they compound without action."
  ],
  "deploymentTopology": "Split deployment: FastAPI backend on Railway (always-on, single worker uvicorn, port 8080, auto-deploy from main branch push); Next.js dashboard on Vercel (rootDirectory: dashboard, server-side rendering); MCP server as local stdio process on Phill's Mac via Claude Desktop config. Railway container includes git + Node.js + claude CLI installed from npm. Sessions run as ephemeral Claude Agent SDK subprocesses inside the Railway container with cloned repos in app/workspaces/. Supabase (PostgreSQL + pgvector) for observability tables. No horizontal scaling — TAO_MAX_SESSIONS=3 caps concurrent builds. Mac Mini M4 Pro local stack (Ollama + n8n + Supabase cloud) approved in board deliberation but Phase 0-2 not yet shipped as of last harness state.",
  "dataModels": [
    "BuildSession — core session entity with id, repo_url, brief, model, status lifecycle, output_lines[], evaluator_score, last_completed_phase, linear_issue_id; persisted to app/logs/.sessions/{sid}.json",
    "LinearIssue — pulled from Linear API, drives autonomy poller; contains team, priority, state, and optional repo: label that routes to build sessions",
    "Telegram Update — .harness/telegram-inbox/{update_id}.json; inbound messages from Phill's phone classified into idea/fix/brief/note routes",
    "ScanResult — .harness/scan-results/{project}/{date}-{scan_type}.json; Pi-SEO findings per repo per scan type with health_score, findings[], severity breakdown",
    "MonitorDigest — .harness/monitor-digests/{YYYYMMDD-HHMM}.json; aggregated portfolio health across all projects with regression detection and cross-repo pattern analysis",
    "CronTrigger — .harness/cron-triggers.json entries with id, type, schedule, last_fired_at; 16 active triggers spanning scans/monitors/board-meetings/scout/feedback-loop",
    "Lesson — .harness/lessons.jsonl JSONL entries with source, category, severity, lesson text; auto-appended by evaluator on low-scoring dimensions, manually via /api/lessons",
    "PipelineState — .harness/pipeline/{pipeline_id}/ directory containing spec.md, plan.md, test results, ship-log; the 6-phase Ship Chain artifact store"
  ]
}
```