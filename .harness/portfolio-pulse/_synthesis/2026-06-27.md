# Cross-Portfolio Synthesis — 2026-06-27

Across the portfolio today, shipping velocity is concentrated in CARSI (50 main-branch commits, all single-author Rana) and RestoreAssist (11 commits across marketing funnel + ship-it readiness), while Disaster Recovery sat idle and Synthex landed one feature (Brand Video Studio). Total MRR holds at $7,500 across the three live revenue businesses (CARSI, RA, Synthex, $2,500 each); combined monthly burn reads $34,157, with CARSI ($13,350) and RA ($11,895) carrying the weight against thin per-unit revenue — a roughly 4.5x burn-to-MRR ratio that the Board has already flagged but is worth restating.

CI is the cross-cutting risk. CARSI logged 5 failed workflow runs on main in 24h against 24 passes — a ~17% failure rate on a solo-developer, high-throughput repo is the classic surface-treatment trap (RA-1109): velocity without a gate. Three named failures sit on recent commits (`cf66993`, `0740ae4`, `fd0986b`) and nobody is triaging because Linear is unreachable (`request_failed` on every project with a mapping). RA had one canary failure on `8f739e5`; Synthex's one failure is on the Brand Video Render workflow for the launch commit itself — the feature shipped with a red render pipeline.

Synthex PR #590 (Google Search Console "Sync from Google") has been stale 3 days — the only stale PR in the portfolio, blocking a revenue-adjacent SEO integration.

Telemetry gaps are now material: `margot`, `pi-ceo`, and `unite-group-crm` all error out with `no_repo_mapping` / `missing_projects_json_entry`. Three of seven portfolio businesses are invisible to the daily pulse. Linear API is failing on all four mapped projects — this is a single shared credential or rate-limit issue, not four separate outages.

Wins: RA shipped the marketing campaign funnel + a governance hardening (rule 18 forbidding auto-PRs into main, directly reinforcing RA-1109). CARSI's onboarding hub expansion is the kind of UX surface that compounds.

[BOARD-TRIGGER score=7 topic="Portfolio telemetry blind spots + CI failure rate"]Linear is down across all mapped projects, three businesses have no repo mapping, and CARSI is shipping at 17% CI-failure rate with no triage path — the daily pulse cannot do its job until projects.json is completed and the Linear auth is restored.[/BOARD-TRIGGER]
