# Cross-Portfolio Synthesis — 2026-06-26

Across the portfolio today, shipping velocity is healthy on the three live revenue businesses but two systemic gaps are showing through the seams: Linear is unreachable across every project (every `linear_movement` section returned `request_failed`), and three project IDs — margot, pi-ceo, unite-group-crm — still have no `repo` mapping in `.harness/projects.json`, so they generate zero signal. Combined burn across CARSI ($13.35k), RestoreAssist ($11.9k), DR ($7.58k), and Synthex ($1.34k) is $34.16k/month against $10k MRR (4 × $2.5k Stripe) — a $24k/month gap that's the dominant strategic fact in this pulse.

Wins: RestoreAssist had its strongest shipping day of the week — 9 commits to main, 47/49 CI green, and the onboarding/walkthrough video series (17 scripted + captioned) landed alongside fixes to the wizard caption loader and signup/forgot-password blank-page bug — both user-visible regressions now closed. CARSI shipped the Professional Floor Care Onboarding program (#220) plus accessibility and roadshow stabilisation, and Synthex cleanly tightened the Reddit posting identity allowlist with 35/35 green and zero failures.

Risks: CARSI's CI is the weakest signal — 6 failed runs in 24h against 15 passed (29% red rate), with failures landing on the head commit `38f0252`. RestoreAssist also failed the Pilot tester canary on the same head — both warrant a look before next deploy. Both stale-PR counts are 0, which is the one piece of unambiguous good news.

Strategic flags: the Linear outage blinds every downstream autonomy gate (RA-1973 orphan recovery, autonomy poller, board triggers) — fix the API key or network path before tomorrow's pulse or the portfolio runs blind. The missing `projects.json` mappings are a 5-minute fix that would unlock real signal from margot, pi-ceo, and unite-group-crm. Burn-to-MRR ratio is the only finding that clears the board-trigger bar.

[BOARD-TRIGGER score=8 topic="Burn vs MRR gap"]Combined $34.16k/month burn against $10k MRR across four live businesses — 3.4× coverage gap is the dominant strategic risk and warrants explicit board review of revenue acceleration vs cost compression.[/BOARD-TRIGGER]
