# Cross-Portfolio Synthesis — 2026-05-07

Across the portfolio today, throughput is concentrated in two engines while the rest of the estate either idles or fails silently. RestoreAssist shipped 50 commits to main with a 48/1 CI pass rate (sole red: pilot-tester canary on `bba6a60`) and only 2 open PRs, none stale — the schema and load-guard work (RA-1225/1226/1331/1222) is landing cleanly. Synthex matched the cadence: 50 commits, 50/50 CI green, the HERMES H-1 chain (SYN-909→911) consolidated on main, and the org-impersonation fix via `business_ownerships` (PR #209) closed a real authz hole. One open Dependabot PR.

CARSI is the active risk. 4 of 7 workflow runs failed in the last week — both CI and Deploy went red on the same commit (`e51bc52`, the specialty-courses seeder), and Deploy also failed on `1f856bb` (LMS share-prompt wiring). Three PRs are stale >2 weeks (#2, #3, #40) including the content-parity PR open since 5 March and the unified DESIGN.md foundation. Rana is shipping into a broken pipeline; merges are blocked behind unreviewed work.

Disaster Recovery shows zero activity for 7 days — no commits, no PRs, no CI. Either dormant by design or abandoned; needs an explicit call.

Cross-cutting infrastructure flags:
1. **Linear is down across every project** (`request_failed` on all 7 pulses). Ticket movement, blockers, and autonomy-poller signal are all invisible today. This is the highest-priority fix — the autonomous mandate depends on Linear being readable.
2. **Three projects have no repo mapping** in `.harness/projects.json` (margot, pi-ceo, unite-group-crm) — they're invisible to this report by construction.
3. **Finance is entirely synthetic** across the board — MRR $0, burn $0, Pi-CEO daily cost $0.00 pending RA-1909. No real-money signal yet.

Wins: RA + Synthex are the proof the autonomous pipeline works at scale. Risks: CARSI deploy pipeline + Linear outage are both blocking visibility into the rest.

[BOARD-TRIGGER score=8 topic="Linear API outage blocks autonomy signal"]All 7 project pulses returned `linear: request_failed`; ticket movement, orphan recovery, and the autonomy poller are blind until the connection is restored.[/BOARD-TRIGGER]

[BOARD-TRIGGER score=7 topic="CARSI CI/Deploy 57% failure rate with 3 stale PRs"]4 of 7 main-branch workflow runs failed last week and three PRs (incl. content-parity #2 from March) sit unreviewed — Rana is shipping into a broken pipeline.[/BOARD-TRIGGER]
