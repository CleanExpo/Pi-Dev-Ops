# Cross-Portfolio Synthesis — 2026-06-15

Across the portfolio today, RestoreAssist and Synthex are doing all the work — 19 and 50 commits to main respectively, with Synthex shipping 48/48 green on CI and RestoreAssist landing 47/49 with two notable failures. The Deepsec weekly security scan and the Pilot tester canary both failed on `106d144`; that combination on a day with heavy merging is the single most important signal in this snapshot, because either could be sitting on a real regression introduced by the RLS isolation harness wiring or the 25-route Prisma try/catch sweep.

Carsi is the opposite story: zero commits, zero CI activity, two PRs aged 30 and 19 days. PR #149 (dependency frontend/Vercel gates) has been open since May 17 — that is a month of drift on a revenue-bearing product ($2.5k MRR). PR #159's Stripe webhook dedupe is the kind of fix you do not want sitting in review while live payments flow.

Portfolio-wide observability is degraded. Linear API is returning `request_failed` on every project that has a mapping (RestoreAssist, Synthex, Carsi, Disaster-Recovery), which means the autonomy poller is silently skipping work. Separately, margot, pi-ceo, and unite-group-crm have no `.harness/projects.json` repo mapping, so they are invisible to the pulse — three businesses with zero telemetry today.

Finance reads as expected scaffolding: $7.5k MRR across three live products against $34.2k combined Xero burn (RA $11.9k, Carsi $13.4k, DR $7.6k). Synthex remains the leverage point at $1.3k burn vs $2.5k MRR.

Wins: Synthex's command-centre + autopilot drafts work, RestoreAssist's NZ-aware GST compliance across all 7 estimators, and the RLS isolation harness reaching CI. Headline risk is the Deepsec scan failure on a heavy-merge day with no Linear visibility to triage from.

[BOARD-TRIGGER score=8 topic="Deepsec security scan failure on RestoreAssist main"]Weekly security scan failed on the same SHA as a 19-commit merge day including a 25-route Prisma rewrite and new RLS harness — needs human eyes before another deploy lands on top.[/BOARD-TRIGGER]

[BOARD-TRIGGER score=7 topic="Linear API outage hides autonomy queue"]All four mapped projects returning request_failed means the autonomy poller is silently skipping Urgent/High tickets across the portfolio.[/BOARD-TRIGGER]
