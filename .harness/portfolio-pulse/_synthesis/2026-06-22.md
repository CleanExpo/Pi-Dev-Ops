# Cross-Portfolio Synthesis — 2026-06-22

Across the portfolio today, CARSI dominates the velocity picture with 50 commits to main and a heavy design-system sweep (hub redesign across courses, jobs, news, YouTube, calendar) — but the CI signal is degraded: 13 of 38 main-branch runs failed (34% red rate), including the latest hero-infographic fix at `3366953`. That failure ratio on a day of broad visual refactor is the single biggest cross-cutting risk on the board. RestoreAssist is the inverse pattern — surgical, security-led: hooks-order crashes cleared on three pages, nonce hardening, ABR config-error handling, and 29/32 green runs. The three RA failures are signal-bearing (pilot canary, Deepsec weekly, Supabase advisor) and all sit on the same commit `6754966` — likely one root cause, not three. Eight open PRs with three stale >3d (1362, 1435, 1436 — all security/E2E) are aging into risk. Synthex shipped the autonomous-worker OAuth-token fix cleanly (17/17 green), proving the Max-plan token migration pattern that should propagate to any worker still on credit-less keys.

Finance is steady: combined MRR $7,500 live across CARSI/DR/RA/Synthex, combined monthly burn $34,157. Synthex remains the margin star ($2,500 MRR vs $1,335 burn — 1.87x). CARSI burn ($13,350) and RA burn ($11,895) against $2,500 MRR each are the real GM drag — both need an NRR or pricing conversation, not a cost one.

Infrastructure gaps to close today: Pi-CEO, Margot, and unite-group-crm all return `no_repo_mapping` and `missing_projects_json_entry` — the daily pulse is blind on three properties. Linear is also failing across every project (`request_failed`), which means the autonomy queue is unobservable right now. That's a fix-first item before tomorrow's pulse.

Strategic flags: (1) CARSI CI red rate during a design refactor — investigate before more merges land on top; (2) RA stale security PRs need a triage pass; (3) `.harness/projects.json` is missing three entries and the Linear MCP is down portfolio-wide.

[BOARD-TRIGGER score=7 topic="Portfolio observability blind"]Linear MCP failing across all projects plus three repos missing `.harness/projects.json` mappings means the daily pulse and autonomy queue are partially blind — fix before tomorrow's cycle.[/BOARD-TRIGGER]
