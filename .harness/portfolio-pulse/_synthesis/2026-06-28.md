# Cross-Portfolio Synthesis — 2026-06-28

Across the portfolio today, CARSI carried the shipping load with 6 commits to main and a clean 19/0 CI sweep — roadshow signup is now wired to live CCW events, the Postgres SSL/Prisma adapter mismatch is resolved, and admin contrast plus calendar-sync retry are in. RestoreAssist sat quiet on commits but ran 34 green workflows against 1 red: the Pilot tester canary failed at `8f739e5` and needs triage today before the next pilot cohort touches it. Synthex held 40/0 green with zero deploys, but PR #590 (one-command browser connect + GSC "Sync from Google") has sat untouched since 24 Jun — five days stale on a launch-relevant integration. Disaster Recovery, Margot, Pi-CEO, and Unite Group CRM are silent: DR had no commits, and the latter three are still missing `repo` and `linear_project_id` entries in `.harness/projects.json`, so deploy/CI/PR/Linear telemetry is blind for them — a recurring observability gap flagged previously and still unfixed.

Cross-cutting risks: Linear is returning `request_failed` across every live project (CARSI, DR, RestoreAssist, Synthex), so ticket-movement signal is completely dark today — likely a token or API-side incident worth verifying before tomorrow's pulse runs. Finance is stable: live MRR $7.5k across the three earning brands (CARSI/DR/RestoreAssist/Synthex each at $2.5k), with combined monthly burn ~$34.2k (RestoreAssist $11.9k, CARSI $13.4k, DR $7.6k, Synthex $1.3k). Daily Pi-CEO LLM spend logged at $0.00 across the board — either genuine idle or the cost ledger isn't catching today's traffic; worth a sanity check.

Strategic flags: (1) burn-to-revenue is ~4.5×, headline unchanged but worth holding against the SOW pipeline; (2) the projects.json mapping gaps mute three portfolio entities from every future pulse — a 30-minute fix with outsized leverage; (3) Synthex PR #590 needs a decision — land or close.

[BOARD-TRIGGER score=7 topic="Linear API outage blinding portfolio telemetry"]Every live project returned `request_failed` on Linear today, killing ticket-movement visibility across CARSI, DR, RestoreAssist, and Synthex — verify token/API status before tomorrow's pulse.[/BOARD-TRIGGER]
