# Cross-Portfolio Synthesis — 2026-06-23

Across the portfolio today, the empire is running quiet on shipping but loud on infrastructure gaps. Zero main-branch deploys across all seven projects in the last 24h — RestoreAssist, Synthex, CARSI, and Disaster Recovery all held the line, with RestoreAssist's 24/1 CI ratio the only material green pulse (one Pilot tester canary failure at 6754966 worth a glance, not a fire).

The structural concern is observability, not velocity. Three projects — margot, pi-ceo, unite-group-crm — are completely dark: no `repo` mapping in `.harness/projects.json` and no `linear_project_id` entry, so deploys, CI, PRs, and Linear movement all return errors. Finance falls back to synthetic $0 for these, meaning the daily pulse is structurally blind to ~40% of the portfolio. Compounding this, Linear API is throwing `request_failed` across every live project (carsi, DR, RA, synthex) — the issue is the Linear connector itself, not per-project config. That's a single point of failure masking ticket movement empire-wide.

PR hygiene is the second flag. RestoreAssist carries 8 open PRs with 3 stale (#1362 from 2026-06-17, plus two security fixes #1435/#1436 from 2026-06-18 covering HTML injection in email templates and 5 TOCTOU write-path fixes for invoice payments / photo lab). Security PRs sitting six days unmerged on a live-revenue product is the sharpest risk in today's pulse. CARSI also has #149 from 2026-05-17 (37 days stale) on dependency/Vercel gates and #159 on Stripe webhook dedupe.

Finance is stable: $7,500 MRR across the three live earners (CARSI, DR, RA, Synthex all at $2,500), monthly burn $34,157 combined (RA $11,895, CARSI $13,350, DR $7,577, Synthex $1,335 — Synthex remains the margin star). Daily Pi-CEO LLM cost $0.00 across the board.

Two actions own tomorrow: merge the RestoreAssist security PRs, and fix the Linear connector + `.harness/projects.json` mappings so the pulse stops lying about half the portfolio.

[BOARD-TRIGGER score=8 topic="RA security PRs stale 6 days"]Two TOCTOU + HTML injection fixes (#1435, #1436) on the highest-revenue product have sat unmerged since 2026-06-18 — live customer exposure window is open.[/BOARD-TRIGGER]

[BOARD-TRIGGER score=7 topic="Portfolio observability blind spots"]Linear API failing across all live projects + 3 of 7 projects missing repo/Linear mappings means the daily pulse is structurally unable to detect movement on ~40% of the empire.[/BOARD-TRIGGER]
