# Cross-Portfolio Synthesis — 2026-06-29

Across the portfolio today, CARSI is the engine — 42 commits to main, 47/48 workflows green, and a single CI failure on `1d697aa` that's worth a same-day look but doesn't threaten the trunk. The day's batch shipped security hardening (Turnstile on /contact + /submit, authenticated workflow proxy routes, public chat rate-limit tightening), a pricing kill on the $44/$99 monthly tier, and UX cleanup (Learning Journey icon swap, six quick-wins merged together). One open PR (#275) targets a build-safe data layer + ISR on home/courses/sitemap — performance leverage, not stale, keep it moving.

RestoreAssist is quiet on commits but three CI canaries failed on the same SHA `8f739e5`: Pilot tester canary, Deepsec weekly security scan, and Supabase advisor gate. Same-SHA triple-failure pattern says one upstream regression, not three independent flakes — triage as one root cause, not three tickets.

Synthex has zero deploys and two PRs going stale (#590 GSC browser-connect at 5 days, #591 faceless-doodle video style at 4 days). Both are feature work that's losing momentum; merge or close this week. Disaster Recovery is dark — no commits, no CI, no PRs — acceptable for a maintenance product but worth confirming intent.

Cross-cutting infra gap: Linear is returning `request_failed` for every project that has a mapping, and four projects (margot, pi-ceo, unite-group-crm, and margot's Linear entry) are missing from `.harness/projects.json`. The pulse is flying half-blind on ticket movement. Finance signal is also distorted — three brands all reporting identical $2,500 MRR from "live" Stripe smells like a shared-customer aggregation bug, not three coincidentally-equal revenues. Worth a 30-minute audit before any board uses these numbers.

Portfolio burn: $34,157/month against $7,500 combined MRR — runway math is the real story, not today's commits.

[BOARD-TRIGGER score=7 topic="Linear API outage blinding portfolio visibility"]Every mapped project returned `request_failed` for Linear movement today; ticket-flow signal is dark across the portfolio until the API key or endpoint is fixed.[/BOARD-TRIGGER]

[BOARD-TRIGGER score=7 topic="Stripe MRR aggregation likely double-counting"]CARSI, RestoreAssist, and Synthex each report exactly $2,500 live MRR — identical figures across three distinct brands indicates a shared-account pull, not real per-brand revenue; finance reporting needs verification before next board pack.[/BOARD-TRIGGER]
