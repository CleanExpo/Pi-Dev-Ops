# Cross-Portfolio Synthesis — 2026-06-20

Across the portfolio today, Synthex carried the load with six commits to main and a clean 50/0 CI sweep, finally landing the jsdom ERR_REQUIRE_ESM cold-start fix after pinning both jsdom 27.3.0 and @asamuzakjp/css-color 4.0.5 — the autopilot crash chain is closed. RestoreAssist shipped one commit and pushed 34 green workflows, with a single Pilot tester canary failure on 1e22c6e worth a glance but not escalation. CARSI is the soft spot: only one autogit commit, a CI failure on 1728941, and PR #149 has been open and stale since 17 May — a month-old dependency/Vercel gate fix sitting unmerged is a governance smell, compounded by PR #159's Stripe webhook dedupe also going cold. Disaster-recovery was silent (zero commits, zero CI, zero PRs) — acceptable for a maintenance-mode asset but worth confirming intent.

Cross-cutting infrastructure flag: Linear is returning request_failed across every project with a mapping (CARSI, DR, RestoreAssist, Synthex). That's a portfolio-wide observability outage, not four independent blips — likely an API key, rate limit, or endpoint regression in the pulse fetcher. Without Linear movement data, the daily pulse is half-blind on ticket throughput. Separately, margot, pi-ceo, and unite-group-crm have no `repo` or `linear_project_id` in `.harness/projects.json` — those three projects are invisible to the pulse and need mappings added.

Finance: combined live MRR holds at $7,500 across the three revenue-generating brands against $33,822 combined monthly burn (CARSI $13,350, RA $11,895, DR $7,577, Synthex $1,335) — burn-to-MRR ratio remains the dominant strategic constraint, and Synthex's lean cost base continues to outperform.

Wins: Synthex autopilot stability restored, RA shipping cleanly. Risks: portfolio Linear outage, CARSI stalling, three projects unmapped. Strategic flag: the MRR:burn gap is the only number that matters this quarter — every other metric is downstream.

[BOARD-TRIGGER score=7 topic="Linear API outage across portfolio pulse"]Linear returned request_failed on every mapped project today, blinding the daily pulse to ticket movement — fix the fetcher or rotate the key before tomorrow's run.[/BOARD-TRIGGER]
