# Cross-Portfolio Synthesis — 2026-06-16

Across the portfolio today, shipping velocity is concentrated in RestoreAssist and Synthex while the rest of the estate is quiet or instrumentation-blind. RestoreAssist landed 14 commits to main with 48/49 CI green — the V2 Sketch workflow advanced through server-authoritative geometry, provenance guard extension, and persisted moisture pins (RA-6761/6763), with the e2e harness (RA-6764) now in draft PR #1353. The single red run is a Pilot tester canary on `259f521`; worth a glance but not a blocker given the surrounding green wall. Synthex pushed 29 commits and shipped SYN-1029 (outcome-first AI workbench), SYN-1033 (auto-research → governed opportunities bridge), and the Mission Control status manifest — but 4 CI failures clustered on `f20c0cf` and `4713e47` (CI + Deploy both red on the same SHA) suggest the manifest exposure changes broke something downstream. Needs eyes today before the next merge stacks on a red baseline.

Cross-cutting risk: Linear is returning `request_failed` on every project that has a mapping, and four projects (margot, pi-ceo, unite-group-crm) are missing `repo` and `linear_project_id` entries in `.harness/projects.json` entirely. The daily pulse is running half-blind — no ticket movement visibility, no risk surfacing (RA-1892 still unwired across all seven projects). That's a meta-issue: the chief-of-staff brief itself is degraded until projects.json is completed and the Linear API auth is restored.

Finance is steady — three live MRR streams at $2.5k each ($7.5k total), CARSI burn ($13.4k) running hottest against its revenue, Synthex the leanest ($1.3k burn vs $2.5k MRR — only cash-positive entity). Pi-CEO daily cost holding at $0.20 across all projects (synthetic floor, not real signal yet).

Strategic flag: the CARSI PR backlog (#149 stale 30 days, #159 stale 19 days) plus zero CI activity in 24h reads as a stalled project against $13.4k/mo burn. Either re-engage or formally pause.

[BOARD-TRIGGER score=7 topic="CARSI burn vs zero velocity"]CARSI burning $13.4k/mo against $2.5k MRR with stale PRs and zero deploys/CI in 24h — decide re-engage or pause this week.[/BOARD-TRIGGER]

[BOARD-TRIGGER score=7 topic="Pulse instrumentation half-blind"]Linear API failing on all mapped projects and 4 projects unmapped in projects.json — the daily brief can't surface ticket risk until fixed.[/BOARD-TRIGGER]
