# Cross-Portfolio Synthesis — 2026-05-05

Across the portfolio today, two repos are doing real work and the rest are either quiet or invisible to the pulse. Synthex shipped 11 commits to main with a clean 50/0 CI sweep — SYN-904/905/906 closed the React.Children.only crash chain in BusinessSwitcher and the sidebar, wired client error boundaries to trackError, and finally booted Sentry for real. SYN-731 extended the Monday CVML scorecard with Journey Moments. No open PRs, no stale work — Synthex is the cleanest surface in the portfolio right now.

RestoreAssist is the opposite signal. Two commits landed for the App Review 1.0(7) push (RA-1842 BillingGate wrap + 3.1.1 CTA strip / 4.2 native features), but CI went 2 passed / 3 failed, and the failures are the wrong ones: both DigitalOcean App Platform deploys failed (on `3feb051` and `2ab2490`), plus the Pilot tester canary on `3feb051`. That means the App Review hardening commits are not actually deployed. PR #885 (Capacitor SPM manifest regen) is open from today and presumably blocks the next clean build.

CARSI is drifting: 5 open PRs, 3 stale >3 days, oldest (#2 content parity) untouched since 2026-04-20. No CI activity in 24h. Disaster Recovery is silent — neither good nor bad, but worth a poke given launch posture.

Cross-cutting infrastructure flags: Linear is returning `request_failed` on every project that does map (carsi, disaster-recovery, restoreassist, synthex) — the API key or rate limit is degraded and ticket movement is invisible across the whole portfolio. Separately, four projects (margot, pi-ceo, unite-group-crm, and partial coverage on others) have no `repo` / `linear_project_id` mapping in `.harness/projects.json` — the pulse is structurally blind to them. Finance is uniformly $0 synthetic until RA-1909 wires real Stripe/Xero.

Priority order: unblock the RestoreAssist DO deploys, fix the Linear API auth, then patch the projects.json gaps.

[BOARD-TRIGGER score=8 topic="RestoreAssist App Review deploy failure"]Two consecutive DigitalOcean deploy failures plus a canary failure on the 1.0(7) App Review hardening commits mean the BillingGate + 3.1.1 CTA strip work is not in production despite green code merges — direct App Store submission risk.[/BOARD-TRIGGER]
