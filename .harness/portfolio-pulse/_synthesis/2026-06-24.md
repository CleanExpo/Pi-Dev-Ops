# Cross-Portfolio Synthesis — 2026-06-24

Across the portfolio today, shipping velocity is strong but the supporting telemetry is degraded — and that gap is the real story.

**Wins.** RestoreAssist landed a hardened new-client onboarding gate plus the RA-6800 ownership-scoping series (atomic org/owner scoping on team + admin writes, real-DB integration tests, inspection-write scoping) — a meaningful security posture lift, 23 main commits, 49/50 CI green, one open dep-bump PR. Synthex shipped 14 commits with a perfect 49/0 CI run, including the IDOR regression test for `/api/quotes/[id]` (UNI-558), a SEO honesty fix that returns `DATA_REQUIRED` instead of fabricated GSC numbers, and a cross-platform PreToolUse Bash hook rewrite. CARSI cleared a cluster of customer-facing breakages: IICRC 504 timeout, manual renewal send failures, a service worker that was eating admin/API fetches, and admin learner directory now reading live LMS enrollment.

**Risks.** CARSI's CI is the one number that doesn't match the narrative: 7/43 failed runs on main in 24h, with three failures on the freshest IICRC/service-worker fixes — those look like the "fix succeeded, test infra didn't" pattern, but it needs a real read. Disaster-recovery shows zero deploy and zero CI activity — confirm intentional, not a stalled feed. Linear is `request_failed` across every live project (CARSI, DR, RestoreAssist, Synthex), so ticket movement is invisible — the API key or MCP route is down and needs rotation/health-check today. Four "projects" (margot, pi-ceo, unite-group-crm, and Linear for the rest) are missing `.harness/projects.json` mappings — the pulse is flying blind on half the empire.

**Finance.** $7,500 live MRR (CARSI + DR + RA + Synthex at $2,500 each). Burn: RA $11,895, CARSI $13,350, DR $7,577, Synthex $1,335 = ~$34k/mo against $7.5k MRR. Daily Pi-CEO LLM cost $0.00 — either Max-plan routing is working as intended or the ledger isn't writing; verify.

**Strategic flag.** Three observability gaps stack: Linear API down, `projects.json` half-populated, RA-1892 risks not wired. The portfolio is shipping faster than it can be watched.

[BOARD-TRIGGER score=7 topic="Linear API + projects.json observability gap"]Linear is failing across all four live projects and four project_ids have no repo/Linear mapping, leaving the daily pulse blind to ticket movement and half the portfolio's signals — fix today before more autonomous loops fire against stale telemetry.[/BOARD-TRIGGER]
