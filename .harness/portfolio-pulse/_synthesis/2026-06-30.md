# Cross-Portfolio Synthesis — 2026-06-30

Across the portfolio today, the operating cadence is strong on the revenue-generating units and degraded on the infrastructure that's supposed to watch them. CARSI shipped 17 commits to main with a clean 50/0 CI sweep — notably a Google Calendar timeout fix and the #137 course-publication canonicalisation, retiring a stale read path. RestoreAssist matched the tempo with 17 commits and a major standards milestone: a verified S500-2021 section index plus 10-persona STORM regression coverage, with only one CI failure (Pilot tester canary on `52529e9`) needing triage. Synthex was quiet on main but is carrying two stale PRs — #590 (GSC one-command connect, 7 days old) and #591 (faceless-doodle video style, 6 days old) — both past the 3-day threshold and blocking Command Center Phase 1.

The cross-cutting risk is observability collapse. Linear is returning `request_failed` on every project that has a mapping (carsi, disaster-recovery, restoreassist, synthex), which means the autonomy poller is blind to Urgent/High tickets across the entire active portfolio. Combined with `.harness/projects.json` missing entries for margot, pi-ceo, and unite-group-crm — all reporting synthetic $0 finance and no_repo_mapping for GitHub — the pulse is structurally incomplete. RA-1892 risk wiring is also still a placeholder across all projects.

Finance reads steady: $7,500 total live MRR (CARSI + DR + RA each $2,500), Synthex at $2,500 with the lowest burn ($1,335) — the cleanest unit-economics story in the portfolio. CARSI ($13,350) and RestoreAssist ($11,895) burn rates remain the cash-drain centres. Disaster-recovery had no commits in 24h, which on a $7,577/mo burn warrants a velocity check.

Strategic flags: (1) fix the Linear API connection before tomorrow's pulse — without it the senior-agent bots and autonomy queue are flying blind; (2) merge or close Synthex #590/#591 to unblock Command Center; (3) add `linear_project_id` + `repo` mappings for margot, pi-ceo, and unite-group-crm to `.harness/projects.json` so they stop reporting synthetic data.

[BOARD-TRIGGER score=8 topic="Linear API outage blinding autonomy across 4 active projects"]Every mapped project returned `request_failed` on linear_movement today, meaning the Urgent/High poller, senior-agent ledgers, and ticket routing are all degraded simultaneously — this is a single point of failure for the autonomous operating model.[/BOARD-TRIGGER]
