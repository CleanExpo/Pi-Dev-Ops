# Cross-Portfolio Synthesis — 2026-06-21

Across the portfolio today, Synthex is the only project actually shipping — 16 commits to main and 49 green CI runs, all from the SYN-1031 marketing-agency outcome-framing push — while RestoreAssist absorbs the operational weight with 28 passed / 1 failed runs and a backlog of seven open PRs, three of them stale security fixes (HTML injection in email templates, five TOCTOU write-path issues on invoice/template/photo paths, and a smoke-prod auth-resilience patch). Those three sitting untouched since 17-18 June is the sharpest cross-cutting risk: security work parked behind a stalled paywall-resilience PR with no merge motion.

CARSI is quiet but carrying two stale PRs of its own (#149 from May 17, #159 dedupe Stripe webhooks from May 29) — both regression-class, both ageing into irrelevance. Disaster-Recovery is fully idle: zero commits, zero CI, zero PRs against an $7,577 monthly burn — the worst burn-to-output ratio in the portfolio.

Telemetry itself is degraded. Linear movement returned `request_failed` on every live project (CARSI, DR, RestoreAssist, Synthex), which means today's pulse is blind to ticket flow — likely the same API-key or auth issue across all four. Margot, Pi-CEO, and unite-group-crm have no `repo` mapping in `.harness/projects.json`, so their deploy/CI/PR sections fail with `no_repo_mapping` — a one-line config fix that's been outstanding long enough to corrupt the daily.

Financially, live MRR holds at $2,500 across CARSI, DR, and RestoreAssist ($7,500 total) against ~$32,822 combined monthly burn — a 4.4× burn multiple. Synthex is the standout on that axis too: $2,500 MRR on $1,335 burn, the only cash-positive line.

Strategic flags: (1) merge or close the RestoreAssist security PRs this week, (2) fix the Linear API auth so tomorrow's pulse isn't blind, (3) add the three missing `projects.json` mappings, (4) decide what DR is for at $7.5k/month with no shipping activity.

[BOARD-TRIGGER score=8 topic="RestoreAssist stale security PRs"]Three security fixes (HTML injection, 5x TOCTOU write-path, smoke-prod auth) sitting untouched 3-5 days while the canary CI run failed — merge-or-close decision needed this week.[/BOARD-TRIGGER]

[BOARD-TRIGGER score=7 topic="Portfolio telemetry degraded"]Linear request_failed on all 4 live projects plus 3 missing projects.json mappings means the daily pulse is half-blind; one-day fix, but every day it slips compounds.[/BOARD-TRIGGER]
