# Cross-Portfolio Synthesis — 2026-06-19

Across the portfolio today, the picture is one of uneven telemetry and a quiet revenue floor that masks two real operational drags. Combined live MRR sits at $10K ($2.5K each from CARSI, Disaster Recovery, RestoreAssist, and Synthex), against ~$34.2K of monthly burn across the four reporting entities — a net cash drag of roughly $24K/month, dominated by CARSI ($13.4K) and RestoreAssist ($11.9K). Synthex remains the efficiency standout at $1.3K burn on $2.5K MRR — the only business currently running gross-positive on cash.

CARSI is the active problem child: two CI runs failed on main in the last 24h (commits db5933d and 8b0e48f, both Phill-authored membership/journey work), and PR #149 has now been stale 33 days — a dependency/Vercel-gate fix that is blocking the frontend pipeline. PR #159 (Stripe webhook dedupe) has also been stale 22 days. RestoreAssist shipped clean (20/1 CI pass ratio) but the lone failure was the Pilot tester canary at 8edc1c5 — that's a customer-facing signal, not a unit test, and warrants a look before the next pilot cohort. RestoreAssist also carries 7 open PRs with the oldest only 2 days old, so flow is healthy.

Disaster Recovery and Synthex were quiet (no main commits). Linear movement is unreadable across every project — the API returned request_failed on all six attempts, which is a portfolio-wide observability outage, not a per-project blip. Margot, Pi-CEO, and Unite-Group-CRM remain unmapped in `.harness/projects.json` (no `repo` or `linear_project_id`), so they contribute nothing to the pulse — a foundational fix that's been deferred long enough to now be hiding real state.

Strategic flag: the RA-1892 risk wiring is still placeholder across all seven projects. Until it lands, this pulse is reporting symptoms, not risks.

[BOARD-TRIGGER score=7 topic="CARSI CI red + stale PRs blocking frontend"]CARSI is bleeding the most cash ($13.4K/mo burn on $2.5K MRR) while its frontend pipeline has been gated by a 33-day-stale dependency PR and two fresh CI failures on main — ship-velocity is structurally stalled on the highest-burn business.[/BOARD-TRIGGER]
