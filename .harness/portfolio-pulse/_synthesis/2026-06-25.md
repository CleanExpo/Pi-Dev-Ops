# Cross-Portfolio Synthesis — 2026-06-25

Across the portfolio today, shipping velocity is strong on the three revenue-generating products (CARSI, RestoreAssist, Synthex) but the observability layer is partially blind — Linear is down across every project and three projects (margot, pi-ceo, unite-group-crm) are still missing `.harness/projects.json` entries, so risk surfacing is degraded.

**Wins.** Synthex had a perfect CI week: 49 passed, 0 failed across 37 commits to main, including a real-data correctness fix (stopping fabricated GSC ranking numbers — the right call, matches RA-1109 surface-treatment doctrine) and a cross-user authz regression test for the IDOR on `/api/quotes/[id]`. RestoreAssist shipped 42 commits with a 45/3 pass rate, including the test-parity guard that ends "green locally, red in CI" — that's infrastructure leverage that compounds. CARSI shipped 50 commits and added webkit accessibility coverage.

**Risks.** CARSI's CI is the weakest of the three actives: 9 failures in 7 days (18% fail rate) with three recent reds on the homepage overflow and roadshow work — the accessibility job is flaky enough to be noise, which trains the team to ignore reds. RestoreAssist has one open PR (#1487) explicitly tagged "unblocks main" — design-lint is gating merges, needs to land today. Disaster-recovery had zero commits and zero CI activity in 7 days while still burning $7,577/month — that's $1,800/week of dead capital with no shipping signal.

**Strategic flags.** Combined live MRR is $7,500 against ~$34k combined burn (CARSI $13,350 + RA $11,895 + Synthex $1,335 + DR $7,577) — Synthex is the only product close to unit economics. DR's burn/output ratio is the standout anomaly. Linear API failure across all four live projects means autonomy poller is likely silently skipping ticks — check `/api/autonomy/status` and the RA-1973 watchdog telemetry.

**Action today:** land RA #1487 to unblock main; investigate the Linear API outage (single root cause across all four); decide whether DR gets a shipping mandate or a wind-down.

[BOARD-TRIGGER score=7 topic="DR burn vs zero output"]Disaster-recovery burned $7,577 this month with zero commits, zero CI runs, zero PRs — decision needed on mandate or wind-down before next month's burn.[/BOARD-TRIGGER]
