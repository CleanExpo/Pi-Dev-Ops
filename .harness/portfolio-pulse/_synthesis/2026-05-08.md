# Cross-Portfolio Synthesis — 2026-05-08

Across the portfolio today, two repos showed real motion and the rest were silent — but the silence is mostly instrumentation gaps, not genuine quiet.

**RestoreAssist is the headline risk.** Five commits landed on main, including the RA-2073 iOS OAuth fix and a 1.0.1 → 1.0.2(12) version bump to unblock the sign-in loop. But CI tells a worse story: 17 passed / **26 failed** in the last 24h, with the three most recent failures all being `Smoke — Production` runs at the very commit (`bd356ae`) that was meant to fix sign-in. A version-train bump shipping with red production smoke is exactly the surface-treatment trap RA-1109 was written to prevent — the fix may be unverified on device.

**Synthex shipped cleanly.** Seven commits, 27 passed / 7 failed CI, AI-commentary migrated off Anthropic onto Gemini 3.1 Pro Preview (SYN-935 → 938 → 939 → 942), with a single in-flight PR (#216) tightening `thinkingConfig` + token budget. The deploy failures appear to be the iteration tail before SYN-942 stabilised — worth a glance but not alarming.

**CARSI is decaying.** Zero CI activity, zero deploys, but **5 open PRs with 3 stale >3 days** — including PR #2 (content parity, 16 pages) untouched since 2026-04-20 and PR #40 (DESIGN.md foundation) similarly cold. This is review debt, not build debt; it blocks the design-spec convention rollout.

**Disaster Recovery is genuinely idle** (zero everything), which is fine if intended.

**Instrumentation gaps to fix today:** `margot`, `pi-ceo`, and `unite-group-crm` are missing `repo` and `linear_project_id` entries in `.harness/projects.json`. Linear is returning `request_failed` across every project that IS mapped — likely an auth or API-key issue affecting the whole pulse. Finance is uniformly synthetic ($0 everywhere) pending RA-1909.

[BOARD-TRIGGER score=8 topic="RestoreAssist 1.0.2 ships with red production smoke"]Five main-branch commits including iOS OAuth sign-in fix and version bump, but 26 CI failures in 24h with the three most recent being Smoke—Production at the fix commit itself — high risk the user-visible auth loop is still broken on the live build.[/BOARD-TRIGGER]
