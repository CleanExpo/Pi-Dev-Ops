# Cross-Portfolio Synthesis — 2026-06-18

Across the portfolio today, ship velocity is strong but the operational telemetry layer is leaking — and that's the headline. Three live products (CARSI, Synthex, RestoreAssist) pushed 50 main-branch commits each in the last seven days, with CARSI and Synthex holding 100% green CI and RestoreAssist landing 38 passes against 8 failures, including two repeated Smoke-Production runs on the same SHA (`d11789b`) plus a fresh Pilot Tester canary failure on `8edc1c5`. That's a smoke-prod regression pattern, not a flake — RA-1109's surface-treatment rule says a green deploy with a red smoke is not shipped.

Disaster Recovery went dark this week: zero commits, zero CI runs, but $7,577/month still burning at Xero against $2,500 MRR. That's the worst unit economics in the portfolio and it's drifting without a builder on it.

The instrumentation gap is the cross-cutting risk. Linear movement returned `request_failed` for every live project (CARSI, DR, RestoreAssist, Synthex) — the Linear MCP or API key is down across the board, blinding autonomy.py's poller and the daily pulse simultaneously. Separately, four project_ids (margot, pi-ceo, unite-group-crm, plus the disaster-recovery duplicate pattern) have no `repo` mapping in `.harness/projects.json`, so they report synthetic zeros for MRR and burn — Margot and Pi-CEO are operationally invisible to the chief-of-staff layer.

Wins: CARSI shipped a focused SEO/schema-hardening sprint (carousel schema, course workload, privacy cache, semrush surface) — disciplined surgical work. Synthex closed the v1 backlog reconciliation and shipped UGC moderation + creator roster (SYN #6). RestoreAssist's idempotent report creation (RA-6799 AC2) closes an orphan-record class of bug.

Portfolio MRR sits at $7,500 against ~$34k combined burn across the three reporting businesses — 4.5x burn-to-revenue ratio, dominated by CARSI ($13,350) and RestoreAssist ($11,895). Synthex is the efficiency leader at $1,335 burn on $2,500 MRR.

Strategic flag: the Linear-API outage and the projects.json gaps together mean autonomous routing is degraded right now. Fix telemetry before queuing more autonomous work.

[BOARD-TRIGGER score=7 topic="Linear API outage + projects.json gaps blinding autonomy"]Linear request_failed across all four live projects plus missing repo mappings for margot/pi-ceo/unite-group-crm means autonomy.py polling and daily pulse are both flying blind — fix before queuing more agent work.[/BOARD-TRIGGER]
