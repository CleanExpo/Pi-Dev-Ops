# Cross-Portfolio Synthesis — 2026-07-01

Across the portfolio today, the pulse is dark — not because the businesses are dark, but because the instrumentation is. Every one of the seven projects (carsi, disaster-recovery, margot, pi-ceo, restoreassist, synthex, unite-group-crm) returned identical placeholder briefings: no GitHub token, no Linear API key, synthetic Stripe/Xero data reporting $0 MRR and $0 burn, and RA-1892 (the real-risk feed) still unwired. This is not a portfolio status; it is a status-collection failure masquerading as a report.

The cross-cutting risk is singular and severe: the daily executive pulse is running blind. GitHub deploy/CI/PR visibility is off across all seven projects, Linear movement is invisible across all seven, and finance is synthetic across all seven. That means real deploy failures, stalled tickets, broken CI, and cash movement would all render as "quiet day" in this brief — indistinguishable from a genuinely calm one. For a portfolio operating under an autonomous-to-green mandate, a silent oracle is worse than no oracle.

The wins column is empty by construction — nothing can be reported as won when nothing can be observed. The strategic flag is that the pulse-generator itself is the highest-leverage fix in the portfolio right now: three missing credentials (GITHUB_TOKEN, LINEAR_API_KEY, and the RA-1892 real-finance wiring) unlock accurate reporting across all seven businesses simultaneously. The GitHub token and Linear key are already documented as required in Pi-Dev-Ops CLAUDE.md and .harness/projects.json; provisioning them into the pulse-generator's environment is a config change, not a build.

Recommended next action: file a Pi-Dev-Ops ticket to inject GITHUB_TOKEN + LINEAR_API_KEY into the daily-pulse runtime (Railway env or the runner that produced this brief) and confirm RA-1892's finance provider swap from `synthetic` to `stripe_xero` per the CFO provider spec. Until that lands, treat every future daily pulse as unverified until the "no_token / no_api_key / synthetic" markers disappear.

[BOARD-TRIGGER score=8 topic="Daily portfolio pulse is running blind across all 7 businesses"]Every project reports placeholder data (no GitHub token, no Linear key, synthetic finance) — the executive oracle cannot distinguish a calm day from a crisis, which is a governance-grade failure for an autonomous portfolio.[/BOARD-TRIGGER]
