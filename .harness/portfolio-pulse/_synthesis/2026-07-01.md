# Cross-Portfolio Synthesis — 2026-07-01

Across the portfolio today, shipping velocity is strong but observability is degrading in ways that will bite if left unattended. RestoreAssist landed 45 commits to main and Synthex 7, with CARSI pushing 15 including the Maintenance course publish, the IICRC-grounded contact-reply Phase 2, and the assistant "not authoritative" reframing — a clean product-safety upgrade that reduces liability exposure on the AI assistant surface. CI is healthy in aggregate (141 passes vs 3 failures across the three live repos), but each failure deserves a look: the RestoreAssist Pilot tester canary at 4b7ea0a is the most concerning signal because canaries are the early-warning layer, a Synthex Dependabot js-yaml update failed, and CARSI has one CI red on the contact-reply commit itself.

The apiError envelope migration on RestoreAssist (RA-1548) is now through batch 41 in a single day — disciplined, mechanical, on track — with PR #1570 the only open item and no staleness. Synthex carries PR #590 stale since 24 June on the GSC "Sync from Google" browser flow; that's a week of drift on a customer-visible integration and should get unblocked or closed.

Cross-cutting infrastructure risk: Linear API is failing across every live project (request_failed on carsi, disaster-recovery, restoreassist, synthex), which means autonomy polling, ticket routing, and the daily-brief board tickets are all blind right now. Combined with margot, pi-ceo, and unite-group-crm all missing `.harness/projects.json` mappings, roughly half the portfolio has no telemetry surface at all. Finance is stable — $7,500 MRR against ~$34.2k combined monthly burn across CARSI, DR, RestoreAssist, Synthex — but the runway math depends on the observability layer we've just lost visibility into.

Disaster-recovery shipped nothing in 24h, which is expected but worth flagging against its $7,577 burn.

[BOARD-TRIGGER score=7 topic="Linear API outage blinding autonomy loop"]Every live project's Linear section returned request_failed today, which silently disables ticket routing, autonomy polling, and daily-brief board triggers portfolio-wide — needs root-caused before tomorrow's cycle.[/BOARD-TRIGGER]
