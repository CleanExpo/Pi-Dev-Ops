# Cross-Portfolio Synthesis — 2026-07-01

Across the portfolio today, execution velocity is strong on the revenue-generating fronts and observability itself is the biggest strategic gap. RestoreAssist shipped 50 commits to main with a clean 50/0 CI run, driving the `apiError` envelope migration through inspections (batches 16-20, RA-1548) — one open PR (#1549) is same-day, no staleness. CARSI matched pace with 50 commits and 48/1 CI, launching Phase 1-2 of the IICRC-grounded assistant (disclaimer framing, no-verbatim) plus the contact-reply drafter; the single failed run on `f6e576f` is the only red flag on that repo and warrants a quick triage. Synthex delivered 11 commits including the /data-deletion page required for Meta App Review — a launch-critical unblock — but is carrying a Dependabot `js-yaml` failure and a stale PR (#590, 7 days) on the GSC one-command connect flow.

Financially, the three live businesses (CARSI, RestoreAssist, Synthex) each hold $2,500 MRR. Synthex's burn is remarkably low at $1,335/mo against $2,500 MRR — gross-margin positive. CARSI ($13,350) and RestoreAssist ($11,895) remain the cash consumers; DR sits idle at $7,577 burn with zero commits in 7 days and $2,500 MRR, which is the portfolio's worst runway ratio.

Strategic flags: (1) Linear API is failing across every project (`request_failed`) — ticket movement, autonomy queue depth, and blocker visibility are all dark; this is a P1 observability outage, not a foundation gap. (2) Four projects (margot, pi-ceo, unite-group-crm, and DR partially) have no `.harness/projects.json` repo/linear mappings — the daily pulse is blind to half the empire. (3) DR's zero-commit week against $7,577 burn is a capital-allocation question, not an execution one. (4) RA-1892 risk-scoring is still unwired everywhere, so the "no risks" line is placeholder, not signal.

Wins to bank: Synthex Meta App Review unblocked, CARSI assistant IICRC-grounded, RestoreAssist envelope migration ~80% through inspections domain.

[BOARD-TRIGGER score=8 topic="Linear API dark across portfolio + missing project mappings"]Ticket movement, autonomy queue, and risk scoring are all invisible today across every project — daily pulse is running half-blind and RA-1892 remains unwired.[/BOARD-TRIGGER]
