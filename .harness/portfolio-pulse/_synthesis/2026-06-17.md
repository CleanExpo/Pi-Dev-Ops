# Cross-Portfolio Synthesis — 2026-06-17

Across the portfolio today, ship velocity is genuinely strong — 95 commits to main across CARSI (45), Synthex (30), and RestoreAssist (20) — but two structural failures are eating into the signal: Linear is down across every project (`request_failed` on all four wired repos), and three projects (margot, pi-ceo, unite-group-crm) still have no `repo` mapping in `.harness/projects.json`, so they're invisible to the pulse. Fix the projects.json gap today; it's a 5-minute edit blocking weeks of observability.

CARSI is the headline win: the CCW roadshow checkout chain landed (Stripe webhook → idempotent booking processor → confirmation email helper → success-page send), shipped clean across ~5 sequential commits with 29/30 CI runs green. Two stale PRs (#149 from 2026-05-17, #159 from 2026-05-29) need triage or close — month-old open PRs are a smell.

RestoreAssist is the risk: 14 CI failures against 35 passes (29% failure rate), all clustered on Smoke — Production at `d11789b`. Three repeated smoke-prod failures on the same commit means production verification is broken, not flaky. This is an RA-1109 surface-treatment violation in spirit — green merges, red smoke, nobody stopping the line. The North Star capstone (live-teacher wet-vs-dry coaching, #1368) shipped under this — unverified in prod.

Synthex is the cleanest signal: 30 commits, 38/38 CI green, OOM build fix (exit 137 on `next build --webpack`) and SYN-1040 workflow→calendar slot mapper both landed. Lowest burn ($1,335) on equivalent $2,500 MRR — best unit economics in the portfolio.

Finance: $7,500 combined live MRR (CARSI + DR + RA + Synthex) against $34,157 combined burn. Disaster-recovery had zero commits in 24h on $7,577/mo burn — that ratio needs a product decision, not a dev decision.

[BOARD-TRIGGER score=8 topic="RestoreAssist prod smoke broken"]Three consecutive Smoke — Production failures on `d11789b` while 20 commits (including North Star live-teacher capstone) merged on top — production verification is offline and the team is shipping blind.[/BOARD-TRIGGER]
