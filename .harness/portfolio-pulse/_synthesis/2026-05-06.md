# Cross-Portfolio Synthesis — 2026-05-06

Across the portfolio today, the engine room is RestoreAssist and Synthex while three projects are silently broken at the observability layer. RestoreAssist shipped 13 commits to main with 32 green CI runs, but three failures stand out: the Pilot tester canary at `4fa0ae5` and two consecutive DigitalOcean App Platform deploys (`17757ab`, `ff23511`). Two failed prod deploys in a row on the same workflow is not noise — it's a deploy-pipeline regression that needs eyes before the next release train. Synthex had a clean run: 5 commits, 31/31 green, HERMES H-1 schema landed (HER-1a through 1e consolidated), plus the SYN-911 org-impersonation fix via `business_ownerships` — a real security tightening, not cosmetic.

CARSI is the clearest drag. Five open PRs, three stale >3 days, with #2 untouched since 20 April — content parity work rotting on the vine for over two weeks. Either merge or close; leaving 16 pages of WordPress-image migration in limbo costs more than either decision.

Linear telemetry is broken across every project (`request_failed`) — likely an API key or rate-limit issue at the harness level, not seven independent failures. This blinds the autonomy poller from picking up Urgent/High Todo work, which is the entire premise of the always-on loop. Fix this before anything else.

Three projects (margot, pi-ceo, unite-group-crm) have no `repo` mapping in `.harness/projects.json` and no `linear_project_id` — they're effectively invisible to the daily pulse. Either wire them up or delete them from the project list; ghost entries inflate the appearance of coverage.

Finance is uniformly synthetic ($0 MRR, $0 burn) — RA-1909 still pending means we have no real cost telemetry on the senior-bot CFO loop yet. Disaster-recovery is genuinely quiet (zero commits, zero PRs, zero CI) — confirm it's intentionally dormant rather than abandoned.

Top priority today: the DO deploy failures and the Linear API outage. Both block autonomy.

[BOARD-TRIGGER score=7 topic="Linear API outage blinding autonomy poller"]Six of seven projects report `linear: request_failed`, which means the Urgent/High Todo poller cannot fire — the always-on autonomous mandate is offline until this is restored.[/BOARD-TRIGGER]
