```json
{
  "zteLevel": 3,
  "zteScore": 41,
  "leveragePoints": [
    {
      "id": 1,
      "name": "Spec Quality",
      "score": 4,
      "rationale": "PITER classifier + ADW templates wired (CLAUDE.md line 74, config.yaml agents block). spec.md confirmed stale by CEO memo 2026-04-13 (RA-675/RA-685). Board minutes 2026-04-12 note '2 days stale'. Deducted 1 point for spec staleness recurrence."
    },
    {
      "id": 2,
      "name": "Context Precision",
      "score": 4,
      "rationale": "_get_lesson_context() injects lessons per-intent (board-meetings/2026-04-08). TurboQuant assessment documents absence of per-project RAG memory layer — TURBOQUANT-ASSESSMENT.md explicitly states 'the per-project memory layer' has not been built. lessons.jsonl at 18-38 entries injected but no retrieval step scopes context to the task's project. Deducted 1."
    },
    {
      "id": 3,
      "name": "Model Selection",
      "score": 5,
      "rationale": "config.yaml lines define opus/sonnet/haiku tiers with temperature and sdk flags. CLAUDE.md table shows Opus 4.6 orchestrator, Sonnet 4.6 specialist, Haiku 4.5 worker. autonomy_budget anchors in config.yaml map budget windows to model tiers. Fully automated."
    },
    {
      "id": 4,
      "name": "Tool Availability",
      "score": 4,
      "rationale": "21 MCP tools documented (CLAUDE.md), .claude/settings.json bypassPermissions with explicit allow list. Fan-out parallelism (RA-464), opus escalation, Telegram bot, n8n workflows. However get_zte_score returning 'No ZTE data found' in 2026-04-08 board minutes, feature_list.json absent at same meeting = 3/5 MCP tools broken. Deducted 1 for persistent MCP data layer gaps."
    },
    {
      "id": 5,
      "name": "Feedback Loops",
      "score": 3,
      "rationale": "Evaluator closed-loop retry exists (sessions.py RA-571/572). BUT: SDK canary Phase A at 0% traffic across 3+ consecutive board cycles — dual-path is dead code (board 2026-04-12 Phase 4 Priority 1). Linear API returning 401 for 2 consecutive cycles (board 2026-04-11, 2026-04-12 Phase 2) — board meetings running blind. evaluate gate exists but the feedback signal from production is absent. Deducted 2."
    },
    {
      "id": 6,
      "name": "Error Recovery",
      "score": 4,
      "rationale": "3-attempt clone backoff, generator retry, phase checkpoints, POST /api/sessions/{sid}/resume (contracts/build-contract.md). _resilient() wrapper on all background tasks (main.py). Watchdog false-positive (INCIDENT-2026-04-12.md) demonstrates environment-dependency in error classification. Deducted 1 for false-positive escalation pattern not yet resolved."
    },
    {
      "id": 7,
      "name": "Session Continuity",
      "score": 4,
      "rationale": "Phase-level checkpoints + resume endpoint confirmed (build-contract.md, sessions.py last_completed_phase field). os.replace() atomic writes documented (CLAUDE.md persistence guidelines). _sessions is in-memory — server restart loses state (CLAUDE.md: 'any server restart loses all running sessions'). Railway deploy resets cron last_fired_at (SWOT threat in 2026-04-12 board). Deducted 1."
    },
    {
      "id": 8,
      "name": "Quality Gating",
      "score": 4,
      "rationale": "Blocking evaluator gate with 2 retries (config.yaml qa.max_rounds:3, eval-contract.md). RA-674 confidence-weighted thresholds in config.yaml. Supabase gate_checks table (RA-651). Gap audit JSON (2026-04-12-gap-audit.json) found 3 critical discrepancies between spec claims and actual code — SSE streaming endpoint claimed but not present, Linear two-way sync claimed but not implemented. Gate scores code quality but not spec-reality alignment. Deducted 1."
    },
    {
      "id": 9,
      "name": "Cost Efficiency",
      "score": 5,
      "rationale": "Claude Max subscription = $0 API cost (CLAUDE.md, board meetings consistently). Budget anchors in config.yaml map session budget to model tier. claude_api_costs Supabase table tracks costs. Zero API spend confirmed across all board meetings."
    },
    {
      "id": 10,
      "name": "Trigger Automation",
      "score": 4,
      "rationale": "16 cron entries in cron-triggers.json (scan, monitor, analyse_lessons, intel_refresh, board_meeting, fallback_dryrun, scout, feedback_loop). GitHub + Linear webhooks. Telegram inbound. BUT: PI_SEO_ACTIVE=0 default (env.example) — Pi-SEO scans disabled until explicitly activated. RA-588 pre-flight checklist requires manual Railway env var set. cron last_fired_at resets on Railway redeploy (SWOT threat). First Pi-SEO sweep never executed (board 2026-04-12). Deducted 1."
    },
    {
      "id": 11,
      "name": "Knowledge Retention",
      "score": 3,
      "rationale": "lessons.jsonl auto-appended from low evaluator dimensions (board-meetings/2026-04-08). 38 entries confirmed (INCIDENT-2026-04-12.md). BUT: analyse_lessons cron last_fired_at is null (cron-triggers.json line 'analyse-lessons-weekly'). Self-improvement loop skeleton not built — CEO memo 2026-04-13 lists RA-689 feedback loop as Sprint 10 work, not done. TurboQuant assessment documents absent RAG retrieval layer. Lessons accumulate but no automated extraction. Deducted 2."
    },
    {
      "id": 12,
      "name": "Workflow Standardization",
      "score": 3,
      "rationale": "PITER at brief entry, 5 ADW templates, ship-chain /spec /plan /build /test /review /ship pipeline (CLAUDE.md). PI-CEO-STANDARD.md defines 7 pillars across portfolio. BUT: gap audit (2026-04-12-gap-audit.json) found Linear two-way sync not implemented (critical severity) — webhook.py has no issueUpdate GraphQL mutation, sessions.py has no post-build Linear callback. Standard defined but not enforced autonomously across portfolio (zero projects at production-ready status, first Pi-SEO sweep never run). Deducted 2."
    }
  ],
  "zteScore": 47,
  "zteLevel": 3,
  "productionGaps": [
    "Linear two-way sync absent: webhook.py has HMAC verification and event parsing but zero outbound Linear API calls — no issueUpdate mutation, no build-outcome comment posted back to issues (gap-audit.json critical #1 and #2)",
    "SSE streaming endpoint missing: main.py claims GET /api/sessions/{sid}/logs/stream but the route is not present — only WebSocket /ws/build/{sid} exists for real-time log delivery (gap-audit.json critical #3). Note: the provided main.py DOES show a GET /api/sessions/{sid}/logs SSE endpoint — gap audit finding may be stale post-RA-583",
    "SDK canary Phase A never activated: TAO_USE_AGENT_SDK_CANARY_RATE=0.10 unset in Railway across 3+ consecutive board cycles — claude_agent_sdk dual-path is dead code accumulating bitrot risk (board-meetings/2026-04-12)",
    "Pi-SEO first full sweep never executed: scanner, triage engine, 10 repos registered, cron rotation all complete but PI_SEO_ACTIVE=0 default means zero baseline scan data exists across portfolio (RA-588-PREFLIGHT.md, board 2026-04-12)",
    "spec.md stale: CEO memo 2026-04-13 identifies RA-675/RA-685 — spec last generated 2026-04-10, system self-assessment cannot be trusted, get_last_analysis returns stale intelligence",
    "Self-improvement loop absent: analyse-lessons-weekly cron last_fired_at is null (cron-triggers.json), RA-689 feedback loop deferred to Sprint 10, lessons accumulate without pattern extraction or CLAUDE.md proposals",
    "Per-project RAG memory layer not built: TURBOQUANT-ASSESSMENT.md explicitly documents the gap — no memory/ folder, no retrieval step before sessions, no weekly summarisation pipeline",
    "triage-cache.json at 353KB with no rotation: flagged in three consecutive board meetings (2026-04-11, 2026-04-12 x2) with no pruning strategy implemented",
    "ZTE scoring reports 60/60 while system is critically degraded: INCIDENT-2026-04-12.md documents zero commits overnight, false-positive CRITICAL alerts, Linear API 401 for 2 cycles — score is a vanity metric not reflecting operational reality (Contrarian, BOARD-DELIBERATION-2026-04-12.md)",
    "No business velocity metric: CEO memo 2026-04-13 introduces BVI but baseline shows 0 CRITICALs resolved, 0 portfolio projects improved, 0 MARATHON completions — primary success metric not yet instrumented"
  ],
  "loadRisks": [
    "Single Railway instance handles all workloads: autonomy poller, Pi-SEO cron (16 triggers), session builds, webhook ingestion, health monitoring — no worker isolation or queue backpressure; TAO_MAX_SESSIONS=3 is the only concurrency limit (env.example)",
    "In-memory _sessions dict: server restart loses all running session state (CLAUDE.md persistence guidelines) — Railway deploys during active build sessions produce silent data loss with no client notification",
    "cron last_fired_at resets on Railway redeploy: every deploy silently suppresses the next scheduled scan window due to bogus-future-timestamp debounce bug documented in SWOT threats (board 2026-04-12) — PI_SEO_ACTIVE=1 + deploy = missed scan cycle",
    "Ollama + Docker on 24GB Mac Mini: memory budget analysis (LINEAR-PACKAGE-ENHANCED.md) shows ~17GB used leaving 7GB headroom — KV cache spikes to 2-4GB on 32K context windows per Custom Oracle warning, concurrent n8n workflows + model inference can exhaust physical RAM",
    "No UPS confirmed for Mac Mini: RA-641 marked as mandatory in LINEAR-PACKAGE-ENHANCED.md but Phase 0 checklist item is Phill-gated — physical purchase not confirmed, power failure during autonomous session = data loss and interrupted builds",
    "Webhook endpoints unsecured in CI: CLAUDE.md warns 'TAO_WEBHOOK_SECRET not set — GitHub webhook endpoint is unprotected' on startup; env.example shows TAO_LINEAR_WEBHOOK_SECRET as optional — unauthenticated webhook triggers possible",
    "Supabase writes are fire-and-forget with no circuit breaker: CLAUDE.md states 'observability failures must never block the build pipeline' — legitimate design but means observability data loss is silent and undetectable under Supabase outage",
    "Rate limit at 30 req/min per IP: TAO_RATE_LIMIT=30 (env.example) — Pi-SEO scanner running 4 concurrent scans across 10 repos plus autonomy poller plus Telegram webhook could saturate this under load",
    "No autonomous push credentials on Railway: INCIDENT-2026-04-12.md root cause — container has no GitHub PAT for git push, requiring manual human push; ARCHITECTURE-V2.md identifies PAT provisioning as Week 3 work not yet completed",
    "Claude CLI health check non-blocking but misleading: /health endpoint sets healthy = disk_free_gb is not None, making claude_cli status informational only — production sessions fail silently if CLI is absent while /health returns 200"
  ]
}
```

**Score breakdown:** 4+4+5+4+3+4+4+4+5+4+3+3 = **47**

**ZTE Level:** 3 (Autonomous: 36-48)

**Key finding:** The self-reported 60/60 score in board meetings is contradicted by the evidence. The actual operational score is 47/60 — Autonomous band, not Zero Touch. The gap between claimed and actual maturity is the primary systemic risk: a system that misreports its own health cannot be trusted to self-correct.