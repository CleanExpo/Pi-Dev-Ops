```json
{
  "zteLevel": 3,
  "zteScore": 41,
  "leveragePoints": [
    {
      "id": 1,
      "name": "Spec Quality",
      "score": 4,
      "rationale": "PITER classifier + ADW templates + skill injection present (CLAUDE.md:SDK Architecture). spec.md admits staleness (board-minutes 2026-04-12: '2 days stale'). Gap audit (board-meetings/2026-04-12-gap-audit.json) found 15 spec-vs-reality discrepancies including 3 critical. Deduct 1 point."
    },
    {
      "id": 2,
      "name": "Context Precision",
      "score": 4,
      "rationale": "_get_lesson_context() injects lessons per-intent (config.yaml). lessons.jsonl active at 38 entries. No per-project memory/RAG retrieval before session start — TURBOQUANT-ASSESSMENT.md documents this gap explicitly. Charter context not wired into session prompts. Deduct 1."
    },
    {
      "id": 3,
      "name": "Model Selection",
      "score": 4,
      "rationale": "Opus/Sonnet/Haiku tier mapping in config.yaml is correct. AUTONOMY_BUDGET anchors (config.yaml:autonomy_budget) dynamically select model by time budget. However, confidence-weighted routing (RA-674) is config-only — no evidence the evaluator_confidence thresholds actually gate auto-ship in production code visible in sessions.py excerpt. Deduct 1."
    },
    {
      "id": 4,
      "name": "Tool Availability",
      "score": 4,
      "rationale": "21 MCP tools, .claude/settings.json bypassPermissions, fan-out parallelism. Three MCP tools broken in board meetings (get_zte_score, feature_list, exec_summary — 2026-04-08 minutes). LINEAR_API_KEY 401 for 2+ consecutive cycles blinded board tooling. Tool availability is degraded in practice. Deduct 1."
    },
    {
      "id": 5,
      "name": "Feedback Loops",
      "score": 3,
      "rationale": "Evaluator closed-loop retry exists (sessions.py RA-571/572). BUT: SDK Canary Phase A never activated — dual-path dead code at 0% real traffic (board-minutes 2026-04-12). Analyse-lessons cron (cron-triggers.json:analyse-lessons-weekly) has last_fired_at=null. Feedback loop from evaluator→lesson→prompt injection exists; feedback loop from production outcomes→skill improvement does not. Deduct 2."
    },
    {
      "id": 6,
      "name": "Error Recovery",
      "score": 4,
      "rationale": "3-attempt clone backoff, generator retry, phase checkpoints, resume endpoint (/api/sessions/{sid}/resume). Session persistence via os.replace atomic writes (CLAUDE.md). The overnight failure (INCIDENT-2026-04-12.md) showed railway running stale code because push credentials are unavailable in sandbox — error recovery exists for agent errors but not for operator dependency failures. Deduct 1."
    },
    {
      "id": 7,
      "name": "Session Continuity",
      "score": 4,
      "rationale": "Phase-level checkpoints + resume endpoint implemented (main.py:resume_session). _sessions is in-memory dict — server restart loses running sessions (CLAUDE.md:'any server restart loses all running sessions'). Persistence is best-effort. Deduct 1."
    },
    {
      "id": 8,
      "name": "Quality Gating",
      "score": 4,
      "rationale": "Blocking evaluator gate with EVALUATOR_THRESHOLD, max_retries, Supabase gate_checks logging (RA-651). Gap audit (2026-04-12-gap-audit.json) found evaluator measures code quality not functional correctness (CEO memo:2026-04-13 'evaluator gate measures code quality, not functional correctness'). Zero autonomous sessions closed real Linear tickets in 8 sprints. Deduct 1."
    },
    {
      "id": 9,
      "name": "Cost Efficiency",
      "score": 5,
      "rationale": "Zero API cost on Claude Max subscription. BudgetTracker wired. TAO_AUTONOMY_BUDGET single-knob. claude_api_costs table in Supabase. SDK-only removes subprocess overhead. No deductions — this is genuinely excellent."
    },
    {
      "id": 10,
      "name": "Trigger Automation",
      "score": 3,
      "rationale": "GitHub + Linear webhooks + 16 cron entries + Telegram inbound. CRITICAL: cron-triggers.json shows last_fired_at=null on scout-weekly, feedback-loop-monthly, board-meeting-weekly, fallback-dryrun-quarterly, analyse-lessons-weekly, intel-refresh-monday. The scheduler had a debounce abs() bug (board-minutes 2026-04-12 SWOT threats). Overnight marathon produced 0 autonomous commits. Autonomy poller self-start bug (10s fix in commit e611b1c) unpushed to Railway during incident. Deduct 2."
    },
    {
      "id": 11,
      "name": "Knowledge Retention",
      "score": 3,
      "rationale": "lessons.jsonl 38 entries, auto-appended by evaluator on low scores. Analyse-lessons cron never fired (last_fired_at=null). No pattern extraction feeds back to CLAUDE.md or skills. Self-improvement loop explicitly called out as 'missing' in 3 consecutive board meetings. Per-project memory layer (charter RAG) designed in TURBOQUANT-ASSESSMENT.md but not built. Deduct 2."
    },
    {
      "id": 12,
      "name": "Workflow Standardization",
      "score": 4,
      "rationale": "PITER at brief entry, 5 ADW templates, Ship Chain pipeline, ESCALATION.md contract, PERMISSIONS.md contract, build-contract.md, eval-contract.md all present. BVI metric not yet introduced (CEO memo plans it for Cycle 24). ZTE scoring reported 60/60 while core autonomy failures occurred — metric does not reflect operational reality. Deduct 1."
    }
  ],
  "productionGaps": [
    "Autonomous push credentials absent: Pi-CEO cannot git push from Railway or any sandbox — every session's commits require a manual `git push origin main` from Phill's Mac (INCIDENT-2026-04-12.md Why #3). No fine-grained PAT, deploy key, or GitHub App configured.",
    "SDK Canary Phase A never activated: TAO_USE_AGENT_SDK_CANARY_RATE=0.10 unset in Railway across 3+ consecutive sprint cycles. Dual-path code (RA-571, RA-572) accumulates bitrot risk at 0% real traffic. (board-meetings/2026-04-12-board-minutes.md)",
    "Pi-SEO first full sweep never executed: Scanner, triage engine, 3 skill layers, 10 repos registered, cron rotation all built. Zero baseline scan data across portfolio. All six cron-triggers scan entries have real last_fired_at values but the portfolio scan results visible in harness show no improvement — CCW still at 1 critical + 196 high as of charter date.",
    "Self-improvement loop absent: analyse-lessons-weekly cron last_fired_at=null. No pattern extraction from lessons.jsonl to CLAUDE.md or skill files. Lesson corpus grows but is never actioned autonomously.",
    "Per-project memory/RAG layer not built: TURBOQUANT-ASSESSMENT.md documents the design (memory/ folder + retrieval before session + summarization). Not implemented. Sessions start without project-specific charter context.",
    "Supabase gate_checks and alert_escalations writes unverified in production: RA-651 and RA-633 listed as complete in CLAUDE.md but gap audit (2026-04-12-gap-audit.json) found Linear two-way sync (issueUpdate mutations) not implemented in webhook.py or sessions.py.",
    "Linear two-way sync missing: Build outcomes do not flow back to Linear (no issueUpdate GraphQL call visible anywhere). Board meetings rely on Linear for sprint tracking but Linear API returned 401 for 2+ consecutive cycles with no automated key-rotation or alerting.",
    "triage-cache.json at 353KB with no pruning strategy: Flagged in 3 consecutive board meetings. Scanner startup degradation risk. No rotation implemented.",
    "ZTE Score metric disconnected from operational reality: System reported 60/60 through a cycle where zero commits landed, autonomy poller was dead, and watchdog sent a false CRITICAL alert. The CEO memo (2026-04-13) explicitly calls this out and proposes replacing ZTE with Business Velocity Index (BVI) as primary metric.",
    "Scout Agent (RA-684) not built: scout-weekly-monday cron exists in cron-triggers.json with last_fired_at=null. No external portfolio sensing. Board meetings run without competitive or technical intelligence input.",
    "Spec.md staleness: Last meaningful regeneration 2026-04-10. Gap audit found 15 discrepancies between spec claims and code reality including 3 critical. CEO memo makes spec regeneration the Day 1 Phill action."
  ],
  "loadRisks": [
    "Single-threaded autonomy poller bottleneck: linear_todo_poller (app/server/autonomy.py) is a single asyncio coroutine with TAO_MAX_SESSIONS=3 hard cap. Under load from 10+ portfolio repos with concurrent Urgent issues, sessions queue behind each other. No priority-weighted dispatch — a trivial DR-NRPG ticket can block an Urgent CCW security fix.",
    "Railway ephemeral disk for workspaces: app/workspaces/ is ephemeral on Railway. A deployment mid-build destroys the workspace. Session resume (POST /api/sessions/{sid}/resume) restores state from JSON but cannot recover the cloned workspace directory — resume from last_completed_phase requires re-cloning, which may fail if the repo is large or rate-limited.",
    "WebSocket session fan-out memory growth: _sessions is an unbounded in-memory dict. Each BuildSession holds output_lines as a list that grows throughout the build. Under sustained load (multiple long-running sessions), memory grows linearly with output volume. gc_loop (app/server/gc.py) prunes by age but not by memory pressure.",
    "Claude SDK subprocess replacement incomplete: CLAUDE.md states 'subprocess fallback paths have been removed from sessions.py' but app/requirements.txt still lists claude-agent-sdk>=0.1 with no pinned version. An SDK breaking change (see sdk-version-policy.md API stability contract) would cause startup ImportError with no fallback path — total service outage.",
    "Cron trigger debounce bug under rapid redeploys: board-minutes 2026-04-12 SWOT threats document 'cron-triggers.json last_fired_at resets on Railway redeploy... bogus-future-timestamp debounce bug'. Every Railway deploy (triggered by CI on main push per RA-583) silently suppresses the next scan window. Under active development with frequent pushes, Pi-SEO scans may never fire.",
    "Telegram webhook secret validation bypass: gap audit (2026-04-12-gap-audit.json, low severity) found 'if incomingSecret is null/missing, the check passes regardless' in dashboard/app/api/telegram/route.ts. An attacker knowing the endpoint URL can inject arbitrary build triggers without the secret token.",
    "SSE streaming endpoint missing: gap audit critical finding — 'GET /api/sessions/{sid}/logs/stream endpoint does not exist'. Dashboard SSE hooks (dashboard/hooks/useSSE.ts) will 404 under load if the frontend depends on SSE rather than WebSocket. Only /ws/build/{sid} WebSocket is confirmed present.",
    "Supabase fire-and-forget writes may silently drop data at scale: CLAUDE.md:'All writes are fire-and-forget — observability failures must never block the build pipeline.' Under Railway memory pressure or Supabase rate limits, gate_checks, alert_escalations, and cost tracking rows are silently lost with no retry queue or DLQ.",
    "Linear webhook route existence unconfirmed: gap audit high finding — 'Linear webhook route POST /webhook/linear existence unconfirmed due to truncated main.py'. The full /webhook/linear handler shown in the provided main.py excerpt IS present (lines visible in the audit context), but the gap audit flagged it as unverifiable. Risk: if the route was removed or misconfigured in a Sprint 8 merge, Linear-triggered builds silently fail."
  ]
}
```