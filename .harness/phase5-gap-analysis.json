```json
{
  "zteLevel": 3,
  "zteScore": 47,
  "leveragePoints": [
    {
      "id": 1,
      "name": "Spec Quality",
      "score": 4,
      "rationale": "PITER classifier in brief.py (classify_intent), 5 ADW templates, skill injection via skills_for_intent(). Quality gate rubric injected into every brief. Gap: CLAUDE.md is still a stub template (placeholder text only — no real project context populated)."
    },
    {
      "id": 2,
      "name": "Context Precision",
      "score": 4,
      "rationale": "_get_lesson_context() injects intent-filtered lessons per brief. _get_skill_context() truncates to 4000 chars. Lesson fallback to 3 most recent entries. Gap: context-compressor skill is partial (no AI summarisation, just truncation). CLAUDE.md stub reduces context precision for cold sessions."
    },
    {
      "id": 3,
      "name": "Model Selection",
      "score": 4,
      "rationale": "_select_model() reads .harness/config.yaml agents block (planner=opus, generator=sonnet, evaluator=sonnet). Explicit model override retained. Gap: auto-generator skill not implemented — no programmatic tier config generation from project type."
    },
    {
      "id": 4,
      "name": "Tool Availability",
      "score": 4,
      "rationale": "fan_out() in orchestrator.py provides parallel session execution. Opus tier escalation on worker failure. Full Claude Code tool suite available in subprocess. Gap: hooks-system 4/6 hooks unimplemented (Stop, SubagentStop missing). claude -p subprocess mode unavailable in cloud (Railway) — ANTHROPIC_API_KEY SDK path not implemented."
    },
    {
      "id": 5,
      "name": "Feedback Loops",
      "score": 4,
      "rationale": "Closed-loop evaluator retry in sessions.py: critique dimensions extracted via _parse_evaluator_dimensions(), injected into retry_brief, re-evaluated up to EVALUATOR_MAX_RETRIES. Gap: evaluator critique injected into retry prompt but generator doesn't receive structured diff — it sees the full spec again with notes appended. No multi-model parallel evaluation."
    },
    {
      "id": 6,
      "name": "Error Recovery",
      "score": 4,
      "rationale": "Clone: 3-attempt exponential backoff (2s/4s), workspace recreated between attempts. Generator: 2-attempt retry with simplified prompt. Push: 3-attempt backoff, auth error hard-stop. Sandbox: Phase 3.5 auto-re-clones if workspace GC'd. Phase checkpoints + session resume. Gap: network blip detection is limited to push; no retry on evaluator subprocess failures."
    },
    {
      "id": 7,
      "name": "Session Continuity",
      "score": 4,
      "rationale": "persistence.py atomic JSON writes (write-to-.tmp + os.replace()) on every status change. restore_sessions() on startup marks in-flight as 'interrupted'. _should_skip() per phase. POST /api/sessions/{sid}/resume skips completed phases. Gap: output_lines not persisted to disk (only count). Resume loses all streamed output from prior run."
    },
    {
      "id": 8,
      "name": "Quality Gating",
      "score": 4,
      "rationale": "Evaluator is a BLOCKING gate (not fire-and-forget). 4-dimension scoring with threshold check. Auto-learning appends lessons below threshold. EVALUATOR_THRESHOLD raised to 8 (config.py). Gap: below-threshold after max retries does NOT block push (sessions.py line: evaluator_status='warned', push still proceeds). True hard block not enforced."
    },
    {
      "id": 9,
      "name": "Cost Efficiency",
      "score": 5,
      "rationale": "Zero API cost on Claude Max subscription via claude -p subprocess. No per-token charges for claude CLI path. BudgetTracker instantiated per session for token accounting. All three model tiers available at zero marginal cost."
    },
    {
      "id": 10,
      "name": "Trigger Automation",
      "score": 4,
      "rationale": "GitHub webhook (HMAC-verified, push/PR events). Linear webhook (issue In Progress → auto-brief). cron.py with cron_loop() background task, 60s check interval, .harness/cron-triggers.json persistence. POST/GET/DELETE /api/triggers API. Gap: cron triggers not reloaded from .harness/cron-triggers.json on server startup — cron_loop() calls _load() each iteration so file is read, but cron_loop() itself is only started once at startup via asyncio.create_task()."
    },
    {
      "id": 11,
      "name": "Knowledge Retention",
      "score": 4,
      "rationale": "lessons.jsonl seeded with 18 entries. _parse_evaluator_dimensions() extracts 4 scores, auto-appends below-threshold lessons. _get_lesson_context() injects per intent. GET/POST /api/lessons API. Gap: no scheduled meta-analysis agent to identify lesson patterns and propose CLAUDE.md updates. BudgetTracker.record() has no cost_usd() method despite token-budgeter/SKILL.md having rates."
    },
    {
      "id": 12,
      "name": "Workflow Standardization",
      "score": 2,
      "rationale": "PITER classifier enforced at brief entry. 5 ADW templates active (feature/bug/chore/spike/hotfix). Template files in .harness/templates/. Gap: CLAUDE.md is a stub — 'Add brief project context here', 'Add details about how to set up the development environment here'. This is the single largest standardization gap: every Claude session starts without project context, conventions, or onboarding. This undoes most of the workflow gains from PITER+ADW."
    }
  ],
  "productionGaps": [
    "CLAUDE.md is a placeholder stub (CLAUDE.md lines 5-20: all sections say 'Add ... here'). Every cold Claude session starts without project context, conventions, or file map. This is the highest-leverage single fix.",
    "Quality gate is non-blocking: sessions.py evaluator_status='warned' path still proceeds to git push regardless of score. True hard block requires adding 'session.status = failed; return' when eval_attempt >= max_retries and not passed.",
    "cloud deployment (Railway) cannot execute claude -p subprocess — ANTHROPIC_API_KEY SDK fallback path not implemented in sessions.py or run_build(). Tool availability drops to 0 on cloud.",
    "BudgetTracker.cost_usd() method absent despite token-budgeter/SKILL.md having accurate pricing (Opus $15, Sonnet $3, Haiku $1.25/M). Token accounting is recorded but never converted to cost.",
    "output_lines not persisted to disk — persistence.py saves only output_line_count. Session resume loses all prior streamed output; WebSocket replay unavailable after restart.",
    "hooks-system Stop and SubagentStop hooks (4 of 6) not implemented. No lifecycle gate to enforce completion criteria before claude -p terminates.",
    "src/tao/agents/__init__.py contains the AgentDispatcher stub referenced in SPRINT_4_COMPLETION.md but the _deploy.py bootstrap writes it as empty. Actual state depends on which file was last written.",
    "auto-generator skill not implemented — no code generates TierConfig from project type. Onboarding new repos requires manual config.yaml authoring.",
    "Daily Anthropic docs fetch (scripts/fetch_anthropic_docs.py) is not wired into cron_loop() — it is described as '5:50am AEST cron' but no trigger exists in cron-triggers.json by default. The 6 fetched docs in .harness/anthropic-docs/ are static snapshots from 2026-04-08.",
    "Fan-out shared workspace (parent_id-shared) is created in WORKSPACE_ROOT but never registered in _sessions, so GC orphan scan will delete it after GC_MAX_AGE regardless of whether workers are still running."
  ],
  "loadRisks": [
    "Single-worker uvicorn (--workers 1 in Dockerfile CMD and app/run.ps1). All sessions, WebSocket connections, and subprocess I/O share one event loop. A long-running claude -p subprocess blocks asyncio.sleep() polling, degrading WebSocket latency for all concurrent sessions.",
    "MAX_CONCURRENT_SESSIONS=3 hard cap enforced in create_session() via RuntimeError. Fan-out with n_workers=3 consumes the entire session budget, blocking all other API-triggered builds until at least one completes.",
    "_sessions is an in-memory dict. Under load, session restoration from disk on restart is O(N) file reads with no pagination or streaming. At 1000+ persisted sessions this becomes a slow startup block.",
    "_req_log GC runs every 300s (auth.py _GC_INTERVAL). Under sustained traffic from many IPs, _req_log grows unbounded between GC cycles. 30 req/min * 300s window * N IPs = potentially large dict.",
    "run_cmd() uses asyncio.wait_for() with fixed timeout. git clone with --depth 1 on a large monorepo (e.g. linux kernel) will hit the 60s timeout. No configurable per-repo timeout override exists.",
    "eval_proc.communicate() in sessions.py has a 120s timeout for the evaluator. If claude -p hangs (network issue, model overload), the entire evaluator phase blocks for 120s before timing out. During this window the session holds its slot in _sessions.",
    "Fan-out _decompose_brief() calls run_cmd() with timeout=60 inside a cloned workspace. If decomposition itself triggers a slow claude -p call, the fan_out() coroutine blocks the event loop for up to 60s before launching any workers.",
    "cron_loop() fires create_session() for every matching trigger in a single 60s tick. If 3 cron triggers fire simultaneously, they hit the MAX_CONCURRENT_SESSIONS cap and raise RuntimeError — silently logged but the trigger's last_fired_at is not updated, so it will retry next minute.",
    "git push retry in sessions.py uses asyncio.sleep(backoff) with backoff up to 8s (push_attempt=2: 2 * 2^2 = 8s). Three consecutive push failures hold the session slot for ~14s extra before completing with error.",
    "WebSocket handler in main.py polls session.output_lines every 150ms (asyncio.sleep(0.15)). Under 3 concurrent sessions each with active WebSocket clients, this generates ~20 poll cycles/second across all connections on the single event loop."
  ]
}
```