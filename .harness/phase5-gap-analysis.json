```json
{
  "zteLevel": 3,
  "zteScore": 38,
  "leveragePoints": [
    { "id": 1, "name": "Spec Quality", "score": 4 },
    { "id": 2, "name": "Context Precision", "score": 4 },
    { "id": 3, "name": "Model Selection", "score": 4 },
    { "id": 4, "name": "Tool Availability", "score": 4 },
    { "id": 5, "name": "Feedback Loops", "score": 3 },
    { "id": 6, "name": "Error Recovery", "score": 3 },
    { "id": 7, "name": "Session Continuity", "score": 3 },
    { "id": 8, "name": "Quality Gating", "score": 3 },
    { "id": 9, "name": "Cost Efficiency", "score": 3 },
    { "id": 10, "name": "Trigger Automation", "score": 3 },
    { "id": 11, "name": "Knowledge Retention", "score": 3 },
    { "id": 12, "name": "Workflow Standardization", "score": 3 }
  ],
  "productionGaps": [
    "Spec Quality (4 not 5): `.harness/agents/evaluator.md`, `generator.md`, `planner.md` define agent roles but NO testable acceptance criteria per feature exist in any shipped spec — `spec.md` is narrative, not BDD/Gherkin. Feature specs lack edge cases and measurable pass/fail gates.",
    "Context Precision (4 not 5): `_get_lesson_context()` in `sessions.py` injects lessons but the mechanism for filtering relevance is keyword/intent-based only — no embedding similarity or semantic compression. `dashboard/app/api/analyze/route.ts` fetches raw harness docs without compression or noise filtering for the analysis phases.",
    "Model Selection (4 not 5): `config.yaml` defines planner=opus/generator=sonnet/evaluator=sonnet tiers, but auto-selection by task complexity is configured statically at build time. No runtime routing logic adapts model choice based on actual task signals — the 5-anchor AUTONOMY_BUDGET interpolation in `budget.py` is the only dynamic element, and it requires a manual env var override.",
    "Tool Availability (4 not 5): `.claude/settings.json` shows `bypassPermissions` and a broad allowlist, but MCP server `mcp/pi-ceo-server.js` v3.0.0 declares `mcp__pi-ceo__*` tools with no evidence of domain-specific custom tools (e.g. Pi-SEO diff tool, Linear two-way sync tool). `mcp/package.json` shows only `@modelcontextprotocol/sdk` — no custom transport or domain MCP servers wired. Gap audit JSON at `.harness/board-meetings/2026-04-12-gap-audit.json` confirms Linear two-way sync (critical) and SSE streaming endpoint (critical) are missing.",
    "Feedback Loops (3 not 5): Evaluator critique loop exists in `sessions.py` (`_phase_evaluate` with retry injection), but `.harness/agents/sdk-phase2-rollout.md` sign-off log shows Phase A/B/C/D all blank — canary was never activated per board minutes 2026-04-11 and 2026-04-12. Zero real-traffic SDK feedback data exists. Lesson injection is one-directional; no weekly analysis pipeline converts lessons to CLAUDE.md or skill file updates automatically.",
    "Error Recovery (3 not 5): `app/server/main.py` `_resilient()` wrapper exists for background tasks, but the gap audit (`2026-04-12-gap-audit.json`) documents `restore_sessions()` truncated before completion — session resume correctness cannot be confirmed. No evidence of error classification taxonomy or graceful degradation paths beyond basic retry backoff.",
    "Session Continuity (3 not 5): `POST /api/sessions/{sid}/resume` endpoint exists in `main.py` and `last_completed_phase` field on `BuildSession` is referenced, but `sessions.py` `restore_sessions()` is documented as truncated in the gap audit. The `.harness/agents/sdk-phase2-rollout.md` Phase D (subprocess removal) is blocked on Phase C which has never been signed off — the actual production path validity is unverified.",
    "Quality Gating (3 not 5): Evaluator gate is described as blocking in `config.yaml` and `leverage-audit.md`, but `TAO_EVALUATOR_ENABLED=false` in `.github/workflows/ci.yml` and `.github/workflows/smoke_test.yml` disables it in CI. The evaluator threshold env var `TAO_EVALUATOR_THRESHOLD=8` in `.env.example` is advisory — no enforcement mechanism prevents a bypass when `evaluator_enabled=False` is passed in `BuildRequest`. Gap audit confirms SSE log streaming endpoint missing entirely.",
    "Cost Efficiency (3 not 5): Claude Max zero-cost claim is valid for CLI sessions but `dashboard/package.json` includes `@anthropic-ai/sdk: ^0.36.3` and `ANALYSIS_MODE=api` path in `dashboard/lib/claude.ts` routes to paid API. No per-session budget tracking visible in the dashboard. `BudgetTracker` in `src/tao/budget/tracker.py` accumulates tokens but board minutes 2026-04-09 note 'BudgetTracker.record() has no cost_usd() method' — cost tracking is incomplete.",
    "Trigger Automation (3 not 5): GitHub + Linear webhooks exist in `main.py`. Cron loop exists in `cron.py`. But `.harness/agents/sdk-phase2-rollout.md` and board minutes for 2026-04-11 and 2026-04-12 confirm the autonomy poller was silent for multiple consecutive cycles. `autonomy.py` documented as skipping polls silently when `LINEAR_API_KEY` missing. Board minutes show Linear API returning 401 for two consecutive cycles with no automated alerting that fired successfully.",
    "Knowledge Retention (3 not 5): `lessons.jsonl` exists and `_get_lesson_context()` injects lessons. However `.harness/.analyse-lessons-last-run` shows last analysis was `2026-04-13T18:10:20` — a single file timestamp with no evidence of automated weekly pipeline. `scripts/analyse_lessons.py` is referenced but board minutes 2026-04-12 list 'Build Self-Improvement Loop Skeleton' as a Priority 3 recommendation — it does not exist yet. No lesson-to-CLAUDE.md promotion pipeline is implemented.",
    "Workflow Standardization (3 not 5): PITER classifier in `brief.py` and ADW templates exist. But board meetings show ad-hoc paths persist: gap audit was triggered manually, Pi-SEO first sweep was deferred across 3 consecutive board cycles, SDK canary was deferred across 3 consecutive cycles. No classifier-enforced gate prevents ad-hoc `POST /api/build` calls with empty brief or unclassified intent."
  ],
  "loadRisks": [
    "Session state is in-memory `_sessions` dict in `main.py` with single uvicorn worker (`--workers 1` in both Dockerfiles). Under concurrent load, multiple simultaneous build requests are serialized through one process — no horizontal scaling path. Railway redeploy loses all in-flight sessions with no graceful handoff despite the `on_shutdown` drain attempt.",
    "Workspace GC in `gc.py` runs on a 30-minute loop and deletes directories older than 4 hours. Under sustained load where sessions accumulate faster than GC clears them, `app/workspaces/` fills Railway ephemeral disk — no disk pressure circuit breaker exists. `TAO_MAX_SESSIONS=3` limits concurrent sessions but does not prevent disk exhaustion from completed session artifacts.",
    "`triage-cache.json` documented at 353KB with no rotation strategy across three consecutive board meetings (2026-04-11, 2026-04-12 x2). Under load from Pi-SEO scanning 10+ repos, this file grows unbounded and will degrade scanner startup performance on every invocation.",
    "Linear webhook handler in `main.py` calls `create_session()` synchronously inside the request handler. Under webhook replay storms or GitHub push floods, the 429 rate limit (30 req/min/IP) is the only protection — no queue, no back-pressure, no deduplication beyond `_find_active_session_for_repo()` which only checks in-memory `_sessions`.",
    "The autonomy poller `linear_todo_poller()` in `autonomy.py` silently skips all polls when `LINEAR_API_KEY` is absent but `/health` returns 200 — documented in `CLAUDE.md` as the silent failure pattern. Under a Railway restart where the env var is missing, the system appears healthy while doing nothing, with no timeout or circuit breaker to force an alert."
  ],
  "topThreeROI": [
    "Feedback Loops — The evaluator retry loop is coded but the SDK canary has been deferred for 3+ consecutive board cycles, meaning zero real-traffic validation has occurred. Activating `TAO_USE_AGENT_SDK_CANARY_RATE=0.10` (a single Railway env var) immediately starts real feedback collection. Simultaneously wiring `scripts/analyse_lessons.py` as a weekly cron job to promote lessons into CLAUDE.md would close the self-improvement loop — converting the largest documented gap into a compounding asset within one sprint.",
    "Quality Gating — `TAO_EVALUATOR_ENABLED=false` in CI means the gate that is claimed to be 'blocking' is disabled in the only reproducible test environment. Fixing CI to run with the evaluator enabled, adding a hard assertion that `evaluator_score >= threshold` blocks the build, and implementing the missing SSE streaming endpoint (`GET /api/sessions/{sid}/logs`) would make the quality gate real rather than aspirational — directly raising trust in every shipped output.",
    "Trigger Automation — The autonomy poller's silent failure mode (Linear 401 for two consecutive cycles, no automated recovery alert that actually fired) is the highest-risk gap for a system claiming Zero Touch operation. Adding a health-check assertion that surfaces `linear_api_key: bool` and `last_successful_poll_at` in `/health`, plus a Telegram alert on first poll failure rather than silent skip, converts the silent-failure pattern into a loud one — making every future overnight autonomous run trustworthy rather than requiring morning manual verification."
  ]
}
```