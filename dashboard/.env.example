# Backend URL — defaults to local FastAPI
# Override this in Vercel env vars if you expose the backend via ngrok/tunnel
NEXT_PUBLIC_API_URL=http://127.0.0.1:7777

# Pi CEO server (for the /builds page proxy)
PI_CEO_URL=http://127.0.0.1:7777
PI_CEO_PASSWORD=your-tao-password
