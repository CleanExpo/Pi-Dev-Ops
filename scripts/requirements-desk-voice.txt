# Desk voice loop (RA-1696) — install into the python used by `op run`:
#   python -m pip install -r scripts/requirements-desk-voice.txt
faster-whisper>=1.0.0
sounddevice>=0.5.0
soundfile>=0.12.0
pynput>=1.8.0
numpy>=1.26.0
openai>=1.0.0
