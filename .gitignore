# ══════════════════════════════════════════════════════════════════════
# Pi-Dev-Ops .gitignore — comprehensive credential & artifact protection
# Last updated: 2026-04-12 (RA-SECURITY)
# ══════════════════════════════════════════════════════════════════════

# ── Credentials & secrets (NEVER commit these) ────────────────────────
.env
.env.*
!.env.example
!.env.*.example
!.env.*.sample
*.env
secrets.json
secrets.yaml
secrets.yml
credentials.json
credentials.yaml
*_credentials.*
*-credentials.*
service-account*.json
gcp-key*.json
railway.toml
railway.json

# Specific Pi-CEO credential files
telegram-bot/.env
telegram-bot/.env.railway
telegram-bot/.env.local
dashboard/.env.local
dashboard/.env.*.local

# API key files
*.key
*.pem
*.p12
*.pfx
id_rsa
id_ed25519
*.ppk

# ── Python artifacts ──────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
*.pyd
*.so
*.egg
*.egg-info/
dist/
build/
.eggs/
.tox/
.nox/
.mypy_cache/
.dmypy.json
.pyre/
.ruff_cache/
*.coverage
.coverage
htmlcov/
.pytest_cache/

# ── Node / Next.js ────────────────────────────────────────────────────
node_modules/
.next/
out/
.vercel/
.turbo/
*.tsbuildinfo
next-env.d.ts
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# ── App runtime data (auto-generated, not source) ────────────────────
app/workspaces/
app/logs/
app/data/
*.log

# ── One-time consolidation dumps (never source) ──────────────────────
_consolidated_scattered/
.harness/artefacts/**/sandbox/
*.db
*.db-journal
*.sqlite
*.sqlite3

# ── Dashboard generated assets (large binary, not source) ─────────────
dashboard/*.png
dashboard/render-images.html

# ── IDE & OS artifacts ────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
desktop.ini
ehthumbs.db

# ── MCP / Claude tooling ──────────────────────────────────────────────
.claude/settings.local.json
mcp/node_modules/
.playwright-mcp/

# ── Harness runtime data (NEVER commit — may contain credential scan findings) ──
.harness/telegram-inbox/
.harness/monitor-digests/
.harness/eval-candidates/
.harness/scan-results/
.harness/build-logs/
.harness/marketing-bridge-state.json
.harness/spec-pipelines/
.harness/llm-cost.jsonl
.harness/agent-sdk-metrics/
.harness/anthropic-docs/2*/
.harness/secrets-scan/
.harness/post-deploy-metrics/
.harness/burndown-state.json

# ── Harness feedback loop / BVI state (runtime, not config) ──────────
.harness/shipped-features.jsonl
.harness/feedback-cache.json
.harness/scout-seen.json
.harness/zte-v2-score.json
.harness/session-outcomes.jsonl
# RA-7085 job-authored last-success records (runtime state)
.harness/job-success/

# ── Docker / deployment ───────────────────────────────────────────────
*.pid
*.seed
.env.docker
docker-compose.override.yml

# ── launchd plists with embedded credentials (use .plist.example instead) ──
scripts/*.plist
!scripts/*.plist.example

# ── Session / temp files ──────────────────────────────────────────────
sessions/
*.session
*.tmp
*.temp
/tmp/
.vercel
# Pi-CEO task-memory (auto-added by Pi-CEO session bootstrap, RA-1374)
.pi-ceo/

# Composio CLI auth (added 2026-04-27)
.composio/
.composio.json
composio.json

# RA-1922 — runtime audit log written by tests/test_portfolio_synthesis_opus_policy.py
.harness/model-policy-violations.jsonl

# Python virtualenvs (added 2026-05-04)
.venv*/
venv/

# Runtime state — auto-generated by cron jobs + agents (added 2026-05-15 dirty-tree triage)
.harness/*.jsonl
.harness/curator/*.jsonl
.harness/curator/state.json
.harness/workspace-intel/*.jsonl
.harness/swarm/*.jsonl
.harness/swarm/*_state.json
.harness/swarm/green_merge_counter.json
.harness/swarm/pr_rate_limit.json
.harness/swarm/telegram_drafts.json
.harness/swarm/voice/desk-loop/
.harness/cron-triggers.json
.harness/notebooklm-freshness.json
.harness/zte-reality-status.json
.harness/integration-health.jsonl
.harness/autonomy.jsonl
.harness/bvi-history.jsonl
.harness/fallback-dryrun-log.jsonl
.harness/linear-pulse-state.json
.harness/sources_processed.jsonl
.harness/triage-cache.json
.harness/Anthropic-Docs-Latest.md

# Backup files — never commit
*.py.bak
*.bak

# Remotion local-only artefacts (rendered audio + screencaps; not source-of-truth)
remotion-studio/public/audio/_cache/
remotion-studio/public/captures/
remotion-studio/public/ra-wave-1-screencaps/
.harness/artefacts/synthex/sandbox/Synthex/

# Session-handoff runtime artifacts (dirs + READMEs tracked; generated files local-only)
docs/session-handoffs/handoff-*.md
.handoff-logs/*.log

# Auto-commit fallout (2026-08-01). An `autogit busy` hook wired into Claude Code's
# UserPromptSubmit/PostToolUse was sweeping these into commits authored as the
# founder and pushing them unreviewed. The hook is dead; these stay ignored so a
# future one cannot re-pollute the branch.
.omx/
.harness/*.txt
.harness/*.diff

# 2026-08-03: .harness/ is generated analysis output and MUST NOT be tracked.
# secrets_check.py skips this path on the stated ground that it is not committed. main made
# that premise true again in #607; this branch still tracked 608 files under it, so the
# exclusion was false HERE and merging would have carried the blind spot back onto main.
# Same fix, same form as #607. The piecemeal .harness/* patterns above are subsumed by this
# and left in place deliberately — removing them is a separate change.

# generated graph visualisation output (not source)
swarm/graphify-out/
.harness/
