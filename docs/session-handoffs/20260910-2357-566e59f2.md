# Session handoff — 2026-09-10 23:57 — session 566e59f2

Read this first, then `/resume-from-handoff`. Nothing below is inferred; every claim names
the command or file that produced it.

## Goal

Founder goal, set this session:

> Understand what DONE looks like, so an overnight run can drive Mission Control to 100%
> active, running from the Mac mini, with all projects connected to the Unite-Group Nexus
> CRM and Pi-Dev-Ops as the operating system.

The Definition of Done is written and **approved**: `~/.claude/plans/enchanted-waddling-koala.md`.
It has 4 levels, each with artifact / verifier / pass condition / proof-it-can-fail.
**Read that file before doing anything else** — it is the actual deliverable of this session.

Two founder rulings recorded there:
1. **Overnight run: safe actions only.** No deletes, no `>` shell redirects, zero approval
   gates raised. It may read, edit via tools, run tests, and commit.
2. **Level 3 scope: all 13 registry projects** (he said 12; the file says 13 — see below).

## Current state

| Thing | Value | How |
|---|---|---|
| Repo | `Pi-Dev-Ops - Mission Control`, branch `fix/mission-control-hollow-panels` | `git rev-parse --abbrev-ref HEAD` |
| Repo HEAD | `2839709c` | `git log --oneline -1` |
| Repo dirty | 1 file | `git status --porcelain \| wc -l` |
| `~/.claude` (macbook) | branch `feat/gauntlet-loop-v2`, 2 dirty | same commands |
| Fleet parity | **FAIL, 5 problems, exit 1** | `python3 ~/.claude/scripts/fleet-parity.py` |

**WARNING — another writer is active on this branch.** Repo HEAD moved twice *during* this
session with no action from me: `c5c4bb63` → `dca94ef6` → `2839709c`. Confirm nothing else
is running before you commit or rebase.

Fleet, measured 23:57:

| Node | Skills | Branch | Uncommitted |
|---|---|---|---|
| pc (`win-ts`) | **386** | `fix/estate-audit-board-20260907` | 3 |
| macbook | **272** | `feat/gauntlet-loop-v2` | 2 |
| mini (`mini-ts`) | **136** | `feat/system-forge` | 37 |

## Done (with evidence)

1. **Built `~/.claude/scripts/fleet-parity.py`** — the control that detects machine drift and,
   critically, never reports a broken probe as a dead machine.
   - Watched RED on the live fleet: exit 1, 5 findings.
   - Controls pass: `ALL CONTROLS PASSED`, exit 0 — asserts exit 0 on agreement, 1 on branch
     drift, 1 on ability drift, and **2, never 0**, when a probe fails.
   - Control file: scratchpad `test_fleet_parity_control.py` (session-local — **re-create it**).
   - **NOT COMMITTED.** `git status` → `?? scripts/fleet-parity.py`. The commit was blocked
     by the phone gate.

2. **Restored `skills/estate-sync` on the mini** from its own git history.
   - Cause: `ls ~/.claude/skills/estate-sync/` → *No such file or directory*. The LaunchAgent
     runs `/bin/zsh <that missing script>` → exit 127. The sync tool had lost itself, so it
     could never receive its own replacement.
   - Fix: `git -C ~/.claude checkout d123417c -- skills/estate-sync` (commit `d123417c` =
     "vendor 132 one-machine skills"). File now present, 21,633 bytes.
   - **STATUS: UNPROVEN, not fixed.** `/bin/zsh -n` parses it (exit 0), which proves only that
     zsh can read the file. `launchctl list` still records exit **127** — the last real run.
     Nobody has watched it go green. A necessary cause was removed; sufficiency is untested.
   - Side effect: mini skills 135 → 136, uncommitted 28 → 37 (the 9 restored files).

3. **Diagnosed why the Telegram approve button has never worked** — three independent breaks:
   - `telegram_intake.py:165` — `allowed_updates: ["message"]`. Taps are `callback_query`;
     never requested, so Telegram never sends them.
   - **No `callback_query` handler exists anywhere in the repo.** Verified across all file
     types excluding `.git`/`node_modules`/`__pycache__`/`workspaces`; positive control on
     `callback_data` returned 8+ hits, so the search was capable of matching.
   - `POST /api/phone/gate/{id}/resolve` (`phone.py:312`) exists and has **zero callers**; its
     own docstring says "called from callback".
   - Plus: **the Hermes gateway has been dead since 2026-09-03** (exit 78, launchd penalty
     box) and the mini is the sole declared poller — so nothing has been polling for a week.

4. **Established the Mac mini CAN run Claude unattended.** The fleet skill says it cannot
   (`fleet-compute/SKILL.md:38`, "Verified 2026-07-29"). That is **stale and wrong**:
   `claude --version` → 2.1.267; `claude -p ... --allowedTools "Bash"` executed a tool and
   returned over plain ssh. **Fix `SKILL.md:38` and `fleet.py` NODES.mini.runtimes.**

5. **Established the PC is live and fully provisioned** — 386 skills, Python 3.11, Claude CLI,
   PowerShell 5.1 — while `fleet.py probe` reports it UNREACHABLE. Its probe sends a POSIX
   script wrapped in `powershell -NoProfile -Command`; the script fails and the failure is
   scored as a dead machine. The same bug was fixed for the *local* path on 2026-08-31
   (`fleet.py:125-131`) and the remote path was left alone.

6. **Memories written** (survive this session):
   `~/.claude/projects/-Users-phillmcgurk-Pi-Dev-Ops---Mission-Control/memory/`
   → `phone-gate-approve-silently-dead.md`, `fleet-three-machines-never-matched.md`.

## Next

In order. Step 1 is the one the founder must authorise.

1. **Run estate-sync on the mini and watch exit 127 change.** No dry-run flag exists, so this
   is a real two-way sync across three machines. **Founder said "go" is required** — he had
   not given it when this session ended.
2. **Land the mini's 37 uncommitted files** before any branch switch, or they are lost.
3. **Converge all three machines to `main`** (`mini/main` exists; the mini is the git remote).
   Re-run `fleet-parity.py` — it must reach exit 0.
4. **Fix `fleet.py`'s Windows probe** so the PC joins the fleet (PowerShell capacity script).
5. **Correct the two stale docs** that cost capability: `fleet-compute/SKILL.md:38` (mini can
   run Claude) and `CLAUDE.md:474` (12 projects → 13).
6. Then Level 1 of the plan — hollow panels, starting with the proxy fallback that returns
   **HTTP 200 with zeros** when the backend is down (`route.ts:45-88`), which the Loop Cockpit
   cannot distinguish from healthy (`loop/page.tsx:128`).

Carried over, still undone (both need a working approve button):
repoint the 3 stale `senior-harness` hook commands in
`Pi-Dev-Ops - Mission Control/.claude/settings.json` lines 60, 70, 85 at
`~/.claude/skills/senior-harness-control/scripts/setup_driver.py`; add
`"hookEventName": "UserPromptSubmit"` to `continuation_horizon_hook.py:36`.

## Blockers

1. **The phone approval gate blocks all harness self-repair, and cannot be satisfied.**
   Protected paths are `(^|/)\.claude/hooks`, `(^|/)\.claude/settings(.local)?.json`,
   `/LaunchAgents` — matching **any** `.claude`, project-level included. Approvals cannot
   arrive (see Done #3). ~10 gates expired this session. **Do not weaken this guard.**
   The gate is correct; its consent channel is broken.
2. **`>` shell redirects and `rm <file>` are tier 3** (`tier_classifier.py:53`, `:40`) — they
   raise a gate. Ordinary `git commit` and feature-branch push are tier 2 and pass.
3. **Another writer is active on `fix/mission-control-hollow-panels`** (HEAD moved twice
   unprompted). Check before committing.
4. **Level 3 is a build, not a repair.** 0 of 13 projects are connected to the Nexus CRM;
   nothing reads `config/harness/projects.json` and writes a `client_workspaces` row.
5. **Linear API is down** — `linear_api_key` returns HTTP 401
   (`.harness/integration-health.jsonl`).
6. **Never trust a project health score of 100** — `scanner.py:760` fabricates it from zero
   data and `.harness/scan-results/` is empty. "Never scanned" and "perfect" are the same
   number today.
7. **`donectl doctor`** → `hook_wired: false`, `enforcement_effective: false`.
   **ENFORCEMENT_UNPROVEN holds. Nothing this session is claimed as Done.**

## Linear

No tickets filed this session. Findings worth filing, routed per
`config/harness/projects.json` (route on `id`, never `repo`):

- **`skills-library`** — fleet.py Windows probe scores a live PC as dead;
  `fleet-compute/SKILL.md:38` stale (mini Claude auth); `gate_test.py:9` hardcodes
  `/Users/phill-mac/...` so it only ever ran on the mini.
- **`pi-dev-ops`** — Telegram approve path never implemented (3 breaks); proxy fallback
  returns 200-with-zeros; `CLAUDE.md:474` says 12 projects, file has 13; four sources
  disagree on the project count (13 / 12 / 10 / 7).
- **Mini services** — `overnight-mesh-supervisor` exit 78 (this *is* the overnight job),
  `ai.hermes.gateway` exit 78 since Sep 3, `estate-sync` exit 127 (partially addressed).
