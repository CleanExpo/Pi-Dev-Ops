# Session Handoff — Synthex FB/IG integration + Meta App Review prep (v2: +/data-deletion page)

**Timestamp:** 20260701-184311 · **Author:** Claude (Opus 4.8)
**Supersedes:** handoff-20260701-182147.md (adds the `/data-deletion` page work).
**Scope:** Synthex (`CleanExpo/Synthex`) social-connect + legal pages; Pi-Dev-Ops (`CleanExpo/Pi-Dev-Ops`) skill (already merged).
**Phase 0 gate log:** `.handoff-logs/handoff-20260701-184309.log` — **BLOCKED** (environmental only; see §9).

---

## 1. Summary
- **SHIPPED earlier this session (merged):** Synthex FB/IG social-connect fix (#597 D1, #598 route tests); config_id wired to Vercel prod + redeployed; FB+IG credentials repointed to founder-controlled Meta app `1316892900613495`. Pi-Dev-Ops skill `connecting-meta-facebook-instagram` (#434 + manifest #436).
- **NEW this turn (pushed, NOT merged):** `/data-deletion` page → **Synthex PR #601** (OPEN, all CI green). Discovered `/privacy` already existed, so only the deletion half was needed.
- **DRAFTED (not in repo):** Meta App Review submission → `scratchpad/meta-app-review-request.md`.
- **NOT DONE (owner/Meta-side):** merge #601; set Data-Deletion + Privacy URLs in the Meta app; add `synthex.social` to Meta App Domains; Meta Business Verification + App Review; rotate Facebook App Secret.

## 2. Where it started
- **Request (this turn):** "Build the /privacy and /data-deletion pages."
- **Branch:** Synthex `feat/data-deletion-page` (off `origin/main`). PR #601 → main.
- **Problem:** Meta App Review requires a working Privacy Policy URL + User Data Deletion URL; `/data-deletion` was missing.
- **Constraints:** prod deploy = human gate; Australian English; match existing page conventions; Server Components by default.

## 3. Decisions locked + what shipped
**Decisions:** `/privacy` already existed (`app/privacy/page.tsx`) — reused, not rebuilt. `/data-deletion` built as a static Server Component matching the privacy page's `MarketingLayout`/`generateMetadata`/card+icon system. Deletion contact = `privacy@synthex.social` (owner to confirm inbox).
**Shipped:** Synthex `main` — #597 (`366d160b`), #598 (`2d92fc71`) [merged]. Pi-Dev-Ops `main` — #434 (`059fef1e`), #436 (`052ee4ac`) [merged].
**Pushed, not merged:** `feat/data-deletion-page` `1ad179e3` → **PR #601** (OPEN, MERGEABLE/CLEAN).

## 4. Key files
| File | Repo | Status |
|---|---|---|
| `app/data-deletion/page.tsx` | Synthex | Created — pushed (PR #601), **Needs review + merge** |
| `app/privacy/page.tsx` | Synthex | Read-only inspected (already existed, adequate) |
| `app/dashboard/integrations/page.tsx` | Synthex | Modified via #597 — merged |
| `tests/unit/api/oauth-starter-meta-config.test.ts` | Synthex | Modified via #598 — merged |
| `skills/connecting-meta-facebook-instagram/SKILL.md` | Pi-Dev-Ops | Created (#434) — merged, symlinked into `~/.claude/skills` |
| `scratchpad/meta-app-review-request.md` | scratchpad | Created — App Review draft, not in repo |

## 5. Running state
- No dev servers or background jobs running (verified — none started).
- Synthex prod: FB/IG OAuth verified earlier sending `client_id=1316892900613495` + `config_id=1544525556521302`.
- PR #601 CI: Build / Type Check / Unit Tests / Lint / Vercel preview (synthex + sandbox) all **SUCCESS** (verified via `gh pr view 601`).

## 6. Verification — exact commands
```bash
# PR #601 gates (all green as of handoff):
gh pr view 601 --json statusCheckRollup   # Build/Type Check/Unit Tests/Lint/Vercel = SUCCESS

# Visual (page is public — no login):
#   <preview-url>/data-deletion  → hero "Data Deletion", FB/IG note, 3 method cards,
#   "What we delete", "Timeline"; the /privacy link works.
#   After merge: https://synthex.social/data-deletion

# Local lint of the page (clean):
cd /Users/phillmcgurk/Synthex && npx eslint app/data-deletion/page.tsx --max-warnings 0
# NOTE: local `npx tsc --noEmit` shows pre-existing command-centre errors — a LOCAL artifact
# (@unite-group/control-module not built locally). CI Type Check passes. Run `npm ci` if needed.

# FB/IG OAuth still correct (authed session):
#   GET https://synthex.social/api/auth/oauth/facebook?returnTo=/dashboard/platforms
```

## 7. Deferred + open questions
**Deferred:**
1. **Merge PR #601** — Owner (prod deploy = human gate). Blocking the live `/data-deletion` URL. All CI green.
2. **Set Meta app URLs** — after merge, set `https://synthex.social/data-deletion` as User Data Deletion URL and confirm `https://synthex.social/privacy` as Privacy Policy URL in app `1316892900613495` → Settings → Basic. Owner. Blocking App Review.
3. **Add `synthex.social` to App Domains** — Owner. Blocking the OAuth dialog loading.
4. **Business Verification + App Review** — Owner submits (draft in scratchpad); Meta approves (multi-day). Blocking live posting.
5. **Rotate Facebook App Secret** — Owner. Recommended (was briefly in the public client_id slot).

**Open questions:**
- Does `privacy@synthex.social` route to a monitored inbox? Owner. Blocking a clean App Review (used as deletion contact). If not, swap the address in `app/data-deletion/page.tsx`.
- Who has admin on Meta app `1316892900613495` to make the Settings→Basic edits? Owner.

## 8. Pick up here
**Start here:** confirm the `/data-deletion` preview renders (§6), then merge PR #601 on the owner's go. After merge, hand the Meta Settings→Basic edits (URLs + App Domains) to the owner.
**Do not redo:** the merged fixes (#597/#598/#434/#436), the credential repoint (verified), the `/privacy` page (already existed). Do not rebuild `/data-deletion` — it's built and green in #601.
**First command to run:**
```bash
cd /Users/phillmcgurk/Synthex && gh pr view 601 --json state,mergeable,statusCheckRollup
```

## 9. Risk notes
- **Phase 0 gate = BLOCKED** (`generated-agentskills`, `type-python-import`) — environmental, NOT this session's work: gate ran on the behind-main Pi-Dev-Ops remotion tree (manifest-regen churn, reverted after) and Python deps aren't installed locally. This session's deliverables are all either merged (verified) or green in CI (#601).
- **Local tsc ≠ CI tsc:** local `npx tsc --noEmit` shows `command-centre` errors because `@unite-group/control-module` isn't built locally; CI Type Check passes. Don't "fix" those — not this session's scope.
- `privacy@synthex.social` mailbox unverified — flagged in the PR.
- Two-browser trap (remote Windows vs local Mac) still applies for any browser verification.
- Synthex `stash@{0}` = unrelated parked CARSI WIP — leave it.

## 10. Handoff quality check
- Tests claimed run? Only CI results cited (from `gh pr view`); local eslint run + cited. ✅
- Shipped claims backed by evidence? Merged items have PR#+SHA; #601 explicitly marked pushed-not-merged. ✅
- Running process claims? None claimed running. ✅
- Completed vs deferred separated? Yes (§1/§3 vs §7). ✅
- First command provided? Yes (§8). ✅

Handoff complete. Next safe action: confirm the `/data-deletion` preview renders, then merge PR #601 on the owner's go.
