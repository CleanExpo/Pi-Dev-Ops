# Session Handoff — Synthex FB/IG integration COMPLETE (code side) + Meta App Review prep

**Timestamp:** 20260701-200350 · **Author:** Claude (Opus 4.8)
**Supersedes:** handoff-20260701-184311.md (final: #602 merged + legal pages live + memory finalized).
**Scope:** Synthex (`CleanExpo/Synthex`) FB/IG social-connect + legal pages; Pi-Dev-Ops skill (merged).
**Phase 0 gate log:** `.handoff-logs/handoff-20260701-200345.log` — **BLOCKED** (environmental only; see §9).

---

## 1. Summary
**All code work for the Facebook/Instagram integration is COMPLETE, merged, deployed, and prod-verified.** Remaining items are exclusively owner actions in the Meta developer console — no code left.
- D1 (integrations BOM crash) + D2 (Connect no-op, a D1 side-effect) — fixed.
- Repointed FB+IG to the founder-controlled Meta app `1316892900613495` with Login-for-Business config `1544525556521302`.
- Built `/data-deletion` + confirmed `/privacy` exists — both the Meta App Review legal URLs are live.
- Authored + shipped a reusable global skill; drafted the full App Review submission.

## 2. Where it started
- **Request arc:** resume the Synthex social-connect SPM spec → complete the Meta integration → make a skill for it → draft the App Review → build `/privacy` + `/data-deletion` → swap deletion email → update memory.
- **Branches:** all work merged to Synthex `main` and Pi-Dev-Ops `main`. Local Pi-Dev-Ops tree on `feature/remotion-local-progress-workspace` (pre-existing, not this session's).
- **Constraints:** prod deploy = human gate (each merge was explicitly authorised); never enter secrets into fields; Australian English.

## 3. Decisions locked + what shipped
**Decisions:** D2 was a symptom of D1 (no client fix). Repoint to founder-owned app `1316892900613495` (not recover orphan `909628005147591`). `/privacy` already existed — reused. Deletion contact = `support@carsi.com.au`.
**Shipped (Synthex `main`, HEAD `be9f151f`):**
- #597 `366d160b` — D1 BOM strip
- #598 `2d92fc71` — Connect-flow route tests
- #601 `10d1b029` — `/data-deletion` page
- #602 `be9f151f` — deletion email → support@carsi.com.au
**Shipped (Pi-Dev-Ops `main`):** #434 `059fef1e` (skill) + #436 `052ee4ac` (manifest).
**Config/prod (non-git):** `META_BUSINESS_LOGIN_CONFIG_ID=1544525556521302` in Vercel prod; FB+IG DB creds repointed to client_id `1316892900613495` via owner UI.

## 4. Key files
| File | Repo | Status |
|---|---|---|
| `app/dashboard/integrations/page.tsx` | Synthex | Modified (#597) — merged |
| `tests/unit/api/oauth-starter-meta-config.test.ts` | Synthex | Modified (#598) — merged |
| `app/data-deletion/page.tsx` | Synthex | Created (#601, email #602) — merged + live |
| `app/privacy/page.tsx` | Synthex | Read-only inspected (pre-existing, adequate) |
| `skills/connecting-meta-facebook-instagram/SKILL.md` | Pi-Dev-Ops | Created (#434) — merged, in `~/.claude/skills` |
| `scratchpad/meta-app-review-request.md` | scratchpad | Created — App Review draft, not in repo |

## 5. Running state
- No dev servers / background jobs running (none started this session).
- Prod verified live: `synthex.social/privacy` = HTTP 200, `synthex.social/data-deletion` = HTTP 200 (shows support@carsi.com.au).
- FB/IG OAuth verified sending client_id `1316892900613495` + config_id `1544525556521302`.

## 6. Verification — exact commands
```bash
# Legal pages live:
curl -s -o /dev/null -w "%{http_code}\n" https://synthex.social/data-deletion   # 200
curl -s https://synthex.social/data-deletion | grep -c support@carsi.com.au      # >=1
curl -s -o /dev/null -w "%{http_code}\n" https://synthex.social/privacy          # 200

# FB/IG OAuth correct (authed session):
#   GET https://synthex.social/api/auth/oauth/facebook?returnTo=/dashboard/platforms
#   → client_id=1316892900613495 & config_id=1544525556521302

# Skill present + registered:
cat ~/.claude/skills/connecting-meta-facebook-instagram/SKILL.md
git -C /Users/phillmcgurk/Pi-Dev-Ops show origin/main:agentskills.json | grep -c connecting-meta-facebook-instagram  # 2

# Meta dialog (owner, authed browser) — currently blocks on App Domains until §7.3 done.
```

## 7. Deferred + open questions
**Deferred — ALL owner/Meta-console (no code):**
1. Meta app `1316892900613495` → Settings→Basic: set **Privacy URL** = `https://synthex.social/privacy`, **User Data Deletion URL** = `https://synthex.social/data-deletion`. Owner. Blocking App Review submission.
2. **Business Verification** (Unite-Group Nexus Pty Ltd) + **App Review** for `pages_manage_posts`/`instagram_content_publish` — Owner submits (draft: `scratchpad/meta-app-review-request.md`); Meta approves (multi-day). Blocking live FB/IG posting.
3. Add **`synthex.social` to App Domains** (Settings→Basic). Owner. Blocking the OAuth dialog from loading ("Can't load URL").
4. **Rotate Facebook App Secret** (briefly in the public client_id slot). Owner. Recommended.
5. **Add founder's own FB account as an app admin** on `1316892900613495` (currently only reachable via the ex-employee "Brooke Carsi" session). Owner. Avoids future lock-out.

**Open questions:**
- Does `support@carsi.com.au` monitor deletion requests? Owner. Non-blocking (it's a real CARSI inbox).
- Confirmed test Page + linked IG Business account for the App Review reviewer? Owner. Blocking a clean review.

## 8. Pick up here
**Start here:** this session's engineering is done — there is **nothing further to build for the FB/IG integration**. The next agent's only role is to *assist the owner* with the Meta-console steps if asked (e.g. draft screencast scripts, adjust the App Review copy, tweak the legal pages).
**Do not redo:** any of #597/#598/#601/#602 (merged+verified), the credential repoint, the config wiring, the `/privacy` page, or the skill. Do not re-enter the Meta credentials — verified correct.
**First command to run (only if resuming to help further):**
```bash
cd /Users/phillmcgurk/Synthex && curl -s -o /dev/null -w "data-deletion %{http_code}\n" https://synthex.social/data-deletion
```

## 9. Risk notes
- **Phase 0 gate = BLOCKED** (`generated-agentskills`, `type-python-import`) — environmental, NOT this session's work (gate runs on the behind-main Pi-Dev-Ops remotion tree; Python deps absent locally). Reverted the gate's manifest-regen churn after the run. All session deliverables merged/verified independently (§5/§6).
- **Local tsc ≠ CI:** Synthex local `tsc --noEmit` shows pre-existing `command-centre` errors (`@unite-group/control-module` not built locally); CI Type Check passes. Not this session's scope — don't "fix".
- **Other open PRs #596 / #590 authored by the same GitHub account are NOT from this session** (command-centre tag-pin; browser-connect/GSC) — out of scope, leave alone.
- **Two-browser trap:** claude-in-chrome drove a REMOTE Windows Chrome; computer-use saw the Mac (read-only). Prefer JSON-endpoint verification.
- Synthex `stash@{0}` = unrelated parked CARSI explainer WIP — leave it. Pi-Dev-Ops remotion branch has 21 WIP paths, untouched.

## 10. Handoff quality check
- Tests claimed run? Only what ran (CI results via gh; curl live checks). ✅
- Shipped claims backed by evidence? All merged items cite PR#+SHA; prod verified by curl. ✅
- Running process claims? None claimed running. ✅
- Completed vs deferred separated? Yes — §1/§3 (done) vs §7 (owner/Meta). ✅
- First command provided? Yes (§8). ✅

Handoff complete. Next safe action: none required — engineering is complete; hand the Meta-console steps (§7) to the owner and assist only if asked.
