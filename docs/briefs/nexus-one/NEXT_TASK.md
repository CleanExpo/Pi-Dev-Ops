# NEXT TASK: Nexus One Slice A and launch readiness

Task ID: `nexus-one-bootstrap-20260908`  
Canonical repository: `CleanExpo/Pi-Dev-Ops`  
Requested by: Phill  
Prepared: 8 September 2026  
Priority: next pending Nexus One bootstrap task after any already-owned work reaches a safe checkpoint  
Status: prepared for pickup; not dispatched or admitted for implementation

## Outcome

Use the completed Nexus One design and PC launch supplement to produce one current, bounded pilot specification and an honest host/authority/readiness report. Do not redesign the whole architecture or begin an estate-wide rebuild.

The intended first delivery journey is Margot -> one canonical task -> one admitted worker -> deterministic checks -> independent review -> an evidence receipt. This pickup performs the finite discovery/SPM/Judge preparation for that journey. Implementation is separate and follows the existing admission controls.

## Read in this order

1. Current repository `AGENTS.md`, `CLAUDE.md`, canonical Senior Harness, model-router, Unlazy, SPM, Judge and handoff instructions. Inspect effective hooks before executing them.
2. This file, then `pc-launch/NEXUS-PC-BOOTSTRAP.txt`.
3. `implementation/README.md` and `implementation/BUILD_PROMPT.md`.
4. `implementation/MASTER_BLUEPRINT.md`, particularly evidence boundaries, host eligibility and slices A-F.
5. `implementation/ACCEPTANCE.md`, `implementation/policy.proposal.yaml` and `implementation/sources.json` as needed for the pilot.

The launch supplement's `BOOTSTRAP-MANIFEST.json` is generated on the selected computer by its launcher. It is not supplied here because that computer's checkout and runtime have not been observed. For manual read-only pickup, follow the supplement's explicit manifest-absent path and use the actual operator-selected repository. Do not invent a manifest or crawl personal folders.

## Reconcile freshness and ownership

Remote `main` observed while preparing this transfer: `5cc82de67baef455eadd6c6f6d7b5e4c142c5d72`. The original blueprint records the older `2c4d346eb8d6723fe48db79c55e34eba1f33f09c` snapshot. Both are observations, not forced checkout targets or proof of the deployed version.

Read current remote/main, the selected local HEAD, branch, dirty state, worktrees, relevant PRs/tasks and latest durable handoff. Preserve dirty roots and work owned by another machine. Search for this exact task ID before creating another task, issue or PR. If a matching owner exists, extend that handoff instead of creating a duplicate.

`QUEUE.md` is read by a Stop hook. Its current instructions distinguish finite actionable work from blocked work. Never queue "rebuild everything" or an authority-blocked implementation as an unchecked Active item. Do not change global queue state merely to claim this task was scheduled.

## Authority and safety boundary

Permitted first pass: read-only discovery, bounded analysis, source verification and the existing approved planning output path. Publication of this folder is not inherited worker authority.

Do not pull/checkout/reset/clean/stash/rebase/merge an occupied root, terminate unrelated processes, install dependencies, change hooks, read credential values, create competing repair branches or dispatch remote workers during initial inspection.

Windows remains review-only until native containment, identity, fencing, checkpoint, cancellation and recovery evidence is proved. A WSL2 installation alone is not worker admission. Prefer an already-admitted Mac worker only if its readiness and authority are actually established.

No merge, deploy, production migration, external send, credential/permission change, policy activation, new subscription, API spend or control-kernel promotion without its separately required authority. Do not publish ChatGPT Sites.

## Clean context and bounded recovery

Original source files remain references. Prototype screenshots/HTML and historical QA do not count as implementation or live acceptance. Requirements and source claims must be checked for current applicability. Repeated copies share a single source lineage.

Keep company/project/client/private-personal scope separate. Preserve source IDs, revisions, negations and failed-method fingerprints. A fresh runtime session does not reset the parent budget or grant authority.

Use one continuation owner. Do not stack Stop hooks, native goals, repository goals and a restart wrapper. Resolve their ownership before invoking continuation. Two distinct failed methods trigger the existing uncertainty process rather than a repeated prompt. Missing authority or exhausted budget produces a named waiting state, not a fabricated pass.

## Deliverables and completion

Produce one concise pickup/readiness report and the current SPM/Judge specification containing:

- Actual checkout/base/branch and existing task/PR owner, with local dirty state left unchanged.
- Input identity and checksum result; differences explicitly reported.
- Relevant existing capability inventory; no replacement for something already working.
- Actual host/runtime/containment state and redacted billing provenance, or explicit UNKNOWN.
- Pilot's critical data, context, authority, cost and continuation pathways.
- Smallest implementation slice, affected paths, test plan and independent-review route.
- Exact remaining admission requirements and next permitted action.
- Durable handoff through an approved output path, with completed work, pending checks, scope and wake condition.

The bootstrap is complete when these finite outputs exist and are grounded in current evidence. A runtime, model or host being unavailable does not make all analysis fail; only the affected execution stays pending. Do not mark implementation complete from this audit.

## One instruction for the next agent

> Read `docs/briefs/nexus-one/NEXT_TASK.md` and follow its current-state reconciliation and finite Slice A scope. Find the existing owner first. Preserve dirty roots. Use the repository's Senior Harness/SPM/Judge workflow. Do not activate, merge, deploy, spend or start the estate rebuild.
