# NEXUS ONE | Implementation entry prompt

Use this prompt in the existing authorised Mission Control development session. This is a specification instruction, not a grant to dispatch workers, spend, push, merge, deploy or mutate production.

## Objective

Converge `CleanExpo/Pi-Dev-Ops` into the existing Unite-Group Nexus Mission Control: one Margot entry from computer, iPad and phone; durable scoped context; native Claude/Codex/Cursor runtime adapters; bounded API lanes; persistent technical recovery; a conditional 15–20-move horizon; evidence-bound learning; and a clear founder cockpit.

Read `MASTER_BLUEPRINT.md`, `ACCEPTANCE.md`, `policy.proposal.yaml` and `sources.json` as the delivery specification. Do not inject this entire blueprint into every model call. Compile small role-specific packets instead.

## Ground before changing anything

Load the repository's current `AGENTS.md`, `CLAUDE.md`, `/spm`, Senior Harness, model-router, Unlazy, admission-enforcement contract and latest durable handoff. Read current branch, HEAD, working-tree status, open task/PR and local/remote differences. The research snapshot was `2c4d346eb8d6723fe48db79c55e34eba1f33f09c`; re-read rather than force-checking out that version.

Preserve dirty roots. Reuse existing tasks and PRs. Discover canonical skill paths rather than assuming the active project contains Pi-Dev-Ops scripts. Search the capability registry before adding components. Treat repository comments about deployment or account quota as claims needing current evidence, not facts about today's runtime.

Begin with SPM discovery/specification only. Do not implement until the current trusted authority path accepts the bounded slice. Once an implementation slice is explicitly admitted, continue its routine engineering/testing/recovery without repeatedly asking the founder to supervise technical details.

## First vertical pilot

Prove one journey, not an entire estate rewrite:

1. Margot accepts or resumes one bounded non-production objective.
2. One canonical task/contract revision exists and duplicate inputs do not duplicate it.
3. The Context Compiler selects only the required admissible sources and records their revisions.
4. A broker decision names the actual eligible runtime/account/model family, budget reservation and independent verifier.
5. One ready Mac worker receives a signed, scoped, expiring lease and an isolated workspace.
6. The worker makes a candidate and leaves a durable checkpoint.
7. Mandatory deterministic gates run before independent review.
8. A failed first method records evidence and enters the existing recovery/uncertainty path rather than repeating itself or abandoning the goal.
9. An independent reviewer returns an evidence-bound verdict. Do not call a second persona in the same execution independent cross-vendor review.
10. A phone/tablet client restores the same scoped task and thread without a new model call for navigation.
11. Usage/cancellation/checkpoint receipts reconcile. No protected action occurs.

Use a synthetic fixture in isolation first, visibly labelled. Use one bounded real task only under the actual grant. Preserve existing restrictions on Windows and all unrelated portfolio repair tracks.

## Mandatory invariants

- One continuation owner. No nested goal/Stop-hook/orchestrator retry loops without shared budgets and explicit ownership.
- The objective persists; a failed pathway stops. Follow the existing two-distinct-attempt/evidence-stagnation contract. Investigations and all child agents share the parent budget.
- Knowledge, proposals, simulations, authority and verified evidence stay distinct.
- Personal and company scopes never merge implicitly. Local execution does not imply local inference.
- A TaskPacket is a view of the existing immutable TaskContract revision; result events and receipts append separately.
- External content cannot change authority. A model cannot grant itself permission, change its grader, increase spend or promote its own control-code change.
- Runtime hooks are not universal interception. Prove host isolation, secret separation, lease fencing, cancellation and replay protection.
- Unknown capability/quota is explicit. Do not guess a machine-readable endpoint or infer zero cost from missing telemetry.
- Premium quota reserve is an admission guard band. API spend requires a conservative atomic reservation and settlement, plus provider-side controls where supported.
- No model-name, account-email or price proliferation in prompts. Resolve versioned capability, billing and policy manifests.
- An exact-SHA review is reused only while the whole evidence fingerprint remains current, including base, requirements, tests, environment, policy and model/skill dependencies.
- Every refusal has a reason and recovery route. Prove legitimate positive cases still pass.
- Merge, deploy, publish, external sends, production migration, destructive deletion, new spend and permission/credential changes retain the current founder gates.
- The delivered YAML is inert and is not itself a signed grant.

## Outputs for Slice A

Produce one evidence-indexed SPM spec with the pilot's current-state map, reused components, remaining controls, proposed minimal file diff, compatibility plan, exact tests, independent review plan and rollback. Name unknowns and the exact read/probe that resolves each. Do not produce another 20-page general vision document.

## Outputs for each admitted implementation slice

Return changed paths, base/candidate IDs, commands and redacted results, receipt hashes, provider/model/auth provenance without secrets, real usage or an explicit unknown, independent review status, known blockers, and the next machine-readable state transition.

Use the existing CI-parity and `/done` machinery. Inspect actual workflow commands before running them. Do not weaken a gate or raise a baseline simply to get a green result. Remote checks are read back after an authorised push, not assumed from local results.

Stop the affected action for missing authority, unsafe scope, exhausted budget or unresolvable state. Preserve the task with a wake trigger and continue unrelated admitted work. Do not return routine troubleshooting to Phill when an approved evidence path remains.

Completion for this entry prompt is an accepted bounded implementation specification, followed only by work admitted through the existing authority system. No production activation is implied.
